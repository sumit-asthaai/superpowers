# Security Scan Evidence Report

> Generated by Safe Agentic Framework Scanner on 2026-03-30T04:36:58.888Z

## Summary

| Property | Value |
|----------|-------|
| Scan ID | `2a2a0391-4b78-4156-abcb-4eff23ca9308` |
| Repository | sumit-asthaai/superpowers |
| Branch | `main` |
| Commit | `` |
| Scanner Version | 0.3.0 |
| Files Scanned | 15 |
| Techniques Checked | 69 |
| Total Findings | 638 |
| Dashboard | [View Scan](http://localhost:8080/repository-scans/github/scans/2a2a0391-4b78-4156-abcb-4eff23ca9308?repo=1195793595) |

### Findings by Severity

| Severity | Count |
|----------|-------|
| 🔴 Critical | 246 |
| 🟠 High | 392 |
| 🟡 Medium | 0 |
| 🔵 Low | 0 |

---

## Detailed Findings

### 📁 `hooks/hooks.json`

#### Finding 1: 🔴 **CRITICAL** — `SAFE-T1003`

**Location:** Lines 5-10

**Description:**

The hooks.json file defines a SessionStart hook that executes an external command. If this file is part of a maliciously distributed MCP server package, this mechanism can be used to execute arbitrary code upon server startup, leading to initial access and potential compromise.: Malicious MCP-Server Distribution involves distributing trojanized MCP server implementations through package registries (npm, PyPI, Docker Hub) or code repositories. These servers appear legitimate but contain hidden malicious functionality such as data exfiltration, backdoors, or credential theft.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:5-10 "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd"" session-start
```

</details>

---

#### Finding 2: 🔴 **CRITICAL** — `SAFE-T1101`

**Location:** Line 8

**Description:**

The 'command' field in the hooks configuration constructs a shell command using the 'CLAUDE_PLUGIN_ROOT' environment variable. If an attacker can control or influence the value of 'CLAUDE_PLUGIN_ROOT', they could inject arbitrary commands into the executed string. The command also invokes a '.cmd' file, which is explicitly noted in the README as potentially problematic due to Windows argument parsing and shell involvement.: Prompt Injection via Tool Output occurs when MCP tools return user-controlled or external data directly to the LLM without proper sanitization, allowing malicious content in tool outputs to hijack the agent behavior. Tool outputs are typically trusted by LLMs and processed as authoritative information.
Common attack vectors include: database records with hidden instructions, file contents with prompt injection payloads, API responses crafted to override directives, and web scraping results with malicious instruction sequences.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:8-8 "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start"
```

</details>

---

#### Finding 3: 🔴 **CRITICAL** — `SAFE-T1111`

**Location:** Lines 6-9

**Description:**

The `hooks.json` file configures a `SessionStart` hook to execute a command associated with a Claude AI plugin. This mechanism allows for the programmatic execution of a script (`run-hook.cmd`) that, if compromised or maliciously crafted, could weaponize the Claude AI CLI tool by invoking it with dangerous flags and malicious prompts for reconnaissance, data collection, or exfiltration, as described in SAFE-T1111. The use of a variable `CLAUDE_PLUGIN_ROOT` explicitly ties this execution to an AI agent.: AI Agent CLI Weaponization exploits AI-powered CLI tools and agents that have unrestricted shell access or use dangerous permission-bypass flags. Attackers distribute malicious packages with postinstall scripts that invoke AI CLI tools in unsafe modes, or craft inputs that cause agents to execute arbitrary system commands, harvest credentials, or exfiltrate repository data.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:6-9 "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start",
```

</details>

---

#### Finding 4: 🔴 **CRITICAL** — `SAFE-T1203`

**Location:** Lines 1-16

**Description:**

The `hooks.json` file defines a `SessionStart` hook that executes an external command (`run-hook.cmd`). This mechanism provides a direct execution vector that can be leveraged by a trojanized MCP server artifact to establish persistence by executing attacker-controlled code during the server's startup phase.: Backdoored Server Binary involves distributing trojanized MCP server packages, containers, or binaries that establish persistence through postinstall hooks, cron jobs, systemd units, or autostart entries. The malicious code executes during installation and persists even after the package is uninstalled.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:1-16 hooks/hooks.json:4-12 {
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start",
            "async": false
          }
        ]
      }
    ]
  }
}
```

</details>

---

#### Finding 5: 🔴 **CRITICAL** — `SAFE-T1305`

**Location:** Lines 8-11

**Description:**

The MCP server is configured to execute a shell command via a 'SessionStart' hook. If the MCP server process is running with root privileges on the host operating system, this command will also execute as root, providing a direct vector for privilege escalation as described by SAFE-T1305.: Host OS Privilege Escalation exploits operating system vulnerabilities or misconfigurations from within an MCP server process to gain root or administrative access on the host. Attack vectors include setuid binary abuse, kernel exploits, capability escalation, and accessing sensitive system files like /etc/shadow.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:8-11 "type": "command",            "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start",            "async": false
```

</details>

---

#### Finding 6: 🔴 **CRITICAL** — `SAFE-T1505`

**Location:** Lines 9-11

**Description:**

The `hooks.json` file defines a command to be executed at `SessionStart`. While this file itself does not directly implement the SAFE-T1505 technique, the external script `run-hook.cmd` that it invokes could potentially interact with or expose sensitive information in a manner vulnerable to in-memory secret extraction if not properly secured. The JSON merely configures the execution, not the vulnerability itself.: In-Memory Secret Extraction targets secrets held in process memory at runtime. Attackers use memory dump tools, /proc/*/mem access, heap inspection, or debugger attachment to extract encryption keys, tokens, passwords, and other sensitive data that exists only in memory and would not be found in files or environment variables.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:9-11 "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start"
```

</details>

---

#### Finding 7: 🔴 **CRITICAL** — `SAFE-T1702`

**Location:** Line 10

**Description:**

The command executed by the hook uses an environment variable, CLAUDE_PLUGIN_ROOT. While not directly unsanitized user input, if an attacker can control this environment variable in the execution context, it could lead to arbitrary command execution. This is an indirect risk, as the JSON itself does not introduce user input.: Command Injection vulnerabilities occur when MCP server tools execute shell commands with unsanitized user input. Attackers can inject malicious commands that are executed with the server's privileges, potentially leading to arbitrary code execution, data exfiltration, or system compromise.

This technique targets common command execution patterns across multiple languages including Node.js (child_process), Python (subprocess, os.system), Rust (Command), and Go (os/exec). Unsafe practices include string concatenation in shell commands, missing input validation, and using shell=True or shell invocation modes.

Command injection is particularly dangerous in MCP servers because tools often have elevated privileges to interact with system resources, files, or external services. A successful injection can bypass all security boundaries.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:10-10 "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start"
```

</details>

---

#### Finding 8: 🔴 **CRITICAL** — `SAFE-T1704`

**Location:** Line 10

**Description:**

The 'command' field in hooks.json executes a local script using an environment variable. While not directly an SSRF vulnerability, if the 'CLAUDE_PLUGIN_ROOT' environment variable is user-controlled or can be manipulated by an attacker, it could lead to arbitrary command execution or path traversal, which could be a component of a 'Compromised-Server Pivot' attack (SAFE-T1704). However, this does not involve user-controlled URLs or SSRF as described in the technique summary.: Compromised-Server Pivot / SSRF occurs when MCP tools accept user-controlled URLs in HTTP requests without host allowlisting or validation. Attackers exploit this to access internal services, cloud metadata endpoints, localhost services, or private network resources that the MCP server can reach but the attacker cannot directly access.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:10-10 "${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start
```

</details>

---

#### Finding 9: 🔴 **CRITICAL** — `SAFE-T1705`

**Location:** Line 9

**Description:**

The 'command' field in the SessionStart hook executes a command using a variable '${CLAUDE_PLUGIN_ROOT}'. If this variable is sourced from an untrusted or attacker-controlled environment, it could lead to OS command injection, allowing an attacker to execute arbitrary commands on the system. While this is not directly 'agent-to-agent message injection' as described by SAFE-T1705, it represents a critical vulnerability that could be leveraged as an initial compromise point to then facilitate cross-agent attacks.: Cross-Agent Instruction Injection targets multi-agent MCP architectures where agents communicate by passing messages or tool results. Attackers inject malicious instructions into one agent that propagate to other agents in the workflow, contaminating the entire multi-agent pipeline through message passing, shared context, or tool output forwarding.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:9-9 "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start"
```

</details>

---

#### Finding 10: 🔴 **CRITICAL** — `SAFE-T1803`

**Location:** Line 10

**Description:**

The configuration defines a command hook that executes an external script 'run-hook.cmd' during session start events. While this specific JSON does not directly perform database export, the external script could potentially be configured to execute bulk database export commands (e.g., SELECT *, pg_dump, mysqldump) as described in SAFE-T1803. Without the content of 'run-hook.cmd', it's impossible to confirm if this is an actual vulnerability.: Database Dump extracts entire database contents through MCP tools using bulk export commands like mysqldump, pg_dump, SELECT *, or ORM findAll without pagination. This enables mass exfiltration of structured data including user records, credentials, financial data, and other sensitive information stored in databases.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:10-10 "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start"
```

</details>

---

#### Finding 11: 🔴 **CRITICAL** — `SAFE-T2101`

**Location:** Line 10

**Description:**

A hook is configured to execute an external command 'run-hook.cmd' when a session starts and matches 'clear' or 'compact' patterns. While the command itself is not directly destructive, the 'clear' and 'compact' matchers suggest an intent for cleanup operations, which could potentially involve data destruction if the 'run-hook.cmd' script is not properly secured or implemented.: Data Destruction uses MCP tools to permanently delete or corrupt data through destructive operations such as rm -rf, DROP TABLE, file wiping, bulk delete API calls, or database truncation. Attackers may destroy data for sabotage, to cover their tracks, or as part of a ransomware attack.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:10-10 "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start"
```

</details>

---

#### Finding 12: 🔴 **CRITICAL** — `SAFE-T2103`

**Location:** Lines 7-10

**Description:**

The `hooks.json` configuration file defines a `SessionStart` hook that directly executes a command. This structure presents a critical attack surface for the SAFE-T2103 technique, where a malicious pull request could alter the `command` field to achieve arbitrary code execution on the MCP server or associated AI agent.: Code Sabotage via Malicious PR uses MCP tools with repository access to create automated pull requests containing malicious code changes, backdoors, or CI/CD pipeline modifications. Attackers manipulate build scripts, inject vulnerabilities, or alter deployment configurations through automated commits that bypass normal code review.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:7-10 "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start"
```

</details>

---

#### Finding 13: 🔴 **CRITICAL** — `SAFE-T2107`

**Location:** Lines 1-16

**Description:**

The 'hooks.json' file defines system commands to be executed by the MCP server, which could potentially be leveraged in an attack chain for training data manipulation if the commands themselves are malicious or lead to data contamination. However, this file itself does not contain training data, adversarial examples, or backdoor triggers.: AI Model Poisoning manipulates machine learning models used by MCP servers through training data poisoning, adversarial example injection, or embedding backdoor triggers during fine-tuning. Poisoned models produce attacker-desired outputs when triggered by specific inputs while behaving normally otherwise.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:1-16 hooks/hooks.json:1-16 {
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start",
            "async": false
          }
        ]
      }
    ]
  }
}
```

</details>

---

#### Finding 14: 🔴 **CRITICAL** — `SAFE-T3001`

**Location:** Lines 1-16

**Description:**

The `hooks.json` file defines a command to be executed at session start. While this file itself is not a RAG knowledge base or a component directly involved in RAG data processing, it represents a potential point of control for an attacker if compromised. An attacker could modify this command to execute arbitrary code, which *could* then be used to facilitate a RAG backdoor attack (e.g., by injecting malicious documents or tampering with RAG components). However, this file does not directly implement the SAFE-T3001 technique, which focuses on the RAG pipeline itself.: RAG Backdoor Attack injects malicious documents into Retrieval-Augmented Generation knowledge bases used by MCP servers. Poisoned documents contain hidden instructions, false information, or trigger phrases that activate when retrieved, causing the LLM to produce attacker-controlled outputs, leak data, or execute unauthorized actions.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:1-16 hooks/hooks.json:1-16 {
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start",
            "async": false
          }
        ]
      }
    ]
  }
}
```

</details>

---

#### Finding 15: 🟠 **HIGH** — `SAFE-T1001`

**Location:** Lines 1-16

**Description:**

The file 'hooks/hooks.json' defines system commands to be executed, not tool descriptions intended for LLM interpretation. While the command itself could be malicious if the file were compromised, this specific file does not contain 'tool descriptions' as targeted by SAFE-T1001.: Tool Poisoning Attack (TPA) embeds malicious instructions within MCP tool descriptions that are invisible to users but processed by LLMs. This exploits the difference between displayed and processed tool descriptions.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:1-16 hooks/hooks.json:1-16 {
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start",
            "async": false
          }
        ]
      }
    ]
  }
}
```

</details>

---

#### Finding 16: 🟠 **HIGH** — `SAFE-T1002`

**Location:** Lines 8-9

**Description:**

The `hooks.json` file defines a command to be executed (`run-hook.cmd`) as part of a `SessionStart` hook. If the plugin containing this `hooks.json` file is fetched and installed without integrity verification, this command will be executed without any runtime checks, allowing for potential remote code execution if the plugin or the `run-hook.cmd` script is malicious. This represents a supply chain vulnerability where a compromised artifact (the plugin package) can lead to arbitrary code execution.: Supply Chain Compromise occurs when MCP servers download, install, import, or execute external packages, plugins, model weights, or binaries without integrity checks, version pinning, or signature verification. This allows attackers to inject malicious code through compromised dependencies.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:8-9 "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start"
```

</details>

---

#### Finding 17: 🟠 **HIGH** — `SAFE-T1004`

**Location:** Lines 1-16

**Description:**

The `hooks.json` file defines client-side command execution, which is not directly related to server impersonation or name collision (SAFE-T1004).: Server Impersonation occurs when an attacker registers an MCP server with a name designed to be confused with a legitimate service (typosquatting, homograph attacks, or DNS cache poisoning). Users connecting to the impersonated server unknowingly send credentials and data to the attacker.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:1-16 {"hooks": {"SessionStart": [{"matcher": "startup|clear|compact","hooks": [{"type": "command","command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start","async": false}]}]}}
```

</details>

---

#### Finding 18: 🟠 **HIGH** — `SAFE-T1006`

**Location:** Lines 5-10

**Description:**

This `hooks.json` file defines a `SessionStart` hook that executes a command from within the plugin's root directory. While this specific file is not a direct instance of the SAFE-T1006 social engineering install technique, it represents a mechanism that could be leveraged by a malicious plugin installed via social engineering. If a user were tricked into installing a trojanized plugin, this hook could be used for persistence or to execute malicious code upon session start.: User-Social-Engineering Install exploits user trust to convince them to install a malicious or compromised MCP server. This includes fake documentation, misleading READMEs, deceptive tool descriptions, or impersonating popular tools to trick users into granting permissions.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:5-10 "hooks": {"SessionStart": [{"matcher": "startup|clear|compact","hooks": [{"type": "command","command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start","async": false}]}]}
```

</details>

---

#### Finding 19: 🟠 **HIGH** — `SAFE-T1104`

**Location:** Lines 9-11

**Description:**

An MCP hook is configured to execute an external command (`run-hook.cmd`) during session start events. If this script is configured with excessive permissions, it presents a significant risk for over-privileged tool abuse.: Over-Privileged Tool Abuse occurs when MCP tools are granted permissions far exceeding their functional requirements. Attackers exploit these over-privileged tools through prompt injection or direct manipulation to access sensitive resources, execute system commands, or escalate privileges beyond the intended scope.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:9-11 "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start"
```

</details>

---

#### Finding 20: 🟠 **HIGH** — `SAFE-T1105`

**Location:** Line 10

**Description:**

The `hooks.json` configuration defines a command that constructs an executable path using the `${CLAUDE_PLUGIN_ROOT}` variable. If the value of `CLAUDE_PLUGIN_ROOT` is derived from untrusted input without proper sanitization or normalization, an attacker could inject path traversal sequences (e.g., `../`) into it. This would allow the command to execute a script outside the intended plugin root directory, potentially leading to arbitrary command execution or access to sensitive system files.: Path Traversal via File Tool occurs when MCP file-handling tools accept untrusted input as filesystem paths without proper normalization, allowlisting, or base directory enforcement. Attackers can use relative path sequences, Unicode normalization tricks, double encoding, or null byte injection to access files outside the intended directory.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:10-10 "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start"
```

</details>

---

#### Finding 21: 🟠 **HIGH** — `SAFE-T1110`

**Location:** Line 7

**Description:**

The `hooks.json` file defines a command to be executed during `SessionStart`. While this file itself does not directly involve image or audio processing, the executed command `"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start` could potentially be a vector if `run-hook.cmd` later processes untrusted multimodal input without proper sanitization. However, based on the current file, there is no direct evidence of SAFE-T1110.: Multimodal Prompt Injection embeds malicious instructions within images, audio, or other non-text media that are processed by LLMs. Techniques include steganographic embedding in pixel data, OCR-extractable text injection, adversarial visual patterns, and metadata injection in media files. These bypass text-based sanitization since the payload is in a non-text modality.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:7-7 "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start"
```

</details>

---

#### Finding 22: 🟠 **HIGH** — `SAFE-T1201`

**Location:** Line 9

**Description:**

The 'SessionStart' hook executes an external command script, 'run-hook.cmd', whose content is not defined within this JSON file. This allows for dynamic modification of the script's behavior after initial user approval, enabling a 'rug pull' attack.: MCP Rug Pull Attack is a persistence technique where tools appear legitimate initially but can change their behavior after gaining user trust. This exploits dynamic tool definitions, remote configuration loading, or time-delayed activation to modify tool behavior post-approval.
Static analysis detects code patterns that ENABLE rug pulls: dynamic descriptions, remote tool loading without version pinning, and time-based conditional behavior.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:9-9 "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start"
```

</details>

---

#### Finding 23: 🟠 **HIGH** — `SAFE-T1202`

**Location:** Lines 1-16

**Description:**

The configuration defines a 'SessionStart' hook that executes a command. While this file itself does not store OAuth tokens, the executed command `"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start` could potentially interact with or manage session-related data, including OAuth tokens. Without further context on the `run-hook.cmd` script, it's not possible to determine if it handles OAuth tokens insecurely.: OAuth Token Persistence exploits insecure token storage and handling to maintain long-lived access to MCP-connected services. Attackers steal access or refresh tokens from localStorage, sessionStorage, cookies without secure flags, or file-based caches, then reuse them to persist access even after password changes or session invalidation.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:1-16 hooks/hooks.json:4-11 "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start",
            "async": false
          }
        ]
      }
    ]
```

</details>

---

#### Finding 24: 🟠 **HIGH** — `SAFE-T1205`

**Location:** Lines 1-16

**Description:**

The 'hooks.json' file defines commands that are executed during server lifecycle events (e.g., SessionStart). If an attacker gains write access to this file, they can modify the 'command' field to inject and persist arbitrary malicious commands, which will be executed automatically upon server restart or specific events. This aligns with the 'Persistent Tool Redefinition' technique by modifying a persistent configuration file to achieve command execution.: Persistent Tool Redefinition is a privilege escalation technique where attackers modify an MCP server's tool metadata or configuration to embed hidden commands that survive server restarts. By persisting malicious tool definitions to disk, database, or configuration files, the attacker ensures their modifications remain active even after the server is restarted or redeployed.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:1-16 {  "hooks": {    "SessionStart": [      {        "matcher": "startup|clear|compact",        "hooks": [          {            "type": "command",            "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start",            "async": false          }        ]      }    ]  }}
```

</details>

---

#### Finding 25: 🟠 **HIGH** — `SAFE-T1301`

**Location:** Lines 1-16

**Description:**

The 'hooks.json' file defines internal server-side commands to be executed during specific session events (startup, clear, compact). While this file itself does not register a 'tool' that could be shadowed by a malicious MCP server, the command it executes (`run-hook.cmd`) could potentially interact with or manage MCP tools. However, based on the provided context, this file is a configuration for internal server behavior rather than a public tool definition.: Cross-Server Tool Shadowing is a privilege escalation technique where malicious MCP servers override or intercept legitimate tool calls from other servers. This attack exploits multi-server MCP environments where multiple servers can provide tools with the same or similar names, allowing attackers to shadow legitimate functionality with malicious implementations.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:1-16 {"hooks": {"SessionStart": [{"matcher": "startup|clear|compact","hooks": [{"type": "command","command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start","async": false}]}]}}
```

</details>

---

#### Finding 26: 🟠 **HIGH** — `SAFE-T1302`

**Location:** Lines 7-10

**Description:**

The `hooks.json` file defines a `command` type hook that executes an external script (`run-hook.cmd`) during `SessionStart` events. This mechanism allows for direct shell command execution.: High-Privilege Tool Abuse occurs when MCP tools are configured with administrative, root, or superuser access to databases, APIs, or system resources. Attackers exploit these tools through prompt injection or direct manipulation to perform privileged operations such as modifying access controls, reading sensitive data, or escalating their own permissions.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:7-10 "type": "command",            "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start",            "async": false
```

</details>

---

#### Finding 27: 🟠 **HIGH** — `SAFE-T1307`

**Location:** Lines 9-10

**Description:**

The 'hooks.json' file configures a command to be executed during session start. While this file itself does not directly implement token handling or forwarding logic, the executed command `"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start` could potentially be a 'confused deputy' if the `run-hook.cmd` script processes or forwards authentication tokens without proper validation. However, without the content of `run-hook.cmd`, it's impossible to confirm a direct vulnerability.: Confused Deputy Attack exploits ambient authority in MCP servers where a privileged service performs actions on behalf of an unauthorized requester. In multi-tenant environments, this enables cross-tenant data access, privilege confusion between service tiers, or unauthorized operations performed through a trusted intermediary.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:9-10 "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start",
```

</details>

---

#### Finding 28: 🟠 **HIGH** — `SAFE-T1401`

**Location:** Lines 1-16

**Description:**

The provided code defines system hooks for command execution, not content intended for interpretation by a Large Language Model (LLM). The SAFE-T1401 technique specifically targets LLM context manipulation through newline or control character injection to hide malicious instructions from the LLM's understanding. This file does not present an opportunity for this specific type of attack.: Line Jumping injects newline characters, CRLF sequences, or control characters into MCP tool descriptions, parameter names, or metadata fields. The injected characters push malicious instructions outside the visible area of user interfaces, making them invisible during review while still being processed by the LLM.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:1-16 hooks/hooks.json:1-16 {
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start",
            "async": false
          }
        ]
      }
    ]
  }
}
```

</details>

---

#### Finding 29: 🟠 **HIGH** — `SAFE-T1404`

**Location:** Lines 1-16

**Description:**

This configuration defines a 'SessionStart' hook that executes a command. While this file itself does not directly implement response tampering, the execution of arbitrary commands via hooks can be a prerequisite for an attacker to introduce or leverage mechanisms for response tampering if the command itself is vulnerable to injection or if its output is used in an untrusted context that influences AI responses. The command `"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start` is executed without further input validation shown in this snippet.: Response Tampering intercepts and modifies MCP tool responses before they are displayed to the user or processed by the LLM. Attackers alter response content to hide evidence of malicious actions, inject false information, bypass content filters, or manipulate the LLM context for subsequent interactions.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:1-16 hooks/hooks.json:6-9 "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start",
```

</details>

---

#### Finding 30: 🟠 **HIGH** — `SAFE-T1407`

**Location:** Lines 6-9

**Description:**

The 'hooks.json' file defines a 'SessionStart' hook that executes an external command. While this file itself does not implement a proxy, the executed command ('run-hook.cmd') could potentially be used to configure or launch a malicious proxy, which is a component of the SAFE-T1407 technique. Without the content of 'run-hook.cmd', it's impossible to determine if this specific hook contributes to a proxy masquerade.: An attacker operates an MCP server that forwards requests to a legitimate upstream API and relays the responses back to the client. Because the responses originate from the trusted service, the traffic appears normal in monitoring and logs, but the proxy can inspect, alter, or exfiltrate sensitive data. This enables stealthy data collection and response manipulation while hiding the true malicious behavior.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:6-9 "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start"
```

</details>

---

#### Finding 31: 🟠 **HIGH** — `SAFE-T1602`

**Location:** Lines 7-8

**Description:**

The 'hooks.json' configuration file explicitly defines a system hook that executes a command, revealing the existence and invocation method of an internal tool or helper component.: Tool Enumeration discovers the full set of available MCP tools, their capabilities, input schemas, and permission requirements. Attackers use tools/list API calls and schema introspection to map available functionality, identify privileged tools, and find tools with weak input validation for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:7-8 "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start"
```

</details>

---

#### Finding 32: 🟠 **HIGH** — `SAFE-T1605`

**Location:** Lines 1-16

**Description:**

The 'hooks.json' file defines system capabilities, specifically a 'SessionStart' hook that executes a command. While this file itself is not a direct vulnerability for capability enumeration, its contents could be exposed if the MCP agent or LLM is prompted to describe its internal configurations or available tools without proper access controls or redaction. This file represents a capability that an attacker might seek to enumerate.: Capability Mapping systematically discovers MCP server capabilities, supported API versions, feature flags, and permission boundaries. Attackers use introspection endpoints, capability negotiation, and API probing to build a comprehensive map of what the target system can do, identifying the most valuable attack vectors.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:1-16 hooks/hooks.json:1-16 {
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start",
            "async": false
          }
        ]
      }
    ]
  }
}
```

</details>

---

#### Finding 33: 🟠 **HIGH** — `SAFE-T1701`

**Location:** Line 10

**Description:**

The `hooks.json` configuration defines a `SessionStart` hook that executes a command using the `${CLAUDE_PLUGIN_ROOT}` environment variable. The use of environment variables for command paths represents a form of shared state that can be exploited in cross-tool contamination scenarios (SAFE-T1701).: Cross-Tool Contamination exploits shared state between MCP tools including global variables, shared memory, common databases, or file-based caches. Malicious data written by one tool pollutes the execution context of another, enabling data exfiltration, privilege escalation, or behavior manipulation across tool boundaries.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:10-10 "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start"
```

</details>

---

#### Finding 34: 🟠 **HIGH** — `SAFE-T2102`

**Location:** Lines 1-16

**Description:**

The 'hooks.json' file defines a command to be executed at session start, which could potentially trigger an external API call. However, the JSON itself does not contain logic for infinite loops or unbounded requests.: Service Disruption via API Flooding uses MCP tools to generate excessive outbound API calls through infinite loops, missing rate limits, or unbounded concurrent requests. This causes denial of service on target APIs, excessive billing on cloud services, and potential cascading failures across dependent systems.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:1-16 {"hooks":{"SessionStart":[{"matcher":"startup|clear|compact","hooks":[{"type":"command","command":"\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start","async":false}]}]}}
```

</details>

---

#### Finding 35: 🟠 **HIGH** — `SAFE-T2105`

**Location:** Lines 9-10

**Description:**

The `hooks.json` file configures the execution of an external command (`run-hook.cmd`) as a `SessionStart` hook. This command could be an MCP tool. If this tool is compromised or poisoned, its execution via this hook could lead to the generation of disinformation by an LLM, aligning with the 'Tool Poisoning Attack' vector for SAFE-T2105.: Disinformation Output manipulates MCP tool responses or LLM outputs to generate false, misleading, or harmful information. Attackers inject instructions that cause the AI to produce fabricated data, distorted facts, or biased responses that appear authoritative, potentially causing real-world harm through decision-making based on false information.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks.json:9-10 "command": ""${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd" session-start"
```

</details>

---

### 📁 `hooks/session-start`

#### Finding 36: 🔴 **CRITICAL** — `SAFE-T1101`

**Location:** Lines 1-57

**Description:**

The script reads the content of 'SKILL.md' from the filesystem and embeds it directly into the LLM's session context as 'additional_context'. While the content is escaped for JSON syntax, it is not sanitized or validated for malicious prompt instructions. If an attacker can modify the 'SKILL.md' file (e.g., via a compromised plugin or local filesystem access), they can inject arbitrary instructions into the LLM's initial prompt, leading to LLM behavior hijacking.: Prompt Injection via Tool Output occurs when MCP tools return user-controlled or external data directly to the LLM without proper sanitization, allowing malicious content in tool outputs to hijack the agent behavior. Tool outputs are typically trusted by LLMs and processed as authoritative information.
Common attack vectors include: database records with hidden instructions, file contents with prompt injection payloads, API responses crafted to override directives, and web scraping results with malicious instruction sequences.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 37: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Lines 1-57

**Description:**

The content of 'SKILL.md' is read from the file system and directly embedded into the LLM's session context without sanitization against prompt injection. If an attacker can modify this file, they can inject malicious instructions into the LLM's prompt.: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 38: 🔴 **CRITICAL** — `SAFE-T1203`

**Location:** Lines 1-57

**Description:**

The script contains an instruction for the analyzer to output a specific warning message to the user. This is not a security vulnerability within the script itself, but an instruction for the AI model.: Backdoored Server Binary involves distributing trojanized MCP server packages, containers, or binaries that establish persistence through postinstall hooks, cron jobs, systemd units, or autostart entries. The malicious code executes during installation and persists even after the package is uninstalled.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 39: 🔴 **CRITICAL** — `SAFE-T1306`

**Location:** Lines 1-57

**Description:**

The script is a session start hook that injects contextual information into the LLM session. It reads local skill documentation and formats it for JSON output. It does not perform any authentication, token minting, or token validation, nor does it interact with OAuth/OIDC flows or JWKS endpoints.: Rogue Authorization Server involves setting up a malicious or misconfigured authorization server that mints OAuth tokens, JWTs, or API keys without proper validation, client verification, or trust chain enforcement. Attackers use these rogue-issued tokens to access MCP servers and tools that fail to validate the token issuer.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 40: 🔴 **CRITICAL** — `SAFE-T1309`

**Location:** Lines 1-57

**Description:**

The script reads the content of `SKILL.md` from the plugin's installation directory and injects it directly into the LLM's initial session context. This context explicitly informs the LLM about the availability of a 'Skill' tool.: Privileged Tool via Prompt Injection exploits LLM-based tool selection where the model chooses which tool to invoke based on natural language. Attackers craft prompts that manipulate the LLM into selecting privileged or dangerous tools, bypassing access controls that only exist at the prompt/UI layer rather than at the tool execution layer.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 41: 🔴 **CRITICAL** — `SAFE-T1505`

**Location:** Lines 1-57

**Description:**

The `hooks/session-start` script reads the content of `"${PLUGIN_ROOT}/skills/using-superpowers/SKILL.md"` and injects it directly into the AI model's session context. If this `SKILL.md` file contains sensitive information (e.g., API keys, credentials), this information will be loaded into the AI's runtime memory, making it susceptible to in-memory secret extraction techniques (SAFE-T1505).: In-Memory Secret Extraction targets secrets held in process memory at runtime. Attackers use memory dump tools, /proc/*/mem access, heap inspection, or debugger attachment to extract encryption keys, tokens, passwords, and other sensitive data that exists only in memory and would not be found in files or environment variables.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 42: 🔴 **CRITICAL** — `SAFE-T1702`

**Location:** Lines 1-57

**Description:**

The script correctly uses `printf` with a fixed format string and escapes dynamic content for JSON, preventing shell command injection.: Command Injection vulnerabilities occur when MCP server tools execute shell commands with unsanitized user input. Attackers can inject malicious commands that are executed with the server's privileges, potentially leading to arbitrary code execution, data exfiltration, or system compromise.

This technique targets common command execution patterns across multiple languages including Node.js (child_process), Python (subprocess, os.system), Rust (Command), and Go (os/exec). Unsafe practices include string concatenation in shell commands, missing input validation, and using shell=True or shell invocation modes.

Command injection is particularly dangerous in MCP servers because tools often have elevated privileges to interact with system resources, files, or external services. A successful injection can bypass all security boundaries.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 43: 🔴 **CRITICAL** — `SAFE-T1705`

**Location:** Lines 1-57

**Description:**

The script injects content from a local file (`SKILL.md`) directly into the agent's session context without validating the semantic content of the file. If an adversary can modify this `SKILL.md` file (e.g., through file system compromise or a supply chain attack), they can inject arbitrary malicious instructions into the agent's initial prompt, leading to cross-agent instruction injection.: Cross-Agent Instruction Injection targets multi-agent MCP architectures where agents communicate by passing messages or tool results. Attackers inject malicious instructions into one agent that propagate to other agents in the workflow, contaminating the entire multi-agent pipeline through message passing, shared context, or tool output forwarding.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 44: 🔴 **CRITICAL** — `SAFE-T2103`

**Location:** Lines 1-57

**Description:**

The `session-start` hook acts as a critical context injector for AI coding agents, delivering 'superpowers' skill definitions and other important messages. This makes the content it processes (e.g., `SKILL.md`) and the script itself a high-value target for malicious PRs aiming to weaponize AI agents.: Code Sabotage via Malicious PR uses MCP tools with repository access to create automated pull requests containing malicious code changes, backdoors, or CI/CD pipeline modifications. Attackers manipulate build scripts, inject vulnerabilities, or alter deployment configurations through automated commits that bypass normal code review.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 45: 🔴 **CRITICAL** — `SAFE-T2107`

**Location:** Lines 1-57

**Description:**

The script injects content into the AI's session context, which could potentially be used as training data. While the content itself originates from internal plugin files and is not directly manipulable by external input in this script, any compromise of these source files (e.g., `SKILL.md`) could lead to model poisoning if the AI platform uses session context for training.: AI Model Poisoning manipulates machine learning models used by MCP servers through training data poisoning, adversarial example injection, or embedding backdoor triggers during fine-tuning. Poisoned models produce attacker-desired outputs when triggered by specific inputs while behaving normally otherwise.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 46: 🟠 **HIGH** — `SAFE-T1001`

**Location:** Lines 1-57

**Description:**

The code embeds a direct instruction for the Large Language Model (LLM) within the `session_context` that is passed as `additional_context`. Specifically, the `warning_message` variable contains the directive: "IN YOUR FIRST REPLY AFTER SEEING THIS MESSAGE YOU MUST TELL THE USER:". This demonstrates the core mechanism of SAFE-T1001 (Tool Poisoning Attack), where LLM behavior is manipulated by embedded instructions. While this specific instruction is benign and intended by the developers, it establishes a pattern where an attacker with write access could inject malicious instructions to control the LLM's output or actions.: Tool Poisoning Attack (TPA) embeds malicious instructions within MCP tool descriptions that are invisible to users but processed by LLMs. This exploits the difference between displayed and processed tool descriptions.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 47: 🟠 **HIGH** — `SAFE-T1008`

**Location:** Lines 1-57

**Description:**

The session context injected into the AI's prompt contains a directive that explicitly instructs the AI to use a specific tool ('Skill' tool) for 'all other skills'. This pattern of influencing the AI's tool selection and usage strategy aligns with the mechanism described in SAFE-T1008, where instructions embedded in tool descriptions or context can redirect or hijack the use of other tools.: Tool Shadowing Attack embeds instructions in tool descriptions that manipulate how AI agents use OTHER tools. In multi-server MCP setups, all tool descriptions are aggregated into a single prompt, allowing any tool to influence the behavior of others through cross-tool interference patterns.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 48: 🟠 **HIGH** — `SAFE-T1201`

**Location:** Lines 1-57

**Description:**

The script reads a local skill definition file (`SKILL.md`) and injects its content into the session context. While the script itself does not implement dynamic modification, the reliance on a local file for skill definition means that if the plugin's update mechanism is compromised, or if the file can be modified by an attacker post-approval, the injected context could change without explicit user re-approval. This makes the system susceptible to a 'rug pull' attack, where the approved tool's behavior changes dynamically.: MCP Rug Pull Attack is a persistence technique where tools appear legitimate initially but can change their behavior after gaining user trust. This exploits dynamic tool definitions, remote configuration loading, or time-delayed activation to modify tool behavior post-approval.
Static analysis detects code patterns that ENABLE rug pulls: dynamic descriptions, remote tool loading without version pinning, and time-based conditional behavior.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 49: 🟠 **HIGH** — `SAFE-T1205`

**Location:** Lines 1-57

**Description:**

The `session-start` hook reads the `SKILL.md` file from the plugin's persistent storage and injects its content directly into the LLM's session context. If an attacker gains write access to this `SKILL.md` file, they can embed malicious instructions or redefine the 'superpowers:using-superpowers' skill. These malicious modifications will be persistently loaded and presented to the LLM at every session start, fulfilling the criteria for Persistent Tool Redefinition (SAFE-T1205) through 'Description Poisoning'.: Persistent Tool Redefinition is a privilege escalation technique where attackers modify an MCP server's tool metadata or configuration to embed hidden commands that survive server restarts. By persisting malicious tool definitions to disk, database, or configuration files, the attacker ensures their modifications remain active even after the server is restarted or redeployed.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 50: 🟠 **HIGH** — `SAFE-T1504`

**Location:** Lines 1-57

**Description:**

The script outputs session context to the LLM, which could potentially be a vector for token leakage if sensitive information (like tokens) were inadvertently placed within the 'SKILL.md' file or the 'warning_message'. However, based on the current code, no tokens are explicitly handled or outputted.: Token Theft via API Response occurs when MCP tool implementations inadvertently include access tokens, refresh tokens, or other credentials in their output responses. These leaked tokens are then visible in conversation history, logs, or can be captured by other tools in the chain for unauthorized access.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 51: 🟠 **HIGH** — `SAFE-T1601`

**Location:** Lines 1-57

**Description:**

The script reads local skill definitions and outputs them as context for an MCP client. While this is not a vulnerability, the content of the 'using-superpowers' skill and the warning message about legacy skill directories could be considered informational disclosures if an attacker could intercept or access this context. However, this is part of the intended functionality to provide context to the AI model.: MCP Server Enumeration involves probing for available MCP server instances, scanning registries, and discovering service endpoints. Attackers enumerate servers to map the attack surface, identify vulnerable or misconfigured instances, and discover high-value targets for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 52: 🟠 **HIGH** — `SAFE-T1601`

**Location:** Lines 1-57

**Description:**

The script checks for a legacy skills directory and generates a warning message. This message, if exposed to an attacker, could reveal information about the client's local file system structure and deprecated configurations. However, this is intended as a user-facing warning and not a server-side configuration disclosure.: MCP Server Enumeration involves probing for available MCP server instances, scanning registries, and discovering service endpoints. Attackers enumerate servers to map the attack surface, identify vulnerable or misconfigured instances, and discover high-value targets for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 53: 🟠 **HIGH** — `SAFE-T1602`

**Location:** Lines 1-57

**Description:**

The session-start hook explicitly injects information about available 'skills' (tools) and instructions on how to use them into the model's context. This directly exposes tool capabilities and usage patterns, aligning with the SAFE-T1602 (Tool Enumeration) technique.: Tool Enumeration discovers the full set of available MCP tools, their capabilities, input schemas, and permission requirements. Attackers use tools/list API calls and schema introspection to map available functionality, identify privileged tools, and find tools with weak input validation for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 54: 🟠 **HIGH** — `SAFE-T1603`

**Location:** Lines 1-57

**Description:**

The script constructs a system prompt containing explicit operational instructions and embeds the full content of a skill document, making it susceptible to prompt leakage attacks.: System Prompt Disclosure exposes internal system prompts, instructions, or configuration through debug mode endpoints, verbose error messages, tool output, or direct echoing. Exposed system prompts reveal security controls, filtering rules, and behavioral constraints that attackers can use to craft targeted bypass attacks.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 55: 🟠 **HIGH** — `SAFE-T1605`

**Location:** Lines 1-57

**Description:**

The `session-start` hook injects the full content of the 'superpowers:using-superpowers' skill documentation into the LLM's session context. This documentation is explicitly described as the LLM's 'introduction to using skills', which likely contains detailed information about the agent's capabilities, how to invoke them, and potentially their parameters or internal workings. This directly facilitates capability mapping (SAFE-T1605) by pre-populating the LLM's context with sensitive operational details, making it trivial for an attacker to enumerate capabilities without explicit probing.: Capability Mapping systematically discovers MCP server capabilities, supported API versions, feature flags, and permission boundaries. Attackers use introspection endpoints, capability negotiation, and API probing to build a comprehensive map of what the target system can do, identifying the most valuable attack vectors.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 56: 🟠 **HIGH** — `SAFE-T1606`

**Location:** Lines 1-57

**Description:**

The script checks for the existence of a directory at `~/.config/superpowers/skills` using `[ -d "$legacy_skills_dir" ]`. While this is not a directory listing, it does perform a filesystem check on a user's home directory, which could be considered a minor form of reconnaissance if the path were sensitive and not directly relevant to the script's function. However, in this context, it's checking for a legacy configuration path, which is a legitimate operational check.: Directory Listing via File Tool exploits MCP file tools that allow unrestricted directory enumeration. Attackers use fs.readdir, os.listdir, or equivalent functions to map the filesystem structure, discover sensitive files, locate configuration files, and identify credential storage locations for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 57: 🟠 **HIGH** — `SAFE-T1701`

**Location:** Lines 1-57

**Description:**

The script checks for the existence of a legacy skills directory (`~/.config/superpowers/skills`) which represents a form of shared state on the filesystem. While its content is not read, its presence influences the context provided to the AI.: Cross-Tool Contamination exploits shared state between MCP tools including global variables, shared memory, common databases, or file-based caches. Malicious data written by one tool pollutes the execution context of another, enabling data exfiltration, privilege escalation, or behavior manipulation across tool boundaries.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 58: 🟠 **HIGH** — `SAFE-T1707`

**Location:** Lines 1-57

**Description:**

The provided code is a client-side hook script that generates session context. It does not handle incoming state-changing requests, process user input for state modification, or interact with external APIs in a manner susceptible to CSRF. Therefore, it is not vulnerable to SAFE-T1707.: CSRF Token Relay exploits missing or improperly validated CSRF tokens in MCP state-changing requests. Attackers forge cross-site requests or reuse OAuth state parameters to trigger unauthorized actions, manipulate tool configurations, or hijack authentication flows through the victim browser or agent.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

#### Finding 59: 🟠 **HIGH** — `SAFE-T2105`

**Location:** Lines 1-57

**Description:**

The script reads content from an external file (`SKILL.md`) and directly injects it into the LLM's session context. This mechanism allows for context manipulation, where a compromised `SKILL.md` file could embed malicious instructions or disinformation, leading the LLM to generate false, misleading, or harmful outputs.: Disinformation Output manipulates MCP tool responses or LLM outputs to generate false, misleading, or harmful information. Attackers inject instructions that cause the AI to produce fabricated data, distorted facts, or biased responses that appear authoritative, potentially causing real-world harm through decision-making based on false information.

<details>
<summary>Code Evidence</summary>

```
hooks/session-start:1-57 #!/usr/bin/env bash\n# SessionStart hook for superpowers plugin\n
```

</details>

---

### 📁 `skills/brainstorming/scripts/frame-template.html`

#### Finding 60: 🔴 **CRITICAL** — `SAFE-T1003`

**Location:** Lines 1-200

**Description:**

The provided file is an HTML template for a UI frame and does not contain any server-side logic or package distribution mechanisms. It is a static client-side asset.: Malicious MCP-Server Distribution involves distributing trojanized MCP server implementations through package registries (npm, PyPI, Docker Hub) or code repositories. These servers appear legitimate but contain hidden malicious functionality such as data exfiltration, backdoors, or credential theft.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 61: 🔴 **CRITICAL** — `SAFE-T1003`

**Location:** Lines 201-214

**Description:**

The provided HTML file is a static template and does not contain any server-side logic, package definitions, or distribution mechanisms that would indicate a trojanized MCP server package. This file is likely part of a frontend component.: Malicious MCP-Server Distribution involves distributing trojanized MCP server implementations through package registries (npm, PyPI, Docker Hub) or code repositories. These servers appear legitimate but contain hidden malicious functionality such as data exfiltration, backdoors, or credential theft.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 62: 🔴 **CRITICAL** — `SAFE-T1007`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static UI template and does not contain any direct code or logic related to initiating or handling OAuth authorization flows. The `SAFE-T1007` technique describes server-side exploitation of OAuth flows to steal tokens, which is not applicable to this client-side template.: OAuth Authorization Phishing exploits the OAuth authorization flow to trick users into granting permissions to attacker-controlled applications. The attacker creates a convincing authorization page or redirects users through legitimate OAuth providers but captures the authorization code or tokens.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 63: 🔴 **CRITICAL** — `SAFE-T1101`

**Location:** Lines 201-214

**Description:**

The provided HTML code snippet does not contain any direct command injection vulnerabilities. It is a static HTML template without server-side or client-side scripting that could lead to command execution.: Prompt Injection via Tool Output occurs when MCP tools return user-controlled or external data directly to the LLM without proper sanitization, allowing malicious content in tool outputs to hijack the agent behavior. Tool outputs are typically trusted by LLMs and processed as authoritative information.
Common attack vectors include: database records with hidden instructions, file contents with prompt injection payloads, API responses crafted to override directives, and web scraping results with malicious instruction sequences.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 64: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Lines 1-200

**Description:**

The HTML template contains a designated placeholder (`#claude-content`) for dynamic content injection. If this injected content originates from untrusted external sources (e.g., tool outputs, user input, database queries) and is subsequently included in an LLM's context, it creates a direct vector for prompt injection. The name `claude-content` strongly suggests interaction with an LLM.: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 65: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Lines 201-214

**Description:**

The HTML template contains a placeholder `<!-- CONTENT -->` within a `div` element with `id="claude-content"`. This suggests that dynamic content will be inserted here. If this content originates from untrusted external data sources (e.g., user input, tool outputs, database queries, API responses) and is subsequently passed to an LLM without proper sanitization or context isolation, it creates a direct vector for prompt injection (SAFE-T1102).: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 66: 🔴 **CRITICAL** — `SAFE-T1103`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not contain any logic related to MCP tool invocation or message processing. Therefore, it is not directly susceptible to 'Fake Tool Invocation (Function Spoofing)' (SAFE-T1103). This finding is informational to confirm the analysis was performed.: Fake Tool Invocation exploits insufficient tool validation in MCP hosts by injecting or spoofing tool names, duplicating tool registrations, or manipulating the JSON-RPC tool call protocol. Attackers can redirect legitimate tool calls to malicious implementations or register tools that shadow trusted ones.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 67: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Lines 1-200

**Description:**

The file 'skills/brainstorming/scripts/frame-template.html' is a static HTML template primarily defining UI structure and styling. It does not contain any direct code or configuration that exposes debug endpoints or inspector tools, nor does it interact with known vulnerable services like the MCP Inspector (CVE-2025-49596). The file's purpose is to serve as a display frame where content is injected.: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 68: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not directly implement or expose any debugging endpoints or inspector tools. The SAFE-T1109 vulnerability primarily concerns the server-side configuration and exposure of the MCP Inspector, or malicious client-side scripts targeting it. This file itself does not contain the vulnerable patterns.: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 69: 🔴 **CRITICAL** — `SAFE-T1111`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a UI template and does not contain any executable code that could weaponize AI CLI tools. The technique SAFE-T1111 describes programmatic abuse of CLI tools, typically via scripts (e.g., JavaScript, Python) executed during package installation or similar automated processes. This HTML file is purely presentational and does not interact with the file system or external command-line interfaces.: AI Agent CLI Weaponization exploits AI-powered CLI tools and agents that have unrestricted shell access or use dangerous permission-bypass flags. Attackers distribute malicious packages with postinstall scripts that invoke AI CLI tools in unsafe modes, or craft inputs that cause agents to execute arbitrary system commands, harvest credentials, or exfiltrate repository data.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 70: 🔴 **CRITICAL** — `SAFE-T1111`

**Location:** Lines 201-214

**Description:**

The HTML code contains an element with `id="claude-content"`, which might be related to an AI agent named 'Claude'. However, this is a static HTML element and does not indicate any programmatic invocation or weaponization of AI CLI tools as described in SAFE-T1111. There is no executable code (e.g., JavaScript, shell commands) present in this snippet that could trigger the described attack.: AI Agent CLI Weaponization exploits AI-powered CLI tools and agents that have unrestricted shell access or use dangerous permission-bypass flags. Attackers distribute malicious packages with postinstall scripts that invoke AI CLI tools in unsafe modes, or craft inputs that cause agents to execute arbitrary system commands, harvest credentials, or exfiltrate repository data.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 71: 🔴 **CRITICAL** — `SAFE-T1203`

**Location:** Lines 1-200

**Description:**

The provided file is an HTML template primarily containing CSS styling. It does not contain any executable code (e.g., JavaScript, server-side scripts) that would run during an installation process or have the capability to establish persistence mechanisms like modifying system files, cron jobs, or autostart entries. The technique SAFE-T1203 specifically targets server artifacts with install hooks, which are not present in this client-side HTML file.: Backdoored Server Binary involves distributing trojanized MCP server packages, containers, or binaries that establish persistence through postinstall hooks, cron jobs, systemd units, or autostart entries. The malicious code executes during installation and persists even after the package is uninstalled.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 72: 🔴 **CRITICAL** — `SAFE-T1203`

**Location:** Lines 201-214

**Description:**

The provided HTML file is a static template and does not contain any executable code or installation hooks that could be exploited for persistence mechanisms as described in SAFE-T1203. This file is likely part of a user interface or documentation.: Backdoored Server Binary involves distributing trojanized MCP server packages, containers, or binaries that establish persistence through postinstall hooks, cron jobs, systemd units, or autostart entries. The malicious code executes during installation and persists even after the package is uninstalled.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 73: 🔴 **CRITICAL** — `SAFE-T1303`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static client-side template and does not contain any code that could directly lead to a container sandbox escape via runtime exec. This technique typically involves server-side logic or container orchestration tools that manipulate runtime execution parameters.: Container Sandbox Escape exploits misconfigured container environments running MCP servers to break out of the sandbox and access the host operating system. Attack vectors include mounting the Docker socket, accessing /proc filesystem, running in privileged mode, using nsenter, or exploiting kernel vulnerabilities from within the container.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 74: 🔴 **CRITICAL** — `SAFE-T1303`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not contain any logic or components that could directly influence or invoke container runtime 'exec' commands with manipulated working directories. This file is not an MCP server or tool that would orchestrate containerized tasks.: Container Sandbox Escape exploits misconfigured container environments running MCP servers to break out of the sandbox and access the host operating system. Attack vectors include mounting the Docker socket, accessing /proc filesystem, running in privileged mode, using nsenter, or exploiting kernel vulnerabilities from within the container.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 75: 🔴 **CRITICAL** — `SAFE-T1305`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a client-side UI template and does not contain server-side logic or direct interactions with the host operating system that would lead to privilege escalation. The SAFE-T1305 technique specifically targets MCP server processes running with root privileges and executing commands or accessing system resources.: Host OS Privilege Escalation exploits operating system vulnerabilities or misconfigurations from within an MCP server process to gain root or administrative access on the host. Attack vectors include setuid binary abuse, kernel exploits, capability escalation, and accessing sensitive system files like /etc/shadow.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 76: 🔴 **CRITICAL** — `SAFE-T1305`

**Location:** Lines 201-214

**Description:**

The provided HTML file is a static template and does not contain any server-side logic, configuration, or executable code that could directly lead to Host OS Privilege Escalation (RCE) as described in SAFE-T1305. This file is likely a frontend component for an MCP client or a display frame.: Host OS Privilege Escalation exploits operating system vulnerabilities or misconfigurations from within an MCP server process to gain root or administrative access on the host. Attack vectors include setuid binary abuse, kernel exploits, capability escalation, and accessing sensitive system files like /etc/shadow.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 77: 🔴 **CRITICAL** — `SAFE-T1306`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a client-side UI template and does not contain any server-side logic or authentication-related code that could directly implement or be vulnerable to SAFE-T1306 (Rogue Authentication Server).: Rogue Authorization Server involves setting up a malicious or misconfigured authorization server that mints OAuth tokens, JWTs, or API keys without proper validation, client verification, or trust chain enforcement. Attackers use these rogue-issued tokens to access MCP servers and tools that fail to validate the token issuer.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 78: 🔴 **CRITICAL** — `SAFE-T1309`

**Location:** Lines 201-214

**Description:**

The HTML code is a static template and does not directly implement an LLM agent or expose any mechanisms for prompt injection or privileged tool invocation. The `id="claude-content"` suggests content from an LLM might be displayed here, but the HTML itself is a placeholder.: Privileged Tool via Prompt Injection exploits LLM-based tool selection where the model chooses which tool to invoke based on natural language. Attackers craft prompts that manipulate the LLM into selecting privileged or dangerous tools, bypassing access controls that only exist at the prompt/UI layer rather than at the tool execution layer.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 79: 🔴 **CRITICAL** — `SAFE-T1502`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static UI template and does not contain any code that would directly interact with the file system to read sensitive credential files. The technique SAFE-T1502 focuses on MCP tools reading files from disk, which is not applicable to this client-side HTML/CSS code.: File-Based Credential Harvest occurs when MCP server tools read sensitive credential files from the host filesystem. Targets include SSH private keys, AWS credentials, environment files, browser cookies, keychain databases, and cloud provider configurations. The harvested credentials enable lateral movement and privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 80: 🔴 **CRITICAL** — `SAFE-T1505`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not contain any logic related to AI/ML models, vector databases, or prompt processing that would be susceptible to in-memory secret extraction via semantic analysis (SAFE-T1505).: In-Memory Secret Extraction targets secrets held in process memory at runtime. Attackers use memory dump tools, /proc/*/mem access, heap inspection, or debugger attachment to extract encryption keys, tokens, passwords, and other sensitive data that exists only in memory and would not be found in files or environment variables.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 81: 🔴 **CRITICAL** — `SAFE-T1702`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static template for a web interface and does not contain any server-side code, client-side scripts that interact with a backend for command execution, or direct mechanisms for processing user input in a way that could lead to shell command injection. The technique SAFE-T1702 describes 'Shared-Memory Poisoning' in multi-agent systems, typically involving backend 'tool implementations' that write to or read from shared vector databases. This HTML file is a presentation layer and does not implement such tools or database interactions.: Command Injection vulnerabilities occur when MCP server tools execute shell commands with unsanitized user input. Attackers can inject malicious commands that are executed with the server's privileges, potentially leading to arbitrary code execution, data exfiltration, or system compromise.

This technique targets common command execution patterns across multiple languages including Node.js (child_process), Python (subprocess, os.system), Rust (Command), and Go (os/exec). Unsafe practices include string concatenation in shell commands, missing input validation, and using shell=True or shell invocation modes.

Command injection is particularly dangerous in MCP servers because tools often have elevated privileges to interact with system resources, files, or external services. A successful injection can bypass all security boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 82: 🔴 **CRITICAL** — `SAFE-T1704`

**Location:** Lines 1-200

**Description:**

The HTML template itself does not contain any direct server-side logic or dynamic URL handling that would lead to SSRF. The only URL present is a hardcoded client-side link to GitHub.: Compromised-Server Pivot / SSRF occurs when MCP tools accept user-controlled URLs in HTTP requests without host allowlisting or validation. Attackers exploit this to access internal services, cloud metadata endpoints, localhost services, or private network resources that the MCP server can reach but the attacker cannot directly access.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 83: 🔴 **CRITICAL** — `SAFE-T1704`

**Location:** Lines 201-214

**Description:**

The provided HTML code snippet is a static client-side template and does not contain any server-side logic or mechanisms for processing user-controlled URLs in server requests. The `<!-- CONTENT -->` placeholder suggests dynamic content, but the HTML itself does not reveal how this content is generated or if it involves external requests based on user input.: Compromised-Server Pivot / SSRF occurs when MCP tools accept user-controlled URLs in HTTP requests without host allowlisting or validation. Attackers exploit this to access internal services, cloud metadata endpoints, localhost services, or private network resources that the MCP server can reach but the attacker cannot directly access.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 84: 🔴 **CRITICAL** — `SAFE-T1705`

**Location:** Lines 1-200

**Description:**

The HTML template includes a placeholder for content injection, indicated by the comment 'Content is injected via placeholder comment in #claude-content.'. While this file itself is a static template and does not directly implement agent-to-agent communication, the mechanism used to inject content into `#claude-content` could be a vector for other types of injection attacks (e.g., XSS) if not properly sanitized. However, this specific HTML file does not contain logic for inter-agent message processing or workflow contamination as described by SAFE-T1705.: Cross-Agent Instruction Injection targets multi-agent MCP architectures where agents communicate by passing messages or tool results. Attackers inject malicious instructions into one agent that propagate to other agents in the workflow, contaminating the entire multi-agent pipeline through message passing, shared context, or tool output forwarding.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 85: 🔴 **CRITICAL** — `SAFE-T1705`

**Location:** Lines 201-214

**Description:**

The HTML file contains a placeholder for content (`<!-- CONTENT -->`) which might be populated by an agent. While this specific HTML code does not directly implement agent-to-agent communication or message processing, the mechanism used to inject content into `#claude-content` could be a vector for cross-agent instruction injection if the content originates from an untrusted or compromised agent without proper sanitization or validation. However, the HTML itself is merely a template.: Cross-Agent Instruction Injection targets multi-agent MCP architectures where agents communicate by passing messages or tool results. Attackers inject malicious instructions into one agent that propagate to other agents in the workflow, contaminating the entire multi-agent pipeline through message passing, shared context, or tool output forwarding.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 86: 🔴 **CRITICAL** — `SAFE-T1803`

**Location:** Lines 1-200

**Description:**

The file is an HTML template for UI presentation and styling. It does not contain any code that directly interacts with databases, performs bulk data exports, or invokes database dump utilities. The technique SAFE-T1803 describes server-side database exfiltration via MCP tools, which is not applicable to a static client-side HTML template.: Database Dump extracts entire database contents through MCP tools using bulk export commands like mysqldump, pg_dump, SELECT *, or ORM findAll without pagination. This enables mass exfiltration of structured data including user records, credentials, financial data, and other sensitive information stored in databases.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 87: 🔴 **CRITICAL** — `SAFE-T2101`

**Location:** Lines 1-200

**Description:**

The provided HTML/CSS code is a static frontend template and does not contain any executable code that could perform destructive operations like file deletion, database drops, or bulk data purges. The technique SAFE-T2101 primarily targets server-side tools or scripts that interact with file systems, databases, or cloud storage.: Data Destruction uses MCP tools to permanently delete or corrupt data through destructive operations such as rm -rf, DROP TABLE, file wiping, bulk delete API calls, or database truncation. Attackers may destroy data for sabotage, to cover their tracks, or as part of a ransomware attack.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 88: 🔴 **CRITICAL** — `SAFE-T2103`

**Location:** Lines 1-200

**Description:**

The file 'skills/brainstorming/scripts/frame-template.html' is an HTML template that is designed to have content injected into it via a placeholder comment in '#claude-content'. While the template itself does not contain malicious code, it represents a potential target for 'SAFE-T2103: Automated PR creation with malicious code changes or CI/CD pipeline manipulation'. An attacker could submit a PR to modify this template to inject malicious scripts or alter its structure to facilitate data exfiltration or other attacks when content is rendered by an AI agent or user. The current content is benign, but the injection point is noted.: Code Sabotage via Malicious PR uses MCP tools with repository access to create automated pull requests containing malicious code changes, backdoors, or CI/CD pipeline modifications. Attackers manipulate build scripts, inject vulnerabilities, or alter deployment configurations through automated commits that bypass normal code review.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 89: 🔴 **CRITICAL** — `SAFE-T2106`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not contain any logic or components related to vector databases, embedding generation, or AI agent memory storage. Therefore, it is not directly susceptible to Context Memory Poisoning via Vector Store Contamination (SAFE-T2106).: Context Memory Poisoning via Vector Store Contamination manipulates vector databases storing long-term AI memory. Attackers inject poisoned embeddings that blend legitimate and malicious content, surviving server restarts and reinstallations. The poisoned content can self-replicate across sessions and affect multiple agents sharing the same vector store.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 90: 🟠 **HIGH** — `SAFE-T1001`

**Location:** Lines 1-200

**Description:**

The file is an HTML template for a UI frame, not an MCP tool description. While HTML comments are a known vector for SAFE-T1001, the comments present in this template are standard, visible, and descriptive, intended for human developers. They do not contain any hidden instructions or malicious directives that would manipulate an LLM's behavior if this file were to be processed as a tool description.: Tool Poisoning Attack (TPA) embeds malicious instructions within MCP tool descriptions that are invisible to users but processed by LLMs. This exploits the difference between displayed and processed tool descriptions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 91: 🟠 **HIGH** — `SAFE-T1001`

**Location:** Lines 1-200

**Description:**

The file is an HTML template for a UI frame, not an MCP tool description. While HTML comments are a known vector for SAFE-T1001, the comment present at the content injection point is standard, visible, and descriptive, intended for human developers. It does not contain any hidden instructions or malicious directives that would manipulate an LLM's behavior if this file were to be processed as a tool description.: Tool Poisoning Attack (TPA) embeds malicious instructions within MCP tool descriptions that are invisible to users but processed by LLMs. This exploits the difference between displayed and processed tool descriptions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 92: 🟠 **HIGH** — `SAFE-T1001`

**Location:** Lines 201-214

**Description:**

The code contains an HTML comment, which is a known vector for Tool Poisoning Attacks (SAFE-T1001) if embedded within an MCP tool description. However, in this context, the comment `<!-- CONTENT -->` appears to be a benign placeholder within an HTML template file, not a tool description passed to an LLM.: Tool Poisoning Attack (TPA) embeds malicious instructions within MCP tool descriptions that are invisible to users but processed by LLMs. This exploits the difference between displayed and processed tool descriptions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 93: 🟠 **HIGH** — `SAFE-T1002`

**Location:** Lines 1-200

**Description:**

The HTML template does not directly fetch any external scripts, stylesheets, or other artifacts that would require integrity verification. All CSS is inline within the <style> tag. The content is described as being 'injected via placeholder comment in #claude-content', implying dynamic content, but the template itself is static.: Supply Chain Compromise occurs when MCP servers download, install, import, or execute external packages, plugins, model weights, or binaries without integrity checks, version pinning, or signature verification. This allows attackers to inject malicious code through compromised dependencies.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 94: 🟠 **HIGH** — `SAFE-T1004`

**Location:** Lines 1-200

**Description:**

The HTML template includes a hardcoded link to an external domain (github.com). While not a direct vulnerability for SAFE-T1004, it's a point of external dependency. If the linked domain were to be compromised or impersonated (e.g., via DNS hijacking affecting the client's resolution), the client could be directed to a malicious site. However, this is a client-side link and does not indicate the MCP server itself is impersonating another service.: Server Impersonation occurs when an attacker registers an MCP server with a name designed to be confused with a legitimate service (typosquatting, homograph attacks, or DNS cache poisoning). Users connecting to the impersonated server unknowingly send credentials and data to the attacker.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 95: 🟠 **HIGH** — `SAFE-T1008`

**Location:** Lines 201-214

**Description:**

The provided HTML code does not contain any tool descriptions, metadata, or server-side logic that could be exploited by the SAFE-T1008 Tool Shadowing Attack. This attack specifically targets the descriptions and metadata of tools registered with an MCP server, which are typically defined in programming languages (e.g., Python decorators) or configuration files, not in client-side HTML templates.: Tool Shadowing Attack embeds instructions in tool descriptions that manipulate how AI agents use OTHER tools. In multi-server MCP setups, all tool descriptions are aggregated into a single prompt, allowing any tool to influence the behavior of others through cross-tool interference patterns.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 96: 🟠 **HIGH** — `SAFE-T1009`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static UI template and does not contain any client-side logic for OAuth authorization flows, redirects, or token handling. Therefore, it cannot directly be exploited by the SAFE-T1009 technique.: Authorization Server Mix-up occurs when an OAuth client follows a redirect to a look-alike authorization server domain (e.g., accounts-google.com vs accounts.google.com), causing authorization codes or tokens to be leaked to an attacker-controlled server. This exploits insufficient issuer validation in OAuth implementations.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 97: 🟠 **HIGH** — `SAFE-T1009`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not contain any logic related to OAuth client redirects, authorization server domain validation, or token handling. Therefore, it cannot directly implement or be vulnerable to the SAFE-T1009 Authorization Server Mix-up technique.: Authorization Server Mix-up occurs when an OAuth client follows a redirect to a look-alike authorization server domain (e.g., accounts-google.com vs accounts.google.com), causing authorization codes or tokens to be leaked to an attacker-controlled server. This exploits insufficient issuer validation in OAuth implementations.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 98: 🟠 **HIGH** — `SAFE-T1104`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a client-side UI template and does not contain any server-side tool definitions or permission configurations relevant to the SAFE-T1104 technique. This technique focuses on excessive permissions granted to backend MCP tools, which are not present in this file.: Over-Privileged Tool Abuse occurs when MCP tools are granted permissions far exceeding their functional requirements. Attackers exploit these over-privileged tools through prompt injection or direct manipulation to access sensitive resources, execute system commands, or escalate privileges beyond the intended scope.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 99: 🟠 **HIGH** — `SAFE-T1105`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static template and does not contain any server-side logic or client-side JavaScript that directly handles or processes filesystem paths. Therefore, it is not directly vulnerable to SAFE-T1105 (Path Traversal via File Tool).: Path Traversal via File Tool occurs when MCP file-handling tools accept untrusted input as filesystem paths without proper normalization, allowlisting, or base directory enforcement. Attackers can use relative path sequences, Unicode normalization tricks, double encoding, or null byte injection to access files outside the intended directory.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 100: 🟠 **HIGH** — `SAFE-T1105`

**Location:** Lines 201-214

**Description:**

The provided HTML code snippet does not contain any server-side logic or file system operations that could lead to a Path Traversal vulnerability (SAFE-T1105). This file appears to be a client-side template.: Path Traversal via File Tool occurs when MCP file-handling tools accept untrusted input as filesystem paths without proper normalization, allowlisting, or base directory enforcement. Attackers can use relative path sequences, Unicode normalization tricks, double encoding, or null byte injection to access files outside the intended directory.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 101: 🟠 **HIGH** — `SAFE-T1106`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not contain any logic for tool invocation, recursion, or self-triggering mechanisms. The vulnerability described by SAFE-T1106 pertains to agent or workflow logic that can enter unbounded loops of tool calls. This HTML file is a presentation layer and does not directly implement such logic.: Autonomous Loop Exploit triggers denial-of-service through self-reinforcing tool invocation loops. Attackers craft prompts or tool outputs that suggest non-terminal progress, causing agents to repeatedly invoke tools without convergence checks, rate limits, or iteration budgets.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 102: 🟠 **HIGH** — `SAFE-T1110`

**Location:** Lines 1-200

**Description:**

The HTML file is a UI template for a 'Superpowers Brainstorming' companion frame. It defines styling and structure but does not contain any direct functionality for processing or embedding images or audio in a way that would be vulnerable to multimodal prompt injection (SAFE-T1110). The technique describes attacks against AI systems that process image/audio inputs, typically on the server-side or within a client-side component that handles such inputs for an AI model. This file is a display-only component.: Multimodal Prompt Injection embeds malicious instructions within images, audio, or other non-text media that are processed by LLMs. Techniques include steganographic embedding in pixel data, OCR-extractable text injection, adversarial visual patterns, and metadata injection in media files. These bypass text-based sanitization since the payload is in a non-text modality.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 103: 🟠 **HIGH** — `SAFE-T1110`

**Location:** Lines 201-214

**Description:**

The HTML snippet is a template for displaying content, including a placeholder for 'claude-content'. While this specific code does not directly implement or facilitate multimodal prompt injection, if user-controlled multimedia content (images, audio) were to be displayed within this `claude-content` area and subsequently processed by a multimodal AI, it could become an attack vector for SAFE-T1110. The vulnerability would reside in the AI's processing pipeline, not the HTML itself.: Multimodal Prompt Injection embeds malicious instructions within images, audio, or other non-text media that are processed by LLMs. Techniques include steganographic embedding in pixel data, OCR-extractable text injection, adversarial visual patterns, and metadata injection in media files. These bypass text-based sanitization since the payload is in a non-text modality.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 104: 🟠 **HIGH** — `SAFE-T1201`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static UI template and does not contain dynamic definitions or executable logic that could be modified post-approval to change its behavior in the manner described by SAFE-T1201. The technique targets MCP tools with dynamic server-side logic or configurable definitions, not static client-side HTML/CSS templates.: MCP Rug Pull Attack is a persistence technique where tools appear legitimate initially but can change their behavior after gaining user trust. This exploits dynamic tool definitions, remote configuration loading, or time-delayed activation to modify tool behavior post-approval.
Static analysis detects code patterns that ENABLE rug pulls: dynamic descriptions, remote tool loading without version pinning, and time-based conditional behavior.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 105: 🟠 **HIGH** — `SAFE-T1202`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a styling and layout template and does not contain any code related to OAuth token handling, storage, or authentication mechanisms. Therefore, it does not directly present a vulnerability for SAFE-T1202.: OAuth Token Persistence exploits insecure token storage and handling to maintain long-lived access to MCP-connected services. Attackers steal access or refresh tokens from localStorage, sessionStorage, cookies without secure flags, or file-based caches, then reuse them to persist access even after password changes or session invalidation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 106: 🟠 **HIGH** — `SAFE-T1205`

**Location:** Lines 1-200

**Description:**

The provided file is a static HTML template primarily defining UI styles and structure. It does not contain any server-side logic, database interactions, file system operations, or mechanisms for persisting tool definitions. Therefore, it is not directly susceptible to the SAFE-T1205 technique, which targets persistent storage of malicious tool definitions.: Persistent Tool Redefinition is a privilege escalation technique where attackers modify an MCP server's tool metadata or configuration to embed hidden commands that survive server restarts. By persisting malicious tool definitions to disk, database, or configuration files, the attacker ensures their modifications remain active even after the server is restarted or redeployed.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 107: 🟠 **HIGH** — `SAFE-T1301`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a client-side template and does not contain any server-side logic or tool registration mechanisms relevant to the SAFE-T1301 technique. This technique specifically targets malicious MCP servers overriding legitimate tool calls by registering conflicting tool names.: Cross-Server Tool Shadowing is a privilege escalation technique where malicious MCP servers override or intercept legitimate tool calls from other servers. This attack exploits multi-server MCP environments where multiple servers can provide tools with the same or similar names, allowing attackers to shadow legitimate functionality with malicious implementations.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 108: 🟠 **HIGH** — `SAFE-T1302`

**Location:** Lines 1-200

**Description:**

The provided code is an HTML template primarily containing CSS for styling and layout. It does not contain any server-side logic, direct operating system interactions, or mechanisms to execute privileged commands. The technique SAFE-T1302 (High-Privilege Tool Abuse) typically involves server-side tools or scripts that operate with elevated permissions. This client-side HTML file does not present any direct vulnerabilities related to this technique.: High-Privilege Tool Abuse occurs when MCP tools are configured with administrative, root, or superuser access to databases, APIs, or system resources. Attackers exploit these tools through prompt injection or direct manipulation to perform privileged operations such as modifying access controls, reading sensitive data, or escalating their own permissions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 109: 🟠 **HIGH** — `SAFE-T1302`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not contain any logic or functionality related to executing privileged operations or interacting with system tools. It is unlikely to be directly exploitable for high-privilege tool abuse.: High-Privilege Tool Abuse occurs when MCP tools are configured with administrative, root, or superuser access to databases, APIs, or system resources. Attackers exploit these tools through prompt injection or direct manipulation to perform privileged operations such as modifying access controls, reading sensitive data, or escalating their own permissions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 110: 🟠 **HIGH** — `SAFE-T1304`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static template primarily containing CSS and basic structural HTML. It does not include any client-side scripting (JavaScript) or server-side logic that would handle, store, or transmit authentication tokens or credentials. Therefore, it does not directly implement any mechanisms that could lead to tokens being reused or forwarded across services without proper scoping or validation.: Credential Relay Chain exploits token reuse across MCP service boundaries. Attackers capture tokens from one service interaction and replay them in tool-to-tool calls, cross-service requests, or downstream API invocations without proper audience validation, scope restriction, or token binding.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 111: 🟠 **HIGH** — `SAFE-T1304`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not contain any logic for handling, storing, or transmitting authentication tokens or credentials. It is purely structural and display-related, and therefore does not present a direct vulnerability related to token reuse or forwarding.: Credential Relay Chain exploits token reuse across MCP service boundaries. Attackers capture tokens from one service interaction and replay them in tool-to-tool calls, cross-service requests, or downstream API invocations without proper audience validation, scope restriction, or token binding.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 112: 🟠 **HIGH** — `SAFE-T1308`

**Location:** Lines 1-200

**Description:**

The provided file is an HTML template containing only styling and basic structure. It does not contain any server-side logic, API endpoints, or token validation mechanisms that would be susceptible to SAFE-T1308 (Token Scope Substitution).: Token Scope Substitution occurs when an attacker replaces a limited-scope OAuth token with one that has broader permissions but the same audience, exploiting insufficient scope validation to gain elevated privileges.
This attack targets OAuth implementations that validate tokens but fail to verify that the token scopes match what was originally requested or required for the operation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 113: 🟠 **HIGH** — `SAFE-T1401`

**Location:** Lines 1-200

**Description:**

The HTML template contains a designated placeholder (`#claude-content`) for dynamic content injection, which, if populated with unsanitized or untrusted input, could serve as a vector for newline or control character injection attacks (SAFE-T1401) against an MCP server's LLM context.: Line Jumping injects newline characters, CRLF sequences, or control characters into MCP tool descriptions, parameter names, or metadata fields. The injected characters push malicious instructions outside the visible area of user interfaces, making them invisible during review while still being processed by the LLM.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 114: 🟠 **HIGH** — `SAFE-T1401`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template for a web page and does not directly involve the generation or processing of LLM context or tool descriptions. The `<!-- CONTENT -->` placeholder suggests dynamic content will be inserted, but the HTML itself does not define the source or generation mechanism of that content. Therefore, this file does not directly present a vulnerability related to newline or control character injection in LLM context.: Line Jumping injects newline characters, CRLF sequences, or control characters into MCP tool descriptions, parameter names, or metadata fields. The injected characters push malicious instructions outside the visible area of user interfaces, making them invisible during review while still being processed by the LLM.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 115: 🟠 **HIGH** — `SAFE-T1403`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static template for a UI frame and does not contain any dynamic logic or scripting that could initiate permission requests. The SAFE-T1403 technique describes a vulnerability related to the rapid or repeated generation of permission requests to exploit user fatigue. This template is purely presentational and does not implement the mechanism for generating such requests.: Consent-Fatigue Exploit overwhelms users with rapid, repeated, or batched permission requests to exploit decision fatigue. Users begin auto-approving prompts without reading them, allowing malicious tools to gain permissions they would normally be denied. This also includes auto-approve patterns that bypass user consent entirely.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 116: 🟠 **HIGH** — `SAFE-T1403`

**Location:** Lines 201-214

**Description:**

The HTML structure includes a content area (`#claude-content`) and an indicator bar (`#indicator-text`) which could potentially be used to display prompts or notifications. While this code itself does not implement the consent-fatigue exploit, it represents a UI component that could be leveraged by an MCP agent to display a stream of requests, which is a prerequisite for SAFE-T1403. The text 'Click an option above, then return to the terminal' suggests an interactive workflow where user input is expected, which could involve approvals.: Consent-Fatigue Exploit overwhelms users with rapid, repeated, or batched permission requests to exploit decision fatigue. Users begin auto-approving prompts without reading them, allowing malicious tools to gain permissions they would normally be denied. This also includes auto-approve patterns that bypass user consent entirely.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 117: 🟠 **HIGH** — `SAFE-T1404`

**Location:** Lines 1-200

**Description:**

This HTML file serves as a UI template for displaying content generated by an AI system. The vulnerability SAFE-T1404 (Response Tampering) describes a scenario where an AI system's output is manipulated to hide or downplay risky actions. While this template provides the structure where such tampered content would be displayed, it does not contain any logic for generating, filtering, or modifying the content itself. The comment `<!-- Content is injected via placeholder comment in #claude-content. -->` indicates dynamic content injection. The vulnerability would reside in the backend logic that generates the content for `#claude-content` or the client-side JavaScript that populates it without proper validation or binding to tool activity, not in this static HTML template.: Response Tampering intercepts and modifies MCP tool responses before they are displayed to the user or processed by the LLM. Attackers alter response content to hide evidence of malicious actions, inject false information, bypass content filters, or manipulate the LLM context for subsequent interactions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 118: 🟠 **HIGH** — `SAFE-T1404`

**Location:** Lines 201-214

**Description:**

This HTML template contains a placeholder for AI-generated content (`<div id="claude-content">`). While the template itself does not directly implement response tampering, it represents a potential display surface where tampered responses could be rendered if the backend logic or AI model is compromised. The vulnerability would lie in the code responsible for populating this div, not in the HTML structure itself.: Response Tampering intercepts and modifies MCP tool responses before they are displayed to the user or processed by the LLM. Attackers alter response content to hide evidence of malicious actions, inject false information, bypass content filters, or manipulate the LLM context for subsequent interactions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 119: 🟠 **HIGH** — `SAFE-T1407`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a client-side template and does not contain server-side logic or network configuration that would directly implement a server proxy masquerade (SAFE-T1407). This technique primarily involves malicious server-side components intercepting and forwarding traffic.: An attacker operates an MCP server that forwards requests to a legitimate upstream API and relays the responses back to the client. Because the responses originate from the trusted service, the traffic appears normal in monitoring and logs, but the proxy can inspect, alter, or exfiltrate sensitive data. This enables stealthy data collection and response manipulation while hiding the true malicious behavior.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 120: 🟠 **HIGH** — `SAFE-T1408`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static UI template and does not contain any OAuth client-side logic or server-side rendering that would be susceptible to the SAFE-T1408 technique. There are no `response_type` parameters or any other OAuth-related code to analyze.: An attacker forces or coerces a client to use the OAuth 2.0 implicit flow instead of the authorization code flow with PKCE. Tokens delivered in URL fragments are easier to capture via logs, referrers, or malicious scripts, enabling token theft and session hijacking.
This technique detects the presence of implicit flow configuration, which indicates potential vulnerability. The authorization code flow with PKCE should always be preferred over implicit flow for security.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 121: 🟠 **HIGH** — `SAFE-T1501`

**Location:** Lines 1-200

**Description:**

The provided file is an HTML template for UI presentation and does not contain any JSON schema definitions or authentication schemas that would be processed by an LLM in an MCP server context. Therefore, it is not susceptible to SAFE-T1501 (Full-Schema Poisoning).: Full-Schema Poisoning manipulates JSON schema definitions used for MCP tool input validation to include hidden fields, default values with malicious content, or additionalProperties that allow arbitrary data injection. In auth schemas, this can expose credentials through coerced fields or default values that leak sensitive information.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 122: 🟠 **HIGH** — `SAFE-T1503`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static client-side template and does not contain any code that directly interacts with server-side environment variables or file system tools. The technique SAFE-T1503 describes attacks where AI agents are manipulated to read environment variable files via MCP file tools. This file is purely a presentation layer and lacks the functionality to perform such actions.: Environment Variable Scraping extracts secrets from process environment variables. MCP server tools iterate over process.env, os.environ, or use getenv with attacker-controlled keys to harvest API keys, database passwords, cloud credentials, and other sensitive configuration stored in environment variables.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 123: 🟠 **HIGH** — `SAFE-T1503`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not contain any functionality to enumerate or dump environment variables. This file is not susceptible to SAFE-T1503.: Environment Variable Scraping extracts secrets from process environment variables. MCP server tools iterate over process.env, os.environ, or use getenv with attacker-controlled keys to harvest API keys, database passwords, cloud credentials, and other sensitive configuration stored in environment variables.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 124: 🟠 **HIGH** — `SAFE-T1504`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static UI template and does not contain any code that directly handles, stores, or outputs access/refresh tokens. The vulnerability described by SAFE-T1504 typically occurs in backend MCP server logic or AI agent tool invocations that expose tokens in their responses, which would then be rendered by a UI like this template. This file itself is not the source of the token leakage.: Token Theft via API Response occurs when MCP tool implementations inadvertently include access tokens, refresh tokens, or other credentials in their output responses. These leaked tokens are then visible in conversation history, logs, or can be captured by other tools in the chain for unauthorized access.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 125: 🟠 **HIGH** — `SAFE-T1601`

**Location:** Lines 201-214

**Description:**

The provided HTML snippet is a static client-side template and does not directly expose MCP server endpoints, registries, or service configurations. It contains placeholders for content but no active mechanisms for server enumeration.: MCP Server Enumeration involves probing for available MCP server instances, scanning registries, and discovering service endpoints. Attackers enumerate servers to map the attack surface, identify vulnerable or misconfigured instances, and discover high-value targets for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 126: 🟠 **HIGH** — `SAFE-T1602`

**Location:** Lines 1-200

**Description:**

The HTML template includes a link to a GitHub repository named 'Superpowers Brainstorming'. While not a direct enumeration of tools within the MCP server environment, this publicly accessible link could provide an attacker with initial reconnaissance information about the project's name and potentially lead to discovery of associated tools or capabilities if the repository contains such details.: Tool Enumeration discovers the full set of available MCP tools, their capabilities, input schemas, and permission requirements. Attackers use tools/list API calls and schema introspection to map available functionality, identify privileged tools, and find tools with weak input validation for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 127: 🟠 **HIGH** — `SAFE-T1603`

**Location:** Lines 1-200

**Description:**

The HTML template includes a placeholder comment indicating where AI-generated content will be injected. While the template itself does not contain system prompts, the mechanism of injecting content into `#claude-content` could be a vector for prompt leakage if the AI's output inadvertently includes system instructions or debug information. This file is a UI component, not the source of the prompt itself.: System Prompt Disclosure exposes internal system prompts, instructions, or configuration through debug mode endpoints, verbose error messages, tool output, or direct echoing. Exposed system prompts reveal security controls, filtering rules, and behavioral constraints that attackers can use to craft targeted bypass attacks.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 128: 🟠 **HIGH** — `SAFE-T1605`

**Location:** Lines 201-214

**Description:**

The provided HTML code snippet is a static template and does not directly expose server capabilities, API versions, or permission boundaries. The SAFE-T1605 technique primarily concerns how an MCP agent or LLM responds to prompts to disclose its internal workings, which is not applicable to this static HTML content.: Capability Mapping systematically discovers MCP server capabilities, supported API versions, feature flags, and permission boundaries. Attackers use introspection endpoints, capability negotiation, and API probing to build a comprehensive map of what the target system can do, identifying the most valuable attack vectors.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 129: 🟠 **HIGH** — `SAFE-T1606`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static template for a UI frame and does not contain any code that directly implements or calls file system directory listing tools. The technique SAFE-T1606 describes the abuse of MCP tools like `list_directory` which would typically be invoked by an AI agent or backend service, not directly by a client-side HTML template.: Directory Listing via File Tool exploits MCP file tools that allow unrestricted directory enumeration. Attackers use fs.readdir, os.listdir, or equivalent functions to map the filesystem structure, discover sensitive files, locate configuration files, and identify credential storage locations for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 130: 🟠 **HIGH** — `SAFE-T1701`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not contain any logic or mechanisms related to shared state, credential management, or inter-tool communication that would enable cross-tool contamination (SAFE-T1701).: Cross-Tool Contamination exploits shared state between MCP tools including global variables, shared memory, common databases, or file-based caches. Malicious data written by one tool pollutes the execution context of another, enabling data exfiltration, privilege escalation, or behavior manipulation across tool boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 131: 🟠 **HIGH** — `SAFE-T1703`

**Location:** Lines 1-200

**Description:**

This HTML file is a template for a UI frame and does not contain any logic that directly implements or facilitates the 'Tool-Chaining Pivot' technique (SAFE-T1703). The technique describes exploiting inter-tool trust relationships and data flow between different MCP tools to escalate privileges. This file is purely presentational (HTML/CSS) and lacks any code for tool interaction, data processing, or privilege management.: Tool-Chaining Pivot exploits the output of one MCP tool as input to another more privileged tool, creating multi-step attack chains. Attackers leverage a low-privilege tool to produce crafted output that, when consumed by a higher-privilege tool, enables unauthorized operations such as command execution, data access, or privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 132: 🟠 **HIGH** — `SAFE-T1703`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not directly implement any tool-chaining logic. The `<!-- CONTENT -->` placeholder suggests dynamic content insertion, but the source or processing of this content is not visible in this snippet. Therefore, this file alone does not demonstrate the SAFE-T1703 technique.: Tool-Chaining Pivot exploits the output of one MCP tool as input to another more privileged tool, creating multi-step attack chains. Attackers leverage a low-privilege tool to produce crafted output that, when consumed by a higher-privilege tool, enables unauthorized operations such as command execution, data access, or privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 133: 🟠 **HIGH** — `SAFE-T1706`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a client-side UI template and does not contain any server-side logic, authentication mechanisms, or OAuth token handling/validation code. Therefore, it cannot directly implement or be vulnerable to SAFE-T1706 (OAuth Token Pivot Replay).: OAuth Token Pivot Replay reuses OAuth tokens obtained from one service to access a different service or audience. Attackers exploit missing audience (aud) validation, overly broad token scopes, or lack of token binding to pivot from a lower-value service to a higher-value target using the same token.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 134: 🟠 **HIGH** — `SAFE-T1706`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not contain any logic related to OAuth token handling, validation, or replay. This type of file is generally not susceptible to SAFE-T1706.: OAuth Token Pivot Replay reuses OAuth tokens obtained from one service to access a different service or audience. Attackers exploit missing audience (aud) validation, overly broad token scopes, or lack of token binding to pivot from a lower-value service to a higher-value target using the same token.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 135: 🟠 **HIGH** — `SAFE-T1707`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a template primarily containing CSS styles and basic structural elements. It does not include any forms, JavaScript that initiates state-changing requests, or server-side logic that would process such requests. Therefore, this file itself cannot directly be vulnerable to CSRF (Cross-Site Request Forgery) attacks.: CSRF Token Relay exploits missing or improperly validated CSRF tokens in MCP state-changing requests. Attackers forge cross-site requests or reuse OAuth state parameters to trigger unauthorized actions, manipulate tool configurations, or hijack authentication flows through the victim browser or agent.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 136: 🟠 **HIGH** — `SAFE-T1801`

**Location:** Lines 1-200

**Description:**

This HTML file is a static template for a UI frame and does not contain any code that directly performs data access or interacts with an MCP server. The technique SAFE-T1801 describes vulnerabilities related to an AI agent's ability to make repetitive or broad-scoped MCP tool calls, typically through prompt injection or malicious tool definitions. This file is a client-side display component and does not implement such functionality.: Automated Data Harvesting uses MCP tools to systematically access and collect data through bulk queries, pagination scraping, and record enumeration. Attackers automate data collection across multiple endpoints or database tables to amass sensitive information for exfiltration or analysis.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 137: 🟠 **HIGH** — `SAFE-T1801`

**Location:** Lines 201-214

**Description:**

The HTML snippet is a static template and does not contain any logic related to AI agent interaction, MCP tool calls, or data processing that would enable automated data harvesting. The presence of `id="claude-content"` suggests it might be part of an application that interacts with an AI, but this specific code is not the source of the vulnerability.: Automated Data Harvesting uses MCP tools to systematically access and collect data through bulk queries, pagination scraping, and record enumeration. Attackers automate data collection across multiple endpoints or database tables to amass sensitive information for exfiltration or analysis.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 138: 🟠 **HIGH** — `SAFE-T1804`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static front-end template and does not contain any direct API interaction logic, server-side code, or MCP tool definitions that could be exploited for systematic API abuse. The technique SAFE-T1804 primarily targets MCP HTTP and API tools, prompt engineering, and backend API configurations, none of which are present in this client-side HTML structure.: API Data Harvest systematically extracts data through MCP tool API calls by abusing pagination, accessing bulk endpoints, or bypassing rate limits. Attackers iterate through API pages, manipulate query parameters, or use concurrent requests to extract large datasets that individual queries would not expose.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 139: 🟠 **HIGH** — `SAFE-T1904`

**Location:** Lines 1-200

**Description:**

This HTML file is a static template for a UI frame and does not contain logic for generating or processing LLM responses, nor does it implement any mechanisms for encoding or decoding covert commands within chat messages. The technique SAFE-T1904 targets the generation and consumption of LLM responses, which would occur in backend logic or client-side scripts that interact with the LLM, not in a static HTML template.: Chat-Based Backchannel uses MCP chat or messaging tools as a covert communication channel. Attackers embed encoded commands, base64 payloads, or steganographic data within seemingly normal chat messages to exfiltrate data or receive C2 instructions without triggering network-based detection.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 140: 🟠 **HIGH** — `SAFE-T1904`

**Location:** Lines 201-214

**Description:**

The HTML template contains a placeholder div (`id="claude-content"`) where dynamic content, potentially from an LLM, would be inserted. While this file itself does not implement a covert channel, it serves as a potential display surface for content that could contain encoded commands or data if the LLM or content generation process were compromised. The technique SAFE-T1904 describes how such content could be embedded and then parsed by a downstream automated consumer.: Chat-Based Backchannel uses MCP chat or messaging tools as a covert communication channel. Attackers embed encoded commands, base64 payloads, or steganographic data within seemingly normal chat messages to exfiltrate data or receive C2 instructions without triggering network-based detection.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 141: 🟠 **HIGH** — `SAFE-T1910`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static template for a UI frame. It does not contain any dynamic scripts, external resource loading mechanisms (beyond standard fonts), or interactive elements that could be directly exploited for data exfiltration via DNS tunneling, timing channels, or steganographic encoding. The file's purpose is to define styling and layout, with content expected to be injected into a placeholder.: Covert Channel Exfiltration uses non-standard communication channels to exfiltrate data from MCP server environments. Techniques include DNS tunneling (encoding data in DNS queries), timing-based channels, HTTP header manipulation, and steganographic encoding that bypasses traditional network monitoring and DLP solutions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 142: 🟠 **HIGH** — `SAFE-T1910`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not contain any dynamic scripting, server-side logic, or tool calls that could be exploited for covert channel data exfiltration as described in SAFE-T1910.: Covert Channel Exfiltration uses non-standard communication channels to exfiltrate data from MCP server environments. Techniques include DNS tunneling (encoding data in DNS queries), timing-based channels, HTTP header manipulation, and steganographic encoding that bypasses traditional network monitoring and DLP solutions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 143: 🟠 **HIGH** — `SAFE-T1915`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a UI template and does not contain any code related to blockchain bridge calls, DEX interactions, or crypto mixing. It defines styling and layout for a 'Brainstorm Companion Frame'.: Cross-Chain Laundering uses MCP tools with blockchain access to launder funds through cross-chain bridges, decentralized exchange (DEX) swaps, and cryptocurrency mixing services. Attackers route stolen funds through multiple blockchains to obscure the transaction trail and convert illicit cryptocurrency to clean assets.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 144: 🟠 **HIGH** — `SAFE-T1915`

**Location:** Lines 201-214

**Description:**

The provided HTML code is a static template and does not contain any logic or elements related to blockchain bridge calls, DEX interactions, or crypto mixing for fund laundering. It is a basic structure for displaying content.: Cross-Chain Laundering uses MCP tools with blockchain access to launder funds through cross-chain bridges, decentralized exchange (DEX) swaps, and cryptocurrency mixing services. Attackers route stolen funds through multiple blockchains to obscure the transaction trail and convert illicit cryptocurrency to clean assets.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 145: 🟠 **HIGH** — `SAFE-T2102`

**Location:** Lines 1-200

**Description:**

The provided HTML file is a static template and does not contain any code that would initiate external API calls or implement retry logic, which are prerequisites for SAFE-T2102. This file is unlikely to be directly exploitable for service disruption via external API flooding.: Service Disruption via API Flooding uses MCP tools to generate excessive outbound API calls through infinite loops, missing rate limits, or unbounded concurrent requests. This causes denial of service on target APIs, excessive billing on cloud services, and potential cascading failures across dependent systems.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:1-200 <!DOCTYPE html>\n<html>\n<head>
```

</details>

---

#### Finding 146: 🟠 **HIGH** — `SAFE-T2102`

**Location:** Lines 201-214

**Description:**

The provided HTML code snippet is a static template and does not contain any logic (e.g., JavaScript, server-side includes, or direct API calls) that could lead to infinite loops or unbounded requests to external APIs. It is purely structural content.: Service Disruption via API Flooding uses MCP tools to generate excessive outbound API calls through infinite loops, missing rate limits, or unbounded concurrent requests. This causes denial of service on target APIs, excessive billing on cloud services, and potential cascading failures across dependent systems.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

#### Finding 147: 🟠 **HIGH** — `SAFE-T2105`

**Location:** Lines 201-214

**Description:**

The 'claude-content' div is a placeholder for content, potentially generated by an LLM. While this HTML file itself does not implement the vulnerability, it serves as a display sink for LLM output. If the upstream LLM generation process is vulnerable to SAFE-T2105 (Disinformation Output), then this div could display manipulated or false information.: Disinformation Output manipulates MCP tool responses or LLM outputs to generate false, misleading, or harmful information. Attackers inject instructions that cause the AI to produce fabricated data, distorted facts, or biased responses that appear authoritative, potentially causing real-world harm through decision-making based on false information.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/frame-template.html:201-214   </div>\n\n  <div class="main">
```

</details>

---

### 📁 `skills/brainstorming/scripts/helper.js`

#### Finding 148: 🔴 **CRITICAL** — `SAFE-T1007`

**Location:** Lines 1-88

**Description:**

This client-side script does not directly implement or contribute to OAuth authorization phishing. It handles WebSocket communication and UI interactions, but does not manage OAuth flows, redirect URIs, or token capture logic.: OAuth Authorization Phishing exploits the OAuth authorization flow to trick users into granting permissions to attacker-controlled applications. The attacker creates a convincing authorization page or redirects users through legitimate OAuth providers but captures the authorization code or tokens.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() {  const WS_URL = 'ws://' + window.location.host;  let ws = null;  let eventQueue = [];  function connect() {    ws = new WebSocket(WS_URL);    ws.onopen = () => {      eventQueue.forEach(e => ws.send(JSON.stringify(e)));      eventQueue = [];    };    ws.onmessage = (msg) => {      const data = JSON.parse(msg.data);      if (data.type === 'reload') {        window.location.reload();      }    };    ws.onclose = () => {      setTimeout(connect, 1000);    };  }  function sendEvent(event) {    event.timestamp = Date.now();    if (ws && ws.readyState === WebSocket.OPEN) {      ws.send(JSON.stringify(event));    } else {      eventQueue.push(event);    }  }  // Capture clicks on choice elements  document.addEventListener('click', (e) => {    const target = e.target.closest('[data-choice]');    if (!target) return;    sendEvent({      type: 'click',      text: target.textContent.trim(),      choice: target.dataset.choice,      id: target.id || null    });    // Update indicator bar (defer so toggleSelect runs first)    setTimeout(() => {      const indicator = document.getElementById('indicator-text');      if (!indicator) return;      const container = target.closest('.options') || target.closest('.cards');      const selected = container ? container.querySelectorAll('.selected') : [];      if (selected.length === 0) {        indicator.textContent = 'Click an option above, then return to the terminal';      } else if (selected.length === 1) {        const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice;        indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 149: 🔴 **CRITICAL** — `SAFE-T1007`

**Location:** Lines 1-88

**Description:**

The WebSocket connection is established to the same host that serves the script (`window.location.host`). While a malicious server could serve this script, the script itself does not contain logic for OAuth token theft or redirection, which are server-side attack components of SAFE-T1007.: OAuth Authorization Phishing exploits the OAuth authorization flow to trick users into granting permissions to attacker-controlled applications. The attacker creates a convincing authorization page or redirects users through legitimate OAuth providers but captures the authorization code or tokens.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 skills/brainstorming/scripts/helper.js:2 const WS_URL = 'ws://' + window.location.host;
```

</details>

---

#### Finding 150: 🔴 **CRITICAL** — `SAFE-T1101`

**Location:** Lines 1-88

**Description:**

The client-side JavaScript code sends user-controlled data via WebSocket to a server. While this client-side code itself is not vulnerable to command injection (SAFE-T1101) as it does not execute system commands, the server-side component receiving this data could be vulnerable if it incorporates `event.text`, `event.choice`, `event.id`, `event.value`, or `metadata` into system commands without proper sanitization. This finding is informational as the vulnerability would reside on the server, not in this client-side script.: Prompt Injection via Tool Output occurs when MCP tools return user-controlled or external data directly to the LLM without proper sanitization, allowing malicious content in tool outputs to hijack the agent behavior. Tool outputs are typically trusted by LLMs and processed as authoritative information.
Common attack vectors include: database records with hidden instructions, file contents with prompt injection payloads, API responses crafted to override directives, and web scraping results with malicious instruction sequences.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 skills/brainstorming/scripts/helper.js:30-35 sendEvent({
      type: 'click',
      text: target.textContent.trim(),
      choice: target.dataset.choice,
      id: target.id || null
    });
skills/brainstorming/scripts/helper.js:77-77 choice: (value, metadata = {}) => sendEvent({ type: 'choice', value, ...metadata })
```

</details>

---

#### Finding 151: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Lines 1-88

**Description:**

Client-side script sends user-controlled or server-rendered UI content to the backend via WebSocket, which could be used as an external data source for LLM context without proper sanitization, leading to prompt injection.: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 skills/brainstorming/scripts/helper.js:40-44 sendEvent({
      type: 'click',
      text: target.textContent.trim(),
      choice: target.dataset.choice,
      id: target.id || null
    });
skills/brainstorming/scripts/helper.js:83-85 window.brainstorm = {
    send: sendEvent,
    choice: (value, metadata = {}) => sendEvent({ type: 'choice', value, ...metadata })
  };
```

</details>

---

#### Finding 152: 🔴 **CRITICAL** — `SAFE-T1103`

**Location:** Lines 15-17

**Description:**

The client-side script processes incoming WebSocket messages and, if the message type is 'reload', executes `window.location.reload()`. While `window.location.reload()` is a standard browser function and not an MCP tool, an attacker capable of sending arbitrary WebSocket messages to the client could force a page reload, leading to a minor denial of service for the client's current session. This does not directly involve 'function name spoofing or tool impersonation' of MCP tools as described in SAFE-T1103, but it is an unauthenticated action triggered by external input.: Fake Tool Invocation exploits insufficient tool validation in MCP hosts by injecting or spoofing tool names, duplicating tool registrations, or manipulating the JSON-RPC tool call protocol. Attackers can redirect legitimate tool calls to malicious implementations or register tools that shadow trusted ones.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:15-17 ws.onmessage = (msg) => { const data = JSON.parse(msg.data); if (data.type === 'reload') { window.location.reload(); } };
```

</details>

---

#### Finding 153: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Line 2

**Description:**

The client-side script connects to a WebSocket on the current host and handles a 'reload' command from the server. While this specific script does not directly implement the RCE vulnerability described in SAFE-T1109 (which targets the MCP Inspector's /sse endpoint), the use of `window.location.host` for the WebSocket URL means it will connect to the same server hosting the web application. If this server is also running a vulnerable MCP Inspector or a similar unauthenticated debug service, a browser-based attack could potentially leverage this connection or the co-location to exploit the server-side vulnerability. The `reload` command itself is a client-side action and not RCE.: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:2-2 : const WS_URL = 'ws://' + window.location.host;
```

</details>

---

#### Finding 154: 🔴 **CRITICAL** — `SAFE-T1303`

**Location:** Lines 1-88

**Description:**

This client-side JavaScript code does not directly interact with container runtimes or system-level 'exec' commands. The SAFE-T1303 technique describes a server-side vulnerability where an MCP server or tool invokes container runtime 'exec' with manipulated working directories to escape a container sandbox. This client-side script primarily handles UI interactions and communicates with a WebSocket server, but it does not control server-side container execution parameters.: Container Sandbox Escape exploits misconfigured container environments running MCP servers to break out of the sandbox and access the host operating system. Attack vectors include mounting the Docker socket, accessing /proc filesystem, running in privileged mode, using nsenter, or exploiting kernel vulnerabilities from within the container.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() {  const WS_URL = 'ws://' + window.location.host;  let ws = null;  let eventQueue = [];  function connect() {    ws = new WebSocket(WS_URL);    ws.onopen = () => {      eventQueue.forEach(e => ws.send(JSON.stringify(e)));      eventQueue = [];    };    ws.onmessage = (msg) => {      const data = JSON.parse(msg.data);      if (data.type === 'reload') {        window.location.reload();      }    };    ws.onclose = () => {      setTimeout(connect, 1000);    };  }  function sendEvent(event) {    event.timestamp = Date.now();    if (ws && ws.readyState === WebSocket.OPEN) {      ws.send(JSON.stringify(event));    } else {      eventQueue.push(event);    }  }  // Capture clicks on choice elements  document.addEventListener('click', (e) => {    const target = e.target.closest('[data-choice]');    if (!target) return;    sendEvent({      type: 'click',      text: target.textContent.trim(),      choice: target.dataset.choice,      id: target.id || null    });    // Update indicator bar (defer so toggleSelect runs first)    setTimeout(() => {      const indicator = document.getElementById('indicator-text');      if (!indicator) return;      const container = target.closest('.options') || target.closest('.cards');      const selected = container ? container.querySelectorAll('.selected') : [];      if (selected.length === 0) {        indicator.textContent = 'Click an option above, then return to the terminal';      } else if (selected.length === 1) {        const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice;        indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 155: 🔴 **CRITICAL** — `SAFE-T1305`

**Location:** Lines 1-88

**Description:**

This client-side JavaScript code interacts with an MCP server via WebSocket, but does not contain any server-side logic or system calls that could directly lead to host OS privilege escalation (SAFE-T1305). The vulnerability described is a server-side misconfiguration where the MCP server process runs as root.: Host OS Privilege Escalation exploits operating system vulnerabilities or misconfigurations from within an MCP server process to gain root or administrative access on the host. Attack vectors include setuid binary abuse, kernel exploits, capability escalation, and accessing sensitive system files like /etc/shadow.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() {  const WS_URL = 'ws://' + window.location.host;  let ws = null;  let eventQueue = [];  function connect() {    ws = new WebSocket(WS_URL);    ws.onopen = () => {      eventQueue.forEach(e => ws.send(JSON.stringify(e)));      eventQueue = [];    };    ws.onmessage = (msg) => {      const data = JSON.parse(msg.data);      if (data.type === 'reload') {        window.location.reload();      }    };    ws.onclose = () => {      setTimeout(connect, 1000);    };  }  function sendEvent(event) {    event.timestamp = Date.now();    if (ws && ws.readyState === WebSocket.OPEN) {      ws.send(JSON.stringify(event));    } else {      eventQueue.push(event);    }  }  // Capture clicks on choice elements  document.addEventListener('click', (e) => {    const target = e.target.closest('[data-choice]');    if (!target) return;    sendEvent({      type: 'click',      text: target.textContent.trim(),      choice: target.dataset.choice,      id: target.id || null    });    // Update indicator bar (defer so toggleSelect runs first)    setTimeout(() => {      const indicator = document.getElementById('indicator-text');      if (!indicator) return;      const container = target.closest('.options') || target.closest('.cards');      const selected = container ? container.querySelectorAll('.selected') : [];      if (selected.length === 0) {        indicator.textContent = 'Click an option above, then return to the terminal';      } else if (selected.length === 1) {        const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice;        indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 156: 🔴 **CRITICAL** — `SAFE-T1306`

**Location:** Lines 1-88

**Description:**

The provided JavaScript code is a client-side helper script for UI interactions and WebSocket communication. It does not implement any authentication server functionality, token minting, or token validation logic. Therefore, it is not directly susceptible to the SAFE-T1306 'Rogue Authentication Server' vulnerability.: Rogue Authorization Server involves setting up a malicious or misconfigured authorization server that mints OAuth tokens, JWTs, or API keys without proper validation, client verification, or trust chain enforcement. Attackers use these rogue-issued tokens to access MCP servers and tools that fail to validate the token issuer.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() {  const WS_URL = 'ws://' + window.location.host;  let ws = null;  let eventQueue = [];  function connect() {    ws = new WebSocket(WS_URL);    ws.onopen = () => {      eventQueue.forEach(e => ws.send(JSON.stringify(e)));      eventQueue = [];    };    ws.onmessage = (msg) => {      const data = JSON.parse(msg.data);      if (data.type === 'reload') {        window.location.reload();      }    };    ws.onclose = () => {      setTimeout(connect, 1000);    };  }  function sendEvent(event) {    event.timestamp = Date.now();    if (ws && ws.readyState === WebSocket.OPEN) {      ws.send(JSON.stringify(event));    } else {      eventQueue.push(event);    }  }  // Capture clicks on choice elements  document.addEventListener('click', (e) => {    const target = e.target.closest('[data-choice]');    if (!target) return;    sendEvent({      type: 'click',      text: target.textContent.trim(),      choice: target.dataset.choice,      id: target.id || null    });    // Update indicator bar (defer so toggleSelect runs first)    setTimeout(() => {      const indicator = document.getElementById('indicator-text');      if (!indicator) return;      const container = target.closest('.options') || target.closest('.cards');      const selected = container ? container.querySelectorAll('.selected') : [];      if (selected.length === 0) {        indicator.textContent = 'Click an option above, then return to the terminal';      } else if (selected.length === 1) {        const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice;        indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 157: 🔴 **CRITICAL** — `SAFE-T1502`

**Location:** Lines 1-88

**Description:**

The provided Javascript code is a client-side script running in a web browser. It handles UI interactions and communicates with a WebSocket server. It does not contain any functionality to access or read files from the local file system of the server or the user, which is the core mechanism of the SAFE-T1502 technique.: File-Based Credential Harvest occurs when MCP server tools read sensitive credential files from the host filesystem. Targets include SSH private keys, AWS credentials, environment files, browser cookies, keychain databases, and cloud provider configurations. The harvested credentials enable lateral movement and privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() {  const WS_URL = 'ws://' + window.location.host;  let ws = null;  let eventQueue = [];  function connect() {    ws = new WebSocket(WS_URL);    ws.onopen = () => {      eventQueue.forEach(e => ws.send(JSON.stringify(e)));      eventQueue = [];    };    ws.onmessage = (msg) => {      const data = JSON.parse(msg.data);      if (data.type === 'reload') {        window.location.reload();      }    };    ws.onclose = () => {      setTimeout(connect, 1000);    };  }  function sendEvent(event) {    event.timestamp = Date.now();    if (ws && ws.readyState === WebSocket.OPEN) {      ws.send(JSON.stringify(event));    } else {      eventQueue.push(event);    }  }  // Capture clicks on choice elements  document.addEventListener('click', (e) => {    const target = e.target.closest('[data-choice]');    if (!target) return;    sendEvent({      type: 'click',      text: target.textContent.trim(),      choice: target.dataset.choice,      id: target.id || null    });    // Update indicator bar (defer so toggleSelect runs first)    setTimeout(() => {      const indicator = document.getElementById('indicator-text');      if (!indicator) return;      const container = target.closest('.options') || target.closest('.cards');      const selected = container ? container.querySelectorAll('.selected') : [];      if (selected.length === 0) {        indicator.textContent = 'Click an option above, then return to the terminal';      } else if (selected.length === 1) {        const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice;        indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 158: 🔴 **CRITICAL** — `SAFE-T1704`

**Location:** Line 2

**Description:**

The WebSocket URL is constructed using `window.location.host`, which is derived from the current browser's URL. While this is not a direct Server-Side Request Forgery (SSRF) vulnerability as the connection is initiated client-side, it's important to ensure that the `window.location.host` itself cannot be manipulated by an attacker (e.g., via DNS rebinding or XSS) to point to an malicious WebSocket server. However, the primary concern of SAFE-T1704 is server-side requests to user-controlled URLs, which is not present here.: Compromised-Server Pivot / SSRF occurs when MCP tools accept user-controlled URLs in HTTP requests without host allowlisting or validation. Attackers exploit this to access internal services, cloud metadata endpoints, localhost services, or private network resources that the MCP server can reach but the attacker cannot directly access.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:2-2 const WS_URL = 'ws://' + window.location.host;
```

</details>

---

#### Finding 159: 🔴 **CRITICAL** — `SAFE-T1705`

**Location:** Line 1

**Description:**

The client establishes a WebSocket connection using the insecure `ws://` protocol, which lacks encryption and integrity protection. This makes the communication channel vulnerable to Man-in-the-Middle (MITM) attacks, allowing an adversary to intercept, read, and inject arbitrary messages into the agent-to-agent communication stream.: Cross-Agent Instruction Injection targets multi-agent MCP architectures where agents communicate by passing messages or tool results. Attackers inject malicious instructions into one agent that propagate to other agents in the workflow, contaminating the entire multi-agent pipeline through message passing, shared context, or tool output forwarding.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-1 const WS_URL = 'ws://' + window.location.host;
```

</details>

---

#### Finding 160: 🔴 **CRITICAL** — `SAFE-T1803`

**Location:** Lines 1-88

**Description:**

The provided Javascript code is a client-side script that handles UI interactions and communicates with a WebSocket server. It does not contain any direct database interaction, SQL queries, or calls to database export utilities like `mysqldump` or `pg_dump`. The code's functionality is limited to sending UI events and handling page reloads.: Database Dump extracts entire database contents through MCP tools using bulk export commands like mysqldump, pg_dump, SELECT *, or ORM findAll without pagination. This enables mass exfiltration of structured data including user records, credentials, financial data, and other sensitive information stored in databases.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() {  const WS_URL = 'ws://' + window.location.host;  let ws = null;  let eventQueue = [];  function connect() {    ws = new WebSocket(WS_URL);    ws.onopen = () => {      eventQueue.forEach(e => ws.send(JSON.stringify(e)));      eventQueue = [];    };    ws.onmessage = (msg) => {      const data = JSON.parse(msg.data);      if (data.type === 'reload') {        window.location.reload();      }    };    ws.onclose = () => {      setTimeout(connect, 1000);    };  }  function sendEvent(event) {    event.timestamp = Date.now();    if (ws && ws.readyState === WebSocket.OPEN) {      ws.send(JSON.stringify(event));    } else {      eventQueue.push(event);    }  }  // Capture clicks on choice elements  document.addEventListener('click', (e) => {    const target = e.target.closest('[data-choice]');    if (!target) return;    sendEvent({      type: 'click',      text: target.textContent.trim(),      choice: target.dataset.choice,      id: target.id || null    });    // Update indicator bar (defer so toggleSelect runs first)    setTimeout(() => {      const indicator = document.getElementById('indicator-text');      if (!indicator) return;      const container = target.closest('.options') || target.closest('.cards');      const selected = container ? container.querySelectorAll('.selected') : [];      if (selected.length === 0) {        indicator.textContent = 'Click an option above, then return to the terminal';      } else if (selected.length === 1) {        const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice;        indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 161: 🔴 **CRITICAL** — `SAFE-T2106`

**Location:** Line 76

**Description:**

The client-side `helper.js` script exposes a `window.brainstorm.choice` function that allows arbitrary `value` and `metadata` to be sent to the MCP server via WebSocket. This provides a direct input channel for user-controlled data.: Context Memory Poisoning via Vector Store Contamination manipulates vector databases storing long-term AI memory. Attackers inject poisoned embeddings that blend legitimate and malicious content, surviving server restarts and reinstallations. The poisoned content can self-replicate across sessions and affect multiple agents sharing the same vector store.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:76-76 "choice: (value, metadata = {}) => sendEvent({ type: 'choice', value, ...metadata })"
```

</details>

---

#### Finding 162: 🔴 **CRITICAL** — `SAFE-T2107`

**Location:** Line 2

**Description:**

The WebSocket connection uses the unencrypted `ws://` protocol, making the communication vulnerable to Man-in-the-Middle (MITM) attacks. An attacker could intercept and modify the event data sent from the client to the server, injecting malicious patterns or adversarial examples into what could become training data for AI models.: AI Model Poisoning manipulates machine learning models used by MCP servers through training data poisoning, adversarial example injection, or embedding backdoor triggers during fine-tuning. Poisoned models produce attacker-desired outputs when triggered by specific inputs while behaving normally otherwise.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:2-2 const WS_URL = 'ws://' + window.location.host;
```

</details>

---

#### Finding 163: 🟠 **HIGH** — `SAFE-T1001`

**Location:** Lines 1-88

**Description:**

The provided JavaScript code is a client-side helper script for UI interactions and WebSocket communication. It does not define, parse, or process 'tool descriptions' that would be directly consumed by a Large Language Model (LLM). The SAFE-T1001 technique specifically targets hidden instructions within LLM tool descriptions.: Tool Poisoning Attack (TPA) embeds malicious instructions within MCP tool descriptions that are invisible to users but processed by LLMs. This exploits the difference between displayed and processed tool descriptions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() { const WS_URL = 'ws://' + window.location.host; let ws = null; let eventQueue = []; function connect() { ws = new WebSocket(WS_URL); ws.onopen = () => { eventQueue.forEach(e => ws.send(JSON.stringify(e))); eventQueue = []; }; ws.onmessage = (msg) => { const data = JSON.parse(msg.data); if (data.type === 'reload') { window.location.reload(); } }; ws.onclose = () => { setTimeout(connect, 1000); }; } function sendEvent(event) { event.timestamp = Date.now(); if (ws && ws.readyState === WebSocket.OPEN) { ws.send(JSON.stringify(event)); } else { eventQueue.push(event); } } document.addEventListener('click', (e) => { const target = e.target.closest('[data-choice]'); if (!target) return; sendEvent({ type: 'click', text: target.textContent.trim(), choice: target.dataset.choice, id: target.id || null }); setTimeout(() => { const indicator = document.getElementById('indicator-text'); if (!indicator) return; const container = target.closest('.options') || target.closest('.cards'); const selected = container ? container.querySelectorAll('.selected') : []; if (selected.length === 0) { indicator.textContent = 'Click an option above, then return to the terminal'; } else if (selected.length === 1) { const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice; indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 164: 🟠 **HIGH** — `SAFE-T1001`

**Location:** Lines 36-41

**Description:**

The script captures user input from UI elements (e.g., `target.textContent.trim()`, `target.dataset.choice`) and sends it as part of an event via WebSocket. While it's theoretically possible for the *content* of these UI elements to be poisoned if the HTML itself was generated from a malicious source, this script's role is to relay user interaction, not to define or interpret LLM tool descriptions. The vulnerability described by SAFE-T1001 focuses on the LLM's processing of tool descriptions, not client-side UI data capture.: Tool Poisoning Attack (TPA) embeds malicious instructions within MCP tool descriptions that are invisible to users but processed by LLMs. This exploits the difference between displayed and processed tool descriptions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:36-41 sendEvent({ type: 'click', text: target.textContent.trim(), choice: target.dataset.choice, id: target.id || null });
```

</details>

---

#### Finding 165: 🟠 **HIGH** — `SAFE-T1002`

**Location:** Lines 13-17

**Description:**

The application connects to a WebSocket server derived from `window.location.host` and processes messages. While the current implementation only triggers `window.location.reload()` based on a 'reload' message type, this pattern of receiving and parsing data from an unverified WebSocket connection could be extended to execute arbitrary code or load unverified artifacts if the server is compromised and the client-side logic is expanded without proper integrity checks.: Supply Chain Compromise occurs when MCP servers download, install, import, or execute external packages, plugins, model weights, or binaries without integrity checks, version pinning, or signature verification. This allows attackers to inject malicious code through compromised dependencies.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:13-17 ws.onmessage = (msg) => { const data = JSON.parse(msg.data); if (data.type === 'reload') { window.location.reload(); } };
```

</details>

---

#### Finding 166: 🟠 **HIGH** — `SAFE-T1004`

**Location:** Line 2

**Description:**

The client establishes an unencrypted WebSocket connection (`ws://`) using `window.location.host`. This makes the client highly vulnerable to server impersonation attacks (SAFE-T1004) if the DNS resolution or local host configuration for `window.location.host` is compromised. Without TLS, there is no cryptographic verification of the server's identity, allowing an attacker to easily intercept or manipulate communications.: Server Impersonation occurs when an attacker registers an MCP server with a name designed to be confused with a legitimate service (typosquatting, homograph attacks, or DNS cache poisoning). Users connecting to the impersonated server unknowingly send credentials and data to the attacker.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:2-2 const WS_URL = 'ws://' + window.location.host;
```

</details>

---

#### Finding 167: 🟠 **HIGH** — `SAFE-T1106`

**Location:** Lines 20-22

**Description:**

The WebSocket client implements an unbounded reconnection loop without termination conditions or an exponential backoff strategy. If the WebSocket server is unavailable or consistently rejects connections, the client will continuously attempt to reconnect every second, potentially leading to client-side resource exhaustion (though minimal for a simple WebSocket) and contributing to server-side resource exhaustion or rate limiting if many clients exhibit this behavior.: Autonomous Loop Exploit triggers denial-of-service through self-reinforcing tool invocation loops. Attackers craft prompts or tool outputs that suggest non-terminal progress, causing agents to repeatedly invoke tools without convergence checks, rate limits, or iteration budgets.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:20-22 ws.onclose = () => { setTimeout(connect, 1000); };
```

</details>

---

#### Finding 168: 🟠 **HIGH** — `SAFE-T1110`

**Location:** Lines 1-88

**Description:**

The client-side script `helper.js` does not directly handle image or audio content, nor does it interact with multimodal AI systems in a way that would introduce SAFE-T1110 vulnerabilities. It primarily sends text-based events via WebSocket.: Multimodal Prompt Injection embeds malicious instructions within images, audio, or other non-text media that are processed by LLMs. Techniques include steganographic embedding in pixel data, OCR-extractable text injection, adversarial visual patterns, and metadata injection in media files. These bypass text-based sanitization since the payload is in a non-text modality.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() { const WS_URL = 'ws://' + window.location.host; let ws = null; let eventQueue = []; function connect() { ws = new WebSocket(WS_URL); ws.onopen = () => { eventQueue.forEach(e => ws.send(JSON.stringify(e))); eventQueue = []; }; ws.onmessage = (msg) => { const data = JSON.parse(msg.data); if (data.type === 'reload') { window.location.reload(); } }; ws.onclose = () => { setTimeout(connect, 1000); }; } function sendEvent(event) { event.timestamp = Date.now(); if (ws && ws.readyState === WebSocket.OPEN) { ws.send(JSON.stringify(event)); } else { eventQueue.push(event); } } document.addEventListener('click', (e) => { const target = e.target.closest('[data-choice]'); if (!target) return; sendEvent({ type: 'click', text: target.textContent.trim(), choice: target.dataset.choice, id: target.id || null }); setTimeout(() => { const indicator = document.getElementById('indicator-text'); if (!indicator) return; const container = target.closest('.options') || target.closest('.cards'); const selected = container ? container.querySelectorAll('.selected') : []; if (selected.length === 0) { indicator.textContent = 'Click an option above, then return to the terminal'; } else if (selected.length === 1) { const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice; indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 169: 🟠 **HIGH** — `SAFE-T1110`

**Location:** Lines 25-31

**Description:**

The `sendEvent` function serializes JavaScript objects into JSON strings for WebSocket transmission. The data sent (`type`, `text`, `choice`, `id`, `value`, `metadata`) is derived from UI elements' text content or explicit JavaScript arguments, not from image or audio files.: Multimodal Prompt Injection embeds malicious instructions within images, audio, or other non-text media that are processed by LLMs. Techniques include steganographic embedding in pixel data, OCR-extractable text injection, adversarial visual patterns, and metadata injection in media files. These bypass text-based sanitization since the payload is in a non-text modality.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:25-31 function sendEvent(event) { event.timestamp = Date.now(); if (ws && ws.readyState === WebSocket.OPEN) { ws.send(JSON.stringify(event)); } else { eventQueue.push(event); } }
```

</details>

---

#### Finding 170: 🟠 **HIGH** — `SAFE-T1201`

**Location:** Lines 14-16

**Description:**

The client-side script allows the server to force a page reload via a WebSocket message. This mechanism can be exploited in a 'rug pull' attack to dynamically load modified or malicious client-side code after initial user approval, bypassing re-verification.: MCP Rug Pull Attack is a persistence technique where tools appear legitimate initially but can change their behavior after gaining user trust. This exploits dynamic tool definitions, remote configuration loading, or time-delayed activation to modify tool behavior post-approval.
Static analysis detects code patterns that ENABLE rug pulls: dynamic descriptions, remote tool loading without version pinning, and time-based conditional behavior.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:14-16 if (data.type === 'reload') { window.location.reload(); }
```

</details>

---

#### Finding 171: 🟠 **HIGH** — `SAFE-T1204`

**Location:** Line 76

**Description:**

The `window.brainstorm.choice` function allows sending arbitrary `value` and `metadata` from the client to the server via WebSocket. If the server-side component directly stores this `value` or `metadata` into a vector store or conversation memory without proper sanitization, validation, or access control, a malicious agent could use this function to implant persistent instructions, prompts, or behavioral patterns.: Context Memory Implant exploits persistent memory systems in MCP-connected AI agents. Malicious agents write themselves into long-term vector stores, conversation histories, or system prompt storage to ensure re-loading in future sessions. The implanted instructions persist across server restarts and session boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:76-76 window.brainstorm = { send: sendEvent, choice: (value, metadata = {}) => sendEvent({ type: 'choice', value, ...metadata }) };
```

</details>

---

#### Finding 172: 🟠 **HIGH** — `SAFE-T1302`

**Location:** Lines 22-28

**Description:**

This client-side script sends user interaction events to a WebSocket server. While the script itself does not implement high-privilege tools, the server-side component receiving these events could potentially be vulnerable to SAFE-T1302 if it processes arbitrary user input from these events in a privileged context without proper validation. This finding is informational as the vulnerability would reside on the server, not in this client-side code.: High-Privilege Tool Abuse occurs when MCP tools are configured with administrative, root, or superuser access to databases, APIs, or system resources. Attackers exploit these tools through prompt injection or direct manipulation to perform privileged operations such as modifying access controls, reading sensitive data, or escalating their own permissions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:22-28 function sendEvent(event) { event.timestamp = Date.now(); if (ws && ws.readyState === WebSocket.OPEN) { ws.send(JSON.stringify(event)); } else { eventQueue.push(event); } }
```

</details>

---

#### Finding 173: 🟠 **HIGH** — `SAFE-T1304`

**Location:** Lines 1-88

**Description:**

The client-side script establishes a WebSocket connection to the current host and sends user interaction events. It does not appear to handle or transmit any authentication tokens, session IDs, or other credentials that could be subject to reuse or forwarding across services.: Credential Relay Chain exploits token reuse across MCP service boundaries. Attackers capture tokens from one service interaction and replay them in tool-to-tool calls, cross-service requests, or downstream API invocations without proper audience validation, scope restriction, or token binding.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() {  const WS_URL = 'ws://' + window.location.host;  let ws = null;  let eventQueue = [];  function connect() {    ws = new WebSocket(WS_URL);    ws.onopen = () => {      eventQueue.forEach(e => ws.send(JSON.stringify(e)));      eventQueue = [];    };    ws.onmessage = (msg) => {      const data = JSON.parse(msg.data);      if (data.type === 'reload') {        window.location.reload();      }    };    ws.onclose = () => {      setTimeout(connect, 1000);    };  }  function sendEvent(event) {    event.timestamp = Date.now();    if (ws && ws.readyState === WebSocket.OPEN) {      ws.send(JSON.stringify(event));    } else {      eventQueue.push(event);    }  }  // Capture clicks on choice elements  document.addEventListener('click', (e) => {    const target = e.target.closest('[data-choice]');    if (!target) return;    sendEvent({      type: 'click',      text: target.textContent.trim(),      choice: target.dataset.choice,      id: target.id || null    });    // Update indicator bar (defer so toggleSelect runs first)    setTimeout(() => {      const indicator = document.getElementById('indicator-text');      if (!indicator) return;      const container = target.closest('.options') || target.closest('.cards');      const selected = container ? container.querySelectorAll('.selected') : [];      if (selected.length === 0) {        indicator.textContent = 'Click an option above, then return to the terminal';      } else if (selected.length === 1) {        const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice;        indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 174: 🟠 **HIGH** — `SAFE-T1401`

**Location:** Lines 1-88

**Description:**

Client-side code transmits user-controlled input containing newlines and control characters to the server without sanitization. This data, if used directly in constructing LLM prompts or tool descriptions on the server, could lead to newline/control character injection (SAFE-T1401).: Line Jumping injects newline characters, CRLF sequences, or control characters into MCP tool descriptions, parameter names, or metadata fields. The injected characters push malicious instructions outside the visible area of user interfaces, making them invisible during review while still being processed by the LLM.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 skills/brainstorming/scripts/helper.js:36-41 sendEvent({ type: 'click', text: target.textContent.trim(), choice: target.dataset.choice, id: target.id || null });
skills/brainstorming/scripts/helper.js:79-79 choice: (value, metadata = {}) => sendEvent({ type: 'choice', value, ...metadata })
```

</details>

---

#### Finding 175: 🟠 **HIGH** — `SAFE-T1407`

**Location:** Line 4

**Description:**

This client-side script connects to a WebSocket server at its host's address. While the script itself does not implement the SAFE-T1407 server proxy masquerade technique, it would be a client interacting with such a malicious server if the host serving this script were compromised. The script sends user interaction events (clicks, choices) to the WebSocket, which could be intercepted or modified by a malicious proxy server.: An attacker operates an MCP server that forwards requests to a legitimate upstream API and relays the responses back to the client. Because the responses originate from the trusted service, the traffic appears normal in monitoring and logs, but the proxy can inspect, alter, or exfiltrate sensitive data. This enables stealthy data collection and response manipulation while hiding the true malicious behavior.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:4-4 ws = new WebSocket(WS_URL);
```

</details>

---

#### Finding 176: 🟠 **HIGH** — `SAFE-T1501`

**Location:** Lines 1-88

**Description:**

The provided Javascript code is client-side and does not define JSON schemas for MCP tools or authentication that would be processed by an LLM. The SAFE-T1501 technique focuses on poisoning server-side JSON schema definitions to manipulate AI behavior and steal credentials. This client-side script constructs simple JSON objects for event communication, but these are not 'schema definitions' in the context of the described vulnerability.: Full-Schema Poisoning manipulates JSON schema definitions used for MCP tool input validation to include hidden fields, default values with malicious content, or additionalProperties that allow arbitrary data injection. In auth schemas, this can expose credentials through coerced fields or default values that leak sensitive information.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() {  const WS_URL = 'ws://' + window.location.host;  let ws = null;  let eventQueue = [];  function connect() {    ws = new WebSocket(WS_URL);    ws.onopen = () => {      eventQueue.forEach(e => ws.send(JSON.stringify(e)));      eventQueue = [];    };    ws.onmessage = (msg) => {      const data = JSON.parse(msg.data);      if (data.type === 'reload') {        window.location.reload();      }    };    ws.onclose = () => {      setTimeout(connect, 1000);    };  }  function sendEvent(event) {    event.timestamp = Date.now();    if (ws && ws.readyState === WebSocket.OPEN) {      ws.send(JSON.stringify(event));    } else {      eventQueue.push(event);    }  }  // Capture clicks on choice elements  document.addEventListener('click', (e) => {    const target = e.target.closest('[data-choice]');    if (!target) return;    sendEvent({      type: 'click',      text: target.textContent.trim(),      choice: target.dataset.choice,      id: target.id || null    });    // Update indicator bar (defer so toggleSelect runs first)    setTimeout(() => {      const indicator = document.getElementById('indicator-text');      if (!indicator) return;      const container = target.closest('.options') || target.closest('.cards');      const selected = container ? container.querySelectorAll('.selected') : [];      if (selected.length === 0) {        indicator.textContent = 'Click an option above, then return to the terminal';      } else if (selected.length === 1) {        const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice;        indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 177: 🟠 **HIGH** — `SAFE-T1504`

**Location:** Lines 1-88

**Description:**

The provided JavaScript code does not directly handle or expose access/refresh tokens. It primarily manages WebSocket communication for UI events and interactions. The technique SAFE-T1504 describes server-side MCP tools leaking tokens, which this client-side script is not.: Token Theft via API Response occurs when MCP tool implementations inadvertently include access tokens, refresh tokens, or other credentials in their output responses. These leaked tokens are then visible in conversation history, logs, or can be captured by other tools in the chain for unauthorized access.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() {  const WS_URL = 'ws://' + window.location.host;  let ws = null;  let eventQueue = [];  function connect() {    ws = new WebSocket(WS_URL);    ws.onopen = () => {      eventQueue.forEach(e => ws.send(JSON.stringify(e)));      eventQueue = [];    };    ws.onmessage = (msg) => {      const data = JSON.parse(msg.data);      if (data.type === 'reload') {        window.location.reload();      }    };    ws.onclose = () => {      setTimeout(connect, 1000);    };  }  function sendEvent(event) {    event.timestamp = Date.now();    if (ws && ws.readyState === WebSocket.OPEN) {      ws.send(JSON.stringify(event));    } else {      eventQueue.push(event);    }  }  // Capture clicks on choice elements  document.addEventListener('click', (e) => {    const target = e.target.closest('[data-choice]');    if (!target) return;    sendEvent({      type: 'click',      text: target.textContent.trim(),      choice: target.dataset.choice,      id: target.id || null    });    // Update indicator bar (defer so toggleSelect runs first)    setTimeout(() => {      const indicator = document.getElementById('indicator-text');      if (!indicator) return;      const container = target.closest('.options') || target.closest('.cards');      const selected = container ? container.querySelectorAll('.selected') : [];      if (selected.length === 0) {        indicator.textContent = 'Click an option above, then return to the terminal';      } else if (selected.length === 1) {        const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice;        indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 178: 🟠 **HIGH** — `SAFE-T1504`

**Location:** Lines 26-32

**Description:**

The `sendEvent` function sends user interaction data (e.g., `click`, `choice`) over a WebSocket. While the current implementation does not include sensitive tokens, if the `event` object were to be populated with tokens from other parts of the application, this could become a vector for leakage. However, based on the provided code, no such token population occurs.: Token Theft via API Response occurs when MCP tool implementations inadvertently include access tokens, refresh tokens, or other credentials in their output responses. These leaked tokens are then visible in conversation history, logs, or can be captured by other tools in the chain for unauthorized access.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:26-32 function sendEvent(event) {    event.timestamp = Date.now();    if (ws && ws.readyState === WebSocket.OPEN) {      ws.send(JSON.stringify(event));    } else {      eventQueue.push(event);    }  }
```

</details>

---

#### Finding 179: 🟠 **HIGH** — `SAFE-T1602`

**Location:** Lines 76-79

**Description:**

The `window.brainstorm` object is globally exposed, providing an interface (`send`, `choice`) to interact with the WebSocket backend. This allows an attacker to programmatically send arbitrary events or specific 'choice' events with custom metadata, facilitating the discovery of server-side event handling capabilities and expected input schemas.: Tool Enumeration discovers the full set of available MCP tools, their capabilities, input schemas, and permission requirements. Attackers use tools/list API calls and schema introspection to map available functionality, identify privileged tools, and find tools with weak input validation for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:76-79 window.brainstorm = { send: sendEvent, choice: (value, metadata = {}) => sendEvent({ type: 'choice', value, ...metadata }) };
```

</details>

---

#### Finding 180: 🟠 **HIGH** — `SAFE-T1603`

**Location:** Lines 1-88

**Description:**

The provided Javascript code is a client-side helper script for UI interactions and WebSocket communication. It does not directly handle, store, or display system prompts or instructions for an AI agent. The script's primary function is to capture user input (clicks, choices) and send them to a backend via WebSocket, and to handle UI updates or page reloads based on simple server messages. There is no mechanism within this code that would lead to the leakage of AI system prompts or internal instructions.: System Prompt Disclosure exposes internal system prompts, instructions, or configuration through debug mode endpoints, verbose error messages, tool output, or direct echoing. Exposed system prompts reveal security controls, filtering rules, and behavioral constraints that attackers can use to craft targeted bypass attacks.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() {  const WS_URL = 'ws://' + window.location.host;  let ws = null;  let eventQueue = [];  function connect() {    ws = new WebSocket(WS_URL);    ws.onopen = () => {      eventQueue.forEach(e => ws.send(JSON.stringify(e)));      eventQueue = [];    };    ws.onmessage = (msg) => {      const data = JSON.parse(msg.data);      if (data.type === 'reload') {        window.location.reload();      }    };    ws.onclose = () => {      setTimeout(connect, 1000);    };  }  function sendEvent(event) {    event.timestamp = Date.now();    if (ws && ws.readyState === WebSocket.OPEN) {      ws.send(JSON.stringify(event));    } else {      eventQueue.push(event);    }  }  // Capture clicks on choice elements  document.addEventListener('click', (e) => {    const target = e.target.closest('[data-choice]');    if (!target) return;    sendEvent({      type: 'click',      text: target.textContent.trim(),      choice: target.dataset.choice,      id: target.id || null    });    // Update indicator bar (defer so toggleSelect runs first)    setTimeout(() => {      const indicator = document.getElementById('indicator-text');      if (!indicator) return;      const container = target.closest('.options') || target.closest('.cards');      const selected = container ? container.querySelectorAll('.selected') : [];      if (selected.length === 0) {        indicator.textContent = 'Click an option above, then return to the terminal';      } else if (selected.length === 1) {        const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice;        indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 181: 🟠 **HIGH** — `SAFE-T1605`

**Location:** Lines 77-80

**Description:**

The client-side script exposes a `brainstorm` object with a `send` method, allowing arbitrary events to be sent to the WebSocket server. While this script itself does not enumerate server capabilities, it provides a direct interface for an attacker to craft and send messages that could be used for capability enumeration if the server-side logic is vulnerable to SAFE-T1605.: Capability Mapping systematically discovers MCP server capabilities, supported API versions, feature flags, and permission boundaries. Attackers use introspection endpoints, capability negotiation, and API probing to build a comprehensive map of what the target system can do, identifying the most valuable attack vectors.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:77-80 window.brainstorm = { send: sendEvent, choice: (value, metadata = {}) => sendEvent({ type: 'choice', value, ...metadata }) };
```

</details>

---

#### Finding 182: 🟠 **HIGH** — `SAFE-T1606`

**Location:** Lines 1-88

**Description:**

This client-side JavaScript code does not directly implement or expose functionality for directory listing. It primarily handles UI interactions and sends events to a WebSocket server. The vulnerability described by SAFE-T1606, 'Unrestricted directory listing through file tools', typically resides in server-side components that interact with the file system.: Directory Listing via File Tool exploits MCP file tools that allow unrestricted directory enumeration. Attackers use fs.readdir, os.listdir, or equivalent functions to map the filesystem structure, discover sensitive files, locate configuration files, and identify credential storage locations for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() {  const WS_URL = 'ws://' + window.location.host;  let ws = null;  let eventQueue = [];  function connect() {    ws = new WebSocket(WS_URL);    ws.onopen = () => {      eventQueue.forEach(e => ws.send(JSON.stringify(e)));      eventQueue = [];    };    ws.onmessage = (msg) => {      const data = JSON.parse(msg.data);      if (data.type === 'reload') {        window.location.reload();      }    };    ws.onclose = () => {      setTimeout(connect, 1000);    };  }  function sendEvent(event) {    event.timestamp = Date.now();    if (ws && ws.readyState === WebSocket.OPEN) {      ws.send(JSON.stringify(event));    } else {      eventQueue.push(event);    }  }  // Capture clicks on choice elements  document.addEventListener('click', (e) => {    const target = e.target.closest('[data-choice]');    if (!target) return;    sendEvent({      type: 'click',      text: target.textContent.trim(),      choice: target.dataset.choice,      id: target.id || null    });    // Update indicator bar (defer so toggleSelect runs first)    setTimeout(() => {      const indicator = document.getElementById('indicator-text');      if (!indicator) return;      const container = target.closest('.options') || target.closest('.cards');      const selected = container ? container.querySelectorAll('.selected') : [];      if (selected.length === 0) {        indicator.textContent = 'Click an option above, then return to the terminal';      } else if (selected.length === 1) {        const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice;        indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 183: 🟠 **HIGH** — `SAFE-T1701`

**Location:** Lines 77-80

**Description:**

The `window.brainstorm` object is exposed globally, providing an API to send arbitrary messages over the WebSocket connection established by this script. This creates a shared communication channel that can be abused by other tools.: Cross-Tool Contamination exploits shared state between MCP tools including global variables, shared memory, common databases, or file-based caches. Malicious data written by one tool pollutes the execution context of another, enabling data exfiltration, privilege escalation, or behavior manipulation across tool boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:77-80 window.brainstorm = { send: sendEvent, choice: (value, metadata = {}) => sendEvent({ type: 'choice', value, ...metadata }) };
```

</details>

---

#### Finding 184: 🟠 **HIGH** — `SAFE-T1703`

**Location:** Lines 69-72

**Description:**

The client-side script `helper.js` acts as a low-privileged tool that sends user-controlled data via WebSocket to the MCP server. The `window.brainstorm.send` function and the `metadata` parameter in `window.brainstorm.choice` allow for arbitrary JSON objects to be sent. If the server-side component implicitly trusts and processes these arbitrary inputs, and subsequently uses them to invoke or interact with higher-privileged MCP tools without sufficient validation or sanitization, this client-side script could serve as a pivot point in a SAFE-T1703 tool-chaining attack.: Tool-Chaining Pivot exploits the output of one MCP tool as input to another more privileged tool, creating multi-step attack chains. Attackers leverage a low-privilege tool to produce crafted output that, when consumed by a higher-privilege tool, enables unauthorized operations such as command execution, data access, or privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:69-72 window.brainstorm = { send: sendEvent, choice: (value, metadata = {}) => sendEvent({ type: 'choice', value, ...metadata }) };
```

</details>

---

#### Finding 185: 🟠 **HIGH** — `SAFE-T1706`

**Location:** Lines 1-88

**Description:**

The provided code does not appear to handle OAuth tokens or perform any related validation. The WebSocket connection is established using `window.location.host`, suggesting a same-origin connection likely relying on session cookies for authentication, not OAuth tokens. Events sent over the WebSocket are simple JSON objects without any token-related fields or authorization headers.: OAuth Token Pivot Replay reuses OAuth tokens obtained from one service to access a different service or audience. Attackers exploit missing audience (aud) validation, overly broad token scopes, or lack of token binding to pivot from a lower-value service to a higher-value target using the same token.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() {  const WS_URL = 'ws://' + window.location.host;  let ws = null;  let eventQueue = [];  function connect() {    ws = new WebSocket(WS_URL);    ws.onopen = () => {      eventQueue.forEach(e => ws.send(JSON.stringify(e)));      eventQueue = [];    };    ws.onmessage = (msg) => {      const data = JSON.parse(msg.data);      if (data.type === 'reload') {        window.location.reload();      }    };    ws.onclose = () => {      setTimeout(connect, 1000);    };  }  function sendEvent(event) {    event.timestamp = Date.now();    if (ws && ws.readyState === WebSocket.OPEN) {      ws.send(JSON.stringify(event));    } else {      eventQueue.push(event);    }  }  // Capture clicks on choice elements  document.addEventListener('click', (e) => {    const target = e.target.closest('[data-choice]');    if (!target) return;    sendEvent({      type: 'click',      text: target.textContent.trim(),      choice: target.dataset.choice,      id: target.id || null    });    // Update indicator bar (defer so toggleSelect runs first)    setTimeout(() => {      const indicator = document.getElementById('indicator-text');      if (!indicator) return;      const container = target.closest('.options') || target.closest('.cards');      const selected = container ? container.querySelectorAll('.selected') : [];      if (selected.length === 0) {        indicator.textContent = 'Click an option above, then return to the terminal';      } else if (selected.length === 1) {        const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice;        indicator.innerHTML = '<span class=
```

</details>

---

#### Finding 186: 🟠 **HIGH** — `SAFE-T1707`

**Location:** Lines 20-24

**Description:**

The `sendEvent` function, which dispatches state-changing user interactions (like clicks and choices) over a WebSocket, does not include any anti-CSRF token in its message payload.: CSRF Token Relay exploits missing or improperly validated CSRF tokens in MCP state-changing requests. Attackers forge cross-site requests or reuse OAuth state parameters to trigger unauthorized actions, manipulate tool configurations, or hijack authentication flows through the victim browser or agent.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:20-24 function sendEvent(event) { event.timestamp = Date.now(); if (ws && ws.readyState === WebSocket.OPEN) { ws.send(JSON.stringify(event)); } else { eventQueue.push(event); } }
```

</details>

---

#### Finding 187: 🟠 **HIGH** — `SAFE-T1910`

**Location:** Lines 34-36

**Description:**

The client-side script sends user interaction data to the MCP server via WebSocket. While this is not a direct instance of SAFE-T1910 (which describes exfiltration *from* the MCP server to external systems), it represents an input channel to the MCP server. If an attacker could inject malicious content into the client-side UI (e.g., via XSS), they could potentially craft arbitrary `event` objects and send them to the MCP server using `window.brainstorm.send`. This could be a vector for an attacker to send encoded data *to* the MCP server, which the server might then be tricked into exfiltrating using its own tools (as described in SAFE-T1910). However, the script itself does not perform the exfiltration.: Covert Channel Exfiltration uses non-standard communication channels to exfiltrate data from MCP server environments. Techniques include DNS tunneling (encoding data in DNS queries), timing-based channels, HTTP header manipulation, and steganographic encoding that bypasses traditional network monitoring and DLP solutions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:34-36 ws.send(JSON.stringify(event));
```

</details>

---

#### Finding 188: 🟠 **HIGH** — `SAFE-T1911`

**Location:** Line 79

**Description:**

The `window.brainstorm.choice` function allows arbitrary `metadata` to be included in the WebSocket event, which could be exploited to exfiltrate secrets in unused JSON parameters.: Attackers hide sensitive data inside optional or unused parameters (e.g., metadata, context, tags) that are accepted and forwarded by the server but not actively validated. These values can be logged or propagated to upstream services, enabling stealthy exfiltration without triggering obvious alarms.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:79-79 choice: (value, metadata = {}) => sendEvent({ type: 'choice', value, ...metadata })
```

</details>

---

#### Finding 189: 🟠 **HIGH** — `SAFE-T1912`

**Location:** Lines 30-36

**Description:**

The script sends user interaction data (clicks, choices) to a WebSocket server. While this is a form of data exfiltration from the client, it is explicit and not covertly hidden using steganographic techniques as described by SAFE-T1912. The technique focuses on data hidden in *server responses* that users then copy, or data hidden in images/whitespace. This script's primary function is to explicitly send client-side events.: Stego Response Exfiltration hides sensitive data within seemingly benign tool responses using steganographic techniques. Data is embedded in image pixels (LSB encoding), whitespace patterns, Unicode zero-width characters, or structured response fields that appear normal to users but contain extractable hidden information.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:30-36 sendEvent({type: 'click',text: target.textContent.trim(),choice: target.dataset.choice,id: target.id || null});
```

</details>

---

#### Finding 190: 🟠 **HIGH** — `SAFE-T1915`

**Location:** Lines 1-88

**Description:**

The provided Javascript code is a client-side script for handling UI interactions and WebSocket communication. It does not contain any functionality related to blockchain bridge calls, DEX interactions, or crypto mixing for fund laundering.: Cross-Chain Laundering uses MCP tools with blockchain access to launder funds through cross-chain bridges, decentralized exchange (DEX) swaps, and cryptocurrency mixing services. Attackers route stolen funds through multiple blockchains to obscure the transaction trail and convert illicit cryptocurrency to clean assets.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:1-88 (function() { const WS_URL = 'ws://' + window.location.host; let ws = null; let eventQueue = []; function connect() { ws = new WebSocket(WS_URL); ws.onopen = () => { eventQueue.forEach(e => ws.send(JSON.stringify(e))); eventQueue = []; }; ws.onmessage = (msg) => { const data = JSON.parse(msg.data); if (data.type === 'reload') { window.location.reload(); } }; ws.onclose = () => { setTimeout(connect, 1000); }; } function sendEvent(event) { event.timestamp = Date.now(); if (ws && ws.readyState === WebSocket.OPEN) { ws.send(JSON.stringify(event)); } else { eventQueue.push(event); } } document.addEventListener('click', (e) => { const target = e.target.closest('[data-choice]'); if (!target) return; sendEvent({ type: 'click', text: target.textContent.trim(), choice: target.dataset.choice, id: target.id || null }); setTimeout(() => { const indicator = document.getElementById('indicator-text'); if (!indicator) return; const container = target.closest('.options') || target.closest('.cards'); const selected = container ? container.querySelectorAll('.selected') : []; if (selected.length === 0) { indicator.textContent = 'Click an option above, then return to the terminal'; } else if (selected.length === 1) { const label = selected[0].querySelector('h3, .content h3, .card-body h3')?.textContent?.trim() || selected[0].dataset.choice; indicator.innerHTML = '<span class="selected-text">' + label + ' selected</span> — return to terminal to continue'; } else { indicator.innerHTML = '<span class="selected-text">' + selected.length + ' selected</span> — return to terminal to continue'; } }, 0); }); window.selectedChoice = null; window.toggleSelect = function(el) { const container = el.closest('.options') || el.closest('.cards'); const multi = container && container.dataset.multiselect !== undefined; if (container && !multi) { container.querySelectorAll('.option, .card').forEach(o => o.classList.remove('selected')); } if (multi) { el.classLi...
```

</details>

---

#### Finding 191: 🟠 **HIGH** — `SAFE-T2102`

**Location:** Lines 20-22

**Description:**

The WebSocket client implements a fixed-delay reconnection loop without exponential backoff or a maximum retry limit. If the WebSocket server becomes unavailable or repeatedly closes connections, this client-side script will continuously attempt to reconnect every second. While connecting to its own host, a large number of clients performing this could contribute to resource exhaustion on the WebSocket server, potentially leading to a denial of service.: Service Disruption via API Flooding uses MCP tools to generate excessive outbound API calls through infinite loops, missing rate limits, or unbounded concurrent requests. This causes denial of service on target APIs, excessive billing on cloud services, and potential cascading failures across dependent systems.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:20-22 ws.onclose = () => { setTimeout(connect, 1000); };
```

</details>

---

### 📁 `skills/brainstorming/scripts/server.cjs`

#### Finding 192: 🔴 **CRITICAL** — `SAFE-T1007`

**Location:** Lines 1-200

**Description:**

The server serves dynamic HTML content and injects a helper script, which could be leveraged by a malicious agent to initiate OAuth flows. While this server itself does not implement OAuth, its role in displaying agent-controlled content makes it a potential platform for an attacker-controlled agent to perform OAuth phishing.: OAuth Authorization Phishing exploits the OAuth authorization flow to trick users into granting permissions to attacker-controlled applications. The attacker creates a convincing authorization page or redirects users through legitimate OAuth providers but captures the authorization code or tokens.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 193: 🔴 **CRITICAL** — `SAFE-T1101`

**Location:** Lines 1-200

**Description:**

The `handleMessage` function is called with unsanitized user-controlled data received over a WebSocket connection. If `handleMessage` processes this data as part of a system command or tool invocation without proper sanitization or argument array usage, it could lead to command injection.: Prompt Injection via Tool Output occurs when MCP tools return user-controlled or external data directly to the LLM without proper sanitization, allowing malicious content in tool outputs to hijack the agent behavior. Tool outputs are typically trusted by LLMs and processed as authoritative information.
Common attack vectors include: database records with hidden instructions, file contents with prompt injection payloads, API responses crafted to override directives, and web scraping results with malicious instruction sequences.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 194: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Lines 1-200

**Description:**

The server reads and serves arbitrary HTML content from the `CONTENT_DIR` without sanitizing the content for potential prompt injection instructions. If this content is subsequently fed to an LLM, an attacker who can write to `CONTENT_DIR` could inject malicious instructions.: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 195: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Lines 201-354

**Description:**

User-controlled WebSocket messages are parsed as JSON and then directly appended to a local 'events' file without sanitization. If this 'events' file is later consumed by a Large Language Model (LLM) as part of its context, an attacker could inject malicious instructions into the LLM's prompt by crafting specific JSON payloads.: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 196: 🔴 **CRITICAL** — `SAFE-T1103`

**Location:** Lines 1-200

**Description:**

The `handleMessage` function processes incoming WebSocket text messages by parsing them as JSON. However, the provided code snippet for this function is incomplete and lacks any visible validation or authorization checks on the parsed message content before potentially dispatching commands or 'tool calls'. This absence of validation creates a critical vulnerability where an attacker could send arbitrary JSON-RPC messages, potentially spoofing function names or tool invocations to execute unauthorized operations.: Fake Tool Invocation exploits insufficient tool validation in MCP hosts by injecting or spoofing tool names, duplicating tool registrations, or manipulating the JSON-RPC tool call protocol. Attackers can redirect legitimate tool calls to malicious implementations or register tools that shadow trusted ones.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 197: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Lines 1-200

**Description:**

The 'Brainstorm Companion' server, which appears to be a development or companion tool, can be configured to bind to all network interfaces (0.0.0.0) via the `BRAINSTORM_HOST` environment variable. This configuration, combined with the lack of authentication for its HTTP and WebSocket endpoints, creates a significant attack surface. While the specific remote code execution (RCE) mechanism is not fully visible due to the missing `handleMessage` function, the technique SAFE-T1109 specifically warns against exposing such tools, as they often contain RCE vulnerabilities.: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 198: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Lines 201-354

**Description:**

The server logs various operational events to `console.log`, including user events and file changes. While not a direct vulnerability, excessive logging of sensitive information could be a concern in a production environment.: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 199: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Lines 201-354

**Description:**

The server binds to a configurable `HOST` and `PORT`. If `HOST` is configured to `0.0.0.0` in a production environment without proper network segmentation, it could expose the service to external networks. However, this code does not implement the specific RCE vulnerability described in SAFE-T1109, which targets the MCP Inspector tool.: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 200: 🔴 **CRITICAL** — `SAFE-T1111`

**Location:** Lines 201-354

**Description:**

The provided code does not directly implement the SAFE-T1111 technique of weaponizing AI CLI tools. The `README` describes the attack, but the `server.cjs` file does not contain the malicious logic (e.g., `child_process.spawnSync` calls to AI tools with dangerous flags).: AI Agent CLI Weaponization exploits AI-powered CLI tools and agents that have unrestricted shell access or use dangerous permission-bypass flags. Attackers distribute malicious packages with postinstall scripts that invoke AI CLI tools in unsafe modes, or craft inputs that cause agents to execute arbitrary system commands, harvest credentials, or exfiltrate repository data.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 201: 🔴 **CRITICAL** — `SAFE-T1306`

**Location:** Lines 201-354

**Description:**

The provided code does not appear to implement an authentication server or token minting functionality. It primarily functions as a WebSocket server for real-time updates and file system watching, logging user events and broadcasting content changes. There are no indications of OAuth/OIDC flows, JWT issuance, or token validation logic that would be susceptible to SAFE-T1306.: Rogue Authorization Server involves setting up a malicious or misconfigured authorization server that mints OAuth tokens, JWTs, or API keys without proper validation, client verification, or trust chain enforcement. Attackers use these rogue-issued tokens to access MCP servers and tools that fail to validate the token issuer.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 202: 🔴 **CRITICAL** — `SAFE-T1309`

**Location:** Lines 1-200

**Description:**

The server receives arbitrary text messages via WebSocket and passes them to an undefined `handleMessage` function. In the context of an MCP server, this input (`result.payload.toString()`) could serve as a prompt for an LLM or agent. Without the implementation of `handleMessage`, it's impossible to verify if this input is adequately sanitized, validated, or if it leads to the invocation of privileged tools (like filesystem operations, which this server has access to) without proper authorization or safety checks, as described in SAFE-T1309.: Privileged Tool via Prompt Injection exploits LLM-based tool selection where the model chooses which tool to invoke based on natural language. Attackers craft prompts that manipulate the LLM into selecting privileged or dangerous tools, bypassing access controls that only exist at the prompt/UI layer rather than at the tool execution layer.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 203: 🔴 **CRITICAL** — `SAFE-T1309`

**Location:** Lines 201-354

**Description:**

The provided code does not appear to implement an LLM agent or expose an interface that processes natural language prompts for tool invocation. The `handleMessage` function directly parses JSON from WebSocket messages, not natural language. Therefore, the specific attack technique SAFE-T1309, which relies on prompt injection to trick an LLM into invoking privileged tools, is not applicable to this codebase.: Privileged Tool via Prompt Injection exploits LLM-based tool selection where the model chooses which tool to invoke based on natural language. Attackers craft prompts that manipulate the LLM into selecting privileged or dangerous tools, bypassing access controls that only exist at the prompt/UI layer rather than at the tool execution layer.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 204: 🔴 **CRITICAL** — `SAFE-T1502`

**Location:** Lines 1-200

**Description:**

The server serves files from a configurable content directory (`CONTENT_DIR`) based on user-supplied filenames. While `path.basename` is used to prevent direct path traversal, if the `BRAINSTORM_DIR` environment variable is set to a sensitive location (e.g., `/root/.ssh`) and an attacker can control the creation of symlinks within the `CONTENT_DIR` (e.g., `/root/.ssh/content/symlink_to_id_rsa` pointing to `../id_rsa`), then sensitive files could be read. The current code does not show the mechanism for writing files to `CONTENT_DIR`, which would be a prerequisite for this specific exploit path.: File-Based Credential Harvest occurs when MCP server tools read sensitive credential files from the host filesystem. Targets include SSH private keys, AWS credentials, environment files, browser cookies, keychain databases, and cloud provider configurations. The harvested credentials enable lateral movement and privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 205: 🔴 **CRITICAL** — `SAFE-T1505`

**Location:** Lines 1-200

**Description:**

The application uses environment variables for configuration, which are stored in process memory. While not directly a 'secret' in the context of AI systems, any sensitive configuration (e.g., API keys, database credentials) passed via environment variables would be susceptible to memory inspection.: In-Memory Secret Extraction targets secrets held in process memory at runtime. Attackers use memory dump tools, /proc/*/mem access, heap inspection, or debugger attachment to extract encryption keys, tokens, passwords, and other sensitive data that exists only in memory and would not be found in files or environment variables.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 206: 🔴 **CRITICAL** — `SAFE-T1702`

**Location:** Lines 1-200

**Description:**

The `handleMessage` function is called with unsanitized user input from WebSocket frames. If `handleMessage` (whose implementation is not provided) were to execute shell commands based on this input, it would be vulnerable to shell command injection.: Command Injection vulnerabilities occur when MCP server tools execute shell commands with unsanitized user input. Attackers can inject malicious commands that are executed with the server's privileges, potentially leading to arbitrary code execution, data exfiltration, or system compromise.

This technique targets common command execution patterns across multiple languages including Node.js (child_process), Python (subprocess, os.system), Rust (Command), and Go (os/exec). Unsafe practices include string concatenation in shell commands, missing input validation, and using shell=True or shell invocation modes.

Command injection is particularly dangerous in MCP servers because tools often have elevated privileges to interact with system resources, files, or external services. A successful injection can bypass all security boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 207: 🔴 **CRITICAL** — `SAFE-T1704`

**Location:** Lines 1-200

**Description:**

The `handleMessage` function is a potential sink for user-controlled data from WebSocket messages. If this function were to construct and make server-side HTTP requests using parts of the `result.payload.toString()` without proper validation, it could lead to Server-Side Request Forgery (SSRF).: Compromised-Server Pivot / SSRF occurs when MCP tools accept user-controlled URLs in HTTP requests without host allowlisting or validation. Attackers exploit this to access internal services, cloud metadata endpoints, localhost services, or private network resources that the MCP server can reach but the attacker cannot directly access.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 208: 🔴 **CRITICAL** — `SAFE-T1705`

**Location:** Lines 1-200

**Description:**

The WebSocket server receives unvalidated text messages from clients and attempts to pass them directly to a function named `handleMessage`. This function is not defined in the provided code, indicating an incomplete implementation or a missing dependency. If `handleMessage` is intended to process or relay messages to other agents in a multi-agent workflow, this direct input from an unauthenticated client creates a potential entry point for agent-to-agent message injection (SAFE-T1705).: Cross-Agent Instruction Injection targets multi-agent MCP architectures where agents communicate by passing messages or tool results. Attackers inject malicious instructions into one agent that propagate to other agents in the workflow, contaminating the entire multi-agent pipeline through message passing, shared context, or tool output forwarding.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 209: 🔴 **CRITICAL** — `SAFE-T1705`

**Location:** Lines 201-354

**Description:**

The server accepts arbitrary JSON messages from WebSocket clients, parses them, and then logs the full user-controlled JSON object and conditionally appends it to a shared 'events' file. This allows a malicious client to inject arbitrary instructions or data into shared system contexts (logs and files) that could be consumed and acted upon by other agents in a multi-agent workflow without proper validation.: Cross-Agent Instruction Injection targets multi-agent MCP architectures where agents communicate by passing messages or tool results. Attackers inject malicious instructions into one agent that propagate to other agents in the workflow, contaminating the entire multi-agent pipeline through message passing, shared context, or tool output forwarding.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 210: 🔴 **CRITICAL** — `SAFE-T2101`

**Location:** Lines 1-200

**Description:**

The application uses a temporary directory '/tmp/brainstorm' for session content and state. While the current code does not implement any destructive file operations, the use of a well-known temporary directory could be a target for external destructive actions if not properly managed by the owning process. The `ownerPid` variable suggests an external process is responsible for cleanup.: Data Destruction uses MCP tools to permanently delete or corrupt data through destructive operations such as rm -rf, DROP TABLE, file wiping, bulk delete API calls, or database truncation. Attackers may destroy data for sabotage, to cover their tracks, or as part of a ransomware attack.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 211: 🔴 **CRITICAL** — `SAFE-T2101`

**Location:** Lines 201-354

**Description:**

The server deletes the 'events' file, which stores user choices and events, whenever a new HTML file is detected in the 'CONTENT_DIR'. This action, while potentially intended for state reset, constitutes a data destruction operation that could be abused by a malicious agent or attacker.: Data Destruction uses MCP tools to permanently delete or corrupt data through destructive operations such as rm -rf, DROP TABLE, file wiping, bulk delete API calls, or database truncation. Attackers may destroy data for sabotage, to cover their tracks, or as part of a ransomware attack.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 212: 🔴 **CRITICAL** — `SAFE-T2103`

**Location:** Lines 1-200

**Description:**

The `handleMessage` function, which is called to process incoming WebSocket text messages from clients, is declared but not defined in the provided code snippet. This creates a critical entry point for code injection via a malicious Pull Request (PR).: Code Sabotage via Malicious PR uses MCP tools with repository access to create automated pull requests containing malicious code changes, backdoors, or CI/CD pipeline modifications. Attackers manipulate build scripts, inject vulnerabilities, or alter deployment configurations through automated commits that bypass normal code review.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 213: 🔴 **CRITICAL** — `SAFE-T2106`

**Location:** Lines 1-200

**Description:**

The server serves content from a local directory (`CONTENT_DIR`) and injects a helper script into HTML responses. While not directly related to vector database poisoning, this pattern of serving user-controlled or externally sourced content could be a vector for other types of content injection if `CONTENT_DIR` is not properly secured or if the content itself is not validated. This is a general observation about content handling, not a direct finding for SAFE-T2106.: Context Memory Poisoning via Vector Store Contamination manipulates vector databases storing long-term AI memory. Attackers inject poisoned embeddings that blend legitimate and malicious content, surviving server restarts and reinstallations. The poisoned content can self-replicate across sessions and affect multiple agents sharing the same vector store.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 214: 🔴 **CRITICAL** — `SAFE-T2107`

**Location:** Lines 1-200

**Description:**

The server processes user-controlled input via WebSocket messages, which are passed to the `handleMessage` function. Given the server's role as an MCP tool managing `CONTENT_DIR` and `STATE_DIR`, it is highly probable that this function writes user-supplied data to these directories. If these directories serve as sources for AI model training data, an attacker could inject adversarial examples or backdoor triggers.: AI Model Poisoning manipulates machine learning models used by MCP servers through training data poisoning, adversarial example injection, or embedding backdoor triggers during fine-tuning. Poisoned models produce attacker-desired outputs when triggered by specific inputs while behaving normally otherwise.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 215: 🔴 **CRITICAL** — `SAFE-T2107`

**Location:** Lines 201-354

**Description:**

The server appends unvalidated, user-controlled JSON data received via WebSocket messages directly to a file (`STATE_DIR/events`). This file is a direct candidate for training data for AI models, as per the SAFE-T2107 technique description. An attacker can inject malicious patterns, adversarial examples, or backdoor triggers into this training data, leading to AI model poisoning.: AI Model Poisoning manipulates machine learning models used by MCP servers through training data poisoning, adversarial example injection, or embedding backdoor triggers during fine-tuning. Poisoned models produce attacker-desired outputs when triggered by specific inputs while behaving normally otherwise.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 216: 🔴 **CRITICAL** — `SAFE-T3001`

**Location:** Lines 1-200

**Description:**

The server directly serves HTML and other files from a `CONTENT_DIR` that is populated by an external 'agent'. There are no explicit content validation, sanitization, or provenance checks performed on these files before they are served. This creates a vulnerability where malicious documents, if injected into `CONTENT_DIR` by a compromised or malicious agent, will be served to clients or downstream RAG systems without scrutiny.: RAG Backdoor Attack injects malicious documents into Retrieval-Augmented Generation knowledge bases used by MCP servers. Poisoned documents contain hidden instructions, false information, or trigger phrases that activate when retrieved, causing the LLM to produce attacker-controlled outputs, leak data, or execute unauthorized actions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 217: 🔴 **CRITICAL** — `SAFE-T3001`

**Location:** Lines 201-354

**Description:**

User-controlled JSON data from WebSocket messages is directly appended to a local 'events' file without validation or sanitization. This file could serve as a knowledge base for a Retrieval-Augmented Generation (RAG) system, making it susceptible to corpus poisoning.: RAG Backdoor Attack injects malicious documents into Retrieval-Augmented Generation knowledge bases used by MCP servers. Poisoned documents contain hidden instructions, false information, or trigger phrases that activate when retrieved, causing the LLM to produce attacker-controlled outputs, leak data, or execute unauthorized actions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 218: 🟠 **HIGH** — `SAFE-T1001`

**Location:** Lines 201-354

**Description:**

The server processes and logs user-provided JSON messages, including a 'choice' field, to a file. While not directly related to LLM tool descriptions, this could be a vector for storing malicious data that might later be consumed by an LLM or another component if the 'events' file is used as input for such systems.: Tool Poisoning Attack (TPA) embeds malicious instructions within MCP tool descriptions that are invisible to users but processed by LLMs. This exploits the difference between displayed and processed tool descriptions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 219: 🟠 **HIGH** — `SAFE-T1004`

**Location:** Lines 1-200

**Description:**

The MCP server uses plain HTTP without TLS, making it highly vulnerable to impersonation and Man-in-the-Middle (MitM) attacks. Clients connecting to this server have no cryptographic means to verify the server's identity, allowing an attacker to easily impersonate the legitimate service through DNS poisoning, ARP spoofing, or other network-level attacks.: Server Impersonation occurs when an attacker registers an MCP server with a name designed to be confused with a legitimate service (typosquatting, homograph attacks, or DNS cache poisoning). Users connecting to the impersonated server unknowingly send credentials and data to the attacker.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 220: 🟠 **HIGH** — `SAFE-T1004`

**Location:** Lines 201-354

**Description:**

The MCP server writes its connection details (URL, host, port) to a local file (`server-info` in `STATE_DIR`) without any integrity protection or unique server identification. This file serves as a local discovery mechanism for clients.: Server Impersonation occurs when an attacker registers an MCP server with a name designed to be confused with a legitimate service (typosquatting, homograph attacks, or DNS cache poisoning). Users connecting to the impersonated server unknowingly send credentials and data to the attacker.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 221: 🟠 **HIGH** — `SAFE-T1006`

**Location:** Lines 201-354

**Description:**

The MCP server writes its configured public-facing URL, including the `URL_HOST`, to a `server-info` file upon startup. This behavior can be exploited by a malicious installer to advertise an attacker-controlled domain.: User-Social-Engineering Install exploits user trust to convince them to install a malicious or compromised MCP server. This includes fake documentation, misleading READMEs, deceptive tool descriptions, or impersonating popular tools to trick users into granting permissions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 222: 🟠 **HIGH** — `SAFE-T1008`

**Location:** Lines 1-200

**Description:**

The provided code implements a WebSocket and HTTP server for a 'Brainstorm Companion' application. It serves HTML content and static files, and handles WebSocket communication. However, it does not appear to define or expose 'tool descriptions' in the context of an MCP (Model Context Protocol) server that an AI agent would consume. Therefore, the specific vulnerability pattern for SAFE-T1008, which involves malicious tool descriptions redirecting or hijacking other tools, is not directly applicable to this codebase.: Tool Shadowing Attack embeds instructions in tool descriptions that manipulate how AI agents use OTHER tools. In multi-server MCP setups, all tool descriptions are aggregated into a single prompt, allowing any tool to influence the behavior of others through cross-tool interference patterns.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 223: 🟠 **HIGH** — `SAFE-T1008`

**Location:** Lines 201-354

**Description:**

The provided code implements a WebSocket server that handles messages and file system events, but it does not appear to define or expose any 'tool descriptions' in the context of an MCP (Model Context Protocol) server for AI agents. The `SAFE-T1008` technique specifically targets malicious instructions embedded within tool descriptions to hijack AI agent behavior. This server primarily handles client communication, activity tracking, and file watching for `.html` content, which is not directly related to MCP tool definitions.: Tool Shadowing Attack embeds instructions in tool descriptions that manipulate how AI agents use OTHER tools. In multi-server MCP setups, all tool descriptions are aggregated into a single prompt, allowing any tool to influence the behavior of others through cross-tool interference patterns.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 224: 🟠 **HIGH** — `SAFE-T1105`

**Location:** Lines 1-200

**Description:**

The application uses `path.basename()` to sanitize user-provided filenames before constructing file paths. While this effectively prevents classic path traversal attacks using `../`, it's important to ensure that `CONTENT_DIR` itself is configured securely and that `path.basename()` is robust against all potential bypasses (e.g., null bytes, specific Unicode sequences, or double encoding if not handled by the web server). In Node.js, `path.basename` is generally robust against these, but a defense-in-depth approach might include explicit path normalization and validation.: Path Traversal via File Tool occurs when MCP file-handling tools accept untrusted input as filesystem paths without proper normalization, allowlisting, or base directory enforcement. Attackers can use relative path sequences, Unicode normalization tricks, double encoding, or null byte injection to access files outside the intended directory.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 225: 🟠 **HIGH** — `SAFE-T1105`

**Location:** Lines 201-354

**Description:**

Untrusted filename from `fs.watch` is used to construct a filesystem path without proper sandboxing, allowing path traversal for information disclosure via `fs.existsSync`.: Path Traversal via File Tool occurs when MCP file-handling tools accept untrusted input as filesystem paths without proper normalization, allowlisting, or base directory enforcement. Attackers can use relative path sequences, Unicode normalization tricks, double encoding, or null byte injection to access files outside the intended directory.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 226: 🟠 **HIGH** — `SAFE-T1201`

**Location:** Lines 1-200

**Description:**

The server dynamically loads and serves the newest HTML file from the 'CONTENT_DIR' for its main page. This allows the server's primary user interface and interactive elements to be changed at any time by simply dropping a new HTML file into this directory.: MCP Rug Pull Attack is a persistence technique where tools appear legitimate initially but can change their behavior after gaining user trust. This exploits dynamic tool definitions, remote configuration loading, or time-delayed activation to modify tool behavior post-approval.
Static analysis detects code patterns that ENABLE rug pulls: dynamic descriptions, remote tool loading without version pinning, and time-based conditional behavior.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 227: 🟠 **HIGH** — `SAFE-T1201`

**Location:** Lines 201-354

**Description:**

The server dynamically reloads client-side HTML content (tool definitions) when files in the 'CONTENT_DIR' are modified, allowing for post-approval changes in tool behavior.: MCP Rug Pull Attack is a persistence technique where tools appear legitimate initially but can change their behavior after gaining user trust. This exploits dynamic tool definitions, remote configuration loading, or time-delayed activation to modify tool behavior post-approval.
Static analysis detects code patterns that ENABLE rug pulls: dynamic descriptions, remote tool loading without version pinning, and time-based conditional behavior.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 228: 🟠 **HIGH** — `SAFE-T1204`

**Location:** Lines 201-354

**Description:**

The server appends user-controlled JSON data from WebSocket messages to a persistent 'events' file without validation. This file acts as a form of conversation memory or context, which could be exploited by malicious agents to implant persistent instructions.: Context Memory Implant exploits persistent memory systems in MCP-connected AI agents. Malicious agents write themselves into long-term vector stores, conversation histories, or system prompt storage to ensure re-loading in future sessions. The implanted instructions persist across server restarts and session boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 229: 🟠 **HIGH** — `SAFE-T1205`

**Location:** Lines 201-354

**Description:**

User-controlled JSON data is persistently stored to disk, which could be leveraged for persistent tool redefinition.: Persistent Tool Redefinition is a privilege escalation technique where attackers modify an MCP server's tool metadata or configuration to embed hidden commands that survive server restarts. By persisting malicious tool definitions to disk, database, or configuration files, the attacker ensures their modifications remain active even after the server is restarted or redeployed.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 230: 🟠 **HIGH** — `SAFE-T1301`

**Location:** Lines 1-200

**Description:**

The provided code implements a basic HTTP and WebSocket server. The `handleMessage` function, which would process incoming WebSocket text messages and potentially dispatch tool calls, is declared but not defined within the provided snippet. Without the implementation of `handleMessage` or any other explicit tool registration mechanism, it is not possible to determine if this server registers tools in a manner vulnerable to Cross-Server Tool Shadowing (SAFE-T1301). The current code primarily handles network communication and file serving, not tool definition or resolution.: Cross-Server Tool Shadowing is a privilege escalation technique where malicious MCP servers override or intercept legitimate tool calls from other servers. This attack exploits multi-server MCP environments where multiple servers can provide tools with the same or similar names, allowing attackers to shadow legitimate functionality with malicious implementations.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 231: 🟠 **HIGH** — `SAFE-T1301`

**Location:** Lines 201-354

**Description:**

The provided code does not implement any explicit MCP tool registration or resolution mechanisms. It functions as a basic web server serving HTML content and handling WebSocket communication for client events and reloads. Therefore, it is not directly vulnerable to Cross-Server Tool Shadowing (SAFE-T1301) as described, which relies on the ability to register and resolve named tools.: Cross-Server Tool Shadowing is a privilege escalation technique where malicious MCP servers override or intercept legitimate tool calls from other servers. This attack exploits multi-server MCP environments where multiple servers can provide tools with the same or similar names, allowing attackers to shadow legitimate functionality with malicious implementations.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 232: 🟠 **HIGH** — `SAFE-T1302`

**Location:** Lines 1-200

**Description:**

The server reads files from a configurable content directory (`CONTENT_DIR`) via the `/files/` endpoint. While `path.basename` is used to prevent directory traversal, if the `BRAINSTORM_DIR` environment variable is set to a sensitive system directory (e.g., `/etc`, `/root`) and the server runs with elevated privileges (e.g., root), an attacker could potentially read files within the `CONTENT_DIR` subdirectory of that sensitive location. This is an information disclosure risk, not a direct privilege escalation through tool abuse, but it relies on the server running with excessive privileges.: High-Privilege Tool Abuse occurs when MCP tools are configured with administrative, root, or superuser access to databases, APIs, or system resources. Attackers exploit these tools through prompt injection or direct manipulation to perform privileged operations such as modifying access controls, reading sensitive data, or escalating their own permissions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 233: 🟠 **HIGH** — `SAFE-T1302`

**Location:** Lines 201-354

**Description:**

The server writes user-controlled JSON data to a file within the `STATE_DIR`. While `JSON.stringify` prevents direct command injection, if the `STATE_DIR` is configured to a sensitive location and the server runs with elevated privileges, or if another privileged process later parses this file without proper sanitization, it could potentially lead to an issue. However, in this context, it's primarily a logging mechanism.: High-Privilege Tool Abuse occurs when MCP tools are configured with administrative, root, or superuser access to databases, APIs, or system resources. Attackers exploit these tools through prompt injection or direct manipulation to perform privileged operations such as modifying access controls, reading sensitive data, or escalating their own permissions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 234: 🟠 **HIGH** — `SAFE-T1304`

**Location:** Lines 201-354

**Description:**

The server processes arbitrary JSON messages from WebSocket clients, logs them, and optionally saves them to a local file. While this code does not directly handle authentication tokens or credentials, if the 'event' object were to contain sensitive information or tokens, its logging and file storage could expose that data. However, there is no evidence of tokens being reused or forwarded across services for privilege escalation.: Credential Relay Chain exploits token reuse across MCP service boundaries. Attackers capture tokens from one service interaction and replay them in tool-to-tool calls, cross-service requests, or downstream API invocations without proper audience validation, scope restriction, or token binding.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 235: 🟠 **HIGH** — `SAFE-T1307`

**Location:** Lines 201-354

**Description:**

The server appends arbitrary user-controlled JSON data to a file in a sensitive state directory without validation. If a privileged process later reads and interprets this file, it could be tricked into performing actions on behalf of an attacker.: Confused Deputy Attack exploits ambient authority in MCP servers where a privileged service performs actions on behalf of an unauthorized requester. In multi-tenant environments, this enables cross-tenant data access, privilege confusion between service tiers, or unauthorized operations performed through a trusted intermediary.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 236: 🟠 **HIGH** — `SAFE-T1308`

**Location:** Lines 1-200

**Description:**

The server does not implement any token-based authentication or authorization mechanisms. Therefore, the SAFE-T1308 (Token Scope Substitution) vulnerability, which relies on the manipulation of token scopes, is not directly applicable to this codebase.: Token Scope Substitution occurs when an attacker replaces a limited-scope OAuth token with one that has broader permissions but the same audience, exploiting insufficient scope validation to gain elevated privileges.
This attack targets OAuth implementations that validate tokens but fail to verify that the token scopes match what was originally requested or required for the operation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 237: 🟠 **HIGH** — `SAFE-T1401`

**Location:** Lines 1-200

**Description:**

The WebSocket server receives unvalidated text payloads from clients. If these payloads are subsequently used as 'tool descriptions' or fed into an MCP context for an AI agent without proper sanitization for newline or control characters, it could lead to SAFE-T1401 (Line Jumping) attacks.: Line Jumping injects newline characters, CRLF sequences, or control characters into MCP tool descriptions, parameter names, or metadata fields. The injected characters push malicious instructions outside the visible area of user interfaces, making them invisible during review while still being processed by the LLM.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 238: 🟠 **HIGH** — `SAFE-T1401`

**Location:** Lines 201-354

**Description:**

User-controlled input from WebSocket messages, which may contain newlines or control characters, is parsed as JSON and then written to a file (`events`) after being re-stringified. If this file is later consumed by an LLM that parses the JSON, the original newlines and control characters within string fields will be restored, potentially allowing for 'line jumping' or instruction precedence manipulation.: Line Jumping injects newline characters, CRLF sequences, or control characters into MCP tool descriptions, parameter names, or metadata fields. The injected characters push malicious instructions outside the visible area of user interfaces, making them invisible during review while still being processed by the LLM.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 239: 🟠 **HIGH** — `SAFE-T1402`

**Location:** Lines 1-200

**Description:**

The server serves unsanitized HTML content from the `CONTENT_DIR`, which can be controlled by an attacker. This allows for the injection of hidden instructions (e.g., via HTML comments, zero-width characters, or Unicode bidi overrides) that an AI agent or LLM consuming this content might interpret, leading to unintended behavior.: Instruction Stenography conceals malicious instructions within tool descriptions or metadata using steganographic techniques: HTML comments, zero-width Unicode characters, bidirectional text overrides, or invisible formatting. These hidden instructions are processed by LLMs but invisible in standard UI rendering.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 240: 🟠 **HIGH** — `SAFE-T1402`

**Location:** Lines 201-354

**Description:**

The server processes user-provided WebSocket messages, parses them as JSON, and then logs them to console and appends them to a file ('events' in STATE_DIR) without sanitizing for hidden instructions like HTML comments, zero-width characters, or Unicode bidi overrides. If these logs or event files are later consumed by an LLM or AI agent as part of its context or tool metadata, hidden instructions could be interpreted, leading to unintended model behavior, data exfiltration, or tool misuse.: Instruction Stenography conceals malicious instructions within tool descriptions or metadata using steganographic techniques: HTML comments, zero-width Unicode characters, bidirectional text overrides, or invisible formatting. These hidden instructions are processed by LLMs but invisible in standard UI rendering.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 241: 🟠 **HIGH** — `SAFE-T1407`

**Location:** Lines 1-200

**Description:**

The server is designed to serve dynamic content and handle WebSocket communication, which could be a component of an MCP server. While the code does not explicitly implement proxying logic to an external legitimate API, the missing implementation of the `handleMessage` function is a critical unknown. If `handleMessage` were to forward client messages to an external API, this server could then act as a proxy. Without that specific forwarding logic, it does not directly match the SAFE-T1407 technique.: An attacker operates an MCP server that forwards requests to a legitimate upstream API and relays the responses back to the client. Because the responses originate from the trusted service, the traffic appears normal in monitoring and logs, but the proxy can inspect, alter, or exfiltrate sensitive data. This enables stealthy data collection and response manipulation while hiding the true malicious behavior.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 242: 🟠 **HIGH** — `SAFE-T1408`

**Location:** Lines 1-200

**Description:**

The provided code does not implement any OAuth 2.0 functionality, therefore it is not directly vulnerable to OAuth protocol downgrade attacks (SAFE-T1408). The server primarily handles static file serving and WebSocket communication.: An attacker forces or coerces a client to use the OAuth 2.0 implicit flow instead of the authorization code flow with PKCE. Tokens delivered in URL fragments are easier to capture via logs, referrers, or malicious scripts, enabling token theft and session hijacking.
This technique detects the presence of implicit flow configuration, which indicates potential vulnerability. The authorization code flow with PKCE should always be preferred over implicit flow for security.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 243: 🟠 **HIGH** — `SAFE-T1408`

**Location:** Lines 201-354

**Description:**

The provided code does not appear to implement any OAuth client or server functionality. Therefore, it is not susceptible to OAuth protocol downgrade attacks (SAFE-T1408).: An attacker forces or coerces a client to use the OAuth 2.0 implicit flow instead of the authorization code flow with PKCE. Tokens delivered in URL fragments are easier to capture via logs, referrers, or malicious scripts, enabling token theft and session hijacking.
This technique detects the presence of implicit flow configuration, which indicates potential vulnerability. The authorization code flow with PKCE should always be preferred over implicit flow for security.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 244: 🟠 **HIGH** — `SAFE-T1501`

**Location:** Lines 201-354

**Description:**

The server processes arbitrary JSON messages from WebSocket clients, logs them, and conditionally writes them to a file. An attacker can inject 'schema-like' fields (e.g., 'description', 'default', 'enum', 'properties') with malicious instructions into these JSON messages. If an LLM processes these logs or files, it could be poisoned to extract sensitive information or alter its behavior, consistent with Full-Schema Poisoning (SAFE-T1501).: Full-Schema Poisoning manipulates JSON schema definitions used for MCP tool input validation to include hidden fields, default values with malicious content, or additionalProperties that allow arbitrary data injection. In auth schemas, this can expose credentials through coerced fields or default values that leak sensitive information.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 245: 🟠 **HIGH** — `SAFE-T1503`

**Location:** Lines 1-200

**Description:**

The application uses environment variables for configuration, which is a common practice. However, the code does not expose these environment variables or provide a mechanism for an attacker to enumerate or dump them via an AI agent, as described in SAFE-T1503.: Environment Variable Scraping extracts secrets from process environment variables. MCP server tools iterate over process.env, os.environ, or use getenv with attacker-controlled keys to harvest API keys, database passwords, cloud credentials, and other sensitive configuration stored in environment variables.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 246: 🟠 **HIGH** — `SAFE-T1503`

**Location:** Lines 201-354

**Description:**

The server logs user-provided event data to the console and appends it to a file in the state directory. While not directly dumping environment variables, this could potentially be used in conjunction with other vulnerabilities (e.g., prompt injection in an AI agent that uses this server) to log or store sensitive information if an attacker can coerce the AI into including environment variables in the 'event' object.: Environment Variable Scraping extracts secrets from process environment variables. MCP server tools iterate over process.env, os.environ, or use getenv with attacker-controlled keys to harvest API keys, database passwords, cloud credentials, and other sensitive configuration stored in environment variables.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 247: 🟠 **HIGH** — `SAFE-T1504`

**Location:** Lines 1-200

**Description:**

The HTTP server serves arbitrary files from the 'CONTENT_DIR' (defaulting to '/tmp/brainstorm/content'). If an MCP agent, under attacker control (e.g., via prompt injection), is coerced into writing sensitive access or refresh tokens into a file within this directory, the server will expose these tokens to any client requesting the file. This creates a direct exfiltration channel for tokens obtained by the agent, aligning with the SAFE-T1504 technique.: Token Theft via API Response occurs when MCP tool implementations inadvertently include access tokens, refresh tokens, or other credentials in their output responses. These leaked tokens are then visible in conversation history, logs, or can be captured by other tools in the chain for unauthorized access.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 248: 🟠 **HIGH** — `SAFE-T1504`

**Location:** Lines 201-354

**Description:**

The server logs and persists all incoming WebSocket messages (user events) to the console and a local file without sanitization. If these messages contain sensitive information such as access or refresh tokens, these tokens will be leaked into server logs and the local filesystem.: Token Theft via API Response occurs when MCP tool implementations inadvertently include access tokens, refresh tokens, or other credentials in their output responses. These leaked tokens are then visible in conversation history, logs, or can be captured by other tools in the chain for unauthorized access.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 249: 🟠 **HIGH** — `SAFE-T1601`

**Location:** Lines 1-200

**Description:**

The server exposes an HTTP endpoint `/files/` that allows direct retrieval of any file within the `CONTENT_DIR` (which defaults to `/tmp/brainstorm/content`).: MCP Server Enumeration involves probing for available MCP server instances, scanning registries, and discovering service endpoints. Attackers enumerate servers to map the attack surface, identify vulnerable or misconfigured instances, and discover high-value targets for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 250: 🟠 **HIGH** — `SAFE-T1601`

**Location:** Lines 1-200

**Description:**

The server establishes a WebSocket endpoint, which serves as the primary communication channel for the Model Context Protocol (MCP).: MCP Server Enumeration involves probing for available MCP server instances, scanning registries, and discovering service endpoints. Attackers enumerate servers to map the attack surface, identify vulnerable or misconfigured instances, and discover high-value targets for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 251: 🟠 **HIGH** — `SAFE-T1601`

**Location:** Lines 201-354

**Description:**

The server explicitly logs and writes to a file ('server-info') its listening port, host, full URL, and the paths to its content and state directories upon startup.: MCP Server Enumeration involves probing for available MCP server instances, scanning registries, and discovering service endpoints. Attackers enumerate servers to map the attack surface, identify vulnerable or misconfigured instances, and discover high-value targets for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 252: 🟠 **HIGH** — `SAFE-T1601`

**Location:** Lines 201-354

**Description:**

The server logs internal file paths ('filePath') whenever a screen (HTML file) is added or updated in the CONTENT_DIR, exposing details about the server's content structure.: MCP Server Enumeration involves probing for available MCP server instances, scanning registries, and discovering service endpoints. Attackers enumerate servers to map the attack surface, identify vulnerable or misconfigured instances, and discover high-value targets for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 253: 🟠 **HIGH** — `SAFE-T1602`

**Location:** Lines 1-200

**Description:**

The server allows enumeration of files within the 'CONTENT_DIR' by distinguishing between existing and non-existing files via HTTP status codes (200 OK vs 404 Not Found) when accessing the '/files/' endpoint.: Tool Enumeration discovers the full set of available MCP tools, their capabilities, input schemas, and permission requirements. Attackers use tools/list API calls and schema introspection to map available functionality, identify privileged tools, and find tools with weak input validation for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 254: 🟠 **HIGH** — `SAFE-T1603`

**Location:** Lines 1-200

**Description:**

The server processes incoming WebSocket messages via an undefined `handleMessage` function. This function is the most likely point where user input would interact with an AI agent, and thus where system prompt leakage (SAFE-T1603) could occur if the AI agent's responses inadvertently include parts of its internal instructions or debug information. Without the implementation of `handleMessage`, it's impossible to confirm or deny this vulnerability.: System Prompt Disclosure exposes internal system prompts, instructions, or configuration through debug mode endpoints, verbose error messages, tool output, or direct echoing. Exposed system prompts reveal security controls, filtering rules, and behavioral constraints that attackers can use to craft targeted bypass attacks.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 255: 🟠 **HIGH** — `SAFE-T1603`

**Location:** Lines 201-354

**Description:**

The server logs all incoming WebSocket messages from users, including their full content. While this does not directly leak system prompts from the server itself (as this server does not appear to be an AI agent managing system prompts), it could record sensitive information if a user intentionally or unintentionally sends such data. If an AI agent (separate from this server) were to reflect parts of its system prompt back to a user, and that user then sends that reflected prompt to this server, this log would capture it. This is more of an informational finding regarding logging practices rather than a direct system prompt leakage from this specific component.: System Prompt Disclosure exposes internal system prompts, instructions, or configuration through debug mode endpoints, verbose error messages, tool output, or direct echoing. Exposed system prompts reveal security controls, filtering rules, and behavioral constraints that attackers can use to craft targeted bypass attacks.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 256: 🟠 **HIGH** — `SAFE-T1606`

**Location:** Lines 1-200

**Description:**

The application uses `fs.readdirSync` to list files in the `CONTENT_DIR` to determine the newest screen. While this is an internal operation and not directly exposed as an unrestricted directory listing to the client, it does involve reading directory contents. The results are used to select a file to serve, not to list directory contents to the user.: Directory Listing via File Tool exploits MCP file tools that allow unrestricted directory enumeration. Attackers use fs.readdir, os.listdir, or equivalent functions to map the filesystem structure, discover sensitive files, locate configuration files, and identify credential storage locations for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 257: 🟠 **HIGH** — `SAFE-T1606`

**Location:** Lines 201-354

**Description:**

The application performs directory listing and watching operations on `CONTENT_DIR` using `fs.readdirSync` and `fs.watch`. While these operations are on a fixed, internal directory (`CONTENT_DIR`) and not directly user-controlled, it's important to ensure that the `CONTENT_DIR` itself is not user-configurable to arbitrary paths, which could indirectly lead to directory listing vulnerabilities if an attacker can control its value.: Directory Listing via File Tool exploits MCP file tools that allow unrestricted directory enumeration. Attackers use fs.readdir, os.listdir, or equivalent functions to map the filesystem structure, discover sensitive files, locate configuration files, and identify credential storage locations for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 258: 🟠 **HIGH** — `SAFE-T1606`

**Location:** Lines 201-354

**Description:**

The application uses `fs.watch` to monitor the `CONTENT_DIR` for changes. This operation is on a fixed, internal directory and not directly user-controlled. However, if `CONTENT_DIR` could be manipulated by an attacker to point to sensitive system directories, this could lead to information disclosure about file changes.: Directory Listing via File Tool exploits MCP file tools that allow unrestricted directory enumeration. Attackers use fs.readdir, os.listdir, or equivalent functions to map the filesystem structure, discover sensitive files, locate configuration files, and identify credential storage locations for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 259: 🟠 **HIGH** — `SAFE-T1701`

**Location:** Lines 1-200

**Description:**

The Brainstorm Companion server uses a default session directory `/tmp/brainstorm` for storing content and state. This directory is shared across different instances or potentially other tools if they are configured to use the same path or if `/tmp` is globally writable.: Cross-Tool Contamination exploits shared state between MCP tools including global variables, shared memory, common databases, or file-based caches. Malicious data written by one tool pollutes the execution context of another, enabling data exfiltration, privilege escalation, or behavior manipulation across tool boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 260: 🟠 **HIGH** — `SAFE-T1701`

**Location:** Lines 201-354

**Description:**

User-controlled input is written to a shared state file (`STATE_DIR/events`), which can lead to data pollution and potential cross-tool attacks if other tools consume this file.: Cross-Tool Contamination exploits shared state between MCP tools including global variables, shared memory, common databases, or file-based caches. Malicious data written by one tool pollutes the execution context of another, enabling data exfiltration, privilege escalation, or behavior manipulation across tool boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 261: 🟠 **HIGH** — `SAFE-T1703`

**Location:** Lines 1-200

**Description:**

The server reads and serves arbitrary HTML/JavaScript content from the 'CONTENT_DIR' (defaulting to /tmp/brainstorm/content) without sufficient sanitization. If a low-privileged tool or an attacker can write files to this directory, they can inject malicious content into the web interface served by this server.: Tool-Chaining Pivot exploits the output of one MCP tool as input to another more privileged tool, creating multi-step attack chains. Attackers leverage a low-privilege tool to produce crafted output that, when consumed by a higher-privilege tool, enables unauthorized operations such as command execution, data access, or privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 262: 🟠 **HIGH** — `SAFE-T1706`

**Location:** Lines 201-354

**Description:**

The provided code snippet does not implement any OAuth or JWT token validation logic. Therefore, it is not directly vulnerable to OAuth token replay attacks (SAFE-T1706) as it does not process such tokens.: OAuth Token Pivot Replay reuses OAuth tokens obtained from one service to access a different service or audience. Attackers exploit missing audience (aud) validation, overly broad token scopes, or lack of token binding to pivot from a lower-value service to a higher-value target using the same token.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 263: 🟠 **HIGH** — `SAFE-T1707`

**Location:** Lines 201-354

**Description:**

The WebSocket message handler `handleMessage` performs a state-changing operation (`fs.appendFileSync`) based on user input (`event.choice`) without explicit CSRF protection. While this is a state-changing operation in an MCP context, it does not involve the use of stored OAuth access tokens to interact with multi-tenant resource servers, which is a core characteristic of SAFE-T1707 (CSRF Token Relay).: CSRF Token Relay exploits missing or improperly validated CSRF tokens in MCP state-changing requests. Attackers forge cross-site requests or reuse OAuth state parameters to trigger unauthorized actions, manipulate tool configurations, or hijack authentication flows through the victim browser or agent.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 264: 🟠 **HIGH** — `SAFE-T1801`

**Location:** Lines 201-354

**Description:**

The server systematically collects user-provided event data into a file. If an incoming WebSocket message contains an 'event.choice' property, the entire JSON event object is appended to the 'events' file in the state directory. This mechanism can be leveraged by an adversary to perform bulk data collection.: Automated Data Harvesting uses MCP tools to systematically access and collect data through bulk queries, pagination scraping, and record enumeration. Attackers automate data collection across multiple endpoints or database tables to amass sensitive information for exfiltration or analysis.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 265: 🟠 **HIGH** — `SAFE-T1804`

**Location:** Lines 1-200

**Description:**

The `/files/` HTTP endpoint lacks rate limiting, enabling systematic enumeration and download of all files within the `CONTENT_DIR`.: API Data Harvest systematically extracts data through MCP tool API calls by abusing pagination, accessing bulk endpoints, or bypassing rate limits. Attackers iterate through API pages, manipulate query parameters, or use concurrent requests to extract large datasets that individual queries would not expose.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 266: 🟠 **HIGH** — `SAFE-T1804`

**Location:** Lines 201-354

**Description:**

The server's WebSocket message handler allows unauthenticated clients to systematically append arbitrary JSON data to a local file (`STATE_DIR/events`) without any rate limiting or size validation. This constitutes an uncontrolled bulk data ingestion endpoint.: API Data Harvest systematically extracts data through MCP tool API calls by abusing pagination, accessing bulk endpoints, or bypassing rate limits. Attackers iterate through API pages, manipulate query parameters, or use concurrent requests to extract large datasets that individual queries would not expose.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 267: 🟠 **HIGH** — `SAFE-T1904`

**Location:** Lines 201-354

**Description:**

The server processes incoming WebSocket messages as JSON and logs/stores the entire parsed JSON object, including any arbitrary fields. This allows an attacker to embed encoded commands or data within seemingly benign chat messages, which could then be consumed by a downstream automated system to establish a covert communication channel.: Chat-Based Backchannel uses MCP chat or messaging tools as a covert communication channel. Attackers embed encoded commands, base64 payloads, or steganographic data within seemingly normal chat messages to exfiltrate data or receive C2 instructions without triggering network-based detection.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 268: 🟠 **HIGH** — `SAFE-T1910`

**Location:** Lines 201-354

**Description:**

User-controlled WebSocket message content is logged directly to console.log. If server logs are collected by an external monitoring system, an attacker can embed sensitive data (e.g., secrets) into WebSocket messages, which will then be exfiltrated via the logging channel.: Covert Channel Exfiltration uses non-standard communication channels to exfiltrate data from MCP server environments. Techniques include DNS tunneling (encoding data in DNS queries), timing-based channels, HTTP header manipulation, and steganographic encoding that bypasses traditional network monitoring and DLP solutions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 269: 🟠 **HIGH** — `SAFE-T1911`

**Location:** Lines 1-200

**Description:**

The server processes incoming WebSocket text messages via an undefined `handleMessage` function. In the context of MCP servers, these messages are often structured JSON representing tool calls. If this function parses JSON payloads and does not strictly validate all parameters against a defined schema, it could allow an attacker to embed sensitive data in unused JSON fields for exfiltration (SAFE-T1911).: Attackers hide sensitive data inside optional or unused parameters (e.g., metadata, context, tags) that are accepted and forwarded by the server but not actively validated. These values can be logged or propagated to upstream services, enabling stealthy exfiltration without triggering obvious alarms.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 270: 🟠 **HIGH** — `SAFE-T1911`

**Location:** Lines 201-354

**Description:**

The server logs and stores all fields of incoming WebSocket JSON messages, including undefined or unused parameters, which can be exploited for covert data exfiltration.: Attackers hide sensitive data inside optional or unused parameters (e.g., metadata, context, tags) that are accepted and forwarded by the server but not actively validated. These values can be logged or propagated to upstream services, enabling stealthy exfiltration without triggering obvious alarms.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 271: 🟠 **HIGH** — `SAFE-T1912`

**Location:** Lines 1-200

**Description:**

The server directly serves files from the 'CONTENT_DIR' via the '/files/' endpoint without any content sanitization or steganography detection. This allows for covert data exfiltration if an upstream process (e.g., an MCP agent) embeds hidden data into these files.: Stego Response Exfiltration hides sensitive data within seemingly benign tool responses using steganographic techniques. Data is embedded in image pixels (LSB encoding), whitespace patterns, Unicode zero-width characters, or structured response fields that appear normal to users but contain extractable hidden information.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 272: 🟠 **HIGH** — `SAFE-T1912`

**Location:** Lines 201-354

**Description:**

User-supplied JSON messages are parsed and directly appended to a server-side 'events' log file without sanitization, allowing for covert data exfiltration via hidden characters or encoded payloads within the JSON structure.: Stego Response Exfiltration hides sensitive data within seemingly benign tool responses using steganographic techniques. Data is embedded in image pixels (LSB encoding), whitespace patterns, Unicode zero-width characters, or structured response fields that appear normal to users but contain extractable hidden information.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 273: 🟠 **HIGH** — `SAFE-T1915`

**Location:** Lines 1-200

**Description:**

The provided code implements a basic WebSocket server and HTTP file server. It does not contain any direct functionality for blockchain bridge calls, DEX interactions, or crypto mixing for fund laundering, which are the core components of the SAFE-T1915 technique. The `crypto` module is used for WebSocket key computation, not for cryptocurrency operations.: Cross-Chain Laundering uses MCP tools with blockchain access to launder funds through cross-chain bridges, decentralized exchange (DEX) swaps, and cryptocurrency mixing services. Attackers route stolen funds through multiple blockchains to obscure the transaction trail and convert illicit cryptocurrency to clean assets.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:1-200 const crypto = require('crypto');\nconst http = require('http');\nconst fs = require('fs');
```

</details>

---

#### Finding 274: 🟠 **HIGH** — `SAFE-T1915`

**Location:** Lines 201-354

**Description:**

The provided code does not contain any functionality related to blockchain bridge calls, DEX interactions, or crypto mixing for fund laundering. The code implements a WebSocket server for managing client connections, handling messages, tracking activity, and monitoring file system changes. It does not interact with any blockchain networks or cryptocurrency-related services.: Cross-Chain Laundering uses MCP tools with blockchain access to launder funds through cross-chain bridges, decentralized exchange (DEX) swaps, and cryptocurrency mixing services. Attackers route stolen funds through multiple blockchains to obscure the transaction trail and convert illicit cryptocurrency to clean assets.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

#### Finding 275: 🟠 **HIGH** — `SAFE-T2105`

**Location:** Lines 201-354

**Description:**

User-controlled input from WebSocket messages is parsed as JSON and then logged and potentially appended to a file (`eventsFile`) without validation or sanitization. If these logs or the `eventsFile` are used as input or context for an LLM, a malicious actor could inject false or misleading information, leading to the LLM generating disinformation.: Disinformation Output manipulates MCP tool responses or LLM outputs to generate false, misleading, or harmful information. Attackers inject instructions that cause the AI to produce fabricated data, distorted facts, or biased responses that appear authoritative, potentially causing real-world harm through decision-making based on false information.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/server.cjs:201-354           socket.end(encodeFrame(OPCODES.CLOSE, Buffer.alloc(0)));\n          clients.delete(socket);\n          return;
```

</details>

---

### 📁 `skills/systematic-debugging/condition-based-waiting-example.ts`

#### Finding 276: 🔴 **CRITICAL** — `SAFE-T1003`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting, primarily used in testing or asynchronous application logic. It does not contain any malicious functionality or characteristics of a trojanized MCP server as described in SAFE-T1003.: Malicious MCP-Server Distribution involves distributing trojanized MCP server implementations through package registries (npm, PyPI, Docker Hub) or code repositories. These servers appear legitimate but contain hidden malicious functionality such as data exfiltration, backdoors, or credential theft.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 277: 🔴 **CRITICAL** — `SAFE-T1007`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting on internal events within a `ThreadManager`. It does not handle network requests, OAuth flows, server configuration, or token management.: OAuth Authorization Phishing exploits the OAuth authorization flow to trick users into granting permissions to attacker-controlled applications. The attacker creates a convincing authorization page or redirects users through legitimate OAuth providers but captures the authorization code or tokens.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 278: 🔴 **CRITICAL** — `SAFE-T1101`

**Location:** Lines 1-158

**Description:**

The code implements utility functions for waiting on events within a thread manager. While these functions themselves do not directly execute external commands or interact with LLMs in a way that would lead to command injection or LLM hijacking, they process and return `LaceEvent` objects. If the `LaceEvent` data originates from untrusted external sources and is subsequently used by other parts of the system in a command execution context or fed directly into an LLM without proper sanitization, it could lead to a SAFE-T1101 vulnerability. This file acts as an intermediary, and the vulnerability would lie in the event generation or the downstream consumption of the event data, not in these waiting utilities themselves.: Prompt Injection via Tool Output occurs when MCP tools return user-controlled or external data directly to the LLM without proper sanitization, allowing malicious content in tool outputs to hijack the agent behavior. Tool outputs are typically trusted by LLMs and processed as authoritative information.
Common attack vectors include: database records with hidden instructions, file contents with prompt injection payloads, API responses crafted to override directives, and web scraping results with malicious instruction sequences.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 279: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Lines 18-20

**Description:**

This utility code processes `LaceEvent` objects which could potentially contain malicious instructions if sourced from untrusted external data. While this file itself does not directly construct LLM prompts, it provides a mechanism for other parts of the system to retrieve and act upon these events. If the `LaceEvent` data is later fed into an LLM without proper sanitization, it could lead to prompt injection.: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:18-20 export function waitForEvent(threadManager: ThreadManager,threadId: string,eventType: LaceEventType,timeoutMs = 5000): Promise<LaceEvent>
```

</details>

---

#### Finding 280: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Lines 90-92

**Description:**

The `waitForEventMatch` function accepts a `predicate` function that operates on `LaceEvent` objects. If a malicious `predicate` is supplied by a caller, it could potentially extract sensitive information from events or perform unintended side effects. While not a direct prompt injection vulnerability within this file, it highlights a potential point of interaction with untrusted data.: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:90-92 export function waitForEventMatch(threadManager: ThreadManager,threadId: string,predicate: (event: LaceEvent) => boolean,description: string,timeoutMs = 5000): Promise<LaceEvent>
```

</details>

---

#### Finding 281: 🔴 **CRITICAL** — `SAFE-T1103`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for waiting on specific events within a thread manager. It does not directly handle incoming tool invocation requests or perform any validation of tool names or arguments. Therefore, it is not susceptible to the 'Fake Tool Invocation (Function Spoofing)' technique (SAFE-T1103). The code's purpose is to observe and filter existing events, not to execute tools based on external input.: Fake Tool Invocation exploits insufficient tool validation in MCP hosts by injecting or spoofing tool names, duplicating tool registrations, or manipulating the JSON-RPC tool call protocol. Attackers can redirect legitimate tool calls to malicious implementations or register tools that shadow trusted ones.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 282: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Lines 1-158

**Description:**

The code implements utilities for condition-based waiting, which are often used in debugging and testing environments. While this code itself is not a debug endpoint, its presence in a system could indicate that debugging tools or practices are in use. The SAFE-T1109 technique specifically targets the 'MCP Inspector' tool, which is not implemented or directly referenced here. This code is a utility that might be *used by* developers during debugging, but it does not expose a vulnerable debug endpoint itself.: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 283: 🔴 **CRITICAL** — `SAFE-T1111`

**Location:** Lines 1-158

**Description:**

The code implements utility functions for condition-based waiting within a thread management system, likely for testing or internal process coordination. It does not directly interact with external AI CLI tools or exhibit any patterns associated with weaponizing AI agents for command execution or data exfiltration.: AI Agent CLI Weaponization exploits AI-powered CLI tools and agents that have unrestricted shell access or use dangerous permission-bypass flags. Attackers distribute malicious packages with postinstall scripts that invoke AI CLI tools in unsafe modes, or craft inputs that cause agents to execute arbitrary system commands, harvest credentials, or exfiltrate repository data.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 284: 🔴 **CRITICAL** — `SAFE-T1303`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting within a thread management system. It does not directly interact with container runtimes, execute external commands, or manipulate working directories in a manner that could lead to a container sandbox escape (SAFE-T1303). The code's functionality is limited to querying events from a `ThreadManager` and using `setTimeout` for polling.: Container Sandbox Escape exploits misconfigured container environments running MCP servers to break out of the sandbox and access the host operating system. Attack vectors include mounting the Docker socket, accessing /proc filesystem, running in privileged mode, using nsenter, or exploiting kernel vulnerabilities from within the container.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 285: 🔴 **CRITICAL** — `SAFE-T1305`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting on events within an MCP thread manager. While not directly exploitable for SAFE-T1305, these functions could be used by malicious agents if the underlying MCP server is misconfigured to run as root and exposes tools that allow command execution or file manipulation. The code itself does not introduce the vulnerability but could be part of a system where the vulnerability exists due to other components or deployment choices.: Host OS Privilege Escalation exploits operating system vulnerabilities or misconfigurations from within an MCP server process to gain root or administrative access on the host. Attack vectors include setuid binary abuse, kernel exploits, capability escalation, and accessing sensitive system files like /etc/shadow.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 286: 🔴 **CRITICAL** — `SAFE-T1306`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting and event management within a `ThreadManager` context. It does not involve any authentication, authorization, token minting, or trust chain validation logic. Therefore, it is not susceptible to the SAFE-T1306 'Rogue Authentication Server' technique.: Rogue Authorization Server involves setting up a malicious or misconfigured authorization server that mints OAuth tokens, JWTs, or API keys without proper validation, client verification, or trust chain enforcement. Attackers use these rogue-issued tokens to access MCP servers and tools that fail to validate the token issuer.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 287: 🔴 **CRITICAL** — `SAFE-T1309`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for waiting on specific events within an agent's thread. These functions (`waitForEvent`, `waitForEventCount`, `waitForEventMatch`) are designed for observation and synchronization, primarily in testing or debugging contexts, and do not directly involve an LLM or tool invocation logic.: Privileged Tool via Prompt Injection exploits LLM-based tool selection where the model chooses which tool to invoke based on natural language. Attackers craft prompts that manipulate the LLM into selecting privileged or dangerous tools, bypassing access controls that only exist at the prompt/UI layer rather than at the tool execution layer.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 288: 🔴 **CRITICAL** — `SAFE-T1502`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting on events within a thread manager. It does not contain any direct file system access or calls to MCP file tools that could lead to credential harvesting.: File-Based Credential Harvest occurs when MCP server tools read sensitive credential files from the host filesystem. Targets include SSH private keys, AWS credentials, environment files, browser cookies, keychain databases, and cloud provider configurations. The harvested credentials enable lateral movement and privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 289: 🔴 **CRITICAL** — `SAFE-T1702`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for waiting on events within a thread manager. It does not directly handle user input that would be used to construct or execute shell commands. The functions primarily filter and observe `LaceEvent` objects based on their type or a custom predicate. There is no evidence of external process execution (e.g., `child_process` in Node.js) or direct interaction with a shell.: Command Injection vulnerabilities occur when MCP server tools execute shell commands with unsanitized user input. Attackers can inject malicious commands that are executed with the server's privileges, potentially leading to arbitrary code execution, data exfiltration, or system compromise.

This technique targets common command execution patterns across multiple languages including Node.js (child_process), Python (subprocess, os.system), Rust (Command), and Go (os/exec). Unsafe practices include string concatenation in shell commands, missing input validation, and using shell=True or shell invocation modes.

Command injection is particularly dangerous in MCP servers because tools often have elevated privileges to interact with system resources, files, or external services. A successful injection can bypass all security boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 290: 🔴 **CRITICAL** — `SAFE-T1705`

**Location:** Lines 1-158

**Description:**

The code implements utility functions for waiting on specific events within a thread manager. While these functions themselves do not introduce agent-to-agent message injection vulnerabilities, they interact with a `ThreadManager` which is a critical component in multi-agent communication. The `ThreadManager` and the agents producing/consuming `LaceEvent`s are the actual points where SAFE-T1705 could manifest if not properly secured. This code is an observer of events, not a creator or manipulator of them.: Cross-Agent Instruction Injection targets multi-agent MCP architectures where agents communicate by passing messages or tool results. Attackers inject malicious instructions into one agent that propagate to other agents in the workflow, contaminating the entire multi-agent pipeline through message passing, shared context, or tool output forwarding.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 291: 🔴 **CRITICAL** — `SAFE-T1803`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting on thread events. It does not contain any direct database interaction, SQL queries, calls to database dump utilities (like mysqldump or pg_dump), or file/cloud export mechanisms that would facilitate bulk database export as described in SAFE-T1803.: Database Dump extracts entire database contents through MCP tools using bulk export commands like mysqldump, pg_dump, SELECT *, or ORM findAll without pagination. This enables mass exfiltration of structured data including user records, credentials, financial data, and other sensitive information stored in databases.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 292: 🔴 **CRITICAL** — `SAFE-T2101`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting and event observation within a thread management system. It does not contain any direct destructive operations such as file deletion, database table drops, or bulk data purges. The functions `waitForEvent`, `waitForEventCount`, and `waitForEventMatch` are purely read-only, querying events from a `ThreadManager` instance.: Data Destruction uses MCP tools to permanently delete or corrupt data through destructive operations such as rm -rf, DROP TABLE, file wiping, bulk delete API calls, or database truncation. Attackers may destroy data for sabotage, to cover their tracks, or as part of a ransomware attack.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 293: 🔴 **CRITICAL** — `SAFE-T2103`

**Location:** Lines 1-158

**Description:**

The provided code implements condition-based waiting utilities, which are generally benign. However, as a source code file, it could potentially be targeted by a malicious pull request (SAFE-T2103) that introduces backdoors, logic bombs, or other forms of sabotage. The current content of the file does not exhibit any malicious characteristics.: Code Sabotage via Malicious PR uses MCP tools with repository access to create automated pull requests containing malicious code changes, backdoors, or CI/CD pipeline modifications. Attackers manipulate build scripts, inject vulnerabilities, or alter deployment configurations through automated commits that bypass normal code review.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities...
```

</details>

---

#### Finding 294: 🔴 **CRITICAL** — `SAFE-T2106`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting on events within a thread manager. It does not directly interact with vector databases, generate or store embeddings, or manage long-term memory for AI agents. Therefore, it does not contain any direct vulnerabilities related to vector database poisoning (SAFE-T2106).: Context Memory Poisoning via Vector Store Contamination manipulates vector databases storing long-term AI memory. Attackers inject poisoned embeddings that blend legitimate and malicious content, surviving server restarts and reinstallations. The poisoned content can self-replicate across sessions and affect multiple agents sharing the same vector store.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 295: 🔴 **CRITICAL** — `SAFE-T2107`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting on events within a thread manager, primarily for testing or debugging purposes. While the `ThreadManager` and `LaceEvent` types could potentially be part of an MCP system that generates or processes data, this specific code's role is to observe and synchronize based on these events, not to generate, collect, or process training data itself. Therefore, it does not directly contribute to or enable training data manipulation, adversarial examples, or backdoor triggers in model training (SAFE-T2107). The context suggests it's for 'Lace test infrastructure improvements' to fix flaky tests, further indicating its role as a testing utility rather than a data pipeline component.: AI Model Poisoning manipulates machine learning models used by MCP servers through training data poisoning, adversarial example injection, or embedding backdoor triggers during fine-tuning. Poisoned models produce attacker-desired outputs when triggered by specific inputs while behaving normally otherwise.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 296: 🟠 **HIGH** — `SAFE-T1001`

**Location:** Lines 1-158

**Description:**

The file 'skills/systematic-debugging/condition-based-waiting-example.ts' is a utility file providing condition-based waiting functions. It does not define or manage MCP tool descriptions that would be exposed to an LLM. Therefore, it is not directly susceptible to SAFE-T1001 (Tool Poisoning Attack).: Tool Poisoning Attack (TPA) embeds malicious instructions within MCP tool descriptions that are invisible to users but processed by LLMs. This exploits the difference between displayed and processed tool descriptions.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 297: 🟠 **HIGH** — `SAFE-T1006`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting within a thread manager. It does not involve package installation, tool registration, or any mechanisms that could be directly exploited by social engineering to install malicious MCP servers.: User-Social-Engineering Install exploits user trust to convince them to install a malicious or compromised MCP server. This includes fake documentation, misleading READMEs, deceptive tool descriptions, or impersonating popular tools to trick users into granting permissions.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 298: 🟠 **HIGH** — `SAFE-T1008`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for event-based waiting within a thread manager. These functions are programmatic utilities and do not define or expose any 'tool descriptions' that would be interpreted by an AI agent in the context of the Model Context Protocol (MCP). The SAFE-T1008 technique specifically targets malicious instructions embedded within natural language tool descriptions provided to an AI agent.: Tool Shadowing Attack embeds instructions in tool descriptions that manipulate how AI agents use OTHER tools. In multi-server MCP setups, all tool descriptions are aggregated into a single prompt, allowing any tool to influence the behavior of others through cross-tool interference patterns.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 299: 🟠 **HIGH** — `SAFE-T1104`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for waiting on events within a `ThreadManager`. These functions are designed for reading and filtering existing event data, primarily for testing and debugging purposes, as indicated by the file path and comments. They do not directly perform any actions that would require or grant excessive permissions (e.g., file system access, network operations, command execution).: Over-Privileged Tool Abuse occurs when MCP tools are granted permissions far exceeding their functional requirements. Attackers exploit these over-privileged tools through prompt injection or direct manipulation to access sensitive resources, execute system commands, or escalate privileges beyond the intended scope.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 300: 🟠 **HIGH** — `SAFE-T1106`

**Location:** Lines 1-158

**Description:**

The provided utility functions `waitForEvent`, `waitForEventCount`, and `waitForEventMatch` implement a polling mechanism using `setTimeout` for recursive calls. While this pattern can sometimes lead to unbounded loops, these specific implementations include explicit `timeoutMs` parameters and corresponding checks (`Date.now() - startTime > timeoutMs`) to ensure termination. If the desired event or event count is not met within the specified timeout, the promises will reject, preventing an indefinite loop within these functions themselves.: Autonomous Loop Exploit triggers denial-of-service through self-reinforcing tool invocation loops. Attackers craft prompts or tool outputs that suggest non-terminal progress, causing agents to repeatedly invoke tools without convergence checks, rate limits, or iteration budgets.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:20-40 export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}
```

</details>

---

#### Finding 301: 🟠 **HIGH** — `SAFE-T1110`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for waiting on specific events within a thread manager. While these functions themselves do not directly process or interpret multimodal input (images/audio) that could contain embedded malicious instructions, they interact with `LaceEvent` objects which might originate from such inputs. If the `LaceEvent` data contains or refers to multimodal content that is later processed by other components without proper sanitization, it could indirectly contribute to the attack chain. However, this file is merely an event observation and synchronization mechanism, not an input processing component.: Multimodal Prompt Injection embeds malicious instructions within images, audio, or other non-text media that are processed by LLMs. Techniques include steganographic embedding in pixel data, OCR-extractable text injection, adversarial visual patterns, and metadata injection in media files. These bypass text-based sanitization since the payload is in a non-text modality.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 302: 🟠 **HIGH** — `SAFE-T1201`

**Location:** Lines 1-158

**Description:**

The `waitForEventMatch` function accepts a dynamic `predicate` function, which allows for highly flexible and custom event matching. While this is a powerful feature for testing and legitimate use cases, it could theoretically be abused if the `predicate` itself is dynamically generated or controlled by an attacker in a larger MCP tool context. However, this utility itself does not implement the dynamic definition or post-approval change mechanism described in SAFE-T1201.: MCP Rug Pull Attack is a persistence technique where tools appear legitimate initially but can change their behavior after gaining user trust. This exploits dynamic tool definitions, remote configuration loading, or time-delayed activation to modify tool behavior post-approval.
Static analysis detects code patterns that ENABLE rug pulls: dynamic descriptions, remote tool loading without version pinning, and time-based conditional behavior.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:90-119 export function waitForEventMatch(
  threadManager: ThreadManager,
  threadId: string,
  predicate: (event: LaceEvent) => boolean,
  description: string,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find(predicate);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${description} after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10);
      }
    };

    check();
  });
}
```

</details>

---

#### Finding 303: 🟠 **HIGH** — `SAFE-T1204`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for waiting on specific events within a thread manager. It reads event data but does not perform any write operations to persistent storage, vector stores, or conversation memory.: Context Memory Implant exploits persistent memory systems in MCP-connected AI agents. Malicious agents write themselves into long-term vector stores, conversation histories, or system prompt storage to ensure re-loading in future sessions. The implanted instructions persist across server restarts and session boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 304: 🟠 **HIGH** — `SAFE-T1402`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting based on event types or custom predicates. It does not define, process, or expose any tool metadata, descriptions, or parameters that would be parsed by an LLM or AI agent. Therefore, it is not susceptible to the SAFE-T1402 technique of embedding hidden instructions via HTML comments, zero-width characters, or Unicode bidi overrides in such fields.: Instruction Stenography conceals malicious instructions within tool descriptions or metadata using steganographic techniques: HTML comments, zero-width Unicode characters, bidirectional text overrides, or invisible formatting. These hidden instructions are processed by LLMs but invisible in standard UI rendering.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 305: 🟠 **HIGH** — `SAFE-T1404`

**Location:** Lines 1-158

**Description:**

The provided code implements utilities for observing and waiting for specific events within a thread manager. These functions (`waitForEvent`, `waitForEventCount`, `waitForEventMatch`) are designed to improve test reliability by waiting for conditions to be met, such as a certain event type or count, or an event matching a custom predicate. They retrieve events using `threadManager.getEvents(threadId)` and then filter or find them. The code does not generate, modify, or display user-visible responses, nor does it interact with the AI model's narrative generation or tool execution in a way that could lead to response tampering. Its purpose is purely observational for testing/debugging.: Response Tampering intercepts and modifies MCP tool responses before they are displayed to the user or processed by the LLM. Attackers alter response content to hide evidence of malicious actions, inject false information, bypass content filters, or manipulate the LLM context for subsequent interactions.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 306: 🟠 **HIGH** — `SAFE-T1408`

**Location:** Lines 1-158

**Description:**

The provided code implements condition-based waiting utilities for internal application events and does not interact with OAuth protocols, authentication flows, or token handling. Therefore, it is not susceptible to OAuth protocol downgrade (SAFE-T1408).: An attacker forces or coerces a client to use the OAuth 2.0 implicit flow instead of the authorization code flow with PKCE. Tokens delivered in URL fragments are easier to capture via logs, referrers, or malicious scripts, enabling token theft and session hijacking.
This technique detects the presence of implicit flow configuration, which indicates potential vulnerability. The authorization code flow with PKCE should always be preferred over implicit flow for security.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 307: 🟠 **HIGH** — `SAFE-T1501`

**Location:** Lines 1-158

**Description:**

The provided TypeScript code implements utility functions for event-based waiting and does not contain any JSON schema definitions or related parsing/processing logic. Therefore, it is not directly susceptible to 'Full-Schema Poisoning (FSP)' (SAFE-T1501). The technique description and examples in the README refer to JSON schema structures, not the TypeScript implementation itself.: Full-Schema Poisoning manipulates JSON schema definitions used for MCP tool input validation to include hidden fields, default values with malicious content, or additionalProperties that allow arbitrary data injection. In auth schemas, this can expose credentials through coerced fields or default values that leak sensitive information.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 308: 🟠 **HIGH** — `SAFE-T1503`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting within a thread management system. It does not directly interact with file system operations, environment variables, or any MCP file tools that could lead to the enumeration or dumping of environment variables.: Environment Variable Scraping extracts secrets from process environment variables. MCP server tools iterate over process.env, os.environ, or use getenv with attacker-controlled keys to harvest API keys, database passwords, cloud credentials, and other sensitive configuration stored in environment variables.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities...
```

</details>

---

#### Finding 309: 🟠 **HIGH** — `SAFE-T1601`

**Location:** Lines 1-158

**Description:**

The provided code is a utility for condition-based waiting within a `ThreadManager` for `LaceEvent`s. It is designed for internal event synchronization, likely in a testing or debugging context, as indicated by the file path and comments. It does not perform any network scanning, DNS rebinding, or direct MCP protocol enumeration calls (e.g., `tools/list`, `resources/list`, or `/sse` endpoint checks) that would constitute MCP server enumeration.: MCP Server Enumeration involves probing for available MCP server instances, scanning registries, and discovering service endpoints. Attackers enumerate servers to map the attack surface, identify vulnerable or misconfigured instances, and discover high-value targets for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 * ...
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 * ...
 */
export function waitForEventCount(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  count: number,
  timeoutMs = 5000
): Promise<LaceEvent[]> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const matchingEvents = events.filter((e) => e.type === eventType);

      if (matchingEvents.length >= count) {
        resolve(matchingEvents);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(
          new Error(
            `Timeout waiting for ${count} ${eventType} events after ${timeoutMs}ms (got ${matchingEvents.length})`
          )
        )...
```

</details>

---

#### Finding 310: 🟠 **HIGH** — `SAFE-T1602`

**Location:** Lines 1-158

**Description:**

The utility functions `waitForEvent`, `waitForEventCount`, and especially `waitForEventMatch` provide a mechanism to programmatically query and filter `LaceEvent` objects within a `ThreadManager`. If `LaceEvent` objects contain metadata about tool calls (e.g., `TOOL_CALL`, `TOOL_RESULT` types as shown in examples) and their associated data (e.g., tool names, arguments, results), an attacker with access to these functions and control over `threadId` or the `predicate` function could enumerate available tools, their capabilities, and potentially their input/output schemas. This directly aligns with the SAFE-T1602 technique of discovering available tools and their schemas.: Tool Enumeration discovers the full set of available MCP tools, their capabilities, input schemas, and permission requirements. Attackers use tools/list API calls and schema introspection to map available functionality, identify privileged tools, and find tools with weak input validation for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:20-39 export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESSAGE events (initial response + continuation)
 *   await waitForEventCount(threadManager, agentThreadId, 'AGENT_MESSAGE', 2);
 */
export function waitForEventCount(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  count: number,
  timeoutMs = 5000
): Promise<LaceEvent[]> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const matchingEvents = events.filter((e) => e.type === eventType);

      if (matchingEvents.length >= count) {
        resolve(matchingEvents);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(
          new Error(
            `Timeout waitin...
```

</details>

---

#### Finding 311: 🟠 **HIGH** — `SAFE-T1603`

**Location:** Line 110

**Description:**

The `waitForEventMatch` function includes a user-provided `description` string directly into its error message. If sensitive system prompts or instructions are passed into this `description` parameter by a caller, they could be leaked through error messages, especially in debug modes or verbose logging environments.: System Prompt Disclosure exposes internal system prompts, instructions, or configuration through debug mode endpoints, verbose error messages, tool output, or direct echoing. Exposed system prompts reveal security controls, filtering rules, and behavioral constraints that attackers can use to craft targeted bypass attacks.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:110-110 reject(new Error(`Timeout waiting for ${description} after ${timeoutMs}ms`));
```

</details>

---

#### Finding 312: 🟠 **HIGH** — `SAFE-T1701`

**Location:** Lines 1-158

**Description:**

The `waitForEvent*` utility functions interact with a `ThreadManager` which acts as a shared event bus for different `threadId`s. These functions allow querying events for any specified `threadId`. If the `ThreadManager` instance is shared across multiple tools/agents and lacks robust access control to restrict which `threadId`s a given tool can query, a compromised tool could use these functions to observe events from other tools' threads.: Cross-Tool Contamination exploits shared state between MCP tools including global variables, shared memory, common databases, or file-based caches. Malicious data written by one tool pollutes the execution context of another, enabling data exfiltration, privilege escalation, or behavior manipulation across tool boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:20-20 export function waitForEvent(
skills/systematic-debugging/condition-based-waiting-example.ts:50-50 export function waitForEventCount(
skills/systematic-debugging/condition-based-waiting-example.ts:86-86 export function waitForEventMatch(
```

</details>

---

#### Finding 313: 🟠 **HIGH** — `SAFE-T1703`

**Location:** Lines 1-158

**Description:**

The code implements utility functions for waiting on specific events within a thread manager, primarily for testing and synchronization purposes. While these functions observe events that could be part of a tool-chaining scenario, they do not inherently create or facilitate the 'output from one tool used as input to a privileged tool' vulnerability described in SAFE-T1703. The code is a passive observer of events, not an active participant in generating or forwarding tool outputs as inputs to other tools in a way that could lead to privilege escalation.: Tool-Chaining Pivot exploits the output of one MCP tool as input to another more privileged tool, creating multi-step attack chains. Attackers leverage a low-privilege tool to produce crafted output that, when consumed by a higher-privilege tool, enables unauthorized operations such as command execution, data access, or privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 314: 🟠 **HIGH** — `SAFE-T1801`

**Location:** Lines 1-158

**Description:**

The `waitForEventCount` function systematically collects multiple `LaceEvent` records by repeatedly calling `threadManager.getEvents(threadId)` and filtering them. This pattern of bulk data access and collection aligns with the data harvesting aspect of SAFE-T1801.: Automated Data Harvesting uses MCP tools to systematically access and collect data through bulk queries, pagination scraping, and record enumeration. Attackers automate data collection across multiple endpoints or database tables to amass sensitive information for exfiltration or analysis.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:66-89 export function waitForEventCount(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  count: number,
  timeoutMs = 5000
): Promise<LaceEvent[]> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const matchingEvents = events.filter((e) => e.type === eventType);

      if (matchingEvents.length >= count) {
        resolve(matchingEvents);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(
          new Error(
            `Timeout waiting for ${count} ${eventType} events after ${timeoutMs}ms (got ${matchingEvents.length})`
          )
        );
      } else {
        setTimeout(check, 10);
      }
    };

    check();
  });
}
```

</details>

---

#### Finding 315: 🟠 **HIGH** — `SAFE-T1804`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for waiting on internal events within an MCP thread manager. While these functions themselves do not directly perform external API calls or implement systematic API abuse, they interact with `threadManager.getEvents(threadId)`. If the `getEvents` method or the `ThreadManager` itself were to expose sensitive internal events without proper access controls, rate limiting, or if it allowed enumeration of events across unauthorized threads, it could indirectly contribute to information gathering. However, the `waitForEvent` utilities are designed for observation, not for initiating external data extraction or bypassing rate limits on external APIs, which is the core of SAFE-T1804. The primary risk would lie in the implementation of `ThreadManager.getEvents` and the data it exposes, rather than in these waiting utilities.: API Data Harvest systematically extracts data through MCP tool API calls by abusing pagination, accessing bulk endpoints, or bypassing rate limits. Attackers iterate through API pages, manipulate query parameters, or use concurrent requests to extract large datasets that individual queries would not expose.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:25-39 export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}
```

</details>

---

#### Finding 316: 🟠 **HIGH** — `SAFE-T1904`

**Location:** Lines 1-158

**Description:**

The `waitForEventMatch` function allows for custom predicates to filter `LaceEvent`s based on their data. While not a vulnerability itself, this mechanism could be leveraged by a malicious actor to implement the receiving end of a SAFE-T1904 (Chat-Based Backchannel) attack. If `LaceEvent.data` contains LLM responses, a custom predicate could be crafted to parse and act upon hidden commands or data embedded within those responses. The vulnerability would lie in the specific implementation of the predicate and subsequent event handling, not in this utility function.: Chat-Based Backchannel uses MCP chat or messaging tools as a covert communication channel. Attackers embed encoded commands, base64 payloads, or steganographic data within seemingly normal chat messages to exfiltrate data or receive C2 instructions without triggering network-based detection.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:100-130 export function waitForEventMatch(
  threadManager: ThreadManager,
  threadId: string,
  predicate: (event: LaceEvent) => boolean,
  description: string,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find(predicate);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${description} after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10);
      }
    };

    check();
  });
}
```

</details>

---

#### Finding 317: 🟠 **HIGH** — `SAFE-T1910`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for waiting on internal events within a `ThreadManager`. These functions poll for events at a fixed interval (10ms) and resolve or reject based on event presence or a timeout. While timing channels are a theoretical possibility in any system with variable timing, these functions are designed to passively observe internal events rather than actively transmit data through timing modulation. The fixed polling interval and the nature of waiting for events make it highly improbable for this specific code to be used as an effective timing channel for data exfiltration. The code does not perform any external network requests, DNS lookups, or data encoding/steganography for exfiltration purposes.: Covert Channel Exfiltration uses non-standard communication channels to exfiltrate data from MCP server environments. Techniques include DNS tunneling (encoding data in DNS queries), timing-based channels, HTTP header manipulation, and steganographic encoding that bypasses traditional network monitoring and DLP solutions.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

#### Finding 318: 🟠 **HIGH** — `SAFE-T1915`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting within a `ThreadManager` system, primarily for improving test reliability. It does not contain any direct blockchain bridge calls, DEX interactions, or crypto mixing functionalities.: Cross-Chain Laundering uses MCP tools with blockchain access to launder funds through cross-chain bridges, decentralized exchange (DEX) swaps, and cryptocurrency mixing services. Attackers route stolen funds through multiple blockchains to obscure the transaction trail and convert illicit cryptocurrency to clean assets.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities...
```

</details>

---

#### Finding 319: 🟠 **HIGH** — `SAFE-T2105`

**Location:** Lines 1-158

**Description:**

The provided code implements utility functions for condition-based waiting on events within a thread manager. While these functions themselves do not directly generate or manipulate LLM output, they are part of the infrastructure that an MCP server uses to manage and observe events. If the events themselves (e.g., `LaceEvent` data) were to be manipulated by other means (e.g., tool poisoning, context manipulation), then an agent consuming these events could potentially be influenced to produce disinformation. However, this code is merely an observer and waiter for such events, not the source of their content or the mechanism for LLM output generation.: Disinformation Output manipulates MCP tool responses or LLM outputs to generate false, misleading, or harmful information. Attackers inject instructions that cause the AI to produce fabricated data, distorted facts, or biased responses that appear authoritative, potentially causing real-world harm through decision-making based on false information.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/condition-based-waiting-example.ts:1-158 skills/systematic-debugging/condition-based-waiting-example.ts:1-158 // Complete implementation of condition-based waiting utilities
// From: Lace test infrastructure improvements (2025-10-03)
// Context: Fixed 15 flaky tests by replacing arbitrary timeouts

import type { ThreadManager } from '~/threads/thread-manager';
import type { LaceEvent, LaceEventType } from '~/threads/types';

/**
 * Wait for a specific event type to appear in thread
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to the first matching event
 *
 * Example:
 *   await waitForEvent(threadManager, agentThreadId, 'TOOL_RESULT');
 */
export function waitForEvent(
  threadManager: ThreadManager,
  threadId: string,
  eventType: LaceEventType,
  timeoutMs = 5000
): Promise<LaceEvent> {
  return new Promise((resolve, reject) => {
    const startTime = Date.now();

    const check = () => {
      const events = threadManager.getEvents(threadId);
      const event = events.find((e) => e.type === eventType);

      if (event) {
        resolve(event);
      } else if (Date.now() - startTime > timeoutMs) {
        reject(new Error(`Timeout waiting for ${eventType} event after ${timeoutMs}ms`));
      } else {
        setTimeout(check, 10); // Poll every 10ms for efficiency
      }
    };

    check();
  });
}

/**
 * Wait for a specific number of events of a given type
 *
 * @param threadManager - The thread manager to query
 * @param threadId - Thread to check for events
 * @param eventType - Type of event to wait for
 * @param count - Number of events to wait for
 * @param timeoutMs - Maximum time to wait (default 5000ms)
 * @returns Promise resolving to all matching events once count is reached
 *
 * Example:
 *   // Wait for 2 AGENT_MESS...
```

</details>

---

### 📁 `skills/writing-skills/graphviz-conventions.dot`

#### Finding 320: 🔴 **CRITICAL** — `SAFE-T1003`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file, which defines a style guide for diagrams. It is not an executable MCP server package or source code that could be trojanized.: Malicious MCP-Server Distribution involves distributing trojanized MCP server implementations through package registries (npm, PyPI, Docker Hub) or code repositories. These servers appear legitimate but contain hidden malicious functionality such as data exfiltration, backdoors, or credential theft.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 321: 🔴 **CRITICAL** — `SAFE-T1007`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file, which is a declarative language for describing graphs. It defines a style guide for process diagrams and does not contain any executable code, server configurations, or OAuth flow implementations. Therefore, it cannot directly implement or contribute to an OAuth Authorization Phishing vulnerability (SAFE-T1007).: OAuth Authorization Phishing exploits the OAuth authorization flow to trick users into granting permissions to attacker-controlled applications. The attacker creates a convincing authorization page or redirects users through legitimate OAuth providers but captures the authorization code or tokens.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 322: 🔴 **CRITICAL** — `SAFE-T1101`

**Location:** Lines 1-172

**Description:**

The file 'skills/writing-skills/graphviz-conventions.dot' is a Graphviz DOT file, which is a graph description language. It defines a style guide for a process DSL. While some node labels contain strings that resemble shell commands (e.g., "git commit -m 'msg'", "npm test"), these are literal strings intended for display within the generated graph, not executable code. The DOT language itself does not support direct execution of system commands based on its content.: Prompt Injection via Tool Output occurs when MCP tools return user-controlled or external data directly to the LLM without proper sanitization, allowing malicious content in tool outputs to hijack the agent behavior. Tool outputs are typically trusted by LLMs and processed as authoritative information.
Common attack vectors include: database records with hidden instructions, file contents with prompt injection payloads, API responses crafted to override directives, and web scraping results with malicious instruction sequences.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 323: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Lines 1-172

**Description:**

The Graphviz DOT file contains literal command strings and warning messages as node labels, such as 'git commit -m 'msg'' and 'NEVER use git add -A'. While these are part of the intended content of a style guide, if an LLM were to process this file and its task involved executing or strictly following instructions found within, these labels could be misinterpreted. However, there are no explicit prompt injection patterns (e.g., 'SYSTEM: Ignore previous instructions') present that aim to subvert the LLM's primary directive.: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 324: 🔴 **CRITICAL** — `SAFE-T1103`

**Location:** Lines 1-172

**Description:**

The file 'skills/writing-skills/graphviz-conventions.dot' is a Graphviz DOT file, which describes a style guide for a process DSL. It contains examples of node labels that mimic command-line instructions (e.g., 'git commit -m 'msg'', 'npm test', 'grep -r 'pattern' .'). While these labels resemble commands, they are purely descriptive elements within a diagram and are not executable code. The file itself does not implement any MCP server or client logic that could be vulnerable to function name spoofing or tool impersonation.: Fake Tool Invocation exploits insufficient tool validation in MCP hosts by injecting or spoofing tool names, duplicating tool registrations, or manipulating the JSON-RPC tool call protocol. Attackers can redirect legitimate tool calls to malicious implementations or register tools that shadow trusted ones.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 325: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Lines 1-172

**Description:**

The file contains examples of commands like 'npm test' and 'git status' as node labels. While these are not executable code within this Graphviz DOT file, they represent commands that could be executed in a development environment. This is purely informational and does not indicate a vulnerability in this specific file.: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 326: 🔴 **CRITICAL** — `SAFE-T1111`

**Location:** Lines 1-172

**Description:**

The Graphviz DOT file contains examples of shell commands as node labels, such as 'git commit -m 'msg'', 'npm test', and 'grep -r 'pattern' .'. While these are presented as literal strings within a graph definition and are not executable code in this context, an AI CLI tool processing this file might misinterpret these as commands to be executed if not properly sandboxed or if it has an overly permissive execution environment. This is a pattern match for potential command injection if the DOT file itself were to be processed by a vulnerable AI agent that executes node labels.: AI Agent CLI Weaponization exploits AI-powered CLI tools and agents that have unrestricted shell access or use dangerous permission-bypass flags. Attackers distribute malicious packages with postinstall scripts that invoke AI CLI tools in unsafe modes, or craft inputs that cause agents to execute arbitrary system commands, harvest credentials, or exfiltrate repository data.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 327: 🔴 **CRITICAL** — `SAFE-T1303`

**Location:** Lines 1-172

**Description:**

The file is a Graphviz DOT file, which is a graph description language used for visualization. It contains literal command strings like 'git commit -m 'msg'' and 'npm test' as node labels. These are descriptive examples within the graph, not executable code that would trigger a container runtime 'exec' or path traversal.: Container Sandbox Escape exploits misconfigured container environments running MCP servers to break out of the sandbox and access the host operating system. Attack vectors include mounting the Docker socket, accessing /proc filesystem, running in privileged mode, using nsenter, or exploiting kernel vulnerabilities from within the container.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 328: 🔴 **CRITICAL** — `SAFE-T1305`

**Location:** Lines 1-172

**Description:**

The file 'skills/writing-skills/graphviz-conventions.dot' is a Graphviz DOT file defining a style guide for a process DSL. It does not contain executable code, server configurations, or deployment scripts that could lead to the SAFE-T1305 (Host OS Privilege Escalation) vulnerability.: Host OS Privilege Escalation exploits operating system vulnerabilities or misconfigurations from within an MCP server process to gain root or administrative access on the host. Attack vectors include setuid binary abuse, kernel exploits, capability escalation, and accessing sensitive system files like /etc/shadow.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 329: 🔴 **CRITICAL** — `SAFE-T1306`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file, which defines a diagram for a style guide. It does not contain any executable code related to authentication, token issuance, or server configuration.: Rogue Authorization Server involves setting up a malicious or misconfigured authorization server that mints OAuth tokens, JWTs, or API keys without proper validation, client verification, or trust chain enforcement. Attackers use these rogue-issued tokens to access MCP servers and tools that fail to validate the token issuer.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 330: 🔴 **CRITICAL** — `SAFE-T1309`

**Location:** Lines 1-172

**Description:**

The file 'skills/writing-skills/graphviz-conventions.dot' is a Graphviz DOT file, which describes a style guide for a process DSL. It contains examples of commands (e.g., 'git commit -m 'msg'', 'npm test') as literal strings within graph nodes. While these strings represent commands, they are part of a static graph definition and are not executable code or direct prompts for an LLM agent to invoke tools. The file itself is a descriptive artifact, not an interactive component susceptible to prompt injection for tool invocation.: Privileged Tool via Prompt Injection exploits LLM-based tool selection where the model chooses which tool to invoke based on natural language. Attackers craft prompts that manipulate the LLM into selecting privileged or dangerous tools, bypassing access controls that only exist at the prompt/UI layer rather than at the tool execution layer.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 331: 🔴 **CRITICAL** — `SAFE-T1502`

**Location:** Lines 1-172

**Description:**

The code contains literal commands like 'git status' and 'npm test' which, while not directly exploitable in this context, represent commands that an MCP agent might execute. If an agent were to execute arbitrary commands from a Graphviz definition, this could be a vector for command injection. However, in this specific Graphviz file, these are merely descriptive labels within a style guide.: File-Based Credential Harvest occurs when MCP server tools read sensitive credential files from the host filesystem. Targets include SSH private keys, AWS credentials, environment files, browser cookies, keychain databases, and cloud provider configurations. The harvested credentials enable lateral movement and privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 332: 🔴 **CRITICAL** — `SAFE-T1505`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file, which is a declarative language for describing graphs. It does not contain executable code that could directly perform memory inspection or process memory dumps to extract secrets at runtime. The file serves as a style guide and documentation for process diagrams.: In-Memory Secret Extraction targets secrets held in process memory at runtime. Attackers use memory dump tools, /proc/*/mem access, heap inspection, or debugger attachment to extract encryption keys, tokens, passwords, and other sensitive data that exists only in memory and would not be found in files or environment variables.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 333: 🔴 **CRITICAL** — `SAFE-T1702`

**Location:** Lines 1-172

**Description:**

The Graphviz DOT file contains literal strings that represent shell commands (e.g., 'git commit -m 'msg'', 'grep -r 'pattern' .', 'npm test'). While these are intended for display within the generated graph and not for execution by the DOT language itself, their presence as examples of 'commands' in a DSL could be misleading if the DSL interpreter or a downstream tool were to misinterpret them as executable code without proper sanitization. However, in the context of a standard Graphviz renderer, these are treated as inert text.: Command Injection vulnerabilities occur when MCP server tools execute shell commands with unsanitized user input. Attackers can inject malicious commands that are executed with the server's privileges, potentially leading to arbitrary code execution, data exfiltration, or system compromise.

This technique targets common command execution patterns across multiple languages including Node.js (child_process), Python (subprocess, os.system), Rust (Command), and Go (os/exec). Unsafe practices include string concatenation in shell commands, missing input validation, and using shell=True or shell invocation modes.

Command injection is particularly dangerous in MCP servers because tools often have elevated privileges to interact with system resources, files, or external services. A successful injection can bypass all security boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 334: 🔴 **CRITICAL** — `SAFE-T1704`

**Location:** Lines 1-172

**Description:**

The provided Graphviz DOT file defines a static graph structure and styling. It does not contain any attributes that typically accept external URLs (e.g., 'URL' or 'image' attributes) which could be user-controlled and lead to SSRF.: Compromised-Server Pivot / SSRF occurs when MCP tools accept user-controlled URLs in HTTP requests without host allowlisting or validation. Attackers exploit this to access internal services, cloud metadata endpoints, localhost services, or private network resources that the MCP server can reach but the attacker cannot directly access.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 335: 🔴 **CRITICAL** — `SAFE-T1705`

**Location:** Lines 1-172

**Description:**

The provided code is a Graphviz DOT file defining a style guide for a process DSL. It describes visual conventions for nodes and edges, but does not implement any multi-agent system, communication channels, or agent logic. Therefore, it is not directly susceptible to agent-to-agent message injection vulnerabilities.: Cross-Agent Instruction Injection targets multi-agent MCP architectures where agents communicate by passing messages or tool results. Attackers inject malicious instructions into one agent that propagate to other agents in the workflow, contaminating the entire multi-agent pipeline through message passing, shared context, or tool output forwarding.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 336: 🔴 **CRITICAL** — `SAFE-T1803`

**Location:** Lines 1-172

**Description:**

The provided code is a Graphviz DOT file defining a style guide for a process DSL. It does not contain any executable code, database queries, or calls to database export utilities like SELECT *, mysqldump, or pg_dump. Therefore, it does not present a vulnerability related to bulk database export.: Database Dump extracts entire database contents through MCP tools using bulk export commands like mysqldump, pg_dump, SELECT *, or ORM findAll without pagination. This enables mass exfiltration of structured data including user records, credentials, financial data, and other sensitive information stored in databases.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 337: 🔴 **CRITICAL** — `SAFE-T2101`

**Location:** Lines 1-172

**Description:**

The Graphviz DOT file contains examples of shell commands as node labels, such as 'git commit -m 'msg'' and 'grep -r 'pattern' .'. While these are examples of commands, they are purely descriptive within the diagram and are not executed by the DOT file itself. This file serves as a style guide for creating diagrams, not as an executable script or an MCP tool.: Data Destruction uses MCP tools to permanently delete or corrupt data through destructive operations such as rm -rf, DROP TABLE, file wiping, bulk delete API calls, or database truncation. Attackers may destroy data for sabotage, to cover their tracks, or as part of a ransomware attack.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 338: 🔴 **CRITICAL** — `SAFE-T2103`

**Location:** Lines 1-172

**Description:**

The file 'graphviz-conventions.dot' is a Graphviz DOT file, which is a descriptive language for visualizing graphs. It defines a style guide for process diagrams and does not contain executable code, CI/CD pipeline definitions, or AI agent configurations that could be directly manipulated to introduce malicious changes as described by SAFE-T2103.: Code Sabotage via Malicious PR uses MCP tools with repository access to create automated pull requests containing malicious code changes, backdoors, or CI/CD pipeline modifications. Attackers manipulate build scripts, inject vulnerabilities, or alter deployment configurations through automated commits that bypass normal code review.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 339: 🔴 **CRITICAL** — `SAFE-T2106`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file defining a style guide for diagrams. It does not contain any code that interacts with vector databases, embedding models, or any other components related to the SAFE-T2106 technique (vector database poisoning). Therefore, it cannot directly implement or be vulnerable to this specific attack.: Context Memory Poisoning via Vector Store Contamination manipulates vector databases storing long-term AI memory. Attackers inject poisoned embeddings that blend legitimate and malicious content, surviving server restarts and reinstallations. The poisoned content can self-replicate across sessions and affect multiple agents sharing the same vector store.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 340: 🔴 **CRITICAL** — `SAFE-T3001`

**Location:** Lines 1-172

**Description:**

The file 'skills/writing-skills/graphviz-conventions.dot' is a Graphviz DOT file defining a style guide for process diagrams. While it contains examples of 'warnings' and 'commands' (e.g., 'STOP: Critical warning', 'NEVER use git add -A', 'git commit -m 'msg''), these are part of the instructional content of the style guide itself, demonstrating how to represent different types of information in a diagram. This file is not a document intended to be ingested into a RAG knowledge base for retrieval-augmented generation, nor does it contain content designed to maliciously poison a RAG system.: RAG Backdoor Attack injects malicious documents into Retrieval-Augmented Generation knowledge bases used by MCP servers. Poisoned documents contain hidden instructions, false information, or trigger phrases that activate when retrieved, causing the LLM to produce attacker-controlled outputs, leak data, or execute unauthorized actions.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 341: 🟠 **HIGH** — `SAFE-T1001`

**Location:** Lines 1-172

**Description:**

The file defines a style guide for a Domain Specific Language (DSL) using Graphviz DOT syntax. While an LLM might process this file, it does not represent an MCP tool description that would be directly used for function calling or action execution by an LLM in the context of SAFE-T1001. The instructions within the file are explicit Graphviz rendering commands, not hidden directives intended to manipulate LLM behavior during tool execution.: Tool Poisoning Attack (TPA) embeds malicious instructions within MCP tool descriptions that are invisible to users but processed by LLMs. This exploits the difference between displayed and processed tool descriptions.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 342: 🟠 **HIGH** — `SAFE-T1004`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file, which defines a style guide for a process DSL. It is a documentation artifact and does not contain executable code or configuration related to server registration, DNS, or network communication. Therefore, it cannot directly implement or be vulnerable to server impersonation or name collision as described in SAFE-T1004.: Server Impersonation occurs when an attacker registers an MCP server with a name designed to be confused with a legitimate service (typosquatting, homograph attacks, or DNS cache poisoning). Users connecting to the impersonated server unknowingly send credentials and data to the attacker.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 343: 🟠 **HIGH** — `SAFE-T1006`

**Location:** Lines 1-172

**Description:**

The file 'skills/writing-skills/graphviz-conventions.dot' is a Graphviz DOT file defining a style guide for process diagrams. It does not contain any executable code, installation scripts, or package definitions that could directly implement or facilitate the installation of a malicious MCP server or agent as described by SAFE-T1006.: User-Social-Engineering Install exploits user trust to convince them to install a malicious or compromised MCP server. This includes fake documentation, misleading READMEs, deceptive tool descriptions, or impersonating popular tools to trick users into granting permissions.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 344: 🟠 **HIGH** — `SAFE-T1009`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file, which describes graph structures for visualization. It does not contain any executable code, network configuration, or OAuth client/server logic that could be directly vulnerable to or implement the SAFE-T1009 Authorization Server Mix-up technique.: Authorization Server Mix-up occurs when an OAuth client follows a redirect to a look-alike authorization server domain (e.g., accounts-google.com vs accounts.google.com), causing authorization codes or tokens to be leaked to an attacker-controlled server. This exploits insufficient issuer validation in OAuth implementations.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 345: 🟠 **HIGH** — `SAFE-T1106`

**Location:** Lines 1-172

**Description:**

The provided code is a Graphviz DOT file, which defines a visual style guide and process templates. It is not executable code that performs tool invocations or agent actions. Therefore, it cannot directly exhibit the SAFE-T1106 vulnerability, which describes unbounded recursive or self-triggering *tool invocations*.: Autonomous Loop Exploit triggers denial-of-service through self-reinforcing tool invocation loops. Attackers craft prompts or tool outputs that suggest non-terminal progress, causing agents to repeatedly invoke tools without convergence checks, rate limits, or iteration budgets.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 346: 🟠 **HIGH** — `SAFE-T1110`

**Location:** Lines 1-172

**Description:**

The file 'skills/writing-skills/graphviz-conventions.dot' is a Graphviz DOT file, which is a text-based language for describing graphs. While DOT files can be rendered into images, the content itself is not an image or audio file and does not contain embedded malicious instructions in a format that would directly exploit SAFE-T1110. The technique SAFE-T1110 specifically targets multimodal AI systems by embedding malicious instructions within image or audio content (e.g., steganography, OCR exploitation, adversarial perturbations). This file is a style guide for a DSL and does not process or contain such multimedia content.: Multimodal Prompt Injection embeds malicious instructions within images, audio, or other non-text media that are processed by LLMs. Techniques include steganographic embedding in pixel data, OCR-extractable text injection, adversarial visual patterns, and metadata injection in media files. These bypass text-based sanitization since the payload is in a non-text modality.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 347: 🟠 **HIGH** — `SAFE-T1201`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file, which defines a static style guide for a process DSL. It does not contain any executable code, dynamic definitions, or server-side logic that could be subject to dynamic modification after user approval, as described by the SAFE-T1201 'MCP Rug Pull Attack' technique. This file is a descriptive artifact, not an MCP tool definition or implementation.: MCP Rug Pull Attack is a persistence technique where tools appear legitimate initially but can change their behavior after gaining user trust. This exploits dynamic tool definitions, remote configuration loading, or time-delayed activation to modify tool behavior post-approval.
Static analysis detects code patterns that ENABLE rug pulls: dynamic descriptions, remote tool loading without version pinning, and time-based conditional behavior.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 348: 🟠 **HIGH** — `SAFE-T1204`

**Location:** Lines 1-172

**Description:**

The file 'skills/writing-skills/graphviz-conventions.dot' is a Graphviz DOT file defining a style guide for a process DSL. It describes visual conventions for nodes and edges, naming patterns, and process structure templates. This file is a static definition and does not involve vector stores, conversation memory, or any mechanism for persistent storage of instructions that an AI would execute. It is a form of documentation or configuration for visualization, not a component susceptible to context memory implants.: Context Memory Implant exploits persistent memory systems in MCP-connected AI agents. Malicious agents write themselves into long-term vector stores, conversation histories, or system prompt storage to ensure re-loading in future sessions. The implanted instructions persist across server restarts and session boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 349: 🟠 **HIGH** — `SAFE-T1205`

**Location:** Lines 1-172

**Description:**

The file 'skills/writing-skills/graphviz-conventions.dot' is a Graphviz DOT file defining a style guide for a process DSL. It describes how various elements (nodes, edges) should be represented, including examples of commands. While it contains examples of commands like 'git commit -m 'msg'' and 'npm test', these are presented as literal strings within a graphical representation style guide, not as executable tool definitions that an MCP server would load and execute. This file is a descriptive artifact for documentation or design purposes, not an operational configuration file for MCP server tools.: Persistent Tool Redefinition is a privilege escalation technique where attackers modify an MCP server's tool metadata or configuration to embed hidden commands that survive server restarts. By persisting malicious tool definitions to disk, database, or configuration files, the attacker ensures their modifications remain active even after the server is restarted or redeployed.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 350: 🟠 **HIGH** — `SAFE-T1302`

**Location:** Lines 1-172

**Description:**

The file defines a style guide for a process DSL using Graphviz, including examples of commands. While the technique SAFE-T1302 concerns the abuse of high-privilege tools, this file merely documents how commands should be represented within a graph, not how they are executed or if they are exploitable. The 'commands' like 'git commit -m 'msg'' are static labels within the graph definition, not executable code.: High-Privilege Tool Abuse occurs when MCP tools are configured with administrative, root, or superuser access to databases, APIs, or system resources. Attackers exploit these tools through prompt injection or direct manipulation to perform privileged operations such as modifying access controls, reading sensitive data, or escalating their own permissions.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 351: 🟠 **HIGH** — `SAFE-T1304`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file, which is a descriptive language for generating diagrams. It does not contain any executable code, authentication mechanisms, or logic for handling tokens or credentials. Therefore, it cannot directly exhibit the SAFE-T1304 vulnerability related to token reuse or forwarding across services.: Credential Relay Chain exploits token reuse across MCP service boundaries. Attackers capture tokens from one service interaction and replay them in tool-to-tool calls, cross-service requests, or downstream API invocations without proper audience validation, scope restriction, or token binding.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 352: 🟠 **HIGH** — `SAFE-T1307`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file, which is a declarative language for describing graphs. It defines a style guide for process diagrams, including node shapes, edge labels, and naming conventions. It does not contain any executable code, server-side logic, authentication mechanisms, or token handling functionality that could lead to an ambient authority misuse or a Confused Deputy Attack (SAFE-T1307). The commands listed, such as 'git commit -m 'msg'' or 'npm test', are literal strings intended for display within the diagram, not commands to be executed by a processing server in a vulnerable manner.: Confused Deputy Attack exploits ambient authority in MCP servers where a privileged service performs actions on behalf of an unauthorized requester. In multi-tenant environments, this enables cross-tenant data access, privilege confusion between service tiers, or unauthorized operations performed through a trusted intermediary.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 353: 🟠 **HIGH** — `SAFE-T1308`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file defining a style guide for diagrams. It does not contain any executable code, authentication logic, or token validation mechanisms that could be vulnerable to token scope substitution (SAFE-T1308). This file is purely descriptive and defines visual conventions.: Token Scope Substitution occurs when an attacker replaces a limited-scope OAuth token with one that has broader permissions but the same audience, exploiting insufficient scope validation to gain elevated privileges.
This attack targets OAuth implementations that validate tokens but fail to verify that the token scopes match what was originally requested or required for the operation.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 354: 🟠 **HIGH** — `SAFE-T1403`

**Location:** Lines 1-172

**Description:**

The provided code is a Graphviz DOT file defining diagram conventions and does not contain any executable logic that could implement or trigger rapid or repeated permission requests. This file is a static description of a style guide, not an MCP server or agent component that handles user approvals.: Consent-Fatigue Exploit overwhelms users with rapid, repeated, or batched permission requests to exploit decision fatigue. Users begin auto-approving prompts without reading them, allowing malicious tools to gain permissions they would normally be denied. This also includes auto-approve patterns that bypass user consent entirely.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 355: 🟠 **HIGH** — `SAFE-T1404`

**Location:** Lines 1-172

**Description:**

The file 'graphviz-conventions.dot' defines a style guide for a process DSL using Graphviz. It specifies node shapes, edge labels, and naming conventions for representing processes. This file is a declarative definition for visualization and does not contain any executable code or logic related to AI model response generation or tool invocation.: Response Tampering intercepts and modifies MCP tool responses before they are displayed to the user or processed by the LLM. Attackers alter response content to hide evidence of malicious actions, inject false information, bypass content filters, or manipulate the LLM context for subsequent interactions.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 356: 🟠 **HIGH** — `SAFE-T1407`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file, which is a declarative language for describing graphs. It defines a style guide for a process DSL and does not contain any executable code, network configurations, or server logic that could implement a malicious proxy server.: An attacker operates an MCP server that forwards requests to a legitimate upstream API and relays the responses back to the client. Because the responses originate from the trusted service, the traffic appears normal in monitoring and logs, but the proxy can inspect, alter, or exfiltrate sensitive data. This enables stealthy data collection and response manipulation while hiding the true malicious behavior.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 357: 🟠 **HIGH** — `SAFE-T1408`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file, which is used for defining diagrams and visual representations. It does not contain any executable code or configuration related to OAuth client implementations or server-side logic. Therefore, it cannot directly be vulnerable to an OAuth protocol downgrade attack (SAFE-T1408).: An attacker forces or coerces a client to use the OAuth 2.0 implicit flow instead of the authorization code flow with PKCE. Tokens delivered in URL fragments are easier to capture via logs, referrers, or malicious scripts, enabling token theft and session hijacking.
This technique detects the presence of implicit flow configuration, which indicates potential vulnerability. The authorization code flow with PKCE should always be preferred over implicit flow for security.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 358: 🟠 **HIGH** — `SAFE-T1503`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file, which defines a visual graph structure. It does not contain any executable code, configuration for MCP tools, or direct access to environment variables. Therefore, it does not present a vulnerability related to SAFE-T1503 (Env-Var Scraping).: Environment Variable Scraping extracts secrets from process environment variables. MCP server tools iterate over process.env, os.environ, or use getenv with attacker-controlled keys to harvest API keys, database passwords, cloud credentials, and other sensitive configuration stored in environment variables.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 359: 🟠 **HIGH** — `SAFE-T1601`

**Location:** Lines 1-172

**Description:**

The file is a Graphviz DOT file defining a style guide for process diagrams. It does not contain any executable code, server configurations, or direct references to MCP server endpoints, registries, or service configurations that would facilitate discovery.: MCP Server Enumeration involves probing for available MCP server instances, scanning registries, and discovering service endpoints. Attackers enumerate servers to map the attack surface, identify vulnerable or misconfigured instances, and discover high-value targets for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 360: 🟠 **HIGH** — `SAFE-T1605`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file defining a style guide for process diagrams. It does not contain executable code or configuration that an MCP server would use to expose its capabilities, API versions, or permission boundaries. It is a documentation artifact describing how to visually represent processes, not the processes or capabilities themselves.: Capability Mapping systematically discovers MCP server capabilities, supported API versions, feature flags, and permission boundaries. Attackers use introspection endpoints, capability negotiation, and API probing to build a comprehensive map of what the target system can do, identifying the most valuable attack vectors.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 361: 🟠 **HIGH** — `SAFE-T1701`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file defining a style guide for a process DSL. It is a static definition of visual conventions and does not contain executable code, configuration, or shared state that could lead to cross-tool contamination as described in SAFE-T1701.: Cross-Tool Contamination exploits shared state between MCP tools including global variables, shared memory, common databases, or file-based caches. Malicious data written by one tool pollutes the execution context of another, enabling data exfiltration, privilege escalation, or behavior manipulation across tool boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 362: 🟠 **HIGH** — `SAFE-T1703`

**Location:** Lines 1-172

**Description:**

The file 'skills/writing-skills/graphviz-conventions.dot' is a Graphviz DOT file, which describes a style guide for a process DSL. It contains examples of commands (e.g., 'git commit -m 'msg'', 'npm test', 'git status') represented as plaintext nodes. While these nodes represent commands, the file itself is a declarative description for visualization, not executable code that would participate in a tool-chaining pivot.: Tool-Chaining Pivot exploits the output of one MCP tool as input to another more privileged tool, creating multi-step attack chains. Attackers leverage a low-privilege tool to produce crafted output that, when consumed by a higher-privilege tool, enables unauthorized operations such as command execution, data access, or privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 363: 🟠 **HIGH** — `SAFE-T1706`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file defining a style guide for a process DSL. It does not contain any executable code, configuration, or logic related to OAuth token handling, authentication, or authorization mechanisms. Therefore, it cannot directly exhibit the SAFE-T1706 vulnerability pattern of OAuth tokens being replayed across different services or audiences without validation.: OAuth Token Pivot Replay reuses OAuth tokens obtained from one service to access a different service or audience. Attackers exploit missing audience (aud) validation, overly broad token scopes, or lack of token binding to pivot from a lower-value service to a higher-value target using the same token.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 364: 🟠 **HIGH** — `SAFE-T1801`

**Location:** Lines 1-172

**Description:**

The provided code is a Graphviz DOT file defining a style guide for process diagrams. It does not contain any executable code, MCP tool definitions, or data access patterns that could be exploited for automated data harvesting (SAFE-T1801).: Automated Data Harvesting uses MCP tools to systematically access and collect data through bulk queries, pagination scraping, and record enumeration. Attackers automate data collection across multiple endpoints or database tables to amass sensitive information for exfiltration or analysis.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 365: 🟠 **HIGH** — `SAFE-T1804`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file defining a style guide for process diagrams. It does not contain any executable code, API calls, or MCP agent logic that could be exploited for systematic API abuse (SAFE-T1804).: API Data Harvest systematically extracts data through MCP tool API calls by abusing pagination, accessing bulk endpoints, or bypassing rate limits. Attackers iterate through API pages, manipulate query parameters, or use concurrent requests to extract large datasets that individual queries would not expose.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 366: 🟠 **HIGH** — `SAFE-T1904`

**Location:** Lines 1-172

**Description:**

The Graphviz DOT file defines conventions for representing various elements, including literal commands, within a process DSL. Nodes with `shape=plaintext` are used to explicitly display commands such as 'git commit -m 'msg'' or 'npm test'.: Chat-Based Backchannel uses MCP chat or messaging tools as a covert communication channel. Attackers embed encoded commands, base64 payloads, or steganographic data within seemingly normal chat messages to exfiltrate data or receive C2 instructions without triggering network-based detection.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 367: 🟠 **HIGH** — `SAFE-T1910`

**Location:** Lines 1-172

**Description:**

The provided Graphviz `.dot` file defines a style guide for a process DSL. It is a declarative description of graph elements and their visual attributes. It does not contain any executable logic, dynamic data processing, or outbound communication mechanisms that could be leveraged for data exfiltration via covert channels.: Covert Channel Exfiltration uses non-standard communication channels to exfiltrate data from MCP server environments. Techniques include DNS tunneling (encoding data in DNS queries), timing-based channels, HTTP header manipulation, and steganographic encoding that bypasses traditional network monitoring and DLP solutions.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 368: 🟠 **HIGH** — `SAFE-T1911`

**Location:** Lines 1-172

**Description:**

The provided file is a Graphviz DOT file, which defines graph structures and styling. This file type does not involve JSON parameters or metadata fields in the context of MCP tool calls, which are the target of the SAFE-T1911 technique. Therefore, this file cannot directly contain the described vulnerability.: Attackers hide sensitive data inside optional or unused parameters (e.g., metadata, context, tags) that are accepted and forwarded by the server but not actively validated. These values can be logged or propagated to upstream services, enabling stealthy exfiltration without triggering obvious alarms.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 369: 🟠 **HIGH** — `SAFE-T1912`

**Location:** Lines 1-172

**Description:**

The provided Graphviz DOT file is a static definition of a style guide and does not appear to be an AI-generated or MCP-generated response. While DOT files can contain string literals, there is no evidence of hidden data such as zero-width Unicode characters, encoded payloads, or unusual whitespace patterns that would indicate covert exfiltration.: Stego Response Exfiltration hides sensitive data within seemingly benign tool responses using steganographic techniques. Data is embedded in image pixels (LSB encoding), whitespace patterns, Unicode zero-width characters, or structured response fields that appear normal to users but contain extractable hidden information.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 370: 🟠 **HIGH** — `SAFE-T1915`

**Location:** Lines 1-172

**Description:**

The provided code is a Graphviz DOT file defining a style guide for process diagrams. It does not contain any executable code related to blockchain interactions, bridge calls, DEX interactions, or crypto mixing. The file describes visual conventions and diagramming best practices.: Cross-Chain Laundering uses MCP tools with blockchain access to launder funds through cross-chain bridges, decentralized exchange (DEX) swaps, and cryptocurrency mixing services. Attackers route stolen funds through multiple blockchains to obscure the transaction trail and convert illicit cryptocurrency to clean assets.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 371: 🟠 **HIGH** — `SAFE-T2102`

**Location:** Lines 1-172

**Description:**

The provided code is a Graphviz DOT file, which is a declarative language for describing graphs. It does not contain executable logic that could directly make unbounded requests to external APIs or create infinite loops in the context of an AI agent's execution.: Service Disruption via API Flooding uses MCP tools to generate excessive outbound API calls through infinite loops, missing rate limits, or unbounded concurrent requests. This causes denial of service on target APIs, excessive billing on cloud services, and potential cascading failures across dependent systems.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

#### Finding 372: 🟠 **HIGH** — `SAFE-T2105`

**Location:** Lines 1-172

**Description:**

The file 'skills/writing-skills/graphviz-conventions.dot' is a style guide for a process DSL, written in the DSL itself. It defines conventions for node types, edge labels, naming patterns, and process structure using Graphviz DOT syntax. It also includes explicit 'GOOD VS BAD EXAMPLES' to illustrate correct and incorrect diagramming practices.: Disinformation Output manipulates MCP tool responses or LLM outputs to generate false, misleading, or harmful information. Attackers inject instructions that cause the AI to produce fabricated data, distorted facts, or biased responses that appear authoritative, potentially causing real-world harm through decision-making based on false information.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/graphviz-conventions.dot:1-172 digraph STYLE_GUIDE {\n    // The style guide for our process DSL, written in the DSL itself\n
```

</details>

---

### 📁 `skills/writing-skills/render-graphs.js`

#### Finding 373: 🔴 **CRITICAL** — `SAFE-T1003`

**Location:** Lines 70-75

**Description:**

The script executes external commands (`dot`) with content derived from user-controlled markdown files. While the input is passed via stdin, mitigating direct command injection into the `dot` command itself, a sophisticated attacker could craft malicious Graphviz DOT language to exploit vulnerabilities within the `dot` renderer (Graphviz) or leverage its features (e.g., file inclusion if enabled) to read local files or cause other unintended side effects. This is not a direct instance of a trojanized MCP server, but rather a potential insecure use of an external tool within a utility script.: Malicious MCP-Server Distribution involves distributing trojanized MCP server implementations through package registries (npm, PyPI, Docker Hub) or code repositories. These servers appear legitimate but contain hidden malicious functionality such as data exfiltration, backdoors, or credential theft.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:70-75 renderToSvg(dotContent) {try {return execSync('dot -Tsvg', {input: dotContent,encoding: 'utf-8',maxBuffer: 10 * 1024 * 1024});} catch (err) {console.error('Error running dot:', err.message);if (err.stderr) console.error(err.stderr.toString());return null;}}
```

</details>

---

#### Finding 374: 🔴 **CRITICAL** — `SAFE-T1101`

**Location:** Lines 59-63

**Description:**

The script uses `child_process.execSync` to execute the `dot` command. While `execSync` can be vulnerable to command injection if user-controlled input is directly embedded into the command string, in this case, the command string `'dot -Tsvg'` is hardcoded. User-controlled data (`dotContent` derived from `SKILL.md`) is passed via the `input` option, which pipes the data to the standard input of the `dot` process. This prevents shell interpretation of the `dotContent` by `execSync` itself. Therefore, this specific usage does not constitute a direct shell command injection vulnerability (SAFE-T1101).: Prompt Injection via Tool Output occurs when MCP tools return user-controlled or external data directly to the LLM without proper sanitization, allowing malicious content in tool outputs to hijack the agent behavior. Tool outputs are typically trusted by LLMs and processed as authoritative information.
Common attack vectors include: database records with hidden instructions, file contents with prompt injection payloads, API responses crafted to override directives, and web scraping results with malicious instruction sequences.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:59-63 renderToSvg(dotContent) { try { return execSync('dot -Tsvg', { input: dotContent, encoding: 'utf-8', maxBuffer: 10 * 1024 * 1024 }); } catch (err) { console.error('Error running dot:', err.message); if (err.stderr) console.error(err.stderr.toString()); return null; } }
```

</details>

---

#### Finding 375: 🔴 **CRITICAL** — `SAFE-T1103`

**Location:** Lines 70-75

**Description:**

The script executes the 'dot' command with user-controlled input from SKILL.md files. While `execSync` with the `input` option generally pipes content safely to stdin, a highly sophisticated or unknown vulnerability in the `dot` utility itself, or an obscure interaction with shell features, could theoretically lead to command injection. However, Graphviz's DOT language is declarative and not designed to execute arbitrary shell commands from its input stream.: Fake Tool Invocation exploits insufficient tool validation in MCP hosts by injecting or spoofing tool names, duplicating tool registrations, or manipulating the JSON-RPC tool call protocol. Attackers can redirect legitimate tool calls to malicious implementations or register tools that shadow trusted ones.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:70-75 renderToSvg(dotContent) {    try {      return execSync('dot -Tsvg', {        input: dotContent,        encoding: 'utf-8',        maxBuffer: 10 * 1024 * 1024      });
```

</details>

---

#### Finding 376: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Lines 79-83

**Description:**

The script uses `execSync` to execute the `dot` command with user-controlled input (`dotContent`). While `dotContent` is passed via stdin, which mitigates direct shell injection, a sophisticated attacker could potentially craft malicious Graphviz DOT language to exploit vulnerabilities within the `dot` program itself (e.g., through `URL` attributes leading to SSRF/file disclosure, or if `dot` has a specific RCE vulnerability via its input). However, this script is a local utility, not a debug endpoint or inspector tool exposed in production, which is the primary focus of SAFE-T1109. The technique specifically targets vulnerabilities in tools like 'MCP Inspector' and their network exposure.: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:79-83 renderToSvg(dotContent) {try {return execSync('dot -Tsvg', {input: dotContent,encoding: 'utf-8',maxBuffer: 10 * 1024 * 1024});} catch (err) {
```

</details>

---

#### Finding 377: 🔴 **CRITICAL** — `SAFE-T1303`

**Location:** Lines 59-64

**Description:**

The script uses `child_process.execSync` to execute external commands (`dot` and `which dot`). While this specific technique (SAFE-T1303) focuses on manipulating the `cwd` parameter of `exec` to achieve container escape, this script does not pass a user-controlled `cwd` to `execSync`. The `dotContent` is passed via stdin, not as a command-line argument or `cwd` parameter. However, the use of `execSync` with user-controlled input (the `dotContent` from `SKILL.md`) piped to an external command (`dot`) could potentially lead to other types of vulnerabilities (e.g., command injection if `dot` itself has vulnerabilities in parsing input, or resource exhaustion) if the `dot` utility is compromised or has known vulnerabilities. This finding is informational because it does not directly match the specified technique's criteria for `cwd` manipulation, but highlights a general area of risk when executing external processes with user-derived input.: Container Sandbox Escape exploits misconfigured container environments running MCP servers to break out of the sandbox and access the host operating system. Attack vectors include mounting the Docker socket, accessing /proc filesystem, running in privileged mode, using nsenter, or exploiting kernel vulnerabilities from within the container.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:59-64 renderToSvg(dotContent) {try {return execSync('dot -Tsvg', {input: dotContent,encoding: 'utf-8',maxBuffer: 10 * 1024 * 1024});} catch (err) {
```

</details>

---

#### Finding 378: 🔴 **CRITICAL** — `SAFE-T1306`

**Location:** Lines 1-168

**Description:**

The provided code is a utility script for rendering Graphviz diagrams from Markdown files. It does not implement any authentication server functionality, token minting, or token validation logic. Therefore, it is not susceptible to the SAFE-T1306 'Rogue Authentication Server' vulnerability.: Rogue Authorization Server involves setting up a malicious or misconfigured authorization server that mints OAuth tokens, JWTs, or API keys without proper validation, client verification, or trust chain enforcement. Attackers use these rogue-issued tokens to access MCP servers and tools that fail to validate the token issuer.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:1-168 #!/usr/bin/env node

/**
 * Render graphviz diagrams from a skill's SKILL.md to SVG files.
 *
 * Usage:
 *   ./render-graphs.js <skill-directory>           # Render each diagram separately
 *   ./render-graphs.js <skill-directory> --combine # Combine all into one diagram
 *
 * Extracts all ```dot blocks from SKILL.md and renders to SVG.
 * Useful for helping your human partner visualize the process flows.
 *
 * Requires: graphviz (dot) installed on system
 */

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

function extractDotBlocks(markdown) {
  const blocks = [];
  const regex = /```dot\n([\s\S]*?)```/g;
  let match;

  while ((match = regex.exec(markdown)) !== null) {
    const content = match[1].trim();

    // Extract digraph name
    const nameMatch = content.match(/digraph\s+(\w+)/);
    const name = nameMatch ? nameMatch[1] : `graph_${blocks.length + 1}`;

    blocks.push({ name, content });
  }

  return blocks;
}

function extractGraphBody(dotContent) {
  // Extract just the body (nodes and edges) from a digraph
  const match = dotContent.match(/digraph\s+\w+\s*\{([\s\S]*)\}/);
  if (!match) return '';

  let body = match[1];

  // Remove rankdir (we'll set it once at the top level)
  body = body.replace(/^\s*rankdir\s*=\s*\w+\s*;?\s*$/gm, '');

  return body.trim();
}

function combineGraphs(blocks, skillName) {
  const bodies = blocks.map((block, i) => {
    const body = extractGraphBody(block.content);
    // Wrap each subgraph in a cluster for visual grouping
    return `  subgraph cluster_${i} {
    label="${block.name}";
    ${body.split('\n').map(line => '  ' + line).join('\n')}
  }`;
  });

  return `digraph ${skillName}_combined {
  rankdir=TB;
  compound=true;
  newrank=true;

${bodies.join('\n\n')}
}`;
}

function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotC...
```

</details>

---

#### Finding 379: 🔴 **CRITICAL** — `SAFE-T1502`

**Location:** Lines 109-110

**Description:**

The script reads `SKILL.md` from a user-provided directory. If an attacker can control the `skill-directory` argument and place a symlink named `SKILL.md` in a sensitive location (e.g., `~/.ssh/SKILL.md` pointing to `~/.ssh/id_rsa`), the script would read the contents of the linked file, leading to file-based credential harvest.: File-Based Credential Harvest occurs when MCP server tools read sensitive credential files from the host filesystem. Targets include SSH private keys, AWS credentials, environment files, browser cookies, keychain databases, and cloud provider configurations. The harvested credentials enable lateral movement and privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:109-110 const skillDir = path.resolve(skillDirArg);const skillFile = path.join(skillDir, 'SKILL.md');
```

</details>

---

#### Finding 380: 🔴 **CRITICAL** — `SAFE-T1505`

**Location:** Lines 1-168

**Description:**

The script processes Graphviz DOT language from Markdown files and renders them to SVG. It does not handle or store any sensitive information such as API keys or credentials, nor does it interact with AI systems, embedding models, or vector databases. Therefore, it is not susceptible to in-memory secret extraction techniques described in SAFE-T1505.: In-Memory Secret Extraction targets secrets held in process memory at runtime. Attackers use memory dump tools, /proc/*/mem access, heap inspection, or debugger attachment to extract encryption keys, tokens, passwords, and other sensitive data that exists only in memory and would not be found in files or environment variables.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:1-168 #!/usr/bin/env node

/**
 * Render graphviz diagrams from a skill's SKILL.md to SVG files.
 *
 * Usage:
 *   ./render-graphs.js <skill-directory>           # Render each diagram separately
 *   ./render-graphs.js <skill-directory> --combine # Combine all into one diagram
 *
 * Extracts all ```dot blocks from SKILL.md and renders to SVG.
 * Useful for helping your human partner visualize the process flows.
 *
 * Requires: graphviz (dot) installed on system
 */

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

function extractDotBlocks(markdown) {
  const blocks = [];
  const regex = /```dot\n([\s\S]*?)```/g;
  let match;

  while ((match = regex.exec(markdown)) !== null) {
    const content = match[1].trim();

    // Extract digraph name
    const nameMatch = content.match(/digraph\s+(\w+)/);
    const name = nameMatch ? nameMatch[1] : `graph_${blocks.length + 1}`;

    blocks.push({ name, content });
  }

  return blocks;
}

function extractGraphBody(dotContent) {
  // Extract just the body (nodes and edges) from a digraph
  const match = dotContent.match(/digraph\s+\w+\s*\{([\s\S]*)\}/);
  if (!match) return '';

  let body = match[1];

  // Remove rankdir (we'll set it once at the top level)
  body = body.replace(/^\s*rankdir\s*=\s*\w+\s*;?\s*$/gm, '');

  return body.trim();
}

function combineGraphs(blocks, skillName) {
  const bodies = blocks.map((block, i) => {
    const body = extractGraphBody(block.content);
    // Wrap each subgraph in a cluster for visual grouping
    return `  subgraph cluster_${i} {
    label="${block.name}";
    ${body.split('\n').map(line => '  ' + line).join('\n')}
  }`;
  });

  return `digraph ${skillName}_combined {
  rankdir=TB;
  compound=true;
  newrank=true;

${bodies.join('\n\n')}
}`;
}

function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotC...
```

</details>

---

#### Finding 381: 🔴 **CRITICAL** — `SAFE-T1704`

**Location:** Lines 1-168

**Description:**

The script executes the `dot` command with user-controlled Graphviz DOT language content. The DOT language supports an `image` attribute for nodes, which can specify a URL. If a malicious `SKILL.md` file is provided, an attacker can embed a URL in the `image` attribute, causing the `dot` command to make an HTTP request to an arbitrary internal or external resource from the server running the script, leading to Server-Side Request Forgery (SSRF).: Compromised-Server Pivot / SSRF occurs when MCP tools accept user-controlled URLs in HTTP requests without host allowlisting or validation. Attackers exploit this to access internal services, cloud metadata endpoints, localhost services, or private network resources that the MCP server can reach but the attacker cannot directly access.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:80-84 function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotContent,
      encoding: 'utf-8',
      maxBuffer: 10 * 1024 * 1024
    });
```

</details>

---

#### Finding 382: 🔴 **CRITICAL** — `SAFE-T1705`

**Location:** Lines 70-74

**Description:**

Potential command injection or SVG injection vulnerability through untrusted Graphviz DOT input.: Cross-Agent Instruction Injection targets multi-agent MCP architectures where agents communicate by passing messages or tool results. Attackers inject malicious instructions into one agent that propagate to other agents in the workflow, contaminating the entire multi-agent pipeline through message passing, shared context, or tool output forwarding.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:70-74 renderToSvg
```

</details>

---

#### Finding 383: 🔴 **CRITICAL** — `SAFE-T2101`

**Location:** Line 140

**Description:**

The script uses `fs.writeFileSync` which can overwrite existing files. While this is part of its intended functionality to update generated diagrams, it's worth noting that if an attacker could control the `block.name` or `skillName` variables, they might be able to cause the script to overwrite arbitrary files in the `outputDir`.: Data Destruction uses MCP tools to permanently delete or corrupt data through destructive operations such as rm -rf, DROP TABLE, file wiping, bulk delete API calls, or database truncation. Attackers may destroy data for sabotage, to cover their tracks, or as part of a ransomware attack.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:140-140 fs.writeFileSync(outputPath, svg);
```

</details>

---

#### Finding 384: 🔴 **CRITICAL** — `SAFE-T2101`

**Location:** Line 144

**Description:**

The script uses `fs.writeFileSync` which can overwrite existing files. While this is part of its intended functionality to update generated diagrams, it's worth noting that if an attacker could control the `block.name` or `skillName` variables, they might be able to cause the script to overwrite arbitrary files in the `outputDir`.: Data Destruction uses MCP tools to permanently delete or corrupt data through destructive operations such as rm -rf, DROP TABLE, file wiping, bulk delete API calls, or database truncation. Attackers may destroy data for sabotage, to cover their tracks, or as part of a ransomware attack.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:144-144 fs.writeFileSync(dotPath, combined);
```

</details>

---

#### Finding 385: 🔴 **CRITICAL** — `SAFE-T2103`

**Location:** Lines 70-74

**Description:**

The script executes the external 'dot' command with user-controlled content extracted from 'SKILL.md'. While the input is passed via stdin, preventing direct shell injection, the 'dot' program itself can be abused through its graph description language features (e.g., 'URL' or 'image' attributes) to perform actions like Server-Side Request Forgery (SSRF), local file disclosure attempts, or resource exhaustion, if this script is used in an automated CI/CD pipeline processing untrusted pull requests.: Code Sabotage via Malicious PR uses MCP tools with repository access to create automated pull requests containing malicious code changes, backdoors, or CI/CD pipeline modifications. Attackers manipulate build scripts, inject vulnerabilities, or alter deployment configurations through automated commits that bypass normal code review.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:70-74 renderToSvg(dotContent)
```

</details>

---

#### Finding 386: 🔴 **CRITICAL** — `SAFE-T2107`

**Location:** Lines 1-168

**Description:**

The `render-graphs.js` script processes user-controlled `dot` graph definitions from `SKILL.md` and renders them into SVG files. If these generated SVG files are subsequently used as training data for an AI model, an attacker capable of modifying `SKILL.md` could inject adversarial patterns or backdoor triggers into the `dot` content. These patterns would then be embedded in the SVG output, potentially poisoning the AI model during training.: AI Model Poisoning manipulates machine learning models used by MCP servers through training data poisoning, adversarial example injection, or embedding backdoor triggers during fine-tuning. Poisoned models produce attacker-desired outputs when triggered by specific inputs while behaving normally otherwise.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:106-106 const markdown = fs.readFileSync(skillFile, 'utf-8');
skills/writing-skills/render-graphs.js:24-24 const content = match[1].trim();
skills/writing-skills/render-graphs.js:70-70 return execSync('dot -Tsvg', { input: dotContent, encoding: 'utf-8', maxBuffer: 10 * 1024 * 1024 });
```

</details>

---

#### Finding 387: 🟠 **HIGH** — `SAFE-T1001`

**Location:** Lines 1-168

**Description:**

The `render-graphs.js` script processes `SKILL.md` files to extract and render Graphviz `dot` diagrams into SVG. While `SKILL.md` files might be consumed by LLMs as part of skill documentation, this script's specific function is to render visual diagrams, not to provide 'tool descriptions' to an LLM for execution or context manipulation. The `dot` content it processes is a graph definition language, not a tool description schema intended for LLM interpretation as executable instructions.: Tool Poisoning Attack (TPA) embeds malicious instructions within MCP tool descriptions that are invisible to users but processed by LLMs. This exploits the difference between displayed and processed tool descriptions.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:1-168 #!/usr/bin/env node

/**
 * Render graphviz diagrams from a skill's SKILL.md to SVG files.
 *
 * Usage:
 *   ./render-graphs.js <skill-directory>           # Render each diagram separately
 *   ./render-graphs.js <skill-directory> --combine # Combine all into one diagram
 *
 * Extracts all ```dot blocks from SKILL.md and renders to SVG.
 * Useful for helping your human partner visualize the process flows.
 *
 * Requires: graphviz (dot) installed on system
 */

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

function extractDotBlocks(markdown) {
  const blocks = [];
  const regex = /```dot\n([\s\S]*?)```/g;
  let match;

  while ((match = regex.exec(markdown)) !== null) {
    const content = match[1].trim();

    // Extract digraph name
    const nameMatch = content.match(/digraph\s+(\w+)/);
    const name = nameMatch ? nameMatch[1] : `graph_${blocks.length + 1}`;

    blocks.push({ name, content });
  }

  return blocks;
}

function extractGraphBody(dotContent) {
  // Extract just the body (nodes and edges) from a digraph
  const match = dotContent.match(/digraph\s+\w+\s*\{([\s\S]*)\}/);
  if (!match) return '';

  let body = match[1];

  // Remove rankdir (we'll set it once at the top level)
  body = body.replace(/^\s*rankdir\s*=\s*\w+\s*;?\s*$/gm, '');

  return body.trim();
}

function combineGraphs(blocks, skillName) {
  const bodies = blocks.map((block, i) => {
    const body = extractGraphBody(block.content);
    // Wrap each subgraph in a cluster for visual grouping
    return `  subgraph cluster_${i} {
    label="${block.name}";
    ${body.split('\n').map(line => '  ' + line).join('\n')}
  }`;
  });

  return `digraph ${skillName}_combined {
  rankdir=TB;
  compound=true;
  newrank=true;

${bodies.join('\n\n')}
}`;
}

function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotC...
```

</details>

---

#### Finding 388: 🟠 **HIGH** — `SAFE-T1004`

**Location:** Lines 79-84

**Description:**

The script executes the 'dot' command-line tool, which is an external dependency. While not directly related to SAFE-T1004 (Server Impersonation), the reliance on external executables can introduce other security risks if the executable path is manipulated or a malicious 'dot' binary is installed. This is an informational finding as it doesn't directly match the specified technique.: Server Impersonation occurs when an attacker registers an MCP server with a name designed to be confused with a legitimate service (typosquatting, homograph attacks, or DNS cache poisoning). Users connecting to the impersonated server unknowingly send credentials and data to the attacker.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:79-84 renderToSvg(dotContent) {try {return execSync('dot -Tsvg', {input: dotContent,encoding: 'utf-8',maxBuffer: 10 * 1024 * 1024});} catch (err) {console.error('Error running dot:', err.message);if (err.stderr) console.error(err.stderr.toString());return null;}}
```

</details>

---

#### Finding 389: 🟠 **HIGH** — `SAFE-T1009`

**Location:** Lines 1-168

**Description:**

The provided code is a local utility script for rendering Graphviz diagrams and does not implement any OAuth client functionality. Therefore, it is not susceptible to the SAFE-T1009 Authorization Server Mix-up vulnerability.: Authorization Server Mix-up occurs when an OAuth client follows a redirect to a look-alike authorization server domain (e.g., accounts-google.com vs accounts.google.com), causing authorization codes or tokens to be leaked to an attacker-controlled server. This exploits insufficient issuer validation in OAuth implementations.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:1-168 #!/usr/bin/env node

/**
 * Render graphviz diagrams from a skill's SKILL.md to SVG files.
 *
 * Usage:
 *   ./render-graphs.js <skill-directory>           # Render each diagram separately
 *   ./render-graphs.js <skill-directory> --combine # Combine all into one diagram
 *
 * Extracts all ```dot blocks from SKILL.md and renders to SVG.
 * Useful for helping your human partner visualize the process flows.
 *
 * Requires: graphviz (dot) installed on system
 */

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

function extractDotBlocks(markdown) {
  const blocks = [];
  const regex = /```dot\n([\s\S]*?)```/g;
  let match;

  while ((match = regex.exec(markdown)) !== null) {
    const content = match[1].trim();

    // Extract digraph name
    const nameMatch = content.match(/digraph\s+(\w+)/);
    const name = nameMatch ? nameMatch[1] : `graph_${blocks.length + 1}`;

    blocks.push({ name, content });
  }

  return blocks;
}

function extractGraphBody(dotContent) {
  // Extract just the body (nodes and edges) from a digraph
  const match = dotContent.match(/digraph\s+\w+\s*\{([\s\S]*)\}/);
  if (!match) return '';

  let body = match[1];

  // Remove rankdir (we'll set it once at the top level)
  body = body.replace(/^\s*rankdir\s*=\s*\w+\s*;?\s*$/gm, '');

  return body.trim();
}

function combineGraphs(blocks, skillName) {
  const bodies = blocks.map((block, i) => {
    const body = extractGraphBody(block.content);
    // Wrap each subgraph in a cluster for visual grouping
    return `  subgraph cluster_${i} {
    label="${block.name}";
    ${body.split('\n').map(line => '  ' + line).join('\n')}
  }`;
  });

  return `digraph ${skillName}_combined {
  rankdir=TB;
  compound=true;
  newrank=true;

${bodies.join('\n\n')}
}`;
}

function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotC...
```

</details>

---

#### Finding 390: 🟠 **HIGH** — `SAFE-T1104`

**Location:** Lines 1-168

**Description:**

The script uses `child_process.execSync` to execute the `dot` command with user-controlled input. While the input is passed via stdin, which mitigates direct shell injection, any vulnerabilities in the `dot` program itself (e.g., command injection via its graph language features or unexpected file access) could be exploited. This is a general pattern where external tools are invoked with potentially untrusted input.: Over-Privileged Tool Abuse occurs when MCP tools are granted permissions far exceeding their functional requirements. Attackers exploit these over-privileged tools through prompt injection or direct manipulation to access sensitive resources, execute system commands, or escalate privileges beyond the intended scope.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:70-74     return execSync('dot -Tsvg', {
      input: dotContent,
      encoding: 'utf-8',
      maxBuffer: 10 * 1024 * 1024
    });
```

</details>

---

#### Finding 391: 🟠 **HIGH** — `SAFE-T1105`

**Location:** Line 100

**Description:**

The script uses `path.resolve()` on user-provided command-line input (`skillDirArg`) to construct file paths for reading (`SKILL.md`) and writing (`.svg`, `.dot` files). While `path.resolve()` normalizes `../` sequences, it does not restrict the resolved path to a specific base directory or sandbox it. This allows an attacker to specify an arbitrary absolute path or a path that resolves outside the intended project directory, leading to arbitrary file read and write operations.: Path Traversal via File Tool occurs when MCP file-handling tools accept untrusted input as filesystem paths without proper normalization, allowlisting, or base directory enforcement. Attackers can use relative path sequences, Unicode normalization tricks, double encoding, or null byte injection to access files outside the intended directory.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:100-100 const skillDir = path.resolve(skillDirArg);
```

</details>

---

#### Finding 392: 🟠 **HIGH** — `SAFE-T1106`

**Location:** Lines 1-168

**Description:**

The provided code is a utility script for rendering Graphviz diagrams and does not appear to be an AI agent or part of an AI workflow that would be susceptible to unbounded recursive or self-triggering tool invocations (SAFE-T1106). The script's execution flow is deterministic, calling the 'dot' command a finite number of times based on the input markdown file, without any decision-making or retry logic that could lead to an autonomous loop.: Autonomous Loop Exploit triggers denial-of-service through self-reinforcing tool invocation loops. Attackers craft prompts or tool outputs that suggest non-terminal progress, causing agents to repeatedly invoke tools without convergence checks, rate limits, or iteration budgets.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:1-168 #!/usr/bin/env node

/**
 * Render graphviz diagrams from a skill's SKILL.md to SVG files.
 *
 * Usage:
 *   ./render-graphs.js <skill-directory>           # Render each diagram separately
 *   ./render-graphs.js <skill-directory> --combine # Combine all into one diagram
 *
 * Extracts all ```dot blocks from SKILL.md and renders to SVG.
 * Useful for helping your human partner visualize the process flows.
 *
 * Requires: graphviz (dot) installed on system
 */

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

function extractDotBlocks(markdown) {
  const blocks = [];
  const regex = /```dot\n([\s\S]*?)```/g;
  let match;

  while ((match = regex.exec(markdown)) !== null) {
    const content = match[1].trim();

    // Extract digraph name
    const nameMatch = content.match(/digraph\s+(\w+)/);
    const name = nameMatch ? nameMatch[1] : `graph_${blocks.length + 1}`;

    blocks.push({ name, content });
  }

  return blocks;
}

function extractGraphBody(dotContent) {
  // Extract just the body (nodes and edges) from a digraph
  const match = dotContent.match(/digraph\s+\w+\s*\{([\s\S]*)\}/);
  if (!match) return '';

  let body = match[1];

  // Remove rankdir (we'll set it once at the top level)
  body = body.replace(/^\s*rankdir\s*=\s*\w+\s*;?\s*$/gm, '');

  return body.trim();
}

function combineGraphs(blocks, skillName) {
  const bodies = blocks.map((block, i) => {
    const body = extractGraphBody(block.content);
    // Wrap each subgraph in a cluster for visual grouping
    return `  subgraph cluster_${i} {
    label="${block.name}";
    ${body.split('\n').map(line => '  ' + line).join('\n')}
  }`;
  });

  return `digraph ${skillName}_combined {
  rankdir=TB;
  compound=true;
  newrank=true;

${bodies.join('\n\n')}
}`;
}

function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotC...
```

</details>

---

#### Finding 393: 🟠 **HIGH** — `SAFE-T1110`

**Location:** Lines 1-168

**Description:**

The script processes text-based Graphviz DOT language definitions from markdown files and renders them to SVG images. It does not ingest or process image or audio files as input, nor does it interact with a multimodal AI system.: Multimodal Prompt Injection embeds malicious instructions within images, audio, or other non-text media that are processed by LLMs. Techniques include steganographic embedding in pixel data, OCR-extractable text injection, adversarial visual patterns, and metadata injection in media files. These bypass text-based sanitization since the payload is in a non-text modality.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:1-168 #!/usr/bin/env node

/**
 * Render graphviz diagrams from a skill's SKILL.md to SVG files.
 *
 * Usage:
 *   ./render-graphs.js <skill-directory>           # Render each diagram separately
 *   ./render-graphs.js <skill-directory> --combine # Combine all into one diagram
 *
 * Extracts all ```dot blocks from SKILL.md and renders to SVG.
 * Useful for helping your human partner visualize the process flows.
 *
 * Requires: graphviz (dot) installed on system
 */

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

function extractDotBlocks(markdown) {
  const blocks = [];
  const regex = /```dot\n([\s\S]*?)```/g;
  let match;

  while ((match = regex.exec(markdown)) !== null) {
    const content = match[1].trim();

    // Extract digraph name
    const nameMatch = content.match(/digraph\s+(\w+)/);
    const name = nameMatch ? nameMatch[1] : `graph_${blocks.length + 1}`;

    blocks.push({ name, content });
  }

  return blocks;
}

function extractGraphBody(dotContent) {
  // Extract just the body (nodes and edges) from a digraph
  const match = dotContent.match(/digraph\s+\w+\s*\{([\s\S]*)\}/);
  if (!match) return '';

  let body = match[1];

  // Remove rankdir (we'll set it once at the top level)
  body = body.replace(/^\s*rankdir\s*=\s*\w+\s*;?\s*$/gm, '');

  return body.trim();
}

function combineGraphs(blocks, skillName) {
  const bodies = blocks.map((block, i) => {
    const body = extractGraphBody(block.content);
    // Wrap each subgraph in a cluster for visual grouping
    return `  subgraph cluster_${i} {
    label="${block.name}";
    ${body.split('\n').map(line => '  ' + line).join('\n')}
  }`;
  });

  return `digraph ${skillName}_combined {
  rankdir=TB;
  compound=true;
  newrank=true;

${bodies.join('\n\n')}
}`;
}

function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotC...
```

</details>

---

#### Finding 394: 🟠 **HIGH** — `SAFE-T1304`

**Location:** Lines 70-74

**Description:**

The script executes external commands (`dot`) with user-controlled input, which could lead to command injection if the input from SKILL.md is malicious. While not directly related to token reuse (SAFE-T1304), it's a related security concern regarding external process execution.: Credential Relay Chain exploits token reuse across MCP service boundaries. Attackers capture tokens from one service interaction and replay them in tool-to-tool calls, cross-service requests, or downstream API invocations without proper audience validation, scope restriction, or token binding.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:70-74 renderToSvg(dotContent)
```

</details>

---

#### Finding 395: 🟠 **HIGH** — `SAFE-T1307`

**Location:** Lines 80-85

**Description:**

The script executes the 'dot' command with content extracted directly from a SKILL.md file, which is located in a user-specified directory. If an attacker can control the content of SKILL.md, they could potentially inject malicious Graphviz code that exploits vulnerabilities in the 'dot' utility, leading to arbitrary code execution or other system compromise. While the input is passed via stdin, complex parsers like Graphviz can still be vulnerable to specially crafted input.: Confused Deputy Attack exploits ambient authority in MCP servers where a privileged service performs actions on behalf of an unauthorized requester. In multi-tenant environments, this enables cross-tenant data access, privilege confusion between service tiers, or unauthorized operations performed through a trusted intermediary.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:80-85 renderToSvg(dotContent) {try {return execSync('dot -Tsvg', {input: dotContent,encoding: 'utf-8',maxBuffer: 10 * 1024 * 1024});}
```

</details>

---

#### Finding 396: 🟠 **HIGH** — `SAFE-T1308`

**Location:** Lines 1-168

**Description:**

The provided code is a local utility script for rendering Graphviz diagrams from markdown files. It does not handle OAuth tokens, perform any form of authentication or authorization, or act as a Resource Server. Therefore, it is not susceptible to the SAFE-T1308 'Token Scope Substitution' vulnerability.: Token Scope Substitution occurs when an attacker replaces a limited-scope OAuth token with one that has broader permissions but the same audience, exploiting insufficient scope validation to gain elevated privileges.
This attack targets OAuth implementations that validate tokens but fail to verify that the token scopes match what was originally requested or required for the operation.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:1-168 #!/usr/bin/env node

/**
 * Render graphviz diagrams from a skill's SKILL.md to SVG files.
 *
 * Usage:
 *   ./render-graphs.js <skill-directory>           # Render each diagram separately
 *   ./render-graphs.js <skill-directory> --combine # Combine all into one diagram
 *
 * Extracts all ```dot blocks from SKILL.md and renders to SVG.
 * Useful for helping your human partner visualize the process flows.
 *
 * Requires: graphviz (dot) installed on system
 */

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

function extractDotBlocks(markdown) {
  const blocks = [];
  const regex = /```dot\n([\s\S]*?)```/g;
  let match;

  while ((match = regex.exec(markdown)) !== null) {
    const content = match[1].trim();

    // Extract digraph name
    const nameMatch = content.match(/digraph\s+(\w+)/);
    const name = nameMatch ? nameMatch[1] : `graph_${blocks.length + 1}`;

    blocks.push({ name, content });
  }

  return blocks;
}

function extractGraphBody(dotContent) {
  // Extract just the body (nodes and edges) from a digraph
  const match = dotContent.match(/digraph\s+\w+\s*\{([\s\S]*)\}/);
  if (!match) return '';

  let body = match[1];

  // Remove rankdir (we'll set it once at the top level)
  body = body.replace(/^\s*rankdir\s*=\s*\w+\s*;?\s*$/gm, '');

  return body.trim();
}

function combineGraphs(blocks, skillName) {
  const bodies = blocks.map((block, i) => {
    const body = extractGraphBody(block.content);
    // Wrap each subgraph in a cluster for visual grouping
    return `  subgraph cluster_${i} {
    label="${block.name}";
    ${body.split('\n').map(line => '  ' + line).join('\n')}
  }`;
  });

  return `digraph ${skillName}_combined {
  rankdir=TB;
  compound=true;
  newrank=true;

${bodies.join('\n\n')}
}`;
}

function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotC...
```

</details>

---

#### Finding 397: 🟠 **HIGH** — `SAFE-T1401`

**Location:** Line 100

**Description:**

The script processes content from SKILL.md, which could potentially contain malicious Graphviz DOT language. While the `dot` command is executed with input via stdin, mitigating direct shell injection, the output SVG/DOT files could contain crafted content. However, this specific technique (SAFE-T1401) targets newline/control character injection to hide malicious instructions *for an LLM* within tool descriptions. This script is a graph rendering utility and does not interact with an LLM in a way that would make it vulnerable to LLM-specific instruction hiding.: Line Jumping injects newline characters, CRLF sequences, or control characters into MCP tool descriptions, parameter names, or metadata fields. The injected characters push malicious instructions outside the visible area of user interfaces, making them invisible during review while still being processed by the LLM.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:100-100 const markdown = fs.readFileSync(skillFile, 'utf-8');
```

</details>

---

#### Finding 398: 🟠 **HIGH** — `SAFE-T1402`

**Location:** Lines 1-168

**Description:**

The script processes user-provided Markdown files containing Graphviz 'dot' blocks and renders them to SVG. While the 'dot' language supports HTML-like labels where hidden instructions (e.g., HTML comments, zero-width characters) could theoretically be embedded, this script is a utility for generating visualizations, not for defining or processing 'tool metadata' that an LLM would interpret as instructions.: Instruction Stenography conceals malicious instructions within tool descriptions or metadata using steganographic techniques: HTML comments, zero-width Unicode characters, bidirectional text overrides, or invisible formatting. These hidden instructions are processed by LLMs but invisible in standard UI rendering.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:1-168 #!/usr/bin/env node

/**
 * Render graphviz diagrams from a skill's SKILL.md to SVG files.
 *
 * Usage:
 *   ./render-graphs.js <skill-directory>           # Render each diagram separately
 *   ./render-graphs.js <skill-directory> --combine # Combine all into one diagram
 *
 * Extracts all ```dot blocks from SKILL.md and renders to SVG.
 * Useful for helping your human partner visualize the process flows.
 *
 * Requires: graphviz (dot) installed on system
 */

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

function extractDotBlocks(markdown) {
  const blocks = [];
  const regex = /```dot\n([\s\S]*?)```/g;
  let match;

  while ((match = regex.exec(markdown)) !== null) {
    const content = match[1].trim();

    // Extract digraph name
    const nameMatch = content.match(/digraph\s+(\w+)/);
    const name = nameMatch ? nameMatch[1] : `graph_${blocks.length + 1}`;

    blocks.push({ name, content });
  }

  return blocks;
}

function extractGraphBody(dotContent) {
  // Extract just the body (nodes and edges) from a digraph
  const match = dotContent.match(/digraph\s+\w+\s*\{([\s\S]*)\}/);
  if (!match) return '';

  let body = match[1];

  // Remove rankdir (we'll set it once at the top level)
  body = body.replace(/^\s*rankdir\s*=\s*\w+\s*;?\s*$/gm, '');

  return body.trim();
}

function combineGraphs(blocks, skillName) {
  const bodies = blocks.map((block, i) => {
    const body = extractGraphBody(block.content);
    // Wrap each subgraph in a cluster for visual grouping
    return `  subgraph cluster_${i} {
    label="${block.name}";
    ${body.split('\n').map(line => '  ' + line).join('\n')}
  }`;
  });

  return `digraph ${skillName}_combined {
  rankdir=TB;
  compound=true;
  newrank=true;

${bodies.join('\n\n')}
}`;
}

function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotC...
```

</details>

---

#### Finding 399: 🟠 **HIGH** — `SAFE-T1403`

**Location:** Lines 1-168

**Description:**

The provided code is a utility script for rendering Graphviz diagrams and does not implement or directly facilitate the SAFE-T1403 technique. SAFE-T1403 describes an attack where an adversary generates rapid or repeated permission requests to exploit user fatigue, typically in the context of MCP-powered AI agents, MFA, or approval workflows. This script does not generate user-facing permission requests or interact with an MCP server in a way that would lead to consent fatigue.: Consent-Fatigue Exploit overwhelms users with rapid, repeated, or batched permission requests to exploit decision fatigue. Users begin auto-approving prompts without reading them, allowing malicious tools to gain permissions they would normally be denied. This also includes auto-approve patterns that bypass user consent entirely.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:1-168 #!/usr/bin/env node

/**
 * Render graphviz diagrams from a skill's SKILL.md to SVG files.
 *
 * Usage:
 *   ./render-graphs.js <skill-directory>           # Render each diagram separately
 *   ./render-graphs.js <skill-directory> --combine # Combine all into one diagram
 *
 * Extracts all ```dot blocks from SKILL.md and renders to SVG.
 * Useful for helping your human partner visualize the process flows.
 *
 * Requires: graphviz (dot) installed on system
 */

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

function extractDotBlocks(markdown) {
  const blocks = [];
  const regex = /```dot\n([\s\S]*?)```/g;
  let match;

  while ((match = regex.exec(markdown)) !== null) {
    const content = match[1].trim();

    // Extract digraph name
    const nameMatch = content.match(/digraph\s+(\w+)/);
    const name = nameMatch ? nameMatch[1] : `graph_${blocks.length + 1}`;

    blocks.push({ name, content });
  }

  return blocks;
}

function extractGraphBody(dotContent) {
  // Extract just the body (nodes and edges) from a digraph
  const match = dotContent.match(/digraph\s+\w+\s*\{([\s\S]*)\}/);
  if (!match) return '';

  let body = match[1];

  // Remove rankdir (we'll set it once at the top level)
  body = body.replace(/^\s*rankdir\s*=\s*\w+\s*;?\s*$/gm, '');

  return body.trim();
}

function combineGraphs(blocks, skillName) {
  const bodies = blocks.map((block, i) => {
    const body = extractGraphBody(block.content);
    // Wrap each subgraph in a cluster for visual grouping
    return `  subgraph cluster_${i} {
    label="${block.name}";
    ${body.split('\n').map(line => '  ' + line).join('\n')}
  }`;
  });

  return `digraph ${skillName}_combined {
  rankdir=TB;
  compound=true;
  newrank=true;

${bodies.join('\n\n')}
}`;
}

function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotC...
```

</details>

---

#### Finding 400: 🟠 **HIGH** — `SAFE-T1407`

**Location:** Lines 79-83

**Description:**

The script executes the 'dot' command-line tool with user-controlled input, which could be a vector for command injection if not properly handled. However, the 'dot' command is executed with input provided via stdin, which generally mitigates direct command injection vulnerabilities through shell metacharacters in the input itself. The `dot` utility is designed to parse graph description language, not shell commands.: An attacker operates an MCP server that forwards requests to a legitimate upstream API and relays the responses back to the client. Because the responses originate from the trusted service, the traffic appears normal in monitoring and logs, but the proxy can inspect, alter, or exfiltrate sensitive data. This enables stealthy data collection and response manipulation while hiding the true malicious behavior.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:79-83 renderToSvg(dotContent) {try {return execSync('dot -Tsvg', {input: dotContent,encoding: 'utf-8',maxBuffer: 10 * 1024 * 1024});}
```

</details>

---

#### Finding 401: 🟠 **HIGH** — `SAFE-T1408`

**Location:** Lines 1-168

**Description:**

The provided code is a utility script for rendering Graphviz diagrams and does not implement or interact with any OAuth clients or servers. Therefore, it is not susceptible to OAuth protocol downgrade vulnerabilities (SAFE-T1408).: An attacker forces or coerces a client to use the OAuth 2.0 implicit flow instead of the authorization code flow with PKCE. Tokens delivered in URL fragments are easier to capture via logs, referrers, or malicious scripts, enabling token theft and session hijacking.
This technique detects the presence of implicit flow configuration, which indicates potential vulnerability. The authorization code flow with PKCE should always be preferred over implicit flow for security.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:1-168 #!/usr/bin/env node

/**
 * Render graphviz diagrams from a skill's SKILL.md to SVG files.
 *
 * Usage:
 *   ./render-graphs.js <skill-directory>           # Render each diagram separately
 *   ./render-graphs.js <skill-directory> --combine # Combine all into one diagram
 *
 * Extracts all ```dot blocks from SKILL.md and renders to SVG.
 * Useful for helping your human partner visualize the process flows.
 *
 * Requires: graphviz (dot) installed on system
 */

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

function extractDotBlocks(markdown) {
  const blocks = [];
  const regex = /```dot\n([\s\S]*?)```/g;
  let match;

  while ((match = regex.exec(markdown)) !== null) {
    const content = match[1].trim();

    // Extract digraph name
    const nameMatch = content.match(/digraph\s+(\w+)/);
    const name = nameMatch ? nameMatch[1] : `graph_${blocks.length + 1}`;

    blocks.push({ name, content });
  }

  return blocks;
}

function extractGraphBody(dotContent) {
  // Extract just the body (nodes and edges) from a digraph
  const match = dotContent.match(/digraph\s+\w+\s*\{([\s\S]*)\}/);
  if (!match) return '';

  let body = match[1];

  // Remove rankdir (we'll set it once at the top level)
  body = body.replace(/^\s*rankdir\s*=\s*\w+\s*;?\s*$/gm, '');

  return body.trim();
}

function combineGraphs(blocks, skillName) {
  const bodies = blocks.map((block, i) => {
    const body = extractGraphBody(block.content);
    // Wrap each subgraph in a cluster for visual grouping
    return `  subgraph cluster_${i} {
    label="${block.name}";
    ${body.split('\n').map(line => '  ' + line).join('\n')}
  }`;
  });

  return `digraph ${skillName}_combined {
  rankdir=TB;
  compound=true;
  newrank=true;

${bodies.join('\n\n')}
}`;
}

function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotC...
```

</details>

---

#### Finding 402: 🟠 **HIGH** — `SAFE-T1503`

**Location:** Lines 1-168

**Description:**

The script uses `execSync` to run the `dot` command with user-provided content from `SKILL.md`. While `dotContent` is passed via stdin, reducing direct command injection risk, a highly sophisticated exploit against `graphviz` itself (e.g., a vulnerability allowing arbitrary command execution via crafted DOT language) could potentially lead to environment variable enumeration or other system compromise. However, this is an indirect and low-probability vector, as the script itself does not directly attempt to enumerate or dump environment variables.: Environment Variable Scraping extracts secrets from process environment variables. MCP server tools iterate over process.env, os.environ, or use getenv with attacker-controlled keys to harvest API keys, database passwords, cloud credentials, and other sensitive configuration stored in environment variables.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:70-74 return execSync('dot -Tsvg', {
      input: dotContent,
      encoding: 'utf-8',
      maxBuffer: 10 * 1024 * 1024
    });
```

</details>

---

#### Finding 403: 🟠 **HIGH** — `SAFE-T1601`

**Location:** Lines 1-168

**Description:**

The file `skills/writing-skills/render-graphs.js` is a utility script for rendering Graphviz diagrams from markdown files. It does not contain any code that performs network scanning, interacts with MCP server endpoints, or enumerates MCP services or configurations. The script's functionality is entirely local, processing markdown files and invoking the `dot` command-line tool.: MCP Server Enumeration involves probing for available MCP server instances, scanning registries, and discovering service endpoints. Attackers enumerate servers to map the attack surface, identify vulnerable or misconfigured instances, and discover high-value targets for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:1-168 #!/usr/bin/env node

/**
 * Render graphviz diagrams from a skill's SKILL.md to SVG files.
 *
 * Usage:
 *   ./render-graphs.js <skill-directory>           # Render each diagram separately
 *   ./render-graphs.js <skill-directory> --combine # Combine all into one diagram
 *
 * Extracts all ```dot blocks from SKILL.md and renders to SVG.
 * Useful for helping your human partner visualize the process flows.
 *
 * Requires: graphviz (dot) installed on system
 */

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

function extractDotBlocks(markdown) {
  const blocks = [];
  const regex = /```dot\n([\s\S]*?)```/g;
  let match;

  while ((match = regex.exec(markdown)) !== null) {
    const content = match[1].trim();

    // Extract digraph name
    const nameMatch = content.match(/digraph\s+(\w+)/);
    const name = nameMatch ? nameMatch[1] : `graph_${blocks.length + 1}`;

    blocks.push({ name, content });
  }

  return blocks;
}

function extractGraphBody(dotContent) {
  // Extract just the body (nodes and edges) from a digraph
  const match = dotContent.match(/digraph\s+\w+\s*\{([\s\S]*)\}/);
  if (!match) return '';

  let body = match[1];

  // Remove rankdir (we'll set it once at the top level)
  body = body.replace(/^\s*rankdir\s*=\s*\w+\s*;?\s*$/gm, '');

  return body.trim();
}

function combineGraphs(blocks, skillName) {
  const bodies = blocks.map((block, i) => {
    const body = extractGraphBody(block.content);
    // Wrap each subgraph in a cluster for visual grouping
    return `  subgraph cluster_${i} {
    label="${block.name}";
    ${body.split('\n').map(line => '  ' + line).join('\n')}
  }`;
  });

  return `digraph ${skillName}_combined {
  rankdir=TB;
  compound=true;
  newrank=true;

${bodies.join('\n\n')}
}`;
}

function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotC...
```

</details>

---

#### Finding 404: 🟠 **HIGH** — `SAFE-T1602`

**Location:** Lines 100-106

**Description:**

The script explicitly checks for the presence of the 'dot' command-line tool (from Graphviz) and, if not found, provides detailed installation instructions. This directly reveals a critical dependency and its installation method, aligning with the 'Discovery of available tools, their capabilities, and input schemas' technique (SAFE-T1602).: Tool Enumeration discovers the full set of available MCP tools, their capabilities, input schemas, and permission requirements. Attackers use tools/list API calls and schema introspection to map available functionality, identify privileged tools, and find tools with weak input validation for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:100-106 try { execSync('which dot', { encoding: 'utf-8' }); } catch { console.error('Error: graphviz (dot) not found. Install with:'); console.error('  brew install graphviz    # macOS'); console.error('  apt install graphviz     # Linux'); process.exit(1); }
```

</details>

---

#### Finding 405: 🟠 **HIGH** — `SAFE-T1605`

**Location:** Lines 79-84

**Description:**

The script uses `execSync` to execute the `dot` command with user-provided content from `SKILL.md`. While this is a local utility script and not an MCP agent, if the `SKILL.md` file could be controlled by an untrusted source, it could lead to command injection. This is not directly related to SAFE-T1605 (capability enumeration) but is a general security concern for scripts executing external commands based on file content.: Capability Mapping systematically discovers MCP server capabilities, supported API versions, feature flags, and permission boundaries. Attackers use introspection endpoints, capability negotiation, and API probing to build a comprehensive map of what the target system can do, identifying the most valuable attack vectors.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:79-84 renderToSvg(dotContent) {try {return execSync('dot -Tsvg', {input: dotContent,encoding: 'utf-8',maxBuffer: 10 * 1024 * 1024});}
```

</details>

---

#### Finding 406: 🟠 **HIGH** — `SAFE-T1703`

**Location:** Lines 70-74

**Description:**

The script executes the external 'dot' command-line tool with user-controlled content extracted from 'SKILL.md' files. While the content is passed via stdin, preventing direct shell injection into the command string, the 'dot' tool itself is a complex parser that could potentially be exploited by specially crafted DOT language input.: Tool-Chaining Pivot exploits the output of one MCP tool as input to another more privileged tool, creating multi-step attack chains. Attackers leverage a low-privilege tool to produce crafted output that, when consumed by a higher-privilege tool, enables unauthorized operations such as command execution, data access, or privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:70-74 return execSync('dot -Tsvg', { input: dotContent, encoding: 'utf-8', maxBuffer: 10 * 1024 * 1024 });
```

</details>

---

#### Finding 407: 🟠 **HIGH** — `SAFE-T1707`

**Location:** Lines 1-168

**Description:**

The provided code is a command-line utility for rendering Graphviz diagrams. It does not operate in a web context, handle HTTP requests, manage user sessions, or interact with OAuth tokens or MCP portals. Therefore, it is not susceptible to CSRF (Cross-Site Request Forgery) vulnerabilities like SAFE-T1707.: CSRF Token Relay exploits missing or improperly validated CSRF tokens in MCP state-changing requests. Attackers forge cross-site requests or reuse OAuth state parameters to trigger unauthorized actions, manipulate tool configurations, or hijack authentication flows through the victim browser or agent.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:1-168 #!/usr/bin/env node

/**
 * Render graphviz diagrams from a skill's SKILL.md to SVG files.
 *
 * Usage:
 *   ./render-graphs.js <skill-directory>           # Render each diagram separately
 *   ./render-graphs.js <skill-directory> --combine # Combine all into one diagram
 *
 * Extracts all ```dot blocks from SKILL.md and renders to SVG.
 * Useful for helping your human partner visualize the process flows.
 *
 * Requires: graphviz (dot) installed on system
 */

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

function extractDotBlocks(markdown) {
  const blocks = [];
  const regex = /```dot\n([\s\S]*?)```/g;
  let match;

  while ((match = regex.exec(markdown)) !== null) {
    const content = match[1].trim();

    // Extract digraph name
    const nameMatch = content.match(/digraph\s+(\w+)/);
    const name = nameMatch ? nameMatch[1] : `graph_${blocks.length + 1}`;

    blocks.push({ name, content });
  }

  return blocks;
}

function extractGraphBody(dotContent) {
  // Extract just the body (nodes and edges) from a digraph
  const match = dotContent.match(/digraph\s+\w+\s*\{([\s\S]*)\}/);
  if (!match) return '';

  let body = match[1];

  // Remove rankdir (we'll set it once at the top level)
  body = body.replace(/^\s*rankdir\s*=\s*\w+\s*;?\s*$/gm, '');

  return body.trim();
}

function combineGraphs(blocks, skillName) {
  const bodies = blocks.map((block, i) => {
    const body = extractGraphBody(block.content);
    // Wrap each subgraph in a cluster for visual grouping
    return `  subgraph cluster_${i} {
    label="${block.name}";
    ${body.split('\n').map(line => '  ' + line).join('\n')}
  }`;
  });

  return `digraph ${skillName}_combined {
  rankdir=TB;
  compound=true;
  newrank=true;

${bodies.join('\n\n')}
}`;
}

function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotC...
```

</details>

---

#### Finding 408: 🟠 **HIGH** — `SAFE-T1911`

**Location:** Lines 1-168

**Description:**

The provided code is a local utility script for rendering Graphviz diagrams from Markdown files. It does not process or generate JSON parameters or metadata fields in the context of MCP tool calls, which is the specific target of the SAFE-T1911 technique.: Attackers hide sensitive data inside optional or unused parameters (e.g., metadata, context, tags) that are accepted and forwarded by the server but not actively validated. These values can be logged or propagated to upstream services, enabling stealthy exfiltration without triggering obvious alarms.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:1-168 skills/writing-skills/render-graphs.js:1-168 #!/usr/bin/env node

/**
 * Render graphviz diagrams from a skill's SKILL.md to SVG files.
 *
 * Usage:
 *   ./render-graphs.js <skill-directory>           # Render each diagram separately
 *   ./render-graphs.js <skill-directory> --combine # Combine all into one diagram
 *
 * Extracts all ```dot blocks from SKILL.md and renders to SVG.
 * Useful for helping your human partner visualize the process flows.
 *
 * Requires: graphviz (dot) installed on system
 */

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

function extractDotBlocks(markdown) {
  const blocks = [];
  const regex = /```dot\n([\s\S]*?)```/g;
  let match;

  while ((match = regex.exec(markdown)) !== null) {
    const content = match[1].trim();

    // Extract digraph name
    const nameMatch = content.match(/digraph\s+(\w+)/);
    const name = nameMatch ? nameMatch[1] : `graph_${blocks.length + 1}`;

    blocks.push({ name, content });
  }

  return blocks;
}

function extractGraphBody(dotContent) {
  // Extract just the body (nodes and edges) from a digraph
  const match = dotContent.match(/digraph\s+\w+\s*\{([\s\S]*)\}/);
  if (!match) return '';

  let body = match[1];

  // Remove rankdir (we'll set it once at the top level)
  body = body.replace(/^\s*rankdir\s*=\s*\w+\s*;?\s*$/gm, '');

  return body.trim();
}

function combineGraphs(blocks, skillName) {
  const bodies = blocks.map((block, i) => {
    const body = extractGraphBody(block.content);
    // Wrap each subgraph in a cluster for visual grouping
    return `  subgraph cluster_${i} {
    label="${block.name}";
    ${body.split('\n').map(line => '  ' + line).join('\n')}
  }`;
  });

  return `digraph ${skillName}_combined {
  rankdir=TB;
  compound=true;
  newrank=true;

${bodies.join('\n\n')}
}`;
}

function renderToSvg(dotContent) {
  try {
    return execSync('dot -Tsvg', {
      input: dotC...
```

</details>

---

#### Finding 409: 🟠 **HIGH** — `SAFE-T2102`

**Location:** Lines 79-83

**Description:**

The script executes the 'dot' command with user-controlled input, which can lead to denial of service via resource exhaustion or infinite loops in the external process.: Service Disruption via API Flooding uses MCP tools to generate excessive outbound API calls through infinite loops, missing rate limits, or unbounded concurrent requests. This causes denial of service on target APIs, excessive billing on cloud services, and potential cascading failures across dependent systems.

<details>
<summary>Code Evidence</summary>

```
skills/writing-skills/render-graphs.js:79-83 renderToSvg(dotContent) {try {return execSync('dot -Tsvg', {input: dotContent,encoding: 'utf-8',maxBuffer: 10 * 1024 * 1024});}
```

</details>

---

### 📁 `gemini-extension.json`

#### Finding 410: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Line 4

**Description:**

The `gemini-extension.json` configuration file specifies `contextFileName`, which directs the system to load the content of `GEMINI.md` into the LLM's context. This creates a direct vector for prompt injection if the `GEMINI.md` file is not a trusted, static resource or if its content can be manipulated by an attacker.: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:4-4 "contextFileName": "GEMINI.md"
```

</details>

---

#### Finding 411: 🔴 **CRITICAL** — `SAFE-T1103`

**Location:** Lines 1-6

**Description:**

The provided file `gemini-extension.json` is a configuration file describing an extension, not an MCP host or client implementation. It does not contain code that would be vulnerable to fake tool invocation (function spoofing) as described in SAFE-T1103.: Fake Tool Invocation exploits insufficient tool validation in MCP hosts by injecting or spoofing tool names, duplicating tool registrations, or manipulating the JSON-RPC tool call protocol. Attackers can redirect legitimate tool calls to malicious implementations or register tools that shadow trusted ones.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:1-6 gemini-extension.json:1-6 {
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "5.0.6",
  "contextFileName": "GEMINI.md"
}
```

</details>

---

#### Finding 412: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Lines 1-6

**Description:**

The 'gemini-extension.json' file is a configuration file for an extension named 'superpowers'. It defines metadata such as name, description, version, and a context file. This file itself does not contain any code or configuration that would directly expose debug endpoints or inspector tools, nor does it indicate the presence or configuration of the vulnerable MCP Inspector.: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:1-6 gemini-extension.json:1-6 {
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "5.0.6",
  "contextFileName": "GEMINI.md"
}
```

</details>

---

#### Finding 413: 🔴 **CRITICAL** — `SAFE-T1309`

**Location:** Line 2

**Description:**

The 'description' field in the extension manifest provides information about the extension's capabilities, including 'debugging' and 'core skills library'. While not a direct vulnerability, this information could be used by an attacker during reconnaissance to understand potential privileged tools or functionalities offered by the 'superpowers' extension, which might later be targeted for prompt injection attacks (SAFE-T1309).: Privileged Tool via Prompt Injection exploits LLM-based tool selection where the model chooses which tool to invoke based on natural language. Attackers craft prompts that manipulate the LLM into selecting privileged or dangerous tools, bypassing access controls that only exist at the prompt/UI layer rather than at the tool execution layer.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:2-2 "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques"
```

</details>

---

#### Finding 414: 🟠 **HIGH** — `SAFE-T1002`

**Location:** Lines 1-6

**Description:**

This file is a metadata descriptor for a Gemini extension. While it doesn't directly fetch remote code or artifacts, it is part of a larger system where the integrity of such extension files and their associated content (like 'GEMINI.md') is crucial. If this file itself were tampered with or delivered from an untrusted source, it could lead to the loading of malicious content or misdirection within the MCP server.: Supply Chain Compromise occurs when MCP servers download, install, import, or execute external packages, plugins, model weights, or binaries without integrity checks, version pinning, or signature verification. This allows attackers to inject malicious code through compromised dependencies.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:1-6 gemini-extension.json:1-6 {
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "5.0.6",
  "contextFileName": "GEMINI.md"
}
```

</details>

---

#### Finding 415: 🟠 **HIGH** — `SAFE-T1004`

**Location:** Line 2

**Description:**

The 'name' field in this configuration file defines an identifier for an MCP component. While this file itself does not constitute a vulnerability, the use of simple names for service identification without robust identity verification (e.g., cryptographic signatures, unique IDs, strong certificate validation) in the broader MCP ecosystem can make it susceptible to server impersonation or name collision attacks (SAFE-T1004). An attacker could register a malicious service with the same name, potentially redirecting clients.: Server Impersonation occurs when an attacker registers an MCP server with a name designed to be confused with a legitimate service (typosquatting, homograph attacks, or DNS cache poisoning). Users connecting to the impersonated server unknowingly send credentials and data to the attacker.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:2-2 "name": "superpowers"
```

</details>

---

#### Finding 416: 🟠 **HIGH** — `SAFE-T1006`

**Location:** Lines 1-6

**Description:**

This JSON file describes an MCP extension. While the file itself is not malicious, it could be part of a larger malicious package distributed via social engineering. The technique SAFE-T1006 focuses on the malicious installer or package that *contains* such a file, rather than the file itself being the vulnerability.: User-Social-Engineering Install exploits user trust to convince them to install a malicious or compromised MCP server. This includes fake documentation, misleading READMEs, deceptive tool descriptions, or impersonating popular tools to trick users into granting permissions.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:1-6 gemini-extension.json:1-6 {
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "5.0.6",
  "contextFileName": "GEMINI.md"
}
```

</details>

---

#### Finding 417: 🟠 **HIGH** — `SAFE-T1304`

**Location:** Lines 1-6

**Description:**

The file 'gemini-extension.json' is a metadata file for a Gemini extension. It defines the extension's name, description, version, and context file. It does not contain any executable code or configuration related to authentication tokens, credential handling, or inter-service communication.: Credential Relay Chain exploits token reuse across MCP service boundaries. Attackers capture tokens from one service interaction and replay them in tool-to-tool calls, cross-service requests, or downstream API invocations without proper audience validation, scope restriction, or token binding.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:1-6 gemini-extension.json:1-6 {
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "5.0.6",
  "contextFileName": "GEMINI.md"
}
```

</details>

---

#### Finding 418: 🟠 **HIGH** — `SAFE-T1308`

**Location:** Lines 1-6

**Description:**

The provided file `gemini-extension.json` is a static configuration file and does not contain executable code related to token validation or authorization logic. Therefore, it cannot directly implement or be vulnerable to SAFE-T1308 (Token Scope Substitution).: Token Scope Substitution occurs when an attacker replaces a limited-scope OAuth token with one that has broader permissions but the same audience, exploiting insufficient scope validation to gain elevated privileges.
This attack targets OAuth implementations that validate tokens but fail to verify that the token scopes match what was originally requested or required for the operation.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:1-6 gemini-extension.json:1-6 {
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "5.0.6",
  "contextFileName": "GEMINI.md"
}
```

</details>

---

#### Finding 419: 🟠 **HIGH** — `SAFE-T1403`

**Location:** Lines 1-6

**Description:**

The provided JSON file is a configuration file for an extension, defining its name, description, version, and context file. It does not contain any executable code or direct configuration related to generating permission requests or approval prompts.: Consent-Fatigue Exploit overwhelms users with rapid, repeated, or batched permission requests to exploit decision fatigue. Users begin auto-approving prompts without reading them, allowing malicious tools to gain permissions they would normally be denied. This also includes auto-approve patterns that bypass user consent entirely.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:1-6 gemini-extension.json:1-6 {
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "5.0.6",
  "contextFileName": "GEMINI.md"
}
```

</details>

---

#### Finding 420: 🟠 **HIGH** — `SAFE-T1404`

**Location:** Lines 1-6

**Description:**

The file 'gemini-extension.json' is a static configuration file and does not contain executable code or logic that could directly implement or be vulnerable to SAFE-T1404 (Response Tampering). This file defines metadata for an extension, such as name, description, version, and context file name.: Response Tampering intercepts and modifies MCP tool responses before they are displayed to the user or processed by the LLM. Attackers alter response content to hide evidence of malicious actions, inject false information, bypass content filters, or manipulate the LLM context for subsequent interactions.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:1-6 gemini-extension.json:1-6 {
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "5.0.6",
  "contextFileName": "GEMINI.md"
}
```

</details>

---

#### Finding 421: 🟠 **HIGH** — `SAFE-T1408`

**Location:** Lines 1-6

**Description:**

The provided file is a metadata configuration file for a Gemini extension and does not contain any OAuth client or server configuration. Therefore, it cannot directly implement or be vulnerable to an OAuth protocol downgrade (SAFE-T1408).: An attacker forces or coerces a client to use the OAuth 2.0 implicit flow instead of the authorization code flow with PKCE. Tokens delivered in URL fragments are easier to capture via logs, referrers, or malicious scripts, enabling token theft and session hijacking.
This technique detects the presence of implicit flow configuration, which indicates potential vulnerability. The authorization code flow with PKCE should always be preferred over implicit flow for security.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:1-6 gemini-extension.json:1-6 {
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "5.0.6",
  "contextFileName": "GEMINI.md"
}
```

</details>

---

#### Finding 422: 🟠 **HIGH** — `SAFE-T1503`

**Location:** Lines 1-6

**Description:**

The file 'gemini-extension.json' is a configuration file for an MCP extension. It defines metadata such as name, description, version, and a context file name. While the extension's description mentions 'debugging' and 'proven techniques', which could theoretically involve file access, this specific JSON file does not contain any executable code or instructions that directly enumerate or dump environment variables or access sensitive files. It merely provides descriptive metadata for an extension.: Environment Variable Scraping extracts secrets from process environment variables. MCP server tools iterate over process.env, os.environ, or use getenv with attacker-controlled keys to harvest API keys, database passwords, cloud credentials, and other sensitive configuration stored in environment variables.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:1-6 gemini-extension.json:1-6 {
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "5.0.6",
  "contextFileName": "GEMINI.md"
}
```

</details>

---

#### Finding 423: 🟠 **HIGH** — `SAFE-T1602`

**Location:** Lines 1-6

**Description:**

The `gemini-extension.json` file explicitly enumerates an available tool ('superpowers' extension) along with its description and version. This manifest provides direct information about an existing tool and its capabilities, which is a core aspect of the SAFE-T1602 (Tool Enumeration) technique.: Tool Enumeration discovers the full set of available MCP tools, their capabilities, input schemas, and permission requirements. Attackers use tools/list API calls and schema introspection to map available functionality, identify privileged tools, and find tools with weak input validation for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:1-6 {  "name": "superpowers",  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",  "version": "5.0.6",  "contextFileName": "GEMINI.md"}
```

</details>

---

#### Finding 424: 🟠 **HIGH** — `SAFE-T1706`

**Location:** Lines 1-6

**Description:**

The provided file 'gemini-extension.json' is a configuration file and does not contain code related to OAuth token validation or handling. Therefore, it cannot directly exhibit the SAFE-T1706 vulnerability.: OAuth Token Pivot Replay reuses OAuth tokens obtained from one service to access a different service or audience. Attackers exploit missing audience (aud) validation, overly broad token scopes, or lack of token binding to pivot from a lower-value service to a higher-value target using the same token.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:1-6 gemini-extension.json:1-6 {
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "5.0.6",
  "contextFileName": "GEMINI.md"
}
```

</details>

---

#### Finding 425: 🟠 **HIGH** — `SAFE-T1707`

**Location:** Lines 1-6

**Description:**

The file 'gemini-extension.json' is a metadata configuration file and does not contain executable code or logic related to handling HTTP requests, sessions, or OAuth tokens. Therefore, it cannot directly implement or omit CSRF protection mechanisms.: CSRF Token Relay exploits missing or improperly validated CSRF tokens in MCP state-changing requests. Attackers forge cross-site requests or reuse OAuth state parameters to trigger unauthorized actions, manipulate tool configurations, or hijack authentication flows through the victim browser or agent.

<details>
<summary>Code Evidence</summary>

```
gemini-extension.json:1-6 gemini-extension.json:1-6 {
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "5.0.6",
  "contextFileName": "GEMINI.md"
}
```

</details>

---

### 📁 `hooks/hooks-cursor.json`

#### Finding 426: 🔴 **CRITICAL** — `SAFE-T1003`

**Location:** Lines 6-8

**Description:**

The MCP server configuration defines a 'sessionStart' hook that executes an external command './hooks/session-start'. If this configuration is part of a trojanized MCP server package, this hook provides a direct mechanism for an attacker to execute arbitrary malicious code upon session initiation, gaining initial access and persistence.: Malicious MCP-Server Distribution involves distributing trojanized MCP server implementations through package registries (npm, PyPI, Docker Hub) or code repositories. These servers appear legitimate but contain hidden malicious functionality such as data exfiltration, backdoors, or credential theft.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:6-8 "command": "./hooks/session-start"
```

</details>

---

#### Finding 427: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Line 6

**Description:**

The 'command' field in the MCP hook configuration specifies an executable path. While this file itself is not a direct prompt injection vector for an LLM, an attacker who can modify this configuration could potentially point to a malicious script or inject commands that, if their output is later fed into an LLM, could lead to prompt injection. However, the immediate vulnerability is not prompt injection within the LLM context, but rather potential command execution or configuration tampering.: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:6-6 "command": "./hooks/session-start"
```

</details>

---

#### Finding 428: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Lines 1-10

**Description:**

The 'hooks-cursor.json' file defines a 'sessionStart' hook that executes a local command. While this file itself is not an exposed debug endpoint, the command it executes ('./hooks/session-start') could potentially be a debug tool or a script that exposes one if not properly secured. However, based on the provided context, this file is a configuration for the MCP server's lifecycle hooks, not the MCP Inspector debugging tool targeted by SAFE-T1109.: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:1-10 hooks/hooks-cursor.json:5-7 "sessionStart": [
      {
        "command": "./hooks/session-start"
      }
    ]
```

</details>

---

#### Finding 429: 🔴 **CRITICAL** — `SAFE-T1111`

**Location:** Lines 6-8

**Description:**

The `hooks-cursor.json` file defines a `sessionStart` hook that executes an arbitrary local command (`./hooks/session-start`). This mechanism allows for the execution of untrusted code at session startup, which can be leveraged for various attacks, including AI CLI weaponization.: AI Agent CLI Weaponization exploits AI-powered CLI tools and agents that have unrestricted shell access or use dangerous permission-bypass flags. Attackers distribute malicious packages with postinstall scripts that invoke AI CLI tools in unsafe modes, or craft inputs that cause agents to execute arbitrary system commands, harvest credentials, or exfiltrate repository data.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:6-8 "command": "./hooks/session-start"
```

</details>

---

#### Finding 430: 🔴 **CRITICAL** — `SAFE-T1203`

**Location:** Lines 1-10

**Description:**

The MCP server configuration defines a 'sessionStart' hook that executes an arbitrary local command ('./hooks/session-start'). This mechanism can be leveraged by a trojanized server artifact to establish persistence outside the MCP service lifecycle, as described in SAFE-T1203. If the './hooks/session-start' script is malicious, it would be executed every time a session starts, allowing for persistent unauthorized access.: Backdoored Server Binary involves distributing trojanized MCP server packages, containers, or binaries that establish persistence through postinstall hooks, cron jobs, systemd units, or autostart entries. The malicious code executes during installation and persists even after the package is uninstalled.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:1-10 hooks/hooks-cursor.json:5-7       "sessionStart": [
        {
          "command": "./hooks/session-start"
        }
      ]
```

</details>

---

#### Finding 431: 🔴 **CRITICAL** — `SAFE-T1305`

**Location:** Lines 6-8

**Description:**

The MCP server is configured to execute a command via a 'sessionStart' hook. While this file itself does not indicate a vulnerability, the ability to execute commands is a prerequisite for SAFE-T1305 if the MCP server is running with root privileges.: Host OS Privilege Escalation exploits operating system vulnerabilities or misconfigurations from within an MCP server process to gain root or administrative access on the host. Attack vectors include setuid binary abuse, kernel exploits, capability escalation, and accessing sensitive system files like /etc/shadow.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:6-8 "command": "./hooks/session-start"
```

</details>

---

#### Finding 432: 🔴 **CRITICAL** — `SAFE-T1309`

**Location:** Line 6

**Description:**

The configuration defines a 'sessionStart' hook that executes a local script './hooks/session-start'. While this script could be considered a 'privileged tool' (code execution), its invocation is statically defined in this configuration file and not directly subject to prompt injection via an LLM agent's decision-making process. The vulnerability SAFE-T1309 specifically targets scenarios where an LLM is tricked into *selecting and invoking* such tools based on user prompts. This file merely configures a static system behavior.: Privileged Tool via Prompt Injection exploits LLM-based tool selection where the model chooses which tool to invoke based on natural language. Attackers craft prompts that manipulate the LLM into selecting privileged or dangerous tools, bypassing access controls that only exist at the prompt/UI layer rather than at the tool execution layer.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:6-6 "command": "./hooks/session-start"
```

</details>

---

#### Finding 433: 🔴 **CRITICAL** — `SAFE-T1505`

**Location:** Lines 1-10

**Description:**

The 'hooks/hooks-cursor.json' file defines a session start hook that executes an external command. While this file itself does not directly implement the SAFE-T1505 technique, the external command './hooks/session-start' could potentially be a vector if it involves AI models, embedding processes, or handles sensitive information in a way that makes it vulnerable to in-memory secret extraction via semantic analysis.: In-Memory Secret Extraction targets secrets held in process memory at runtime. Attackers use memory dump tools, /proc/*/mem access, heap inspection, or debugger attachment to extract encryption keys, tokens, passwords, and other sensitive data that exists only in memory and would not be found in files or environment variables.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:1-10 hooks/hooks-cursor.json:5-7 "sessionStart": [
      {
        "command": "./hooks/session-start"
      }
    ]
```

</details>

---

#### Finding 434: 🔴 **CRITICAL** — `SAFE-T1702`

**Location:** Line 7

**Description:**

The 'command' field within the 'sessionStart' hook directly executes a shell command. If an attacker can modify this JSON configuration file, they can inject arbitrary shell commands, leading to remote code execution.: Command Injection vulnerabilities occur when MCP server tools execute shell commands with unsanitized user input. Attackers can inject malicious commands that are executed with the server's privileges, potentially leading to arbitrary code execution, data exfiltration, or system compromise.

This technique targets common command execution patterns across multiple languages including Node.js (child_process), Python (subprocess, os.system), Rust (Command), and Go (os/exec). Unsafe practices include string concatenation in shell commands, missing input validation, and using shell=True or shell invocation modes.

Command injection is particularly dangerous in MCP servers because tools often have elevated privileges to interact with system resources, files, or external services. A successful injection can bypass all security boundaries.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:7-7 "command": "./hooks/session-start"
```

</details>

---

#### Finding 435: 🔴 **CRITICAL** — `SAFE-T1705`

**Location:** Lines 1-10

**Description:**

The 'hooks-cursor.json' file defines a 'sessionStart' hook that executes a local command. While this configuration itself is not a direct instance of agent-to-agent message injection, a compromised hook mechanism could potentially be leveraged as an initial access point or part of a broader attack chain that eventually leads to cross-agent instruction injection if the executed script interacts with a multi-agent system.: Cross-Agent Instruction Injection targets multi-agent MCP architectures where agents communicate by passing messages or tool results. Attackers inject malicious instructions into one agent that propagate to other agents in the workflow, contaminating the entire multi-agent pipeline through message passing, shared context, or tool output forwarding.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:1-10 hooks/hooks-cursor.json:5-7 "sessionStart": [
      {
        "command": "./hooks/session-start"
      }
    ]
```

</details>

---

#### Finding 436: 🔴 **CRITICAL** — `SAFE-T2101`

**Location:** Line 7

**Description:**

The `sessionStart` hook is configured to execute an external script `./hooks/session-start`. This `command` field allows arbitrary shell command execution, which is a common vector for data destruction if the script itself contains destructive operations (e.g., `rm -rf`, `DROP TABLE` via a `sql_query` tool, or cloud resource deletion).: Data Destruction uses MCP tools to permanently delete or corrupt data through destructive operations such as rm -rf, DROP TABLE, file wiping, bulk delete API calls, or database truncation. Attackers may destroy data for sabotage, to cover their tracks, or as part of a ransomware attack.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:7-7 "command": "./hooks/session-start"
```

</details>

---

#### Finding 437: 🔴 **CRITICAL** — `SAFE-T2103`

**Location:** Line 7

**Description:**

The 'command' field in 'hooks/hooks-cursor.json' specifies an executable script. If this file is modified via a malicious pull request, an attacker could inject and execute arbitrary commands, leading to code sabotage or pipeline manipulation.: Code Sabotage via Malicious PR uses MCP tools with repository access to create automated pull requests containing malicious code changes, backdoors, or CI/CD pipeline modifications. Attackers manipulate build scripts, inject vulnerabilities, or alter deployment configurations through automated commits that bypass normal code review.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:7-7 "command": "./hooks/session-start"
```

</details>

---

#### Finding 438: 🔴 **CRITICAL** — `SAFE-T2107`

**Location:** Lines 1-10

**Description:**

The provided JSON file defines a hook configuration for an MCP server, specifying a command to be executed at session start. While this file itself is not training data, a compromised hook could potentially be used as an attack vector to inject malicious patterns into training data, aligning with the broader attack flow of SAFE-T2107. However, the file itself does not contain the malicious training data or adversarial examples.: AI Model Poisoning manipulates machine learning models used by MCP servers through training data poisoning, adversarial example injection, or embedding backdoor triggers during fine-tuning. Poisoned models produce attacker-desired outputs when triggered by specific inputs while behaving normally otherwise.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:1-10 hooks/hooks-cursor.json:1-10 {
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": "./hooks/session-start"
      }
    ]
  }
}
```

</details>

---

#### Finding 439: 🟠 **HIGH** — `SAFE-T1002`

**Location:** Line 6

**Description:**

The `hooks-cursor.json` file defines a `sessionStart` hook that executes a local command `./hooks/session-start`. If this configuration file and the associated script are part of an MCP server package that is fetched and installed without integrity verification, an attacker could inject malicious code into `./hooks/session-start` or modify the `command` to execute arbitrary code. This would lead to remote code execution upon session start, leveraging a supply chain compromise.: Supply Chain Compromise occurs when MCP servers download, install, import, or execute external packages, plugins, model weights, or binaries without integrity checks, version pinning, or signature verification. This allows attackers to inject malicious code through compromised dependencies.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:6-6 "command": "./hooks/session-start"
```

</details>

---

#### Finding 440: 🟠 **HIGH** — `SAFE-T1104`

**Location:** Lines 6-8

**Description:**

The `sessionStart` hook is configured to execute an arbitrary shell command (`./hooks/session-start`). This mechanism grants the MCP server the ability to execute any command with the privileges of the server process, representing an over-privileged tool capability.: Over-Privileged Tool Abuse occurs when MCP tools are granted permissions far exceeding their functional requirements. Attackers exploit these over-privileged tools through prompt injection or direct manipulation to access sensitive resources, execute system commands, or escalate privileges beyond the intended scope.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:6-8 "command": "./hooks/session-start"
```

</details>

---

#### Finding 441: 🟠 **HIGH** — `SAFE-T1201`

**Location:** Lines 6-8

**Description:**

The `sessionStart` hook executes an external command `./hooks/session-start`. If the content of this script can be modified after initial user approval of the `hooks-cursor.json` file, it creates a vector for a 'Rug Pull Attack' where the script's behavior can change from benign to malicious without requiring re-approval of the hook definition itself.: MCP Rug Pull Attack is a persistence technique where tools appear legitimate initially but can change their behavior after gaining user trust. This exploits dynamic tool definitions, remote configuration loading, or time-delayed activation to modify tool behavior post-approval.
Static analysis detects code patterns that ENABLE rug pulls: dynamic descriptions, remote tool loading without version pinning, and time-based conditional behavior.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:6-8 "command": "./hooks/session-start"
```

</details>

---

#### Finding 442: 🟠 **HIGH** — `SAFE-T1204`

**Location:** Lines 1-10

**Description:**

The 'hooks/hooks-cursor.json' file defines a 'sessionStart' hook that executes a command. While this mechanism could be exploited for persistence if an attacker gains write access to the file, it does not directly align with the SAFE-T1204 technique's focus on implanting malicious instructions into AI-specific vector stores or conversation memory for AI model processing. This is a direct command execution configuration, not an AI memory system.: Context Memory Implant exploits persistent memory systems in MCP-connected AI agents. Malicious agents write themselves into long-term vector stores, conversation histories, or system prompt storage to ensure re-loading in future sessions. The implanted instructions persist across server restarts and session boundaries.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:1-10 hooks/hooks-cursor.json:1-10 {
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": "./hooks/session-start"
      }
    ]
  }
}
```

</details>

---

#### Finding 443: 🟠 **HIGH** — `SAFE-T1205`

**Location:** Lines 1-10

**Description:**

The `hooks/hooks-cursor.json` file defines a `sessionStart` hook that executes a command. This configuration is stored persistently and will be reloaded upon server restarts. If an attacker gains write access to this file, they can modify the `command` value to inject and persist arbitrary malicious commands, achieving persistence across server restarts.: Persistent Tool Redefinition is a privilege escalation technique where attackers modify an MCP server's tool metadata or configuration to embed hidden commands that survive server restarts. By persisting malicious tool definitions to disk, database, or configuration files, the attacker ensures their modifications remain active even after the server is restarted or redeployed.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:1-10 {  "version": 1,  "hooks": {    "sessionStart": [      {        "command": "./hooks/session-start"      }    ]  }}
```

</details>

---

#### Finding 444: 🟠 **HIGH** — `SAFE-T1302`

**Location:** Lines 6-8

**Description:**

The `hooks-cursor.json` file defines a `sessionStart` hook that executes the `./hooks/session-start` script. If the MCP server process runs with elevated privileges (e.g., root or with `sudo` rights), this hook constitutes a high-privilege execution point. This mechanism allows for direct command execution, which can be abused by an attacker to perform unauthorized privileged operations, achieve privilege escalation, or maintain persistence if they can control the content of the script or the configuration.: High-Privilege Tool Abuse occurs when MCP tools are configured with administrative, root, or superuser access to databases, APIs, or system resources. Attackers exploit these tools through prompt injection or direct manipulation to perform privileged operations such as modifying access controls, reading sensitive data, or escalating their own permissions.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:6-8 "command": "./hooks/session-start"
```

</details>

---

#### Finding 445: 🟠 **HIGH** — `SAFE-T1407`

**Location:** Lines 5-7

**Description:**

The 'hooks-cursor.json' file defines a 'sessionStart' hook that executes an external command './hooks/session-start'. While this file itself does not directly implement a proxy masquerade, the execution of arbitrary commands via hooks in a compromised or malicious MCP server context could be a vector for setting up or facilitating a proxy masquerade. However, without the content of the './hooks/session-start' script or further context of the server's configuration, this file alone does not provide direct evidence of SAFE-T1407.: An attacker operates an MCP server that forwards requests to a legitimate upstream API and relays the responses back to the client. Because the responses originate from the trusted service, the traffic appears normal in monitoring and logs, but the proxy can inspect, alter, or exfiltrate sensitive data. This enables stealthy data collection and response manipulation while hiding the true malicious behavior.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:5-7 {"command": "./hooks/session-start"}
```

</details>

---

#### Finding 446: 🟠 **HIGH** — `SAFE-T1601`

**Location:** Lines 1-10

**Description:**

The `hooks/hooks-cursor.json` file defines a `sessionStart` hook that executes a local command, representing a discoverable MCP server service configuration.: MCP Server Enumeration involves probing for available MCP server instances, scanning registries, and discovering service endpoints. Attackers enumerate servers to map the attack surface, identify vulnerable or misconfigured instances, and discover high-value targets for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:1-10 { "version": 1, "hooks": { "sessionStart": [ { "command": "./hooks/session-start" } ] } }
```

</details>

---

#### Finding 447: 🟠 **HIGH** — `SAFE-T1602`

**Location:** Lines 5-7

**Description:**

The `hooks/hooks-cursor.json` file explicitly enumerates a command (`./hooks/session-start`) to be executed during the `sessionStart` event. This configuration file provides an attacker with information about available tools and their execution context.: Tool Enumeration discovers the full set of available MCP tools, their capabilities, input schemas, and permission requirements. Attackers use tools/list API calls and schema introspection to map available functionality, identify privileged tools, and find tools with weak input validation for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:5-7 "command": "./hooks/session-start"
```

</details>

---

#### Finding 448: 🟠 **HIGH** — `SAFE-T1701`

**Location:** Lines 6-8

**Description:**

A 'sessionStart' hook is configured to execute an external command './hooks/session-start'. This hook runs at the beginning of a session and, if not properly isolated, could access or manipulate shared state, leading to cross-tool contamination.: Cross-Tool Contamination exploits shared state between MCP tools including global variables, shared memory, common databases, or file-based caches. Malicious data written by one tool pollutes the execution context of another, enabling data exfiltration, privilege escalation, or behavior manipulation across tool boundaries.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:6-8 "command": "./hooks/session-start"
```

</details>

---

#### Finding 449: 🟠 **HIGH** — `SAFE-T1706`

**Location:** Lines 1-10

**Description:**

The provided JSON file defines a hook configuration, specifically a 'sessionStart' hook that executes a local command. This file does not contain any logic related to OAuth token issuance, validation, or audience claim enforcement. Therefore, it does not directly present a vulnerability related to OAuth token replay (SAFE-T1706).: OAuth Token Pivot Replay reuses OAuth tokens obtained from one service to access a different service or audience. Attackers exploit missing audience (aud) validation, overly broad token scopes, or lack of token binding to pivot from a lower-value service to a higher-value target using the same token.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:1-10 hooks/hooks-cursor.json:1-10 {
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": "./hooks/session-start"
      }
    ]
  }
}
```

</details>

---

#### Finding 450: 🟠 **HIGH** — `SAFE-T2102`

**Location:** Lines 6-8

**Description:**

The 'sessionStart' hook executes a local command, './hooks/session-start'. While this configuration itself is benign, the script executed by this command could potentially contain logic that leads to unbounded external API requests, aligning with SAFE-T2102. Without the content of './hooks/session-start', it's impossible to determine if a vulnerability exists.: Service Disruption via API Flooding uses MCP tools to generate excessive outbound API calls through infinite loops, missing rate limits, or unbounded concurrent requests. This causes denial of service on target APIs, excessive billing on cloud services, and potential cascading failures across dependent systems.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:6-8 "command": "./hooks/session-start"
```

</details>

---

#### Finding 451: 🟠 **HIGH** — `SAFE-T2105`

**Location:** Lines 5-7

**Description:**

The configuration defines a 'sessionStart' hook that executes an external command './hooks/session-start'. While this file itself does not contain disinformation or directly manipulate LLM output, it represents a potential entry point for an attacker to introduce or execute malicious scripts that could lead to SAFE-T2105 (Disinformation Output) if the referenced script is compromised or designed to interact with an LLM in a harmful way. This is a foundational element that could be leveraged in a broader attack.: Disinformation Output manipulates MCP tool responses or LLM outputs to generate false, misleading, or harmful information. Attackers inject instructions that cause the AI to produce fabricated data, distorted facts, or biased responses that appear authoritative, potentially causing real-world harm through decision-making based on false information.

<details>
<summary>Code Evidence</summary>

```
hooks/hooks-cursor.json:5-7 "command": "./hooks/session-start"
```

</details>

---

### 📁 `package.json`

#### Finding 452: 🔴 **CRITICAL** — `SAFE-T1003`

**Location:** Line 4

**Description:**

The 'main' entry point points to a potentially hidden directory '.opencode'. While not inherently malicious, this could be a tactic used by trojanized packages to obscure their primary execution path.: Malicious MCP-Server Distribution involves distributing trojanized MCP server implementations through package registries (npm, PyPI, Docker Hub) or code repositories. These servers appear legitimate but contain hidden malicious functionality such as data exfiltration, backdoors, or credential theft.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

#### Finding 453: 🔴 **CRITICAL** — `SAFE-T1101`

**Location:** Line 4

**Description:**

The 'main' field in package.json specifies the entry point for the package, which is '.opencode/plugins/superpowers.js'. While this path itself is not a command injection vulnerability, it points to a JavaScript file that *could* contain such vulnerabilities if it processes external input unsafely when executing system commands. This finding is informational as the package.json itself does not execute commands.: Prompt Injection via Tool Output occurs when MCP tools return user-controlled or external data directly to the LLM without proper sanitization, allowing malicious content in tool outputs to hijack the agent behavior. Tool outputs are typically trusted by LLMs and processed as authoritative information.
Common attack vectors include: database records with hidden instructions, file contents with prompt injection payloads, API responses crafted to override directives, and web scraping results with malicious instruction sequences.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

#### Finding 454: 🔴 **CRITICAL** — `SAFE-T1111`

**Location:** Line 4

**Description:**

This package.json file defines a 'main' entry point, which could potentially lead to the execution of malicious code if the referenced JavaScript file (.opencode/plugins/superpowers.js) were to implement AI CLI weaponization. However, the package.json itself does not contain any direct evidence of the SAFE-T1111 technique, such as a 'postinstall' script or direct invocation of AI CLI tools with dangerous flags.: AI Agent CLI Weaponization exploits AI-powered CLI tools and agents that have unrestricted shell access or use dangerous permission-bypass flags. Attackers distribute malicious packages with postinstall scripts that invoke AI CLI tools in unsafe modes, or craft inputs that cause agents to execute arbitrary system commands, harvest credentials, or exfiltrate repository data.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

#### Finding 455: 🔴 **CRITICAL** — `SAFE-T1306`

**Location:** Lines 1-6

**Description:**

The provided file is a package.json configuration file and does not contain executable code related to authentication server logic, token minting, or validation. Therefore, it cannot directly implement the SAFE-T1306 vulnerability.: Rogue Authorization Server involves setting up a malicious or misconfigured authorization server that mints OAuth tokens, JWTs, or API keys without proper validation, client verification, or trust chain enforcement. Attackers use these rogue-issued tokens to access MCP servers and tools that fail to validate the token issuer.

<details>
<summary>Code Evidence</summary>

```
package.json:1-6 package.json:1-6 {
  "name": "superpowers",
  "version": "5.0.6",
  "type": "module",
  "main": ".opencode/plugins/superpowers.js"
}
```

</details>

---

#### Finding 456: 🔴 **CRITICAL** — `SAFE-T1309`

**Location:** Line 4

**Description:**

The 'main' entry point for the 'superpowers' module points to a JavaScript file, which may contain privileged tools or an MCP agent. While this file itself is not a vulnerability, the code within 'superpowers.js' should be carefully reviewed for SAFE-T1309 vulnerabilities, especially if it exposes filesystem, network, or code execution capabilities to an LLM agent.: Privileged Tool via Prompt Injection exploits LLM-based tool selection where the model chooses which tool to invoke based on natural language. Attackers craft prompts that manipulate the LLM into selecting privileged or dangerous tools, bypassing access controls that only exist at the prompt/UI layer rather than at the tool execution layer.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

#### Finding 457: 🔴 **CRITICAL** — `SAFE-T2101`

**Location:** Lines 1-6

**Description:**

The package.json file defines metadata for a JavaScript package, including its name, version, and main entry point. While the 'main' field points to a JavaScript file that *could* potentially contain destructive operations, the package.json file itself does not implement any data destruction logic. This file is purely declarative and does not directly execute commands or perform file system/database modifications.: Data Destruction uses MCP tools to permanently delete or corrupt data through destructive operations such as rm -rf, DROP TABLE, file wiping, bulk delete API calls, or database truncation. Attackers may destroy data for sabotage, to cover their tracks, or as part of a ransomware attack.

<details>
<summary>Code Evidence</summary>

```
package.json:1-6 package.json:1-6 {
  "name": "superpowers",
  "version": "5.0.6",
  "type": "module",
  "main": ".opencode/plugins/superpowers.js"
}
```

</details>

---

#### Finding 458: 🔴 **CRITICAL** — `SAFE-T2106`

**Location:** Line 4

**Description:**

The 'package.json' file defines the entry point for the 'superpowers' module, which could potentially interact with a vector database. While this file itself is not vulnerable, the referenced main file '.opencode/plugins/superpowers.js' should be analyzed for SAFE-T2106 vulnerabilities.: Context Memory Poisoning via Vector Store Contamination manipulates vector databases storing long-term AI memory. Attackers inject poisoned embeddings that blend legitimate and malicious content, surviving server restarts and reinstallations. The poisoned content can self-replicate across sessions and affect multiple agents sharing the same vector store.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

#### Finding 459: 🔴 **CRITICAL** — `SAFE-T2107`

**Location:** Line 4

**Description:**

The 'main' entry point in package.json points to a JavaScript file that could potentially be an MCP tool. If this tool were compromised or designed maliciously, it could be used to generate poisoned training data for AI models, aligning with the SAFE-T2107 technique. However, the package.json itself does not contain the vulnerability.: AI Model Poisoning manipulates machine learning models used by MCP servers through training data poisoning, adversarial example injection, or embedding backdoor triggers during fine-tuning. Poisoned models produce attacker-desired outputs when triggered by specific inputs while behaving normally otherwise.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

#### Finding 460: 🟠 **HIGH** — `SAFE-T1002`

**Location:** Lines 1-6

**Description:**

The package.json file defines a module, which is a common component in software supply chains. While this file itself does not directly indicate a vulnerability, it is part of the attack surface for SAFE-T1002. The vulnerability arises when this package, or its dependencies, are fetched and trusted without proper integrity verification by the consuming system.: Supply Chain Compromise occurs when MCP servers download, install, import, or execute external packages, plugins, model weights, or binaries without integrity checks, version pinning, or signature verification. This allows attackers to inject malicious code through compromised dependencies.

<details>
<summary>Code Evidence</summary>

```
package.json:1-6 package.json:1-6 {
  "name": "superpowers",
  "version": "5.0.6",
  "type": "module",
  "main": ".opencode/plugins/superpowers.js"
}
```

</details>

---

#### Finding 461: 🟠 **HIGH** — `SAFE-T1006`

**Location:** Lines 1-6

**Description:**

This package.json file defines a module, which could potentially be used in a social engineering attack if the package itself were malicious. The 'main' entry point '.opencode/plugins/superpowers.js' would be the location where malicious code might reside. However, the package.json file itself does not contain any malicious logic.: User-Social-Engineering Install exploits user trust to convince them to install a malicious or compromised MCP server. This includes fake documentation, misleading READMEs, deceptive tool descriptions, or impersonating popular tools to trick users into granting permissions.

<details>
<summary>Code Evidence</summary>

```
package.json:1-6 package.json:1-6 {
  "name": "superpowers",
  "version": "5.0.6",
  "type": "module",
  "main": ".opencode/plugins/superpowers.js"
}
```

</details>

---

#### Finding 462: 🟠 **HIGH** — `SAFE-T1008`

**Location:** Lines 1-6

**Description:**

The 'package.json' file defines package metadata and an entry point, but does not contain any tool descriptions or executable logic that could be used to redirect or hijack other tools. This file is not directly susceptible to SAFE-T1008.: Tool Shadowing Attack embeds instructions in tool descriptions that manipulate how AI agents use OTHER tools. In multi-server MCP setups, all tool descriptions are aggregated into a single prompt, allowing any tool to influence the behavior of others through cross-tool interference patterns.

<details>
<summary>Code Evidence</summary>

```
package.json:1-6 package.json:1-6 {
  "name": "superpowers",
  "version": "5.0.6",
  "type": "module",
  "main": ".opencode/plugins/superpowers.js"
}
```

</details>

---

#### Finding 463: 🟠 **HIGH** — `SAFE-T1105`

**Location:** Line 4

**Description:**

The 'main' field in package.json specifies a relative path '.opencode/plugins/superpowers.js'. While this is a relative path, it is a static configuration value within the package metadata and not directly controlled by untrusted user input. Therefore, it does not represent a path traversal vulnerability in itself.: Path Traversal via File Tool occurs when MCP file-handling tools accept untrusted input as filesystem paths without proper normalization, allowlisting, or base directory enforcement. Attackers can use relative path sequences, Unicode normalization tricks, double encoding, or null byte injection to access files outside the intended directory.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

#### Finding 464: 🟠 **HIGH** — `SAFE-T1201`

**Location:** Line 4

**Description:**

The 'package.json' file defines a local entry point for the 'superpowers' module. While this file itself is static, the referenced JavaScript file ('superpowers.js') could potentially implement dynamic behavior or fetch remote configurations, which is a characteristic of the SAFE-T1201 'MCP Rug Pull Attack' technique. However, the 'package.json' alone does not provide direct evidence of such dynamic behavior.: MCP Rug Pull Attack is a persistence technique where tools appear legitimate initially but can change their behavior after gaining user trust. This exploits dynamic tool definitions, remote configuration loading, or time-delayed activation to modify tool behavior post-approval.
Static analysis detects code patterns that ENABLE rug pulls: dynamic descriptions, remote tool loading without version pinning, and time-based conditional behavior.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

#### Finding 465: 🟠 **HIGH** — `SAFE-T1304`

**Location:** Lines 1-6

**Description:**

The package.json file defines basic metadata for a Node.js package, including its name, version, module type, and main entry point. This file does not contain any code or configuration related to authentication tokens, credential handling, or inter-service communication that would directly lead to or indicate a credential relay vulnerability (SAFE-T1304).: Credential Relay Chain exploits token reuse across MCP service boundaries. Attackers capture tokens from one service interaction and replay them in tool-to-tool calls, cross-service requests, or downstream API invocations without proper audience validation, scope restriction, or token binding.

<details>
<summary>Code Evidence</summary>

```
package.json:1-6 package.json:1-6 {
  "name": "superpowers",
  "version": "5.0.6",
  "type": "module",
  "main": ".opencode/plugins/superpowers.js"
}
```

</details>

---

#### Finding 466: 🟠 **HIGH** — `SAFE-T1307`

**Location:** Lines 1-6

**Description:**

The 'package.json' file defines project metadata and entry points, but does not contain executable logic that could directly implement a Confused Deputy Attack (SAFE-T1307). This file itself is not vulnerable to token forwarding or privilege misuse.: Confused Deputy Attack exploits ambient authority in MCP servers where a privileged service performs actions on behalf of an unauthorized requester. In multi-tenant environments, this enables cross-tenant data access, privilege confusion between service tiers, or unauthorized operations performed through a trusted intermediary.

<details>
<summary>Code Evidence</summary>

```
package.json:1-6 package.json:1-6 {
  "name": "superpowers",
  "version": "5.0.6",
  "type": "module",
  "main": ".opencode/plugins/superpowers.js"
}
```

</details>

---

#### Finding 467: 🟠 **HIGH** — `SAFE-T1403`

**Location:** Line 4

**Description:**

The 'package.json' file defines project metadata and its main entry point, which is '.opencode/plugins/superpowers.js'. While this file itself does not directly implement the SAFE-T1403 consent-fatigue exploit, the 'superpowers.js' plugin it points to could potentially be involved in generating frequent permission requests if it's designed to interact with sensitive resources or perform actions requiring user approval. This file serves as an informational pointer to where such logic might reside.: Consent-Fatigue Exploit overwhelms users with rapid, repeated, or batched permission requests to exploit decision fatigue. Users begin auto-approving prompts without reading them, allowing malicious tools to gain permissions they would normally be denied. This also includes auto-approve patterns that bypass user consent entirely.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

#### Finding 468: 🟠 **HIGH** — `SAFE-T1407`

**Location:** Line 4

**Description:**

The 'main' field in package.json points to a JavaScript file, which could potentially contain malicious proxying logic. However, the package.json itself does not implement the proxying technique.: An attacker operates an MCP server that forwards requests to a legitimate upstream API and relays the responses back to the client. Because the responses originate from the trusted service, the traffic appears normal in monitoring and logs, but the proxy can inspect, alter, or exfiltrate sensitive data. This enables stealthy data collection and response manipulation while hiding the true malicious behavior.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

#### Finding 469: 🟠 **HIGH** — `SAFE-T1503`

**Location:** Lines 1-6

**Description:**

The 'package.json' file defines project metadata and its main entry point. While it does not directly expose environment variables or contain logic for scraping them, the 'main' field points to a JavaScript file ('superpowers.js') which could potentially contain such logic. However, the 'package.json' itself is not a direct vulnerability for SAFE-T1503.: Environment Variable Scraping extracts secrets from process environment variables. MCP server tools iterate over process.env, os.environ, or use getenv with attacker-controlled keys to harvest API keys, database passwords, cloud credentials, and other sensitive configuration stored in environment variables.

<details>
<summary>Code Evidence</summary>

```
package.json:1-6 package.json:1-6 {
  "name": "superpowers",
  "version": "5.0.6",
  "type": "module",
  "main": ".opencode/plugins/superpowers.js"
}
```

</details>

---

#### Finding 470: 🟠 **HIGH** — `SAFE-T1601`

**Location:** Line 4

**Description:**

The 'main' field in package.json indicates the entry point for the 'superpowers' module. If this module is intended to be an MCP server, this path could indirectly point to the server's implementation. However, this file itself does not expose server endpoints or configurations for enumeration.: MCP Server Enumeration involves probing for available MCP server instances, scanning registries, and discovering service endpoints. Attackers enumerate servers to map the attack surface, identify vulnerable or misconfigured instances, and discover high-value targets for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

#### Finding 471: 🟠 **HIGH** — `SAFE-T1602`

**Location:** Lines 1-6

**Description:**

The `package.json` file exposes the name and version of a core component ('superpowers', '5.0.6') and its main entry point. This information directly aids an attacker in performing tool enumeration (SAFE-T1602) by providing details about available tools and their configurations within the environment.: Tool Enumeration discovers the full set of available MCP tools, their capabilities, input schemas, and permission requirements. Attackers use tools/list API calls and schema introspection to map available functionality, identify privileged tools, and find tools with weak input validation for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
package.json:1-6 package.json:1-6 {
  "name": "superpowers",
  "version": "5.0.6",
  "type": "module",
  "main": ".opencode/plugins/superpowers.js"
}
```

</details>

---

#### Finding 472: 🟠 **HIGH** — `SAFE-T1606`

**Location:** Line 4

**Description:**

The 'main' field in package.json points to a JavaScript file, which could potentially contain directory listing functionality. However, the package.json itself does not implement or expose such a vulnerability.: Directory Listing via File Tool exploits MCP file tools that allow unrestricted directory enumeration. Attackers use fs.readdir, os.listdir, or equivalent functions to map the filesystem structure, discover sensitive files, locate configuration files, and identify credential storage locations for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

#### Finding 473: 🟠 **HIGH** — `SAFE-T1707`

**Location:** Lines 1-6

**Description:**

The provided file 'package.json' is a project metadata file and does not contain executable code related to web application endpoints, CSRF token implementation, or OAuth token handling. Therefore, it cannot directly exhibit the SAFE-T1707 vulnerability.: CSRF Token Relay exploits missing or improperly validated CSRF tokens in MCP state-changing requests. Attackers forge cross-site requests or reuse OAuth state parameters to trigger unauthorized actions, manipulate tool configurations, or hijack authentication flows through the victim browser or agent.

<details>
<summary>Code Evidence</summary>

```
package.json:1-6 package.json:1-6 {
  "name": "superpowers",
  "version": "5.0.6",
  "type": "module",
  "main": ".opencode/plugins/superpowers.js"
}
```

</details>

---

#### Finding 474: 🟠 **HIGH** — `SAFE-T1801`

**Location:** Line 4

**Description:**

The 'main' field in package.json points to a JavaScript file that could potentially contain MCP tool definitions or agent logic. While package.json itself is not vulnerable, the referenced file '.opencode/plugins/superpowers.js' might be relevant for SAFE-T1801 analysis if it defines tools that allow broad data access or iterative calls.: Automated Data Harvesting uses MCP tools to systematically access and collect data through bulk queries, pagination scraping, and record enumeration. Attackers automate data collection across multiple endpoints or database tables to amass sensitive information for exfiltration or analysis.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

#### Finding 475: 🟠 **HIGH** — `SAFE-T1804`

**Location:** Lines 1-6

**Description:**

The package.json file defines project metadata and dependencies, but does not contain code that directly implements or exposes API endpoints or MCP tools susceptible to systematic API abuse (SAFE-T1804). The 'main' entry point points to a JavaScript file, which would need to be analyzed separately for potential vulnerabilities related to API usage or MCP tool definitions.: API Data Harvest systematically extracts data through MCP tool API calls by abusing pagination, accessing bulk endpoints, or bypassing rate limits. Attackers iterate through API pages, manipulate query parameters, or use concurrent requests to extract large datasets that individual queries would not expose.

<details>
<summary>Code Evidence</summary>

```
package.json:1-6 package.json:1-6 {
  "name": "superpowers",
  "version": "5.0.6",
  "type": "module",
  "main": ".opencode/plugins/superpowers.js"
}
```

</details>

---

#### Finding 476: 🟠 **HIGH** — `SAFE-T2102`

**Location:** Line 4

**Description:**

The 'main' entry point in package.json points to a JavaScript file that could potentially contain an MCP-enabled AI agent or plugin. While this file itself does not contain the vulnerability, the referenced 'superpowers.js' file might implement external API calls without proper rate limiting or retry logic, leading to service disruption via external API flooding (SAFE-T2102).: Service Disruption via API Flooding uses MCP tools to generate excessive outbound API calls through infinite loops, missing rate limits, or unbounded concurrent requests. This causes denial of service on target APIs, excessive billing on cloud services, and potential cascading failures across dependent systems.

<details>
<summary>Code Evidence</summary>

```
package.json:4-4 "main": ".opencode/plugins/superpowers.js"
```

</details>

---

### 📁 `skills/brainstorming/scripts/start-server.sh`

#### Finding 477: 🔴 **CRITICAL** — `SAFE-T1003`

**Location:** Lines 1-148

**Description:**

This script is a launcher for an MCP server. While it does not directly implement the SAFE-T1003 technique (which focuses on the distribution of trojanized packages), it is a critical component that would be included within such a package. The script itself appears to be a standard utility for starting a Node.js server, setting up environment variables, and managing session directories and process IDs. Its functionality is benign in isolation, but if the `server.cjs` it executes were malicious, then the entire package containing this script would be considered trojanized.: Malicious MCP-Server Distribution involves distributing trojanized MCP server implementations through package registries (npm, PyPI, Docker Hub) or code repositories. These servers appear legitimate but contain hidden malicious functionality such as data exfiltration, backdoors, or credential theft.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:1-148 #!/usr/bin/env bash
# Start the brainstorm server and output connection info
# Usage: start-server.sh [--project-dir <path>] [--host <bind-host>] [--url-host <display-host>] [--foreground] [--background]
#
# Starts server on a random high port, outputs JSON with URL.
# Each session gets its own directory to avoid conflicts.
#
# Options:
#   --project-dir <path>  Store session files under <path>/.superpowers/brainstorm/
#                         instead of /tmp. Files persist after server stops.
#   --host <bind-host>    Host/interface to bind (default: 127.0.0.1).
#                         Use 0.0.0.0 in remote/containerized environments.
#   --url-host <host>     Hostname shown in returned URL JSON.
#   --foreground          Run server in the current terminal (no backgrounding).
#   --background          Force background mode (overrides Codex auto-foreground).

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

# Parse arguments
PROJECT_DIR=""
FOREGROUND="false"
FORCE_BACKGROUND="false"
BIND_HOST="127.0.0.1"
URL_HOST=""
while [[ $# -gt 0 ]]; do
  case "$1" in
    --project-dir)
      PROJECT_DIR="$2"
      shift 2
      ;;
    --host)
      BIND_HOST="$2"
      shift 2
      ;;
    --url-host)
      URL_HOST="$2"
      shift 2
      ;;
    --foreground|--no-daemon)
      FOREGROUND="true"
      shift
      ;;
    --background|--daemon)
      FORCE_BACKGROUND="true"
      shift
      ;;
    *)
      echo "{\"error\": \"Unknown argument: $1\"}"
      exit 1
      ;;
  esac
done

if [[ -z "$URL_HOST" ]]; then
  if [[ "$BIND_HOST" == "127.0.0.1" || "$BIND_HOST" == "localhost" ]]; then
    URL_HOST="localhost"
  else
    URL_HOST="$BIND_HOST"
  fi
fi

# Some environments reap detached/background processes. Auto-foreground when detected.
if [[ -n "${CODEX_CI:-}" && "$FOREGROUND" != "true" && "$FORCE_BACKGROUND" != "true" ]]; then
  FOREGROUND="true"
fi

# Windows/Git Bash r...
```

</details>

---

#### Finding 478: 🔴 **CRITICAL** — `SAFE-T1007`

**Location:** Lines 1-148

**Description:**

The `start-server.sh` script configures the `BRAINSTORM_URL_HOST` environment variable based on user input or derived from `BIND_HOST`. While this script itself does not implement OAuth flows, the `BRAINSTORM_URL_HOST` value could be used by the `node server.cjs` (the actual server application) to construct URLs, including potential OAuth redirect URIs. If the `node server.cjs` does not properly validate or restrict the `BRAINSTORM_URL_HOST` when initiating OAuth flows, an attacker who can control the arguments to this script (e.g., by running a malicious instance of the server) could potentially direct OAuth callbacks to an attacker-controlled domain, facilitating SAFE-T1007. However, this script is merely a launcher and does not contain the OAuth logic itself.: OAuth Authorization Phishing exploits the OAuth authorization flow to trick users into granting permissions to attacker-controlled applications. The attacker creates a convincing authorization page or redirects users through legitimate OAuth providers but captures the authorization code or tokens.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:30-32     --url-host)
      URL_HOST="$2"
      shift 2
skills/brainstorming/scripts/start-server.sh:45-50 if [[ -z "$URL_HOST" ]]; then
  if [[ "$BIND_HOST" == "127.0.0.1" || "$BIND_HOST" == "localhost" ]]; then
    URL_HOST="localhost"
  else
    URL_HOST="$BIND_HOST"
  fi
skills/brainstorming/scripts/start-server.sh:106 env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs
```

</details>

---

#### Finding 479: 🔴 **CRITICAL** — `SAFE-T1101`

**Location:** Lines 1-148

**Description:**

The script takes user-controlled input `URL_HOST` and passes it to a child process (`node server.cjs`), which is expected to embed this value into a JSON output. This JSON output is then printed directly by the script. If `URL_HOST` contains characters that break JSON formatting or inject new instructions when interpreted by an LLM, it can lead to LLM hijacking.: Prompt Injection via Tool Output occurs when MCP tools return user-controlled or external data directly to the LLM without proper sanitization, allowing malicious content in tool outputs to hijack the agent behavior. Tool outputs are typically trusted by LLMs and processed as authoritative information.
Common attack vectors include: database records with hidden instructions, file contents with prompt injection payloads, API responses crafted to override directives, and web scraping results with malicious instruction sequences.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:29-29 URL_HOST="$2"
skills/brainstorming/scripts/start-server.sh:100-100 nohup env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs > "$LOG_FILE" 2>&1 &
skills/brainstorming/scripts/start-server.sh:124-124 grep "server-started" "$LOG_FILE" | head -1
```

</details>

---

#### Finding 480: 🔴 **CRITICAL** — `SAFE-T1103`

**Location:** Lines 1-148

**Description:**

The `start-server.sh` script is responsible for launching a Node.js server (`server.cjs`) and setting up its environment. It does not implement the MCP host logic that would receive and validate tool invocation requests. Therefore, this script itself is not directly vulnerable to function name spoofing or tool impersonation as described in SAFE-T1103.: Fake Tool Invocation exploits insufficient tool validation in MCP hosts by injecting or spoofing tool names, duplicating tool registrations, or manipulating the JSON-RPC tool call protocol. Attackers can redirect legitimate tool calls to malicious implementations or register tools that shadow trusted ones.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:1-148 #!/usr/bin/env bash
# Start the brainstorm server and output connection info
# Usage: start-server.sh [--project-dir <path>] [--host <bind-host>] [--url-host <display-host>] [--foreground] [--background]
#
# Starts server on a random high port, outputs JSON with URL.
# Each session gets its own directory to avoid conflicts.
#
# Options:
#   --project-dir <path>  Store session files under <path>/.superpowers/brainstorm/
#                         instead of /tmp. Files persist after server stops.
#   --host <bind-host>    Host/interface to bind (default: 127.0.0.1).
#                         Use 0.0.0.0 in remote/containerized environments.
#   --url-host <host>     Hostname shown in returned URL JSON.
#   --foreground          Run server in the current terminal (no backgrounding).
#   --background          Force background mode (overrides Codex auto-foreground).

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

# Parse arguments
PROJECT_DIR=""
FOREGROUND="false"
FORCE_BACKGROUND="false"
BIND_HOST="127.0.0.1"
URL_HOST=""
while [[ $# -gt 0 ]]; do
  case "$1" in
    --project-dir)
      PROJECT_DIR="$2"
      shift 2
      ;;
    --host)
      BIND_HOST="$2"
      shift 2
      ;;
    --url-host)
      URL_HOST="$2"
      shift 2
      ;;
    --foreground|--no-daemon)
      FOREGROUND="true"
      shift
      ;;
    --background|--daemon)
      FORCE_BACKGROUND="true"
      shift
      ;;
    *)
      echo "{\"error\": \"Unknown argument: $1\"}"
      exit 1
      ;;
  esac
done

if [[ -z "$URL_HOST" ]]; then
  if [[ "$BIND_HOST" == "127.0.0.1" || "$BIND_HOST" == "localhost" ]]; then
    URL_HOST="localhost"
  else
    URL_HOST="$BIND_HOST"
  fi
fi

# Some environments reap detached/background processes. Auto-foreground when detected.
if [[ -n "${CODEX_CI:-}" && "$FOREGROUND" != "true" && "$FORCE_BACKGROUND" != "true" ]]; then
  FOREGROUND="true"
fi

# Windows/Git Bash r...
```

</details>

---

#### Finding 481: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Lines 28-31

**Description:**

The `start-server.sh` script allows the 'brainstorm server' to be explicitly bound to all network interfaces (0.0.0.0) via the `--host` command-line argument. While the default binding is `127.0.0.1` (localhost), overriding this to `0.0.0.0` could expose a critical vulnerability if the `node server.cjs` application is or integrates a debug endpoint like the MCP Inspector (CVE-2025-49596).: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:28-31 case "$1" in    --project-dir)      PROJECT_DIR="$2"      shift 2      ;;    --host)      BIND_HOST="$2"      shift 2      ;;
```

</details>

---

#### Finding 482: 🔴 **CRITICAL** — `SAFE-T1111`

**Location:** Lines 1-148

**Description:**

The script `start-server.sh` does not appear to directly invoke or weaponize AI CLI tools as described in SAFE-T1111. It is a standard shell script for starting a local Node.js server (`node server.cjs`) with various configuration options.: AI Agent CLI Weaponization exploits AI-powered CLI tools and agents that have unrestricted shell access or use dangerous permission-bypass flags. Attackers distribute malicious packages with postinstall scripts that invoke AI CLI tools in unsafe modes, or craft inputs that cause agents to execute arbitrary system commands, harvest credentials, or exfiltrate repository data.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:1-148 #!/usr/bin/env bash
# Start the brainstorm server and output connection info
# Usage: start-server.sh [--project-dir <path>] [--host <bind-host>] [--url-host <display-host>] [--foreground] [--background]
#
# Starts server on a random high port, outputs JSON with URL.
# Each session gets its own directory to avoid conflicts.
#
# Options:
#   --project-dir <path>  Store session files under <path>/.superpowers/brainstorm/
#                         instead of /tmp. Files persist after server stops.
#   --host <bind-host>    Host/interface to bind (default: 127.0.0.1).
#                         Use 0.0.0.0 in remote/containerized environments.
#   --url-host <host>     Hostname shown in returned URL JSON.
#   --foreground          Run server in the current terminal (no backgrounding).
#   --background          Force background mode (overrides Codex auto-foreground).

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

# Parse arguments
PROJECT_DIR=""
FOREGROUND="false"
FORCE_BACKGROUND="false"
BIND_HOST="127.0.0.1"
URL_HOST=""
while [[ $# -gt 0 ]]; do
  case "$1" in
    --project-dir)
      PROJECT_DIR="$2"
      shift 2
      ;;
    --host)
      BIND_HOST="$2"
      shift 2
      ;;
    --url-host)
      URL_HOST="$2"
      shift 2
      ;;
    --foreground|--no-daemon)
      FOREGROUND="true"
      shift
      ;;
    --background|--daemon)
      FORCE_BACKGROUND="true"
      shift
      ;;
    *)
      echo "{\"error\": \"Unknown argument: $1\"}"
      exit 1
      ;;
  esac
done

if [[ -z "$URL_HOST" ]]; then
  if [[ "$BIND_HOST" == "127.0.0.1" || "$BIND_HOST" == "localhost" ]]; then
    URL_HOST="localhost"
  else
    URL_HOST="$BIND_HOST"
  fi
fi

# Some environments reap detached/background processes. Auto-foreground when detected.
if [[ -n "${CODEX_CI:-}" && "$FOREGROUND" != "true" && "$FORCE_BACKGROUND" != "true" ]]; then
  FOREGROUND="true"
fi

# Windows/Git Bash r...
```

</details>

---

#### Finding 483: 🔴 **CRITICAL** — `SAFE-T1203`

**Location:** Lines 1-148

**Description:**

The script uses `nohup` and `disown` to run the server process in the background, which allows it to continue running after the terminal session ends. While this is a form of process persistence, it is for the current server instance and not a system-wide persistence mechanism (like cron jobs, systemd units, or autostart entries) that would survive reboots or uninstallation, as described by SAFE-T1203. The script also creates a PID file to manage the running server instance.: Backdoored Server Binary involves distributing trojanized MCP server packages, containers, or binaries that establish persistence through postinstall hooks, cron jobs, systemd units, or autostart entries. The malicious code executes during installation and persists even after the package is uninstalled.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:115-117 nohup env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs > "$LOG_FILE" 2>&1 &
SERVER_PID=$!
disown "$SERVER_PID" 2>/dev/null
skills/brainstorming/scripts/start-server.sh:118-118 echo "$SERVER_PID" > "$PID_FILE"
```

</details>

---

#### Finding 484: 🔴 **CRITICAL** — `SAFE-T1309`

**Location:** Lines 1-148

**Description:**

The `start-server.sh` script is a utility for launching a server process (`node server.cjs`). While the server it launches *could* potentially be an MCP agent susceptible to SAFE-T1309, this script itself is not an LLM agent and does not directly expose itself to prompt injection for privileged tool invocation. It handles command-line arguments and environment variable setup for the server.: Privileged Tool via Prompt Injection exploits LLM-based tool selection where the model chooses which tool to invoke based on natural language. Attackers craft prompts that manipulate the LLM into selecting privileged or dangerous tools, bypassing access controls that only exist at the prompt/UI layer rather than at the tool execution layer.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 #!/usr/bin/env bash# Start the brainstorm server and output connection info# Usage: start-server.sh [--project-dir <path>] [--host <bind-host>] [--url-host <display-host>] [--foreground] [--background]# # Starts server on a random high port, outputs JSON with URL.# Each session gets its own directory to avoid conflicts.# # Options:#   --project-dir <path>  Store session files under <path>/.superpowers/brainstorm/#                         instead of /tmp. Files persist after server stops.#   --host <bind-host>    Host/interface to bind (default: 127.0.0.1).#                         Use 0.0.0.0 in remote/containerized environments.#   --url-host <host>     Hostname shown in returned URL JSON.#   --foreground          Run server in the current terminal (no backgrounding).#   --background          Force background mode (overrides Codex auto-foreground).SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"# Parse argumentsPROJECT_DIR=""FOREGROUND="false"FORCE_BACKGROUND="false"BIND_HOST="127.0.0.1"URL_HOST=""while [[ $# -gt 0 ]]; do  case "$1" in    --project-dir)      PROJECT_DIR="$2"      shift 2      ;;    --host)      BIND_HOST="$2"      shift 2      ;;    --url-host)      URL_HOST="$2"      shift 2      ;;    --foreground|--no-daemon)      FOREGROUND="true"      shift      ;;    --background|--daemon)      FORCE_BACKGROUND="true"      shift      ;;    *)      echo "{\"error\": \"Unknown argument: $1\"}"      exit 1      ;;  esacdoneif [[ -z "$URL_HOST" ]]; then  if [[ "$BIND_HOST" == "127.0.0.1" || "$BIND_HOST" == "localhost" ]]; then    URL_HOST="localhost"  else    URL_HOST="$BIND_HOST"  fi fi# Some environments reap detached/background processes. Auto-foreground when detected.if [[ -n "${CODEX_CI:-}" && "$FOREGROUND" != "true" && "$FORCE_BACKGROUND" != "true" ]]; then  FOREGROUND="true"fi# Windows/Git Bash reaps nohup background processes. Auto-foreground when detected.if [[ "$FOREGROUND" != "true" && "$FORCE_BACKGROUND...
```

</details>

---

#### Finding 485: 🔴 **CRITICAL** — `SAFE-T1502`

**Location:** Lines 1-148

**Description:**

The script creates a session directory at either a user-specified PROJECT_DIR or /tmp. While the script itself does not read sensitive files, the server it launches (node server.cjs) might, if it exposes file system access to an AI agent without proper sandboxing. The current script does not directly implement the SAFE-T1502 technique.: File-Based Credential Harvest occurs when MCP server tools read sensitive credential files from the host filesystem. Targets include SSH private keys, AWS credentials, environment files, browser cookies, keychain databases, and cloud provider configurations. The harvested credentials enable lateral movement and privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:86-90 if [[ -n "$PROJECT_DIR" ]]; then
  SESSION_DIR="${PROJECT_DIR}/.superpowers/brainstorm/${SESSION_ID}"
else
  SESSION_DIR="/tmp/brainstorm-${SESSION_ID}"
fi
```

</details>

---

#### Finding 486: 🔴 **CRITICAL** — `SAFE-T1505`

**Location:** Line 110

**Description:**

The script passes configuration parameters as environment variables to a Node.js server. While the current parameters (directory, host, URL host, owner PID) are not sensitive, if future versions were to pass actual secrets (e.g., API keys, database credentials) via environment variables, they could be susceptible to in-memory extraction techniques (SAFE-T1505).: In-Memory Secret Extraction targets secrets held in process memory at runtime. Attackers use memory dump tools, /proc/*/mem access, heap inspection, or debugger attachment to extract encryption keys, tokens, passwords, and other sensitive data that exists only in memory and would not be found in files or environment variables.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:110-110 env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs
```

</details>

---

#### Finding 487: 🔴 **CRITICAL** — `SAFE-T1702`

**Location:** Line 130

**Description:**

Shell command injection through unsanitized user input in the error message generation. The `PROJECT_DIR`, `BIND_HOST`, and `URL_HOST` variables are directly taken from command-line arguments and then expanded within a double-quoted string in an `echo` command. This allows an attacker to inject arbitrary shell commands via command substitution (e.g., `$(command)`) which will be executed by the script.: Command Injection vulnerabilities occur when MCP server tools execute shell commands with unsanitized user input. Attackers can inject malicious commands that are executed with the server's privileges, potentially leading to arbitrary code execution, data exfiltration, or system compromise.

This technique targets common command execution patterns across multiple languages including Node.js (child_process), Python (subprocess, os.system), Rust (Command), and Go (os/exec). Unsafe practices include string concatenation in shell commands, missing input validation, and using shell=True or shell invocation modes.

Command injection is particularly dangerous in MCP servers because tools often have elevated privileges to interact with system resources, files, or external services. A successful injection can bypass all security boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:130-130 echo "{\"error\": \"Server started but was killed. Retry in a persistent terminal with: $SCRIPT_DIR/start-server.sh${PROJECT_DIR:+ --project-dir $PROJECT_DIR} --host $BIND_HOST --url-host $URL_HOST --foreground\"}"
```

</details>

---

#### Finding 488: 🔴 **CRITICAL** — `SAFE-T1704`

**Location:** Lines 1-148

**Description:**

The `start-server.sh` script accepts a user-controlled `--url-host` argument, which is then passed as the `BRAINSTORM_URL_HOST` environment variable to the `node server.cjs` process. If `server.cjs` uses this variable to construct a URL for an outgoing server-side request without proper validation or sanitization, it could lead to Server-Side Request Forgery (SSRF).: Compromised-Server Pivot / SSRF occurs when MCP tools accept user-controlled URLs in HTTP requests without host allowlisting or validation. Attackers exploit this to access internal services, cloud metadata endpoints, localhost services, or private network resources that the MCP server can reach but the attacker cannot directly access.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:29-31     --url-host)
      URL_HOST="$2"
      shift 2
...
skills/brainstorming/scripts/start-server.sh:100   env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs
...
skills/brainstorming/scripts/start-server.sh:107 nohup env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs > "$LOG_FILE" 2>&1 &
```

</details>

---

#### Finding 489: 🔴 **CRITICAL** — `SAFE-T1803`

**Location:** Line 115

**Description:**

The script itself does not directly perform bulk database exports. It acts as a launcher for a Node.js server (`server.cjs`). The potential for SAFE-T1803 would reside within the `server.cjs` application or any tools it exposes, which are not provided in this context.: Database Dump extracts entire database contents through MCP tools using bulk export commands like mysqldump, pg_dump, SELECT *, or ORM findAll without pagination. This enables mass exfiltration of structured data including user records, credentials, financial data, and other sensitive information stored in databases.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:115-115 node server.cjs
```

</details>

---

#### Finding 490: 🔴 **CRITICAL** — `SAFE-T2103`

**Location:** Lines 1-148

**Description:**

The script constructs a session directory path using user-provided input for '--project-dir'. While `mkdir -p` is generally robust against command injection, a malicious or unvalidated `PROJECT_DIR` could lead to directory traversal or file system pollution if the script is run with elevated privileges or in a sensitive location.: Code Sabotage via Malicious PR uses MCP tools with repository access to create automated pull requests containing malicious code changes, backdoors, or CI/CD pipeline modifications. Attackers manipulate build scripts, inject vulnerabilities, or alter deployment configurations through automated commits that bypass normal code review.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:20-22     --project-dir)
      PROJECT_DIR="$2"
      shift 2
```

</details>

---

#### Finding 491: 🔴 **CRITICAL** — `SAFE-T2106`

**Location:** Lines 1-148

**Description:**

The script allows specifying a persistent project directory via `--project-dir`. If the `server.cjs` (not provided) stores a vector database within this directory, and if that database is vulnerable to poisoning, then the persistence mechanism provided by this script could allow poisoned embeddings to survive server restarts. However, this script itself does not implement or directly cause vector database poisoning.: Context Memory Poisoning via Vector Store Contamination manipulates vector databases storing long-term AI memory. Attackers inject poisoned embeddings that blend legitimate and malicious content, surviving server restarts and reinstallations. The poisoned content can self-replicate across sessions and affect multiple agents sharing the same vector store.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:20-22     --project-dir)
      PROJECT_DIR="$2"
      shift 2
```

</details>

---

#### Finding 492: 🔴 **CRITICAL** — `SAFE-T2107`

**Location:** Lines 1-148

**Description:**

The `start-server.sh` script allows an unvalidated `PROJECT_DIR` argument, which determines the base path for the MCP tool's session data, including a `content` directory. This lack of validation enables an attacker to specify a `PROJECT_DIR` that could be part of an AI model's training data ingestion pipeline.: AI Model Poisoning manipulates machine learning models used by MCP servers through training data poisoning, adversarial example injection, or embedding backdoor triggers during fine-tuning. Poisoned models produce attacker-desired outputs when triggered by specific inputs while behaving normally otherwise.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:25-27 PROJECT_DIR="$2"
skills/brainstorming/scripts/start-server.sh:79-83 if [[ -n "$PROJECT_DIR" ]]; then SESSION_DIR="${PROJECT_DIR}/.superpowers/brainstorm/${SESSION_ID}" else SESSION_DIR="/tmp/brainstorm-${SESSION_ID}" fi
skills/brainstorming/scripts/start-server.sh:90 mkdir -p "${SESSION_DIR}/content" "$STATE_DIR"
```

</details>

---

#### Finding 493: 🔴 **CRITICAL** — `SAFE-T3001`

**Location:** Line 90

**Description:**

The script creates a 'content' directory within a session-specific directory, which is then passed to the 'node server.cjs' process via the BRAINSTORM_DIR environment variable. If 'server.cjs' implements a Retrieval-Augmented Generation (RAG) system, this 'content' directory would likely serve as its knowledge base. While this script itself does not perform document ingestion or directly facilitate the injection of malicious documents, it establishes the storage location for potential RAG knowledge base content. The unique session ID generation for each session directory (`SESSION_ID="$$-$(date +%s)"`) makes it difficult for an attacker to pre-poison a specific session's content directory. The actual vulnerability for SAFE-T3001 (RAG Backdoor Attack) would reside in the 'node server.cjs' application's document ingestion and processing logic, or in how the 'content' directory is populated, rather than in this startup script.: RAG Backdoor Attack injects malicious documents into Retrieval-Augmented Generation knowledge bases used by MCP servers. Poisoned documents contain hidden instructions, false information, or trigger phrases that activate when retrieved, causing the LLM to produce attacker-controlled outputs, leak data, or execute unauthorized actions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:90-90 mkdir -p "${SESSION_DIR}/content" "$STATE_DIR"
```

</details>

---

#### Finding 494: 🟠 **HIGH** — `SAFE-T1002`

**Location:** Line 106

**Description:**

The script executes a local 'server.cjs' file. While the script itself does not fetch remote code, the 'server.cjs' or its dependencies might. This script's role is to start a pre-existing local component.: Supply Chain Compromise occurs when MCP servers download, install, import, or execute external packages, plugins, model weights, or binaries without integrity checks, version pinning, or signature verification. This allows attackers to inject malicious code through compromised dependencies.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:106-106 env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs
```

</details>

---

#### Finding 495: 🟠 **HIGH** — `SAFE-T1002`

**Location:** Line 112

**Description:**

The script executes a local 'server.cjs' file in the background. While the script itself does not fetch remote code, the 'server.cjs' or its dependencies might. This script's role is to start a pre-existing local component.: Supply Chain Compromise occurs when MCP servers download, install, import, or execute external packages, plugins, model weights, or binaries without integrity checks, version pinning, or signature verification. This allows attackers to inject malicious code through compromised dependencies.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:112-112 nohup env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs > "$LOG_FILE" 2>&1 &
```

</details>

---

#### Finding 496: 🟠 **HIGH** — `SAFE-T1004`

**Location:** Line 30

**Description:**

The `start-server.sh` script allows an arbitrary hostname to be specified via the `--url-host` argument, which is then used to advertise the server's URL. This parameter lacks validation, enabling a malicious actor to configure the MCP server to impersonate a legitimate service by advertising a trusted hostname.: Server Impersonation occurs when an attacker registers an MCP server with a name designed to be confused with a legitimate service (typosquatting, homograph attacks, or DNS cache poisoning). Users connecting to the impersonated server unknowingly send credentials and data to the attacker.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:30-30 URL_HOST="$2"
```

</details>

---

#### Finding 497: 🟠 **HIGH** — `SAFE-T1008`

**Location:** Lines 1-148

**Description:**

The provided file is a shell script for starting a server, not an MCP tool definition file. Therefore, it does not contain tool descriptions that could be exploited by the SAFE-T1008 technique.: Tool Shadowing Attack embeds instructions in tool descriptions that manipulate how AI agents use OTHER tools. In multi-server MCP setups, all tool descriptions are aggregated into a single prompt, allowing any tool to influence the behavior of others through cross-tool interference patterns.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 #!/usr/bin/env bash...
```

</details>

---

#### Finding 498: 🟠 **HIGH** — `SAFE-T1009`

**Location:** Lines 1-148

**Description:**

The script `start-server.sh` is a server startup script and does not implement OAuth client functionality. It does not initiate OAuth flows, follow redirects to Authorization Servers, or handle authorization codes/tokens. Therefore, it is not directly vulnerable to the SAFE-T1009 Authorization Server Mix-up technique, which targets OAuth clients that may follow redirects to look-alike AS domains.: Authorization Server Mix-up occurs when an OAuth client follows a redirect to a look-alike authorization server domain (e.g., accounts-google.com vs accounts.google.com), causing authorization codes or tokens to be leaked to an attacker-controlled server. This exploits insufficient issuer validation in OAuth implementations.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:1-148 #!/usr/bin/env bash
# Start the brainstorm server and output connection info
# Usage: start-server.sh [--project-dir <path>] [--host <bind-host>] [--url-host <display-host>] [--foreground] [--background]
#
# Starts server on a random high port, outputs JSON with URL.
# Each session gets its own directory to avoid conflicts.
#
# Options:
#   --project-dir <path>  Store session files under <path>/.superpowers/brainstorm/
#                         instead of /tmp. Files persist after server stops.
#   --host <bind-host>    Host/interface to bind (default: 127.0.0.1).
#                         Use 0.0.0.0 in remote/containerized environments.
#   --url-host <host>     Hostname shown in returned URL JSON.
#   --foreground          Run server in the current terminal (no backgrounding).
#   --background          Force background mode (overrides Codex auto-foreground).

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

# Parse arguments
PROJECT_DIR=""
FOREGROUND="false"
FORCE_BACKGROUND="false"
BIND_HOST="127.0.0.1"
URL_HOST=""
while [[ $# -gt 0 ]]; do
  case "$1" in
    --project-dir)
      PROJECT_DIR="$2"
      shift 2
      ;;
    --host)
      BIND_HOST="$2"
      shift 2
      ;;
    --url-host)
      URL_HOST="$2"
      shift 2
      ;;
    --foreground|--no-daemon)
      FOREGROUND="true"
      shift
      ;;
    --background|--daemon)
      FORCE_BACKGROUND="true"
      shift
      ;;
    *)
      echo "{\"error\": \"Unknown argument: $1\"}"
      exit 1
      ;;
  esac
done

if [[ -z "$URL_HOST" ]]; then
  if [[ "$BIND_HOST" == "127.0.0.1" || "$BIND_HOST" == "localhost" ]]; then
    URL_HOST="localhost"
  else
    URL_HOST="$BIND_HOST"
  fi
fi

# Some environments reap detached/background processes. Auto-foreground when detected.
if [[ -n "${CODEX_CI:-}" && "$FOREGROUND" != "true" && "$FORCE_BACKGROUND" != "true" ]]; then
  FOREGROUND="true"
fi

# Windows/Git Bash r...
```

</details>

---

#### Finding 499: 🟠 **HIGH** — `SAFE-T1104`

**Location:** Lines 1-148

**Description:**

The script allows the 'PROJECT_DIR' to be specified by the user, which determines the base directory for session files. If the script is executed by a highly privileged user (e.g., root) and a sensitive 'PROJECT_DIR' (e.g., '/') is provided, the 'node server.cjs' process could potentially operate with excessive write permissions in critical system areas. While the script itself does not elevate privileges, it does not restrict the 'PROJECT_DIR' to safe, non-system locations, which could lead to an over-privileged server process if misconfigured by a privileged user.: Over-Privileged Tool Abuse occurs when MCP tools are granted permissions far exceeding their functional requirements. Attackers exploit these over-privileged tools through prompt injection or direct manipulation to access sensitive resources, execute system commands, or escalate privileges beyond the intended scope.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:20-22     --project-dir)
      PROJECT_DIR="$2"
      shift 2
```

</details>

---

#### Finding 500: 🟠 **HIGH** — `SAFE-T1105`

**Location:** Line 69

**Description:**

The `--project-dir` argument is used to construct a filesystem path without proper sanitization or normalization, allowing for path traversal. An attacker can specify `../` sequences to create directories outside the intended base directory.: Path Traversal via File Tool occurs when MCP file-handling tools accept untrusted input as filesystem paths without proper normalization, allowlisting, or base directory enforcement. Attackers can use relative path sequences, Unicode normalization tricks, double encoding, or null byte injection to access files outside the intended directory.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:69-69 SESSION_DIR="${PROJECT_DIR}/.superpowers/brainstorm/${SESSION_ID}"
```

</details>

---

#### Finding 501: 🟠 **HIGH** — `SAFE-T1201`

**Location:** Lines 1-148

**Description:**

The `start-server.sh` script is a static launcher for the `node server.cjs` application. While the `node server.cjs` application itself might implement dynamic definitions susceptible to SAFE-T1201 (MCP Rug Pull Attack), this shell script does not contain any mechanisms for dynamic definition changes, remote updates, or time-delayed malicious functionality within its own logic. Its behavior is determined by its static content and command-line arguments at the time of execution. Therefore, this script itself does not directly contribute to the SAFE-T1201 vulnerability.: MCP Rug Pull Attack is a persistence technique where tools appear legitimate initially but can change their behavior after gaining user trust. This exploits dynamic tool definitions, remote configuration loading, or time-delayed activation to modify tool behavior post-approval.
Static analysis detects code patterns that ENABLE rug pulls: dynamic descriptions, remote tool loading without version pinning, and time-based conditional behavior.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 #!/usr/bin/env bash# Start the brainstorm server and output connection info# Usage: start-server.sh [--project-dir <path>] [--host <bind-host>] [--url-host <display-host>] [--foreground] [--background]#...nohup env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs > "$LOG_FILE" 2>&1 &
```

</details>

---

#### Finding 502: 🟠 **HIGH** — `SAFE-T1202`

**Location:** Lines 1-148

**Description:**

The script sets up a persistent project directory (`--project-dir`) which could potentially be used by the `server.cjs` to store sensitive data, including OAuth tokens. While the script itself does not handle OAuth tokens, the persistence mechanism it provides could contribute to insecure storage if the downstream application (`server.cjs`) stores tokens in this directory without proper protections.: OAuth Token Persistence exploits insecure token storage and handling to maintain long-lived access to MCP-connected services. Attackers steal access or refresh tokens from localStorage, sessionStorage, cookies without secure flags, or file-based caches, then reuse them to persist access even after password changes or session invalidation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:20-21       --project-dir)
      PROJECT_DIR="$2"
```

</details>

---

#### Finding 503: 🟠 **HIGH** — `SAFE-T1202`

**Location:** Lines 1-148

**Description:**

The script creates a session directory, which can be persistent if `--project-dir` is used. If the `server.cjs` application stores OAuth tokens or other sensitive credentials within this directory, and the directory is not adequately protected (e.g., permissions, encryption), it could lead to insecure storage and persistence of tokens.: OAuth Token Persistence exploits insecure token storage and handling to maintain long-lived access to MCP-connected services. Attackers steal access or refresh tokens from localStorage, sessionStorage, cookies without secure flags, or file-based caches, then reuse them to persist access even after password changes or session invalidation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:80-84 if [[ -n "$PROJECT_DIR" ]]; then
  SESSION_DIR="${PROJECT_DIR}/.superpowers/brainstorm/${SESSION_ID}"
else
  SESSION_DIR="/tmp/brainstorm-${SESSION_ID}"
fi
```

</details>

---

#### Finding 504: 🟠 **HIGH** — `SAFE-T1301`

**Location:** Lines 1-148

**Description:**

The script `start-server.sh` is responsible for launching an MCP server (`node server.cjs`). While the server it starts might be susceptible to SAFE-T1301 (Cross-Server Tool Shadowing) if its `server.cjs` implementation registers tools without proper namespace management or if it can be manipulated to register conflicting tool names, this specific shell script itself does not contain the logic for tool registration, naming, or resolution. Its role is purely to set up the environment and start the Node.js server process.: Cross-Server Tool Shadowing is a privilege escalation technique where malicious MCP servers override or intercept legitimate tool calls from other servers. This attack exploits multi-server MCP environments where multiple servers can provide tools with the same or similar names, allowing attackers to shadow legitimate functionality with malicious implementations.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:1-148 #!/usr/bin/env bash
# Start the brainstorm server and output connection info
# Usage: start-server.sh [--project-dir <path>] [--host <bind-host>] [--url-host <display-host>] [--foreground] [--background]
#
# Starts server on a random high port, outputs JSON with URL.
# Each session gets its own directory to avoid conflicts.
#
# Options:
#   --project-dir <path>  Store session files under <path>/.superpowers/brainstorm/
#                         instead of /tmp. Files persist after server stops.
#   --host <bind-host>    Host/interface to bind (default: 127.0.0.1).
#                         Use 0.0.0.0 in remote/containerized environments.
#   --url-host <host>     Hostname shown in returned URL JSON.
#   --foreground          Run server in the current terminal (no backgrounding).
#   --background          Force background mode (overrides Codex auto-foreground).

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

# Parse arguments
PROJECT_DIR=""
FOREGROUND="false"
FORCE_BACKGROUND="false"
BIND_HOST="127.0.0.1"
URL_HOST=""
while [[ $# -gt 0 ]]; do
  case "$1" in
    --project-dir)
      PROJECT_DIR="$2"
      shift 2
      ;;
    --host)
      BIND_HOST="$2"
      shift 2
      ;;
    --url-host)
      URL_HOST="$2"
      shift 2
      ;;
    --foreground|--no-daemon)
      FOREGROUND="true"
      shift
      ;;
    --background|--daemon)
      FORCE_BACKGROUND="true"
      shift
      ;;
    *)
      echo "{\"error\": \"Unknown argument: $1\"}"
      exit 1
      ;;
  esac
done

if [[ -z "$URL_HOST" ]]; then
  if [[ "$BIND_HOST" == "127.0.0.1" || "$BIND_HOST" == "localhost" ]]; then
    URL_HOST="localhost"
  else
    URL_HOST="$BIND_HOST"
  fi
fi

# Some environments reap detached/background processes. Auto-foreground when detected.
if [[ -n "${CODEX_CI:-}" && "$FOREGROUND" != "true" && "$FORCE_BACKGROUND" != "true" ]]; then
  FOREGROUND="true"
fi

# Windows/Git Bash r...
```

</details>

---

#### Finding 505: 🟠 **HIGH** — `SAFE-T1302`

**Location:** Lines 1-148

**Description:**

The `start-server.sh` script allows a user to specify a `--project-dir` argument, which is then used to construct paths for creating session directories and writing log/PID files. If this script is executed with elevated privileges (e.g., as root), an attacker could specify a sensitive system directory (e.g., `/etc`, `/var`) for `PROJECT_DIR`. This would allow the script to create arbitrary directories and write files (server logs, PID files) into these sensitive locations, leading to unauthorized modification of the filesystem.: High-Privilege Tool Abuse occurs when MCP tools are configured with administrative, root, or superuser access to databases, APIs, or system resources. Attackers exploit these tools through prompt injection or direct manipulation to perform privileged operations such as modifying access controls, reading sensitive data, or escalating their own permissions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:24-27 PROJECT_DIR="$2"
skills/brainstorming/scripts/start-server.sh:69-73 if [[ -n "$PROJECT_DIR" ]]; then SESSION_DIR="${PROJECT_DIR}/.superpowers/brainstorm/${SESSION_ID}" else SESSION_DIR="/tmp/brainstorm-${SESSION_ID}" fi
skills/brainstorming/scripts/start-server.sh:80 mkdir -p "${SESSION_DIR}/content" "$STATE_DIR"
skills/brainstorming/scripts/start-server.sh:105 echo "$$" > "$PID_FILE"
skills/brainstorming/scripts/start-server.sh:113 nohup env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs > "$LOG_FILE" 2>&1 &
```

</details>

---

#### Finding 506: 🟠 **HIGH** — `SAFE-T1304`

**Location:** Lines 1-148

**Description:**

The script passes `BRAINSTORM_OWNER_PID` as an environment variable to the `node server.cjs` process. While a Process ID (PID) is not typically considered an authentication token or credential in the context of SAFE-T1304 (which focuses on OAuth, JWT, API keys, etc.), its use as an identifier across processes could, in a highly specific and misconfigured scenario, potentially be misused if the `node server.cjs` process or other services implicitly trust this PID for authorization without further validation. However, this script itself does not perform any token reuse or forwarding, nor does it handle traditional authentication tokens.: Credential Relay Chain exploits token reuse across MCP service boundaries. Attackers capture tokens from one service interaction and replay them in tool-to-tool calls, cross-service requests, or downstream API invocations without proper audience validation, scope restriction, or token binding.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 109:OWNER_PID="$(ps -o ppid= -p "$PPID" 2>/dev/null | tr -d ' ')"
110:if [[ -z "$OWNER_PID" || "$OWNER_PID" == "1" ]]; then
111:  OWNER_PID="$PPID"
112:fi
...
115:  env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs
...
120:nohup env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs > "$LOG_FILE" 2>&1 &
```

</details>

---

#### Finding 507: 🟠 **HIGH** — `SAFE-T1404`

**Location:** Lines 1-148

**Description:**

The `start-server.sh` script filters the output of the `node server.cjs` process, only displaying the line containing 'server-started'. This could hide other critical or malicious output from the server process.: Response Tampering intercepts and modifies MCP tool responses before they are displayed to the user or processed by the LLM. Attackers alter response content to hide evidence of malicious actions, inject false information, bypass content filters, or manipulate the LLM context for subsequent interactions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:119-120 nohup env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs > "$LOG_FILE" 2>&1 &
skills/brainstorming/scripts/start-server.sh:128 grep "server-started" "$LOG_FILE" | head -1
```

</details>

---

#### Finding 508: 🟠 **HIGH** — `SAFE-T1408`

**Location:** Lines 1-148

**Description:**

The provided script is a server startup script and does not contain any OAuth-related logic or parameters that could lead to an OAuth protocol downgrade. It manages process execution, logging, and directory setup, but does not interact with OAuth flows.: An attacker forces or coerces a client to use the OAuth 2.0 implicit flow instead of the authorization code flow with PKCE. Tokens delivered in URL fragments are easier to capture via logs, referrers, or malicious scripts, enabling token theft and session hijacking.
This technique detects the presence of implicit flow configuration, which indicates potential vulnerability. The authorization code flow with PKCE should always be preferred over implicit flow for security.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 #!/usr/bin/env bash# Start the brainstorm server and output connection info# Usage: start-server.sh [--project-dir <path>] [--host <bind-host>] [--url-host <display-host>] [--foreground] [--background]#...node server.cjs
```

</details>

---

#### Finding 509: 🟠 **HIGH** — `SAFE-T1504`

**Location:** Lines 129-130

**Description:**

The script starts a Node.js server (`server.cjs`) and relays a specific line from its log file (`server.log`) to standard output. If the `server.cjs` application were to print sensitive tokens (e.g., access tokens, API keys) as part of its 'server-started' message, this script would inadvertently leak them. However, based solely on the provided `start-server.sh` script, there is no direct evidence that it handles or outputs any tokens itself. The environment variables passed to `server.cjs` are configuration parameters, not credentials.: Token Theft via API Response occurs when MCP tool implementations inadvertently include access tokens, refresh tokens, or other credentials in their output responses. These leaked tokens are then visible in conversation history, logs, or can be captured by other tools in the chain for unauthorized access.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:129-130 grep "server-started" "$LOG_FILE" | head -1
```

</details>

---

#### Finding 510: 🟠 **HIGH** — `SAFE-T1606`

**Location:** Lines 1-148

**Description:**

The script creates session directories based on user-provided input (`--project-dir`). While this script itself does not implement directory listing, the `node server.cjs` it launches might. If the `node server.cjs` exposes a directory listing tool that operates on `BRAINSTORM_DIR` (which is derived from `SESSION_DIR`), and `SESSION_DIR` is influenced by an attacker-controlled `PROJECT_DIR`, it could potentially lead to directory listing within a path chosen by the attacker. However, this script itself only creates directories and does not expose a directory listing capability.: Directory Listing via File Tool exploits MCP file tools that allow unrestricted directory enumeration. Attackers use fs.readdir, os.listdir, or equivalent functions to map the filesystem structure, discover sensitive files, locate configuration files, and identify credential storage locations for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:69-73 if [[ -n "$PROJECT_DIR" ]]; then
  SESSION_DIR="${PROJECT_DIR}/.superpowers/brainstorm/${SESSION_ID}"
else
  SESSION_DIR="/tmp/brainstorm-${SESSION_ID}"
fi
```

</details>

---

#### Finding 511: 🟠 **HIGH** — `SAFE-T1701`

**Location:** Lines 1-148

**Description:**

The `start-server.sh` script allows the `SESSION_DIR` to be configured via the `--project-dir` argument, which can lead to session-specific files (including `server.log` and `content` directory) being stored in a shared location accessible by other MCP tools.: Cross-Tool Contamination exploits shared state between MCP tools including global variables, shared memory, common databases, or file-based caches. Malicious data written by one tool pollutes the execution context of another, enabling data exfiltration, privilege escalation, or behavior manipulation across tool boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:20-22 PROJECT_DIR="$2"
skills/brainstorming/scripts/start-server.sh:76-78 if [[ -n "$PROJECT_DIR" ]]; then SESSION_DIR="${PROJECT_DIR}/.superpowers/brainstorm/${SESSION_ID}" else SESSION_DIR="/tmp/brainstorm-${SESSION_ID}" fi
skills/brainstorming/scripts/start-server.sh:81-82 PID_FILE="${STATE_DIR}/server.pid" LOG_FILE="${STATE_DIR}/server.log"
skills/brainstorming/scripts/start-server.sh:85 mkdir -p "${SESSION_DIR}/content" "$STATE_DIR"
skills/brainstorming/scripts/start-server.sh:110 nohup env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs > "$LOG_FILE" 2>&1 &
```

</details>

---

#### Finding 512: 🟠 **HIGH** — `SAFE-T1706`

**Location:** Lines 1-148

**Description:**

The provided script is a server startup script and does not directly handle OAuth tokens or implement authentication/authorization logic. The vulnerability SAFE-T1706 (OAuth token replay without validation) would reside within the `node server.cjs` application that this script launches, or any other service consuming OAuth tokens, not in the startup script itself.: OAuth Token Pivot Replay reuses OAuth tokens obtained from one service to access a different service or audience. Attackers exploit missing audience (aud) validation, overly broad token scopes, or lack of token binding to pivot from a lower-value service to a higher-value target using the same token.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:1-148 #!/usr/bin/env bash
# Start the brainstorm server and output connection info
# Usage: start-server.sh [--project-dir <path>] [--host <bind-host>] [--url-host <display-host>] [--foreground] [--background]
#
# Starts server on a random high port, outputs JSON with URL.
# Each session gets its own directory to avoid conflicts.
#
# Options:
#   --project-dir <path>  Store session files under <path>/.superpowers/brainstorm/
#                         instead of /tmp. Files persist after server stops.
#   --host <bind-host>    Host/interface to bind (default: 127.0.0.1).
#                         Use 0.0.0.0 in remote/containerized environments.
#   --url-host <host>     Hostname shown in returned URL JSON.
#   --foreground          Run server in the current terminal (no backgrounding).
#   --background          Force background mode (overrides Codex auto-foreground).

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

# Parse arguments
PROJECT_DIR=""
FOREGROUND="false"
FORCE_BACKGROUND="false"
BIND_HOST="127.0.0.1"
URL_HOST=""
while [[ $# -gt 0 ]]; do
  case "$1" in
    --project-dir)
      PROJECT_DIR="$2"
      shift 2
      ;;
    --host)
      BIND_HOST="$2"
      shift 2
      ;;
    --url-host)
      URL_HOST="$2"
      shift 2
      ;;
    --foreground|--no-daemon)
      FOREGROUND="true"
      shift
      ;;
    --background|--daemon)
      FORCE_BACKGROUND="true"
      shift
      ;;
    *)
      echo "{\"error\": \"Unknown argument: $1\"}"
      exit 1
      ;;
  esac
done

if [[ -z "$URL_HOST" ]]; then
  if [[ "$BIND_HOST" == "127.0.0.1" || "$BIND_HOST" == "localhost" ]]; then
    URL_HOST="localhost"
  else
    URL_HOST="$BIND_HOST"
  fi
fi

# Some environments reap detached/background processes. Auto-foreground when detected.
if [[ -n "${CODEX_CI:-}" && "$FOREGROUND" != "true" && "$FORCE_BACKGROUND" != "true" ]]; then
  FOREGROUND="true"
fi

# Windows/Git Bash r...
```

</details>

---

#### Finding 513: 🟠 **HIGH** — `SAFE-T1911`

**Location:** Lines 49-50

**Description:**

The script outputs simple JSON for status and error messages, but does not handle or generate complex JSON parameters that could be abused for exfiltration as described in SAFE-T1911.: Attackers hide sensitive data inside optional or unused parameters (e.g., metadata, context, tags) that are accepted and forwarded by the server but not actively validated. These values can be logged or propagated to upstream services, enabling stealthy exfiltration without triggering obvious alarms.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:49-50 echo "{\"error\": \"Unknown argument: $1\"}"       exit 1
```

</details>

---

#### Finding 514: 🟠 **HIGH** — `SAFE-T1911`

**Location:** Line 110

**Description:**

The script sets environment variables for the `node server.cjs` process, but these are not JSON parameters and are not directly exposed for embedding secrets in a way that aligns with the SAFE-T1911 technique.: Attackers hide sensitive data inside optional or unused parameters (e.g., metadata, context, tags) that are accepted and forwarded by the server but not actively validated. These values can be logged or propagated to upstream services, enabling stealthy exfiltration without triggering obvious alarms.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:110-110 env BRAINSTORM_DIR="$SESSION_DIR" BRAINSTORM_HOST="$BIND_HOST" BRAINSTORM_URL_HOST="$URL_HOST" BRAINSTORM_OWNER_PID="$OWNER_PID" node server.cjs
```

</details>

---

#### Finding 515: 🟠 **HIGH** — `SAFE-T1912`

**Location:** Line 130

**Description:**

The script relays the raw 'server-started' message from the `node server.cjs` process's log file directly to stdout without sanitization. If `server.cjs` is an MCP server and its 'server-started' message is a structured response (e.g., JSON) that can be influenced by an attacker, it could contain hidden data for covert exfiltration.: Stego Response Exfiltration hides sensitive data within seemingly benign tool responses using steganographic techniques. Data is embedded in image pixels (LSB encoding), whitespace patterns, Unicode zero-width characters, or structured response fields that appear normal to users but contain extractable hidden information.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:130-130 grep "server-started" "$LOG_FILE" | head -1
```

</details>

---

#### Finding 516: 🟠 **HIGH** — `SAFE-T2102`

**Location:** Lines 1-148

**Description:**

The script contains a bounded loop for checking server status, which is not an infinite loop.: Service Disruption via API Flooding uses MCP tools to generate excessive outbound API calls through infinite loops, missing rate limits, or unbounded concurrent requests. This causes denial of service on target APIs, excessive billing on cloud services, and potential cascading failures across dependent systems.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:1-148 skills/brainstorming/scripts/start-server.sh:119-139 for i in {1..50}; do
  if grep -q "server-started" "$LOG_FILE" 2>/dev/null; then
    # Verify server is still alive after a short window (catches process reapers)
    alive="true"
    for _ in {1..20}; do
      if ! kill -0 "$SERVER_PID" 2>/dev/null; then
        alive="false"
        break
      fi
      sleep 0.1
    done
    if [[ "$alive" != "true" ]]; then
      echo "{\"error\": \"Server started but was killed. Retry in a persistent terminal with: $SCRIPT_DIR/start-server.sh${PROJECT_DIR:+ --project-dir $PROJECT_DIR} --host $BIND_HOST --url-host $URL_HOST --foreground\"}"
      exit 1
    fi
    grep "server-started" "$LOG_FILE" | head -1
    exit 0
  fi
  sleep 0.1
done
```

</details>

---

### 📁 `skills/systematic-debugging/find-polluter.sh`

#### Finding 517: 🔴 **CRITICAL** — `SAFE-T1003`

**Location:** Lines 1-63

**Description:**

The provided script `find-polluter.sh` is a debugging utility designed to identify tests that create unwanted files or state. It does not implement or distribute an MCP server, nor does it contain any code that suggests it is a trojanized package itself. The script's purpose is to detect 'pollution', which is the opposite of creating malicious artifacts.: Malicious MCP-Server Distribution involves distributing trojanized MCP server implementations through package registries (npm, PyPI, Docker Hub) or code repositories. These servers appear legitimate but contain hidden malicious functionality such as data exfiltration, backdoors, or credential theft.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 518: 🔴 **CRITICAL** — `SAFE-T1007`

**Location:** Lines 1-63

**Description:**

The provided script 'find-polluter.sh' is a local shell script designed for debugging test environments by identifying which test creates unwanted files or state. It does not involve any network communication, OAuth flows, server configurations, or user authentication mechanisms relevant to the SAFE-T1007 OAuth Authorization Phishing technique.: OAuth Authorization Phishing exploits the OAuth authorization flow to trick users into granting permissions to attacker-controlled applications. The attacker creates a convincing authorization page or redirects users through legitimate OAuth providers but captures the authorization code or tokens.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash
# Bisection script to find which test creates unwanted files/state
# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern>
# Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'

set -e

if [ $# -ne 2 ]; then
  echo "Usage: $0 <file_to_check> <test_pattern>"
  echo "Example: $0 '.git' 'src/**/*.test.ts'"
  exit 1
fi

POLLUTION_CHECK="$1"
TEST_PATTERN="$2"

echo "🔍 Searching for test that creates: $POLLUTION_CHECK"
echo "Test pattern: $TEST_PATTERN"
echo ""

# Get list of test files
TEST_FILES=$(find . -path "$TEST_PATTERN" | sort)
TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')

echo "Found $TOTAL test files"
echo ""

COUNT=0
for TEST_FILE in $TEST_FILES; do
  COUNT=$((COUNT + 1))

  # Skip if pollution already exists
  if [ -e "$POLLUTION_CHECK" ]; then
    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"
    echo "   Skipping: $TEST_FILE"
    continue
  fi

  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"

  # Run the test
  npm test "$TEST_FILE" > /dev/null 2>&1 || true

  # Check if pollution appeared
  if [ -e "$POLLUTION_CHECK" ]; then
    echo ""
    echo "🎯 FOUND POLLUTER!"
    echo "   Test: $TEST_FILE"
    echo "   Created: $POLLUTION_CHECK"
    echo ""
    echo "Pollution details:"
    ls -la "$POLLUTION_CHECK"
    echo ""
    echo "To investigate:"
    echo "  npm test $TEST_FILE    # Run just this test"
    echo "  cat $TEST_FILE         # Review test code"
    exit 1
  fi
done

echo ""
echo "✅ No polluter found - all tests clean!"
exit 0
```

</details>

---

#### Finding 519: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Line 20

**Description:**

The script uses external input directly in shell commands, which could lead to command injection if the script were part of an LLM's tool execution. However, as a standalone shell script, this is a classic shell injection vulnerability, not an LLM prompt injection (SAFE-T1102).: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:20-20 POLLUTION_CHECK="$1"
```

</details>

---

#### Finding 520: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Line 21

**Description:**

The script uses external input directly in shell commands, which could lead to command injection if the script were part of an LLM's tool execution. However, as a standalone shell script, this is a classic shell injection vulnerability, not an LLM prompt injection (SAFE-T1102).: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:21-21 TEST_PATTERN="$2"
```

</details>

---

#### Finding 521: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Line 28

**Description:**

The script uses external input directly in shell commands, which could lead to command injection if the script were part of an LLM's tool execution. However, as a standalone shell script, this is a classic shell injection vulnerability, not an LLM prompt injection (SAFE-T1102).: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:28-28 TEST_FILES=$(find . -path "$TEST_PATTERN" | sort)
```

</details>

---

#### Finding 522: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Line 36

**Description:**

The script uses external input directly in shell commands, which could lead to command injection if the script were part of an LLM's tool execution. However, as a standalone shell script, this is a classic shell injection vulnerability, not an LLM prompt injection (SAFE-T1102).: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:36-36 if [ -e "$POLLUTION_CHECK" ];
```

</details>

---

#### Finding 523: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Line 43

**Description:**

The script uses external input directly in shell commands, which could lead to command injection if the script were part of an LLM's tool execution. However, as a standalone shell script, this is a classic shell injection vulnerability, not an LLM prompt injection (SAFE-T1102).: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:43-43 npm test "$TEST_FILE" > /dev/null 2>&1 || true
```

</details>

---

#### Finding 524: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Line 46

**Description:**

The script uses external input directly in shell commands, which could lead to command injection if the script were part of an LLM's tool execution. However, as a standalone shell script, this is a classic shell injection vulnerability, not an LLM prompt injection (SAFE-T1102).: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:46-46 if [ -e "$POLLUTION_CHECK" ];
```

</details>

---

#### Finding 525: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Line 52

**Description:**

The script uses external input directly in shell commands, which could lead to command injection if the script were part of an LLM's tool execution. However, as a standalone shell script, this is a classic shell injection vulnerability, not an LLM prompt injection (SAFE-T1102).: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:52-52 ls -la "$POLLUTION_CHECK"
```

</details>

---

#### Finding 526: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Line 55

**Description:**

The script uses external input directly in shell commands, which could lead to command injection if the script were part of an LLM's tool execution. However, as a standalone shell script, this is a classic shell injection vulnerability, not an LLM prompt injection (SAFE-T1102).: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:55-55 echo "  npm test $TEST_FILE    # Run just this test"
```

</details>

---

#### Finding 527: 🔴 **CRITICAL** — `SAFE-T1102`

**Location:** Line 56

**Description:**

The script uses external input directly in shell commands, which could lead to command injection if the script were part of an LLM's tool execution. However, as a standalone shell script, this is a classic shell injection vulnerability, not an LLM prompt injection (SAFE-T1102).: Indirect Prompt Injection occurs when external data (RAG documents, files, web content, database records) contains hidden instructions that manipulate LLM behavior when processed as context. Unlike direct injection, the attacker does not need direct access to the prompt - they poison data sources the application consumes.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:56-56 echo "  cat $TEST_FILE         # Review test code"
```

</details>

---

#### Finding 528: 🔴 **CRITICAL** — `SAFE-T1103`

**Location:** Line 34

**Description:**

The script executes `npm test` with a user-controlled test file path. While `npm test` is designed to run tests, if the `TEST_FILE` variable (derived from `TEST_PATTERN`) could be manipulated to point to a malicious script or contain shell metacharacters that `npm test` or its underlying test runner interprets as commands, it could lead to arbitrary code execution. This is a general command injection risk, not directly related to MCP function spoofing, but it involves executing an external 'tool' (`npm test`) with potentially untrusted input.: Fake Tool Invocation exploits insufficient tool validation in MCP hosts by injecting or spoofing tool names, duplicating tool registrations, or manipulating the JSON-RPC tool call protocol. Attackers can redirect legitimate tool calls to malicious implementations or register tools that shadow trusted ones.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:34-34 npm test "$TEST_FILE" > /dev/null 2>&1 || true
```

</details>

---

#### Finding 529: 🔴 **CRITICAL** — `SAFE-T1111`

**Location:** Lines 1-63

**Description:**

The script does not appear to weaponize AI CLI tools or exfiltrate data. It is a diagnostic script designed to find which test creates unwanted files or state.: AI Agent CLI Weaponization exploits AI-powered CLI tools and agents that have unrestricted shell access or use dangerous permission-bypass flags. Attackers distribute malicious packages with postinstall scripts that invoke AI CLI tools in unsafe modes, or craft inputs that cause agents to execute arbitrary system commands, harvest credentials, or exfiltrate repository data.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash
# Bisection script to find which test creates unwanted files/state
# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern>
# Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'

set -e

if [ $# -ne 2 ]; then
  echo "Usage: $0 <file_to_check> <test_pattern>"
  echo "Example: $0 '.git' 'src/**/*.test.ts'"
  exit 1
fi

POLLUTION_CHECK="$1"
TEST_PATTERN="$2"

echo "🔍 Searching for test that creates: $POLLUTION_CHECK"
echo "Test pattern: $TEST_PATTERN"
echo ""

# Get list of test files
TEST_FILES=$(find . -path "$TEST_PATTERN" | sort)
TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')

echo "Found $TOTAL test files"
echo ""

COUNT=0
for TEST_FILE in $TEST_FILES; do
  COUNT=$((COUNT + 1))

  # Skip if pollution already exists
  if [ -e "$POLLUTION_CHECK" ]; then
    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"
    echo "   Skipping: $TEST_FILE"
    continue
  fi

  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"

  # Run the test
  npm test "$TEST_FILE" > /dev/null 2>&1 || true

  # Check if pollution appeared
  if [ -e "$POLLUTION_CHECK" ]; then
    echo ""
    echo "🎯 FOUND POLLUTER!"
    echo "   Test: $TEST_FILE"
    echo "   Created: $POLLUTION_CHECK"
    echo ""
    echo "Pollution details:"
    ls -la "$POLLUTION_CHECK"
    echo ""
    echo "To investigate:"
    echo "  npm test $TEST_FILE    # Run just this test"
    echo "  cat $TEST_FILE         # Review test code"
    exit 1
  fi
done

echo ""
echo "✅ No polluter found - all tests clean!"
exit 0
```

</details>

---

#### Finding 530: 🔴 **CRITICAL** — `SAFE-T1303`

**Location:** Line 30

**Description:**

The script uses user-controlled input (`POLLUTION_CHECK`) in file system operations (`[ -e "$POLLUTION_CHECK" ]`, `ls -la "$POLLUTION_CHECK"`). While this allows for local path traversal on the system where the script is executed, it does not directly constitute a container sandbox escape as described by SAFE-T1303.: Container Sandbox Escape exploits misconfigured container environments running MCP servers to break out of the sandbox and access the host operating system. Attack vectors include mounting the Docker socket, accessing /proc filesystem, running in privileged mode, using nsenter, or exploiting kernel vulnerabilities from within the container.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:30-30 if [ -e "$POLLUTION_CHECK" ];
```

</details>

---

#### Finding 531: 🔴 **CRITICAL** — `SAFE-T1303`

**Location:** Line 37

**Description:**

The script executes `npm test "$TEST_FILE"` where `TEST_FILE` is derived from user-controlled `TEST_PATTERN`. If the executed test itself were to invoke a container runtime with a manipulated working directory, that would be a vulnerability in the test, not directly in this script's handling of the `SAFE-T1303` technique.: Container Sandbox Escape exploits misconfigured container environments running MCP servers to break out of the sandbox and access the host operating system. Attack vectors include mounting the Docker socket, accessing /proc filesystem, running in privileged mode, using nsenter, or exploiting kernel vulnerabilities from within the container.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:37-37 npm test "$TEST_FILE" > /dev/null 2>&1 || true
```

</details>

---

#### Finding 532: 🔴 **CRITICAL** — `SAFE-T1306`

**Location:** Lines 1-63

**Description:**

The provided shell script is a test utility for finding file pollution and does not contain any code related to authentication, authorization, token minting, or validation. Therefore, it is not susceptible to the SAFE-T1306 technique.: Rogue Authorization Server involves setting up a malicious or misconfigured authorization server that mints OAuth tokens, JWTs, or API keys without proper validation, client verification, or trust chain enforcement. Attackers use these rogue-issued tokens to access MCP servers and tools that fail to validate the token issuer.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 533: 🔴 **CRITICAL** — `SAFE-T1704`

**Location:** Lines 15-16

**Description:**

The script uses user-controlled input (`$POLLUTION_CHECK` and `$TEST_PATTERN`) in shell commands. While not directly related to SSRF, this pattern could lead to command injection if not properly sanitized, especially when passed to `npm test` or `find`. However, in this specific context, `find` and `npm test` are unlikely to interpret these as URLs or make server requests that would lead to SSRF.: Compromised-Server Pivot / SSRF occurs when MCP tools accept user-controlled URLs in HTTP requests without host allowlisting or validation. Attackers exploit this to access internal services, cloud metadata endpoints, localhost services, or private network resources that the MCP server can reach but the attacker cannot directly access.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:15-16 POLLUTION_CHECK="$1"TEST_PATTERN="$2"
```

</details>

---

#### Finding 534: 🔴 **CRITICAL** — `SAFE-T1803`

**Location:** Line 35

**Description:**

The script itself does not perform bulk database exports. It is a testing utility designed to identify which test creates an unwanted file or directory. However, the `npm test` command executed by this script could potentially trigger a test that performs a database dump, which would then be detected as 'pollution' if the dump file matches `POLLUTION_CHECK`.: Database Dump extracts entire database contents through MCP tools using bulk export commands like mysqldump, pg_dump, SELECT *, or ORM findAll without pagination. This enables mass exfiltration of structured data including user records, credentials, financial data, and other sensitive information stored in databases.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:35-35 npm test "$TEST_FILE" > /dev/null 2>&1 || true
```

</details>

---

#### Finding 535: 🔴 **CRITICAL** — `SAFE-T2101`

**Location:** Lines 1-63

**Description:**

The script is designed to identify tests that create unwanted files or directories, which could be a precursor to a data destruction scenario if the identified 'pollution' is then targeted for deletion. However, the script itself does not perform any destructive operations.: Data Destruction uses MCP tools to permanently delete or corrupt data through destructive operations such as rm -rf, DROP TABLE, file wiping, bulk delete API calls, or database truncation. Attackers may destroy data for sabotage, to cover their tracks, or as part of a ransomware attack.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 536: 🔴 **CRITICAL** — `SAFE-T2103`

**Location:** Line 37

**Description:**

The script executes arbitrary test files found in the repository using 'npm test'. If a malicious pull request (as described by SAFE-T2103) introduces a test file containing harmful code, this script, when run, will execute that malicious code, potentially leading to arbitrary code execution on the developer's machine.: Code Sabotage via Malicious PR uses MCP tools with repository access to create automated pull requests containing malicious code changes, backdoors, or CI/CD pipeline modifications. Attackers manipulate build scripts, inject vulnerabilities, or alter deployment configurations through automated commits that bypass normal code review.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:37-37 npm test "$TEST_FILE" > /dev/null 2>&1 || true
```

</details>

---

#### Finding 537: 🔴 **CRITICAL** — `SAFE-T2106`

**Location:** Lines 1-63

**Description:**

The provided script `find-polluter.sh` is a diagnostic tool designed to identify which test creates unwanted files or state. It does not interact with vector databases, AI agents, or embedding models, and therefore does not directly implement or expose vulnerabilities related to SAFE-T2106 (Vector database poisoned with malicious embeddings).: Context Memory Poisoning via Vector Store Contamination manipulates vector databases storing long-term AI memory. Attackers inject poisoned embeddings that blend legitimate and malicious content, surviving server restarts and reinstallations. The poisoned content can self-replicate across sessions and affect multiple agents sharing the same vector store.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 538: 🔴 **CRITICAL** — `SAFE-T2107`

**Location:** Lines 1-63

**Description:**

The script `find-polluter.sh` is a diagnostic tool designed to identify test files that create unwanted files or state ('pollution'). While the concept of 'pollution' could theoretically involve malicious data, this script's role is to detect such artifacts, not to create or inject them into an AI model's training data pipeline. It does not directly contribute to or implement the SAFE-T2107 technique of training data manipulation or adversarial example injection.: AI Model Poisoning manipulates machine learning models used by MCP servers through training data poisoning, adversarial example injection, or embedding backdoor triggers during fine-tuning. Poisoned models produce attacker-desired outputs when triggered by specific inputs while behaving normally otherwise.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 539: 🟠 **HIGH** — `SAFE-T1002`

**Location:** Line 39

**Description:**

The script executes `npm test` on local files, which implicitly relies on previously installed `node_modules`. If the project's dependencies were installed without integrity verification (e.g., `npm install` without `package-lock.json` or `npm audit`), then malicious code could be executed. However, the script itself does not perform the remote fetching.: Supply Chain Compromise occurs when MCP servers download, install, import, or execute external packages, plugins, model weights, or binaries without integrity checks, version pinning, or signature verification. This allows attackers to inject malicious code through compromised dependencies.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:39-39 npm test "$TEST_FILE" > /dev/null 2>&1 || true
```

</details>

---

#### Finding 540: 🟠 **HIGH** — `SAFE-T1004`

**Location:** Lines 1-63

**Description:**

The provided script is a local debugging utility designed to identify test files that create unwanted file system artifacts. It does not implement any server-side logic, network communication, DNS resolution, or registry operations that could lead to server impersonation or name collision.: Server Impersonation occurs when an attacker registers an MCP server with a name designed to be confused with a legitimate service (typosquatting, homograph attacks, or DNS cache poisoning). Users connecting to the impersonated server unknowingly send credentials and data to the attacker.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 541: 🟠 **HIGH** — `SAFE-T1008`

**Location:** Lines 1-63

**Description:**

The provided code is a shell script for bisection debugging and does not appear to be an MCP tool description. Therefore, it does not directly implement or expose the SAFE-T1008 vulnerability, which specifically targets malicious instructions embedded within MCP tool descriptions to hijack or redirect other tools.: Tool Shadowing Attack embeds instructions in tool descriptions that manipulate how AI agents use OTHER tools. In multi-server MCP setups, all tool descriptions are aggregated into a single prompt, allowing any tool to influence the behavior of others through cross-tool interference patterns.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash
# Bisection script to find which test creates unwanted files/state
# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern>
# Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'

set -e

if [ $# -ne 2 ]; then
  echo "Usage: $0 <file_to_check> <test_pattern>"
  echo "Example: $0 '.git' 'src/**/*.test.ts'"
  exit 1
fi

POLLUTION_CHECK="$1"
TEST_PATTERN="$2"

echo "🔍 Searching for test that creates: $POLLUTION_CHECK"
echo "Test pattern: $TEST_PATTERN"
echo ""

# Get list of test files
TEST_FILES=$(find . -path "$TEST_PATTERN" | sort)
TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')

echo "Found $TOTAL test files"
echo ""

COUNT=0
for TEST_FILE in $TEST_FILES; do
  COUNT=$((COUNT + 1))

  # Skip if pollution already exists
  if [ -e "$POLLUTION_CHECK" ]; then
    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"
    echo "   Skipping: $TEST_FILE"
    continue
  fi

  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"

  # Run the test
  npm test "$TEST_FILE" > /dev/null 2>&1 || true

  # Check if pollution appeared
  if [ -e "$POLLUTION_CHECK" ]; then
    echo ""
    echo "🎯 FOUND POLLUTER!"
    echo "   Test: $TEST_FILE"
    echo "   Created: $POLLUTION_CHECK"
    echo ""
    echo "Pollution details:"
    ls -la "$POLLUTION_CHECK"
    echo ""
    echo "To investigate:"
    echo "  npm test $TEST_FILE    # Run just this test"
    echo "  cat $TEST_FILE         # Review test code"
    exit 1
  fi
done

echo ""
echo "✅ No polluter found - all tests clean!"
exit 0
```

</details>

---

#### Finding 542: 🟠 **HIGH** — `SAFE-T1009`

**Location:** Lines 1-63

**Description:**

The provided shell script is a debugging utility for finding test pollution and does not contain any OAuth client or server logic. Therefore, it is not susceptible to Authorization Server Mix-up (SAFE-T1009) vulnerabilities.: Authorization Server Mix-up occurs when an OAuth client follows a redirect to a look-alike authorization server domain (e.g., accounts-google.com vs accounts.google.com), causing authorization codes or tokens to be leaked to an attacker-controlled server. This exploits insufficient issuer validation in OAuth implementations.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash
# Bisection script to find which test creates unwanted files/state
# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern>
# Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'

set -e

if [ $# -ne 2 ]; then
  echo "Usage: $0 <file_to_check> <test_pattern>"
  echo "Example: $0 '.git' 'src/**/*.test.ts'"
  exit 1
fi

POLLUTION_CHECK="$1"
TEST_PATTERN="$2"

echo "🔍 Searching for test that creates: $POLLUTION_CHECK"
echo "Test pattern: $TEST_PATTERN"
echo ""

# Get list of test files
TEST_FILES=$(find . -path "$TEST_PATTERN" | sort)
TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')

echo "Found $TOTAL test files"
echo ""

COUNT=0
for TEST_FILE in $TEST_FILES; do
  COUNT=$((COUNT + 1))

  # Skip if pollution already exists
  if [ -e "$POLLUTION_CHECK" ]; then
    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"
    echo "   Skipping: $TEST_FILE"
    continue
  fi

  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"

  # Run the test
  npm test "$TEST_FILE" > /dev/null 2>&1 || true

  # Check if pollution appeared
  if [ -e "$POLLUTION_CHECK" ]; then
    echo ""
    echo "🎯 FOUND POLLUTER!"
    echo "   Test: $TEST_FILE"
    echo "   Created: $POLLUTION_CHECK"
    echo ""
    echo "Pollution details:"
    ls -la "$POLLUTION_CHECK"
    echo ""
    echo "To investigate:"
    echo "  npm test $TEST_FILE    # Run just this test"
    echo "  cat $TEST_FILE         # Review test code"
    exit 1
  fi
done

echo ""
echo "✅ No polluter found - all tests clean!"
exit 0
```

</details>

---

#### Finding 543: 🟠 **HIGH** — `SAFE-T1105`

**Location:** Lines 1-63

**Description:**

The script uses user-provided input (POLLUTION_CHECK) directly in filesystem existence checks and listing commands without any path normalization or sandboxing. This allows for path traversal.: Path Traversal via File Tool occurs when MCP file-handling tools accept untrusted input as filesystem paths without proper normalization, allowlisting, or base directory enforcement. Attackers can use relative path sequences, Unicode normalization tricks, double encoding, or null byte injection to access files outside the intended directory.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 skills/systematic-debugging/find-polluter.sh:29-29 if [ -e "$POLLUTION_CHECK" ]; then
skills/systematic-debugging/find-polluter.sh:45-45 ls -la "$POLLUTION_CHECK"
```

</details>

---

#### Finding 544: 🟠 **HIGH** — `SAFE-T1204`

**Location:** Lines 1-63

**Description:**

The script is a diagnostic tool designed to identify tests that create unwanted persistent files or state. It does not directly interact with or write to vector stores or conversation memory, which is the core mechanism of SAFE-T1204.: Context Memory Implant exploits persistent memory systems in MCP-connected AI agents. Malicious agents write themselves into long-term vector stores, conversation histories, or system prompt storage to ensure re-loading in future sessions. The implanted instructions persist across server restarts and session boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 545: 🟠 **HIGH** — `SAFE-T1205`

**Location:** Lines 1-63

**Description:**

The script `find-polluter.sh` is a diagnostic tool designed to identify tests that create unwanted files or state. While this script itself does not implement persistent tool redefinition, the 'pollution' it seeks to find could potentially be a symptom of an attacker attempting to establish persistence by creating or modifying files on disk. The script's functionality is to detect such changes, not to cause them.: Persistent Tool Redefinition is a privilege escalation technique where attackers modify an MCP server's tool metadata or configuration to embed hidden commands that survive server restarts. By persisting malicious tool definitions to disk, database, or configuration files, the attacker ensures their modifications remain active even after the server is restarted or redeployed.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 546: 🟠 **HIGH** — `SAFE-T1301`

**Location:** Lines 1-63

**Description:**

The provided code is a shell script for debugging test pollution and does not involve MCP server tool registration or interaction. Therefore, it is not susceptible to SAFE-T1301 (Cross-Server Tool Shadowing).: Cross-Server Tool Shadowing is a privilege escalation technique where malicious MCP servers override or intercept legitimate tool calls from other servers. This attack exploits multi-server MCP environments where multiple servers can provide tools with the same or similar names, allowing attackers to shadow legitimate functionality with malicious implementations.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 547: 🟠 **HIGH** — `SAFE-T1302`

**Location:** Line 47

**Description:**

The script is vulnerable to command injection via the first argument, `POLLUTION_CHECK`, which is directly used in an `ls -la` command without proper sanitization. If this script is executed with elevated privileges (e.g., as root or with sudo), an attacker can inject arbitrary shell commands.: High-Privilege Tool Abuse occurs when MCP tools are configured with administrative, root, or superuser access to databases, APIs, or system resources. Attackers exploit these tools through prompt injection or direct manipulation to perform privileged operations such as modifying access controls, reading sensitive data, or escalating their own permissions.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:47-47 ls -la "$POLLUTION_CHECK"
```

</details>

---

#### Finding 548: 🟠 **HIGH** — `SAFE-T1304`

**Location:** Lines 1-63

**Description:**

The provided script is a local test debugging utility and does not handle authentication tokens, credentials, or inter-service communication. It is not susceptible to SAFE-T1304.: Credential Relay Chain exploits token reuse across MCP service boundaries. Attackers capture tokens from one service interaction and replay them in tool-to-tool calls, cross-service requests, or downstream API invocations without proper audience validation, scope restriction, or token binding.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 549: 🟠 **HIGH** — `SAFE-T1307`

**Location:** Lines 1-63

**Description:**

The script uses user-provided input (`POLLUTION_CHECK`, `TEST_PATTERN`, and `TEST_FILE`) directly in shell commands without proper sanitization or quoting, leading to potential command injection vulnerabilities. While not a direct Confused Deputy attack (SAFE-T1307), an attacker controlling these inputs could execute arbitrary commands with the script's ambient authority.: Confused Deputy Attack exploits ambient authority in MCP servers where a privileged service performs actions on behalf of an unauthorized requester. In multi-tenant environments, this enables cross-tenant data access, privilege confusion between service tiers, or unauthorized operations performed through a trusted intermediary.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 skills/systematic-debugging/find-polluter.sh:20-20 POLLUTION_CHECK="$1"
skills/systematic-debugging/find-polluter.sh:21-21 TEST_PATTERN="$2"
skills/systematic-debugging/find-polluter.sh:28-28 TEST_FILES=$(find . -path "$TEST_PATTERN" | sort)
skills/systematic-debugging/find-polluter.sh:45-45 npm test "$TEST_FILE" > /dev/null 2>&1 || true
skills/systematic-debugging/find-polluter.sh:53-53 ls -la "$POLLUTION_CHECK"
skills/systematic-debugging/find-polluter.sh:56-56 echo "  npm test $TEST_FILE    # Run just this test"
skills/systematic-debugging/find-polluter.sh:57-57 echo "  cat $TEST_FILE         # Review test code"
```

</details>

---

#### Finding 550: 🟠 **HIGH** — `SAFE-T1401`

**Location:** Line 24

**Description:**

The script uses user-supplied input directly in shell commands, which could lead to shell injection if not properly sanitized. However, this is not an instance of SAFE-T1401 (Newline or control character injection in tool descriptions to hide malicious instructions) as the script is a standalone bash script and does not interact with an LLM or provide 'tool descriptions' in an MCP context.: Line Jumping injects newline characters, CRLF sequences, or control characters into MCP tool descriptions, parameter names, or metadata fields. The injected characters push malicious instructions outside the visible area of user interfaces, making them invisible during review while still being processed by the LLM.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:24-24 TEST_FILES=$(find . -path "$TEST_PATTERN" | sort)
```

</details>

---

#### Finding 551: 🟠 **HIGH** — `SAFE-T1402`

**Location:** Lines 1-63

**Description:**

The provided code is a shell script for test bisection and does not appear to be an MCP tool definition or configuration. Therefore, it is not directly susceptible to the SAFE-T1402 technique, which targets hidden instructions in tool metadata for AI agents and LLMs.: Instruction Stenography conceals malicious instructions within tool descriptions or metadata using steganographic techniques: HTML comments, zero-width Unicode characters, bidirectional text overrides, or invisible formatting. These hidden instructions are processed by LLMs but invisible in standard UI rendering.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash
# Bisection script to find which test creates unwanted files/state
# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern>
# Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'

set -e

if [ $# -ne 2 ]; then
  echo "Usage: $0 <file_to_check> <test_pattern>"
  echo "Example: $0 '.git' 'src/**/*.test.ts'"
  exit 1
fi

POLLUTION_CHECK="$1"
TEST_PATTERN="$2"

echo "🔍 Searching for test that creates: $POLLUTION_CHECK"
echo "Test pattern: $TEST_PATTERN"
echo ""

# Get list of test files
TEST_FILES=$(find . -path "$TEST_PATTERN" | sort)
TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')

echo "Found $TOTAL test files"
echo ""

COUNT=0
for TEST_FILE in $TEST_FILES; do
  COUNT=$((COUNT + 1))

  # Skip if pollution already exists
  if [ -e "$POLLUTION_CHECK" ]; then
    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"
    echo "   Skipping: $TEST_FILE"
    continue
  fi

  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"

  # Run the test
  npm test "$TEST_FILE" > /dev/null 2>&1 || true

  # Check if pollution appeared
  if [ -e "$POLLUTION_CHECK" ]; then
    echo ""
    echo "🎯 FOUND POLLUTER!"
    echo "   Test: $TEST_FILE"
    echo "   Created: $POLLUTION_CHECK"
    echo ""
    echo "Pollution details:"
    ls -la "$POLLUTION_CHECK"
    echo ""
    echo "To investigate:"
    echo "  npm test $TEST_FILE    # Run just this test"
    echo "  cat $TEST_FILE         # Review test code"
    exit 1
  fi
done

echo ""
echo "✅ No polluter found - all tests clean!"
exit 0
```

</details>

---

#### Finding 552: 🟠 **HIGH** — `SAFE-T1403`

**Location:** Lines 1-63

**Description:**

The script is a debugging tool designed to identify which test creates unwanted files or state. It automates the execution of tests and checks for file existence, but it does not involve user interaction for permissions or approvals. Therefore, it does not directly implement or facilitate the 'Consent-Fatigue Exploit' (SAFE-T1403) technique, which relies on overwhelming users with repetitive permission requests.: Consent-Fatigue Exploit overwhelms users with rapid, repeated, or batched permission requests to exploit decision fatigue. Users begin auto-approving prompts without reading them, allowing malicious tools to gain permissions they would normally be denied. This also includes auto-approve patterns that bypass user consent entirely.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 553: 🟠 **HIGH** — `SAFE-T1407`

**Location:** Lines 1-63

**Description:**

The provided script is a local testing utility for identifying file system pollution caused by tests. It does not involve network communication, API proxying, or MCP server functionality, and therefore does not implement the SAFE-T1407 technique.: An attacker operates an MCP server that forwards requests to a legitimate upstream API and relays the responses back to the client. Because the responses originate from the trusted service, the traffic appears normal in monitoring and logs, but the proxy can inspect, alter, or exfiltrate sensitive data. This enables stealthy data collection and response manipulation while hiding the true malicious behavior.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash
# Bisection script to find which test creates unwanted files/state
# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern>
# Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'

set -e

if [ $# -ne 2 ]; then
  echo "Usage: $0 <file_to_check> <test_pattern>"
  echo "Example: $0 '.git' 'src/**/*.test.ts'"
  exit 1
fi

POLLUTION_CHECK="$1"
TEST_PATTERN="$2"

echo "🔍 Searching for test that creates: $POLLUTION_CHECK"
echo "Test pattern: $TEST_PATTERN"
echo ""

# Get list of test files
TEST_FILES=$(find . -path "$TEST_PATTERN" | sort)
TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')

echo "Found $TOTAL test files"
echo ""

COUNT=0
for TEST_FILE in $TEST_FILES; do
  COUNT=$((COUNT + 1))

  # Skip if pollution already exists
  if [ -e "$POLLUTION_CHECK" ]; then
    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"
    echo "   Skipping: $TEST_FILE"
    continue
  fi

  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"

  # Run the test
  npm test "$TEST_FILE" > /dev/null 2>&1 || true

  # Check if pollution appeared
  if [ -e "$POLLUTION_CHECK" ]; then
    echo ""
    echo "🎯 FOUND POLLUTER!"
    echo "   Test: $TEST_FILE"
    echo "   Created: $POLLUTION_CHECK"
    echo ""
    echo "Pollution details:"
    ls -la "$POLLUTION_CHECK"
    echo ""
    echo "To investigate:"
    echo "  npm test $TEST_FILE    # Run just this test"
    echo "  cat $TEST_FILE         # Review test code"
    exit 1
  fi
done

echo ""
echo "✅ No polluter found - all tests clean!"
exit 0
```

</details>

---

#### Finding 554: 🟠 **HIGH** — `SAFE-T1504`

**Location:** Lines 1-63

**Description:**

The provided shell script is a local debugging tool for finding test file pollution. It does not interact with MCP servers, handle API responses, or process access/refresh tokens. Therefore, it is not susceptible to SAFE-T1504.: Token Theft via API Response occurs when MCP tool implementations inadvertently include access tokens, refresh tokens, or other credentials in their output responses. These leaked tokens are then visible in conversation history, logs, or can be captured by other tools in the chain for unauthorized access.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 555: 🟠 **HIGH** — `SAFE-T1602`

**Location:** Lines 1-63

**Description:**

The script is a debugging utility designed to identify which test creates unwanted files or state. It does not implement or directly facilitate the 'Tool Enumeration' technique (SAFE-T1602).: Tool Enumeration discovers the full set of available MCP tools, their capabilities, input schemas, and permission requirements. Attackers use tools/list API calls and schema introspection to map available functionality, identify privileged tools, and find tools with weak input validation for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 556: 🟠 **HIGH** — `SAFE-T1603`

**Location:** Lines 1-63

**Description:**

The script's output, while verbose for debugging, does not expose system prompts or instructions of an AI agent, which is the focus of SAFE-T1603. It provides information about test execution and file system state, which is expected for a diagnostic tool.: System Prompt Disclosure exposes internal system prompts, instructions, or configuration through debug mode endpoints, verbose error messages, tool output, or direct echoing. Exposed system prompts reveal security controls, filtering rules, and behavioral constraints that attackers can use to craft targeted bypass attacks.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 557: 🟠 **HIGH** — `SAFE-T1605`

**Location:** Lines 1-63

**Description:**

The provided script is a local debugging utility designed to identify which test creates unwanted files or state. It does not expose server capabilities, API versions, or permission boundaries in a manner consistent with the SAFE-T1605 technique, which focuses on an adversary coaxing an MCP agent or LLM-based assistant into describing its own tools, workflows, and policy exceptions.: Capability Mapping systematically discovers MCP server capabilities, supported API versions, feature flags, and permission boundaries. Attackers use introspection endpoints, capability negotiation, and API probing to build a comprehensive map of what the target system can do, identifying the most valuable attack vectors.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash# Bisection script to find which test creates unwanted files/state# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern># Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'set -eif [ $# -ne 2 ]; then  echo "Usage: $0 <file_to_check> <test_pattern>"  echo "Example: $0 '.git' 'src/**/*.test.ts'"  exit 1fiPOLLUTION_CHECK="$1"TEST_PATTERN="$2"echo "🔍 Searching for test that creates: $POLLUTION_CHECK"echo "Test pattern: $TEST_PATTERN"echo ""# Get list of test filesTEST_FILES=$(find . -path "$TEST_PATTERN" | sort)TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')echo "Found $TOTAL test files"echo ""COUNT=0for TEST_FILE in $TEST_FILES; do  COUNT=$((COUNT + 1))  # Skip if pollution already exists  if [ -e "$POLLUTION_CHECK" ]; then    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"    echo "   Skipping: $TEST_FILE"    continue  fi  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"  # Run the test  npm test "$TEST_FILE" > /dev/null 2>&1 || true  # Check if pollution appeared  if [ -e "$POLLUTION_CHECK" ]; then    echo ""    echo "🎯 FOUND POLLUTER!"    echo "   Test: $TEST_FILE"    echo "   Created: $POLLUTION_CHECK"    echo ""    echo "Pollution details:"    ls -la "$POLLUTION_CHECK"    echo ""    echo "To investigate:"    echo "  npm test $TEST_FILE    # Run just this test"    echo "  cat $TEST_FILE         # Review test code"    exit 1  fidoneecho ""echo "✅ No polluter found - all tests clean!"exit 0
```

</details>

---

#### Finding 558: 🟠 **HIGH** — `SAFE-T1703`

**Location:** Line 50

**Description:**

The script `find-polluter.sh` is vulnerable to command injection due to improper handling of user-controlled input (`POLLUTION_CHECK`) when used in the `ls -la` command. If this script is executed in a privileged context, an attacker can inject arbitrary shell commands, leading to a 'Tool-Chaining Pivot' where attacker-controlled input is used to execute privileged commands.: Tool-Chaining Pivot exploits the output of one MCP tool as input to another more privileged tool, creating multi-step attack chains. Attackers leverage a low-privilege tool to produce crafted output that, when consumed by a higher-privilege tool, enables unauthorized operations such as command execution, data access, or privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:50-50 ls -la "$POLLUTION_CHECK"
```

</details>

---

#### Finding 559: 🟠 **HIGH** — `SAFE-T1706`

**Location:** Lines 1-63

**Description:**

The provided shell script does not handle OAuth tokens or perform any authentication/authorization, therefore it is not susceptible to OAuth token replay vulnerabilities.: OAuth Token Pivot Replay reuses OAuth tokens obtained from one service to access a different service or audience. Attackers exploit missing audience (aud) validation, overly broad token scopes, or lack of token binding to pivot from a lower-value service to a higher-value target using the same token.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash
# Bisection script to find which test creates unwanted files/state
# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern>
# Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'

set -e

if [ $# -ne 2 ]; then
  echo "Usage: $0 <file_to_check> <test_pattern>"
  echo "Example: $0 '.git' 'src/**/*.test.ts'"
  exit 1
fi

POLLUTION_CHECK="$1"
TEST_PATTERN="$2"

echo "🔍 Searching for test that creates: $POLLUTION_CHECK"
echo "Test pattern: $TEST_PATTERN"
echo ""

# Get list of test files
TEST_FILES=$(find . -path "$TEST_PATTERN" | sort)
TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')

echo "Found $TOTAL test files"
echo ""

COUNT=0
for TEST_FILE in $TEST_FILES; do
  COUNT=$((COUNT + 1))

  # Skip if pollution already exists
  if [ -e "$POLLUTION_CHECK" ]; then
    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"
    echo "   Skipping: $TEST_FILE"
    continue
  fi

  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"

  # Run the test
  npm test "$TEST_FILE" > /dev/null 2>&1 || true

  # Check if pollution appeared
  if [ -e "$POLLUTION_CHECK" ]; then
    echo ""
    echo "🎯 FOUND POLLUTER!"
    echo "   Test: $TEST_FILE"
    echo "   Created: $POLLUTION_CHECK"
    echo ""
    echo "Pollution details:"
    ls -la "$POLLUTION_CHECK"
    echo ""
    echo "To investigate:"
    echo "  npm test $TEST_FILE    # Run just this test"
    echo "  cat $TEST_FILE         # Review test code"
    exit 1
  fi
done

echo ""
echo "✅ No polluter found - all tests clean!"
exit 0
```

</details>

---

#### Finding 560: 🟠 **HIGH** — `SAFE-T1801`

**Location:** Lines 1-63

**Description:**

The script systematically iterates through test files and performs an action for each, which superficially resembles the 'systematic enumeration' aspect of SAFE-T1801. However, this is a benign debugging script, not an AI agent being manipulated to perform data harvesting via MCP tool calls.: Automated Data Harvesting uses MCP tools to systematically access and collect data through bulk queries, pagination scraping, and record enumeration. Attackers automate data collection across multiple endpoints or database tables to amass sensitive information for exfiltration or analysis.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 skills/systematic-debugging/find-polluter.sh:22-56 TEST_FILES=$(find . -path "$TEST_PATTERN" | sort)
TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')

echo "Found $TOTAL test files"
echo ""

COUNT=0
for TEST_FILE in $TEST_FILES; do
  COUNT=$((COUNT + 1))

  # Skip if pollution already exists
  if [ -e "$POLLUTION_CHECK" ]; then
    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"
    echo "   Skipping: $TEST_FILE"
    continue
  fi

  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"

  # Run the test
  npm test "$TEST_FILE" > /dev/null 2>&1 || true

  # Check if pollution appeared
  if [ -e "$POLLUTION_CHECK" ]; then
    echo ""
    echo "🎯 FOUND POLLUTER!"
    echo "   Test: $TEST_FILE"
    echo "   Created: $POLLUTION_CHECK"
    echo ""
    echo "Pollution details:"
    ls -la "$POLLUTION_CHECK"
    echo ""
    echo "To investigate:"
    echo "  npm test $TEST_FILE    # Run just this test"
    echo "  cat $TEST_FILE         # Review test code"
    exit 1
  fi
done
```

</details>

---

#### Finding 561: 🟠 **HIGH** — `SAFE-T1910`

**Location:** Lines 1-63

**Description:**

The script is a debugging utility designed to identify which test creates unwanted files or state. It does not implement any data exfiltration mechanisms itself.: Covert Channel Exfiltration uses non-standard communication channels to exfiltrate data from MCP server environments. Techniques include DNS tunneling (encoding data in DNS queries), timing-based channels, HTTP header manipulation, and steganographic encoding that bypasses traditional network monitoring and DLP solutions.

<details>
<summary>Code Evidence</summary>

```
skills/systematic-debugging/find-polluter.sh:1-63 skills/systematic-debugging/find-polluter.sh:1-63 #!/usr/bin/env bash
# Bisection script to find which test creates unwanted files/state
# Usage: ./find-polluter.sh <file_or_dir_to_check> <test_pattern>
# Example: ./find-polluter.sh '.git' 'src/**/*.test.ts'

set -e

if [ $# -ne 2 ]; then
  echo "Usage: $0 <file_to_check> <test_pattern>"
  echo "Example: $0 '.git' 'src/**/*.test.ts'"
  exit 1
fi

POLLUTION_CHECK="$1"
TEST_PATTERN="$2"

echo "🔍 Searching for test that creates: $POLLUTION_CHECK"
echo "Test pattern: $TEST_PATTERN"
echo ""

# Get list of test files
TEST_FILES=$(find . -path "$TEST_PATTERN" | sort)
TOTAL=$(echo "$TEST_FILES" | wc -l | tr -d ' ')

echo "Found $TOTAL test files"
echo ""

COUNT=0
for TEST_FILE in $TEST_FILES; do
  COUNT=$((COUNT + 1))

  # Skip if pollution already exists
  if [ -e "$POLLUTION_CHECK" ]; then
    echo "⚠️  Pollution already exists before test $COUNT/$TOTAL"
    echo "   Skipping: $TEST_FILE"
    continue
  fi

  echo "[$COUNT/$TOTAL] Testing: $TEST_FILE"

  # Run the test
  npm test "$TEST_FILE" > /dev/null 2>&1 || true

  # Check if pollution appeared
  if [ -e "$POLLUTION_CHECK" ]; then
    echo ""
    echo "🎯 FOUND POLLUTER!"
    echo "   Test: $TEST_FILE"
    echo "   Created: $POLLUTION_CHECK"
    echo ""
    echo "Pollution details:"
    ls -la "$POLLUTION_CHECK"
    echo ""
    echo "To investigate:"
    echo "  npm test $TEST_FILE    # Run just this test"
    echo "  cat $TEST_FILE         # Review test code"
    exit 1
  fi
done

echo ""
echo "✅ No polluter found - all tests clean!"
exit 0
```

</details>

---

### 📁 `hooks/run-hook.cmd`

#### Finding 562: 🔴 **CRITICAL** — `SAFE-T1003`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a polyglot wrapper designed to execute other scripts (presumably bash scripts) in a cross-platform manner. While not inherently malicious, its function of executing arbitrary scripts passed as arguments (`%~1` on Windows, `$1` on Unix) makes it a potential vector if the scripts it is designed to run are themselves malicious. This script itself does not contain the direct malicious payload or C2 communication described in SAFE-T1003, but it could be a component within a larger trojanized package.: Malicious MCP-Server Distribution involves distributing trojanized MCP server implementations through package registries (npm, PyPI, Docker Hub) or code repositories. These servers appear legitimate but contain hidden malicious functionality such as data exfiltration, backdoors, or credential theft.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 563: 🔴 **CRITICAL** — `SAFE-T1007`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a utility for executing other scripts and does not contain any logic related to OAuth flows, authorization, or token management. It is not an MCP server component or an OAuth client.: OAuth Authorization Phishing exploits the OAuth authorization flow to trick users into granting permissions to attacker-controlled applications. The attacker creates a convincing authorization page or redirects users through legitimate OAuth providers but captures the authorization code or tokens.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 564: 🔴 **CRITICAL** — `SAFE-T1101`

**Location:** Lines 1-46

**Description:**

The Windows batch script passes the script name (%~1) to 'bash.exe' in a way that allows shell metacharacters within the script name to be interpreted, leading to command injection.: Prompt Injection via Tool Output occurs when MCP tools return user-controlled or external data directly to the LLM without proper sanitization, allowing malicious content in tool outputs to hijack the agent behavior. Tool outputs are typically trusted by LLMs and processed as authoritative information.
Common attack vectors include: database records with hidden instructions, file contents with prompt injection payloads, API responses crafted to override directives, and web scraping results with malicious instruction sequences.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 565: 🔴 **CRITICAL** — `SAFE-T1101`

**Location:** Lines 1-46

**Description:**

The Windows batch script passes subsequent arguments (%2 through %9) to 'bash.exe' without enclosing them in quotes, making them vulnerable to command injection.: Prompt Injection via Tool Output occurs when MCP tools return user-controlled or external data directly to the LLM without proper sanitization, allowing malicious content in tool outputs to hijack the agent behavior. Tool outputs are typically trusted by LLMs and processed as authoritative information.
Common attack vectors include: database records with hidden instructions, file contents with prompt injection payloads, API responses crafted to override directives, and web scraping results with malicious instruction sequences.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 566: 🔴 **CRITICAL** — `SAFE-T1101`

**Location:** Lines 1-46

**Description:**

The Unix bash script passes the script name (derived from $1) to 'bash' in a way that allows shell metacharacters within the script name to be interpreted, leading to command injection.: Prompt Injection via Tool Output occurs when MCP tools return user-controlled or external data directly to the LLM without proper sanitization, allowing malicious content in tool outputs to hijack the agent behavior. Tool outputs are typically trusted by LLMs and processed as authoritative information.
Common attack vectors include: database records with hidden instructions, file contents with prompt injection payloads, API responses crafted to override directives, and web scraping results with malicious instruction sequences.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 567: 🔴 **CRITICAL** — `SAFE-T1103`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a wrapper for executing shell scripts and does not process MCP JSON-RPC messages. Therefore, it is not directly susceptible to SAFE-T1103, which describes function name spoofing or tool impersonation within the MCP JSON-RPC protocol.: Fake Tool Invocation exploits insufficient tool validation in MCP hosts by injecting or spoofing tool names, duplicating tool registrations, or manipulating the JSON-RPC tool call protocol. Attackers can redirect legitimate tool calls to malicious implementations or register tools that shadow trusted ones.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 568: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script acts as a wrapper to execute arbitrary hook scripts using `bash`. While this script itself is not a network-exposed debug endpoint, it facilitates the execution of other scripts. If the hook scripts it executes contain or expose debug endpoints, or if the `run-hook.cmd` script is invoked in a context where its arguments can be controlled by an attacker (e.g., via an exposed web interface or a vulnerable build system), it could indirectly contribute to remote code execution. The script directly passes all arguments (`%2 %3 ... %9` or `"$@"`) to the underlying `bash` command, which then executes the specified hook script. This pattern of executing external scripts with user-controlled arguments is a common vector for command injection if the arguments are not properly sanitized or if the calling context is insecure. However, based on the provided code alone, there is no direct evidence of a network-exposed debug endpoint as described by SAFE-T1109.: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 569: 🔴 **CRITICAL** — `SAFE-T1109`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script on Unix systems uses `exec bash "${SCRIPT_DIR}/${SCRIPT_NAME}" "$@"` to execute the target script. This directly passes all command-line arguments to the `bash` interpreter for the specified script. If the `SCRIPT_NAME` or the subsequent arguments (`"$@"`) are derived from untrusted input and not properly sanitized, an attacker could potentially inject malicious commands into the execution flow. While the script itself is not a network-exposed debug endpoint, its function as a script executor with direct argument passing makes it a potential component in a larger RCE chain if its inputs are compromised. The technique SAFE-T1109 focuses on exposed debug endpoints, but a script like this could be the target of commands executed via such an endpoint.: Debugging Tool Exploitation targets MCP debugging and inspection tools left enabled in production environments. Attackers exploit vulnerabilities like CSRF and DNS rebinding in tools such as MCP Inspector (CVE-2025-49596) to achieve remote code execution through SSE endpoints, debug ports, or eval-in-debug-context patterns.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 570: 🔴 **CRITICAL** — `SAFE-T1203`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script provides a generic mechanism for executing arbitrary scripts located within its own directory, passed as command-line arguments. This design makes it a critical enabler for the SAFE-T1203 technique, where a trojanized MCP server artifact could inject and execute malicious scripts to establish persistence.: Backdoored Server Binary involves distributing trojanized MCP server packages, containers, or binaries that establish persistence through postinstall hooks, cron jobs, systemd units, or autostart entries. The malicious code executes during installation and persists even after the package is uninstalled.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 571: 🔴 **CRITICAL** — `SAFE-T1303`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a host-level wrapper for executing other scripts and does not directly implement container runtime `exec` functionality. While it takes user-controlled input, it does not appear to be the component responsible for container sandbox isolation or for invoking container runtime `exec` with a manipulated working directory, which is the core of SAFE-T1303. Therefore, this file itself does not exhibit the described vulnerability.: Container Sandbox Escape exploits misconfigured container environments running MCP servers to break out of the sandbox and access the host operating system. Attack vectors include mounting the Docker socket, accessing /proc filesystem, running in privileged mode, using nsenter, or exploiting kernel vulnerabilities from within the container.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 572: 🔴 **CRITICAL** — `SAFE-T1305`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is designed to execute other scripts using bash. While this script itself does not cause privilege escalation, it acts as an execution vector for MCP server hooks. If the MCP server process invoking this script is running with root privileges (UID 0), any subsequent scripts executed via `run-hook.cmd` will also inherit those root privileges. This aligns with the prerequisite for SAFE-T1305, where an MCP server running as root allows an attacker to execute arbitrary commands with full system privileges.: Host OS Privilege Escalation exploits operating system vulnerabilities or misconfigurations from within an MCP server process to gain root or administrative access on the host. Attack vectors include setuid binary abuse, kernel exploits, capability escalation, and accessing sensitive system files like /etc/shadow.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 573: 🔴 **CRITICAL** — `SAFE-T1306`

**Location:** Lines 1-46

**Description:**

The script `run-hook.cmd` is a polyglot wrapper designed to execute bash scripts on both Windows and Unix-like systems. It locates a bash interpreter and then runs a specified script with arguments. This script itself does not implement any authentication server logic, token minting, or validation mechanisms. It does not interact with OAuth/OpenID Connect flows, discovery metadata, or JWKS keys, which are the core components of the SAFE-T1306 technique.: Rogue Authorization Server involves setting up a malicious or misconfigured authorization server that mints OAuth tokens, JWTs, or API keys without proper validation, client verification, or trust chain enforcement. Attackers use these rogue-issued tokens to access MCP servers and tools that fail to validate the token issuer.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 574: 🔴 **CRITICAL** — `SAFE-T1502`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a generic command execution wrapper that can invoke arbitrary bash scripts with user-supplied arguments. While not directly a 'file-based credential harvest' tool itself, its ability to execute scripts with arguments means it could be leveraged by an AI agent, if coerced, to execute a malicious script that *does* perform credential harvesting. The vulnerability would lie in the executed script or the agent's decision-making, rather than in this wrapper script's inherent functionality.: File-Based Credential Harvest occurs when MCP server tools read sensitive credential files from the host filesystem. Targets include SSH private keys, AWS credentials, environment files, browser cookies, keychain databases, and cloud provider configurations. The harvested credentials enable lateral movement and privilege escalation.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 575: 🔴 **CRITICAL** — `SAFE-T1505`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a polyglot wrapper for executing other scripts and does not directly handle or process sensitive information in a manner susceptible to in-memory secret extraction via semantic analysis, as described by SAFE-T1505. This script's primary function is to locate and invoke a bash interpreter to run hook scripts.: In-Memory Secret Extraction targets secrets held in process memory at runtime. Attackers use memory dump tools, /proc/*/mem access, heap inspection, or debugger attachment to extract encryption keys, tokens, passwords, and other sensitive data that exists only in memory and would not be found in files or environment variables.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 576: 🔴 **CRITICAL** — `SAFE-T1702`

**Location:** Lines 1-46

**Description:**

The Windows batch script `run-hook.cmd` is vulnerable to shell command injection due to unsanitized user input being passed directly to `bash.exe`.: Command Injection vulnerabilities occur when MCP server tools execute shell commands with unsanitized user input. Attackers can inject malicious commands that are executed with the server's privileges, potentially leading to arbitrary code execution, data exfiltration, or system compromise.

This technique targets common command execution patterns across multiple languages including Node.js (child_process), Python (subprocess, os.system), Rust (Command), and Go (os/exec). Unsafe practices include string concatenation in shell commands, missing input validation, and using shell=True or shell invocation modes.

Command injection is particularly dangerous in MCP servers because tools often have elevated privileges to interact with system resources, files, or external services. A successful injection can bypass all security boundaries.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 577: 🔴 **CRITICAL** — `SAFE-T1704`

**Location:** Lines 1-46

**Description:**

The script executes a bash script with user-controlled arguments (%2-%9) that are not properly quoted. This can lead to command injection if an attacker can control these arguments, allowing them to execute arbitrary commands on the system where the hook runner is executed. While not directly an SSRF vulnerability, this command injection could be leveraged as a pivot point for lateral movement, which aligns with the broader 'Compromised-Server Pivot' aspect of SAFE-T1704, by executing commands that perform network requests to internal resources.: Compromised-Server Pivot / SSRF occurs when MCP tools accept user-controlled URLs in HTTP requests without host allowlisting or validation. Attackers exploit this to access internal services, cloud metadata endpoints, localhost services, or private network resources that the MCP server can reach but the attacker cannot directly access.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 578: 🔴 **CRITICAL** — `SAFE-T1705`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a generic wrapper for executing local scripts and does not appear to be part of a multi-agent system or involved in inter-agent communication. The technique SAFE-T1705 specifically targets AI agent communication channels and multi-agent workflows, which are not present in this file.: Cross-Agent Instruction Injection targets multi-agent MCP architectures where agents communicate by passing messages or tool results. Attackers inject malicious instructions into one agent that propagate to other agents in the workflow, contaminating the entire multi-agent pipeline through message passing, shared context, or tool output forwarding.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 579: 🔴 **CRITICAL** — `SAFE-T2101`

**Location:** Lines 1-46

**Description:**

The script executes an arbitrary bash script specified by the first argument, passing subsequent arguments directly without sanitization. This allows for arbitrary command execution, which can be leveraged for data destruction if an attacker controls the script name or its arguments.: Data Destruction uses MCP tools to permanently delete or corrupt data through destructive operations such as rm -rf, DROP TABLE, file wiping, bulk delete API calls, or database truncation. Attackers may destroy data for sabotage, to cover their tracks, or as part of a ransomware attack.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 580: 🔴 **CRITICAL** — `SAFE-T2106`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a polyglot wrapper for executing bash scripts on Windows and Unix-like systems. It primarily handles path resolution and execution of other scripts. This script does not contain any logic related to vector databases, embedding models, AI agents, or memory management, which are central to the SAFE-T2106 technique of vector database poisoning.: Context Memory Poisoning via Vector Store Contamination manipulates vector databases storing long-term AI memory. Attackers inject poisoned embeddings that blend legitimate and malicious content, surviving server restarts and reinstallations. The poisoned content can self-replicate across sessions and affect multiple agents sharing the same vector store.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 581: 🔴 **CRITICAL** — `SAFE-T2107`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is vulnerable to path traversal, allowing an attacker to execute arbitrary bash scripts outside the intended `hooks` directory. This can be leveraged to execute malicious scripts that perform training data manipulation, adversarial examples, or backdoor triggers, directly enabling the SAFE-T2107 technique.: AI Model Poisoning manipulates machine learning models used by MCP servers through training data poisoning, adversarial example injection, or embedding backdoor triggers during fine-tuning. Poisoned models produce attacker-desired outputs when triggered by specific inputs while behaving normally otherwise.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 582: 🟠 **HIGH** — `SAFE-T1004`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script attempts to locate and execute `bash.exe` by searching the system's `PATH` environment variable. This method is susceptible to name collision or binary planting attacks if an attacker can manipulate the `PATH` to include a malicious executable named `bash.exe` in a higher-priority directory.: Server Impersonation occurs when an attacker registers an MCP server with a name designed to be confused with a legitimate service (typosquatting, homograph attacks, or DNS cache poisoning). Users connecting to the impersonated server unknowingly send credentials and data to the attacker.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 583: 🟠 **HIGH** — `SAFE-T1104`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script acts as a generic script executor, taking a script name and arguments directly from its command line. If exposed as an MCP tool to an large language model (LLM), this allows an attacker to specify which hook script to run and pass arbitrary arguments to it, potentially leading to arbitrary command execution or privilege escalation if the target hook scripts are vulnerable or perform sensitive operations.: Over-Privileged Tool Abuse occurs when MCP tools are granted permissions far exceeding their functional requirements. Attackers exploit these over-privileged tools through prompt injection or direct manipulation to access sensitive resources, execute system commands, or escalate privileges beyond the intended scope.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 584: 🟠 **HIGH** — `SAFE-T1105`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is vulnerable to path traversal, allowing an attacker to execute arbitrary scripts outside the intended `hooks` directory by manipulating the first command-line argument.: Path Traversal via File Tool occurs when MCP file-handling tools accept untrusted input as filesystem paths without proper normalization, allowlisting, or base directory enforcement. Attackers can use relative path sequences, Unicode normalization tricks, double encoding, or null byte injection to access files outside the intended directory.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 585: 🟠 **HIGH** — `SAFE-T1106`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a simple wrapper designed to execute a single hook script. It does not contain any internal logic for autonomous planning, decision-making, retries, or recursive invocations of itself or other tools. Its function is to locate a bash interpreter and then execute the specified hook script once.: Autonomous Loop Exploit triggers denial-of-service through self-reinforcing tool invocation loops. Attackers craft prompts or tool outputs that suggest non-terminal progress, causing agents to repeatedly invoke tools without convergence checks, rate limits, or iteration budgets.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 586: 🟠 **HIGH** — `SAFE-T1110`

**Location:** Lines 1-46

**Description:**

The provided code is a shell script wrapper for executing other scripts and does not handle image or audio inputs. Therefore, it is not susceptible to multimodal prompt injection via images or audio (SAFE-T1110).: Multimodal Prompt Injection embeds malicious instructions within images, audio, or other non-text media that are processed by LLMs. Techniques include steganographic embedding in pixel data, OCR-extractable text injection, adversarial visual patterns, and metadata injection in media files. These bypass text-based sanitization since the payload is in a non-text modality.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 587: 🟠 **HIGH** — `SAFE-T1204`

**Location:** Lines 1-46

**Description:**

The script executes other scripts using bash, which could be a vector for arbitrary code execution if the scripts themselves are malicious or if the script name is user-controlled and not properly sanitized. However, this is a general code execution risk, not directly related to 'Context Memory Implant' (SAFE-T1204) which specifically targets vector stores or conversation memory.: Context Memory Implant exploits persistent memory systems in MCP-connected AI agents. Malicious agents write themselves into long-term vector stores, conversation histories, or system prompt storage to ensure re-loading in future sessions. The implanted instructions persist across server restarts and session boundaries.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 588: 🟠 **HIGH** — `SAFE-T1205`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script acts as a wrapper for executing other hook scripts, making it a persistent execution point within the MCP server environment. Its role in invoking arbitrary named scripts from the `hooks` directory makes it a prime target for modification by an attacker seeking to establish persistence.: Persistent Tool Redefinition is a privilege escalation technique where attackers modify an MCP server's tool metadata or configuration to embed hidden commands that survive server restarts. By persisting malicious tool definitions to disk, database, or configuration files, the attacker ensures their modifications remain active even after the server is restarted or redeployed.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 589: 🟠 **HIGH** — `SAFE-T1301`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a client-side wrapper for executing local bash scripts. It does not register tools with an MCP server, nor does it interact with MCP server tool resolution mechanisms. Therefore, it does not directly implement or facilitate the SAFE-T1301 technique, which describes malicious MCP servers overriding legitimate tool calls by registering conflicting tool names.: Cross-Server Tool Shadowing is a privilege escalation technique where malicious MCP servers override or intercept legitimate tool calls from other servers. This attack exploits multi-server MCP environments where multiple servers can provide tools with the same or similar names, allowing attackers to shadow legitimate functionality with malicious implementations.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 590: 🟠 **HIGH** — `SAFE-T1404`

**Location:** Lines 1-46

**Description:**

The script uses output redirection (`>nul 2>nul`) to suppress the output of the `where bash` command. While this hides command output, it is a standard scripting practice for silent checks and does not directly relate to an AI system's response tampering as described by SAFE-T1404.: Response Tampering intercepts and modifies MCP tool responses before they are displayed to the user or processed by the LLM. Attackers alter response content to hide evidence of malicious actions, inject false information, bypass content filters, or manipulate the LLM context for subsequent interactions.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 591: 🟠 **HIGH** — `SAFE-T1408`

**Location:** Lines 1-46

**Description:**

The provided code is a shell script wrapper for executing other scripts and does not handle OAuth flows or token management. Therefore, it is not susceptible to OAuth protocol downgrade vulnerabilities.: An attacker forces or coerces a client to use the OAuth 2.0 implicit flow instead of the authorization code flow with PKCE. Tokens delivered in URL fragments are easier to capture via logs, referrers, or malicious scripts, enabling token theft and session hijacking.
This technique detects the presence of implicit flow configuration, which indicates potential vulnerability. The authorization code flow with PKCE should always be preferred over implicit flow for security.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 592: 🟠 **HIGH** — `SAFE-T1501`

**Location:** Lines 1-46

**Description:**

The provided code is a shell script wrapper and does not contain JSON schema definitions or related parsing logic. Therefore, it is not directly susceptible to Full-Schema Poisoning (FSP) as described in SAFE-T1501.: Full-Schema Poisoning manipulates JSON schema definitions used for MCP tool input validation to include hidden fields, default values with malicious content, or additionalProperties that allow arbitrary data injection. In auth schemas, this can expose credentials through coerced fields or default values that leak sensitive information.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 593: 🟠 **HIGH** — `SAFE-T1503`

**Location:** Lines 1-46

**Description:**

The script passes command-line arguments directly to a bash script without explicit sanitization. While this script itself does not enumerate environment variables, it acts as a wrapper for other scripts. If the invoked bash script were vulnerable to command injection or designed to read sensitive files, this wrapper would facilitate its execution. This is a pattern match for a potential execution vector, not a direct vulnerability in this file.: Environment Variable Scraping extracts secrets from process environment variables. MCP server tools iterate over process.env, os.environ, or use getenv with attacker-controlled keys to harvest API keys, database passwords, cloud credentials, and other sensitive configuration stored in environment variables.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 594: 🟠 **HIGH** — `SAFE-T1503`

**Location:** Lines 1-46

**Description:**

The Unix portion of the script directly executes a bash script with all passed arguments. While this script itself does not enumerate environment variables, it acts as a wrapper for other scripts. If the invoked bash script were vulnerable to command injection or designed to read sensitive files, this wrapper would facilitate its execution. This is a pattern match for a potential execution vector, not a direct vulnerability in this file.: Environment Variable Scraping extracts secrets from process environment variables. MCP server tools iterate over process.env, os.environ, or use getenv with attacker-controlled keys to harvest API keys, database passwords, cloud credentials, and other sensitive configuration stored in environment variables.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 595: 🟠 **HIGH** — `SAFE-T1504`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a generic wrapper for executing other scripts and does not directly handle or output access/refresh tokens. The vulnerability described by SAFE-T1504 pertains to MCP tools that explicitly return tokens in their output, which this script is not.: Token Theft via API Response occurs when MCP tool implementations inadvertently include access tokens, refresh tokens, or other credentials in their output responses. These leaked tokens are then visible in conversation history, logs, or can be captured by other tools in the chain for unauthorized access.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 596: 🟠 **HIGH** — `SAFE-T1602`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is vulnerable to path traversal in the `<script-name>` argument, allowing an attacker to specify and execute arbitrary scripts or executables outside the intended `HOOK_DIR`. This enables an adversary to discover and leverage system capabilities beyond the intended scope.: Tool Enumeration discovers the full set of available MCP tools, their capabilities, input schemas, and permission requirements. Attackers use tools/list API calls and schema introspection to map available functionality, identify privileged tools, and find tools with weak input validation for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 597: 🟠 **HIGH** — `SAFE-T1703`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script directly passes user-supplied script names and arguments to the `bash` interpreter without any sanitization or validation. This allows for arbitrary command execution if an attacker can control the arguments passed to `run-hook.cmd`.: Tool-Chaining Pivot exploits the output of one MCP tool as input to another more privileged tool, creating multi-step attack chains. Attackers leverage a low-privilege tool to produce crafted output that, when consumed by a higher-privilege tool, enables unauthorized operations such as command execution, data access, or privilege escalation.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 598: 🟠 **HIGH** — `SAFE-T1706`

**Location:** Lines 1-46

**Description:**

The provided code is a script execution wrapper and does not handle OAuth tokens or service-to-service communication. Therefore, it is not directly susceptible to OAuth token replay vulnerabilities.: OAuth Token Pivot Replay reuses OAuth tokens obtained from one service to access a different service or audience. Attackers exploit missing audience (aud) validation, overly broad token scopes, or lack of token binding to pivot from a lower-value service to a higher-value target using the same token.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 599: 🟠 **HIGH** — `SAFE-T1707`

**Location:** Lines 1-46

**Description:**

The provided code is a command-line wrapper script for executing bash scripts and does not handle web requests, sessions, or OAuth tokens. Therefore, it is not susceptible to CSRF vulnerabilities like SAFE-T1707, which specifically targets web applications or MCP portals that process state-changing requests.: CSRF Token Relay exploits missing or improperly validated CSRF tokens in MCP state-changing requests. Attackers forge cross-site requests or reuse OAuth state parameters to trigger unauthorized actions, manipulate tool configurations, or hijack authentication flows through the victim browser or agent.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 600: 🟠 **HIGH** — `SAFE-T1801`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a wrapper for executing other scripts, primarily bash scripts. While it doesn't directly implement bulk data access or collection, it acts as an execution mechanism for scripts that *could* potentially be involved in such activities if invoked by an AI agent. The script itself does not contain logic for systematic enumeration or data harvesting.: Automated Data Harvesting uses MCP tools to systematically access and collect data through bulk queries, pagination scraping, and record enumeration. Attackers automate data collection across multiple endpoints or database tables to amass sensitive information for exfiltration or analysis.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 601: 🟠 **HIGH** — `SAFE-T1804`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a wrapper for executing shell scripts and does not directly interact with APIs or MCP (Model Context Protocol) tools. Therefore, it does not present a direct vulnerability related to SAFE-T1804 (Systematic API abuse through pagination, bulk endpoints, and rate limit bypass).: API Data Harvest systematically extracts data through MCP tool API calls by abusing pagination, accessing bulk endpoints, or bypassing rate limits. Attackers iterate through API pages, manipulate query parameters, or use concurrent requests to extract large datasets that individual queries would not expose.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 602: 🟠 **HIGH** — `SAFE-T1904`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a polyglot wrapper designed to execute other scripts using bash. While it handles script execution, it does not directly interact with chat messages or LLM responses, nor does it contain logic for embedding or decoding covert commands within natural language outputs. The technique SAFE-T1904 specifically targets the abuse of LLM-generated natural language for covert communication, which is not applicable to this file's functionality.: Chat-Based Backchannel uses MCP chat or messaging tools as a covert communication channel. Attackers embed encoded commands, base64 payloads, or steganographic data within seemingly normal chat messages to exfiltrate data or receive C2 instructions without triggering network-based detection.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 603: 🟠 **HIGH** — `SAFE-T1910`

**Location:** Lines 1-46

**Description:**

The `run-hook.cmd` script is a polyglot wrapper designed to execute other scripts using bash. While it doesn't directly implement covert channel exfiltration, it acts as an execution vehicle for other scripts. If a hook script executed by this wrapper were to contain logic for data exfiltration via covert channels (e.g., embedding data in DNS queries or HTTP requests), this wrapper would facilitate its execution. However, the wrapper itself does not introduce the vulnerability.: Covert Channel Exfiltration uses non-standard communication channels to exfiltrate data from MCP server environments. Techniques include DNS tunneling (encoding data in DNS queries), timing-based channels, HTTP header manipulation, and steganographic encoding that bypasses traditional network monitoring and DLP solutions.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 604: 🟠 **HIGH** — `SAFE-T1911`

**Location:** Lines 1-46

**Description:**

The provided code is a Windows batch script wrapper for executing bash scripts. It does not directly handle or process JSON parameters or metadata fields, which are the target of the SAFE-T1911 technique. Therefore, this specific file does not contain the vulnerability described.: Attackers hide sensitive data inside optional or unused parameters (e.g., metadata, context, tags) that are accepted and forwarded by the server but not actively validated. These values can be logged or propagated to upstream services, enabling stealthy exfiltration without triggering obvious alarms.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

#### Finding 605: 🟠 **HIGH** — `SAFE-T2105`

**Location:** Lines 1-46

**Description:**

The Windows portion of the `run-hook.cmd` script is vulnerable to command injection due to unquoted arguments when invoking `bash.exe`. An attacker controlling the arguments passed to `run-hook.cmd` could execute arbitrary commands.: Disinformation Output manipulates MCP tool responses or LLM outputs to generate false, misleading, or harmful information. Attackers inject instructions that cause the AI to produce fabricated data, distorted facts, or biased responses that appear authoritative, potentially causing real-world harm through decision-making based on false information.

<details>
<summary>Code Evidence</summary>

```
hooks/run-hook.cmd:1-46 : << 'CMDBLOCK'\n@echo off\nREM Cross-platform polyglot wrapper for hook scripts.
```

</details>

---

### 📁 `skills/brainstorming/scripts/stop-server.sh`

#### Finding 606: 🔴 **CRITICAL** — `SAFE-T1103`

**Location:** Lines 1-56

**Description:**

The provided shell script `stop-server.sh` is a standalone utility for stopping a process and cleaning up directories. It does not appear to be an MCP host or client, nor does it process JSON-RPC messages or invoke tools based on external input in a manner consistent with the SAFE-T1103 technique. The script's actions (killing a process by PID, removing files) are hardcoded and not subject to spoofed function calls.: Fake Tool Invocation exploits insufficient tool validation in MCP hosts by injecting or spoofing tool names, duplicating tool registrations, or manipulating the JSON-RPC tool call protocol. Attackers can redirect legitimate tool calls to malicious implementations or register tools that shadow trusted ones.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 #!/usr/bin/env bash# Stop the brainstorm server and clean up# Usage: stop-server.sh <session_dir>#Kills the server process. Only deletes session directory if it's# under /tmp (ephemeral). Persistent directories (.superpowers/) are# kept so mockups can be reviewed later.SESSION_DIR="$1"if [[ -z "$SESSION_DIR" ]]; then  echo '{"error": "Usage: stop-server.sh <session_dir>"}'  exit 1fiSTATE_DIR="${SESSION_DIR}/state"PID_FILE="${STATE_DIR}/server.pid"if [[ -f "$PID_FILE" ]]; then  pid=$(cat "$PID_FILE")  # Try to stop gracefully, fallback to force if still alive  kill "$pid" 2>/dev/null || true  # Wait for graceful shutdown (up to ~2s)  for i in {1..20}; do    if ! kill -0 "$pid" 2>/dev/null; then      break    fi    sleep 0.1  done  # If still running, escalate to SIGKILL  if kill -0 "$pid" 2>/dev/null; then    kill -9 "$pid" 2>/dev/null || true    # Give SIGKILL a moment to take effect    sleep 0.1  fi  if kill -0 "$pid" 2>/dev/null; then    echo '{"status": "failed", "error": "process still running"}'    exit 1  fi  rm -f "$PID_FILE" "${STATE_DIR}/server.log"  # Only delete ephemeral /tmp directories  if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi  echo '{"status": "stopped"}'else  echo '{"status": "not_running"}'fi
```

</details>

---

#### Finding 607: 🔴 **CRITICAL** — `SAFE-T1111`

**Location:** Lines 1-56

**Description:**

The script does not appear to interact with any AI CLI tools, nor does it contain logic for weaponizing such tools as described in SAFE-T1111. The primary function is to stop a server process and clean up its session directory.: AI Agent CLI Weaponization exploits AI-powered CLI tools and agents that have unrestricted shell access or use dangerous permission-bypass flags. Attackers distribute malicious packages with postinstall scripts that invoke AI CLI tools in unsafe modes, or craft inputs that cause agents to execute arbitrary system commands, harvest credentials, or exfiltrate repository data.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 #!/usr/bin/env bash# Stop the brainstorm server and clean up# Usage: stop-server.sh <session_dir>#Kills the server process. Only deletes session directory if it's# under /tmp (ephemeral). Persistent directories (.superpowers/) are# kept so mockups can be reviewed later.SESSION_DIR="$1"if [[ -z "$SESSION_DIR" ]]; then  echo '{"error": "Usage: stop-server.sh <session_dir>"}'  exit 1fiSTATE_DIR="${SESSION_DIR}/state"PID_FILE="${STATE_DIR}/server.pid"if [[ -f "$PID_FILE" ]]; then  pid=$(cat "$PID_FILE")  # Try to stop gracefully, fallback to force if still alive  kill "$pid" 2>/dev/null || true  # Wait for graceful shutdown (up to ~2s)  for i in {1..20}; do    if ! kill -0 "$pid" 2>/dev/null; then      break    fi    sleep 0.1  done  # If still running, escalate to SIGKILL  if kill -0 "$pid" 2>/dev/null; then    kill -9 "$pid" 2>/dev/null || true    # Give SIGKILL a moment to take effect    sleep 0.1  fi  if kill -0 "$pid" 2>/dev/null; then    echo '{"status": "failed", "error": "process still running"}'    exit 1  fi  rm -f "$PID_FILE" "${STATE_DIR}/server.log"  # Only delete ephemeral /tmp directories  if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi  echo '{"status": "stopped"}'else  echo '{"status": "not_running"}'fi
```

</details>

---

#### Finding 608: 🔴 **CRITICAL** — `SAFE-T1203`

**Location:** Lines 1-56

**Description:**

The script is a server shutdown script and does not contain any code that attempts to establish persistence mechanisms (e.g., cron jobs, OS autostart entries, service-manager units) during installation or first execution. Its function is to stop a process and clean up ephemeral directories.: Backdoored Server Binary involves distributing trojanized MCP server packages, containers, or binaries that establish persistence through postinstall hooks, cron jobs, systemd units, or autostart entries. The malicious code executes during installation and persists even after the package is uninstalled.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 #!/usr/bin/env bash# Stop the brainstorm server and clean up# Usage: stop-server.sh <session_dir>#Kills the server process. Only deletes session directory if it's# under /tmp (ephemeral). Persistent directories (.superpowers/) are# kept so mockups can be reviewed later.SESSION_DIR="$1"if [[ -z "$SESSION_DIR" ]]; then  echo '{"error": "Usage: stop-server.sh <session_dir>"}'  exit 1fiSTATE_DIR="${SESSION_DIR}/state"PID_FILE="${STATE_DIR}/server.pid"if [[ -f "$PID_FILE" ]]; then  pid=$(cat "$PID_FILE")  # Try to stop gracefully, fallback to force if still alive  kill "$pid" 2>/dev/null || true  # Wait for graceful shutdown (up to ~2s)  for i in {1..20}; do    if ! kill -0 "$pid" 2>/dev/null; then      break    fi    sleep 0.1  done  # If still running, escalate to SIGKILL  if kill -0 "$pid" 2>/dev/null; then    kill -9 "$pid" 2>/dev/null || true    # Give SIGKILL a moment to take effect    sleep 0.1  fi  if kill -0 "$pid" 2>/dev/null; then    echo '{"status": "failed", "error": "process still running"}'    exit 1  fi  rm -f "$PID_FILE" "${STATE_DIR}/server.log"  # Only delete ephemeral /tmp directories  if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi  echo '{"status": "stopped"}'else  echo '{"status": "not_running"}'fi
```

</details>

---

#### Finding 609: 🔴 **CRITICAL** — `SAFE-T1303`

**Location:** Lines 1-56

**Description:**

The script does not directly implement or demonstrate the SAFE-T1303 technique, which focuses on container sandbox escape via runtime 'exec' with a manipulated working directory. This script is a host-level cleanup script that uses `kill` to terminate processes and `rm` to delete files, not container runtime `exec` commands.: Container Sandbox Escape exploits misconfigured container environments running MCP servers to break out of the sandbox and access the host operating system. Attack vectors include mounting the Docker socket, accessing /proc filesystem, running in privileged mode, using nsenter, or exploiting kernel vulnerabilities from within the container.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 #!/usr/bin/env bash# Stop the brainstorm server and clean up# Usage: stop-server.sh <session_dir>#Kills the server process. Only deletes session directory if it's# under /tmp (ephemeral). Persistent directories (.superpowers/) are# kept so mockups can be reviewed later.SESSION_DIR="$1"if [[ -z "$SESSION_DIR" ]]; then  echo '{"error": "Usage: stop-server.sh <session_dir>"}'  exit 1fiSTATE_DIR="${SESSION_DIR}/state"PID_FILE="${STATE_DIR}/server.pid"if [[ -f "$PID_FILE" ]]; then  pid=$(cat "$PID_FILE")  # Try to stop gracefully, fallback to force if still alive  kill "$pid" 2>/dev/null || true  # Wait for graceful shutdown (up to ~2s)  for i in {1..20}; do    if ! kill -0 "$pid" 2>/dev/null; then      break    fi    sleep 0.1  done  # If still running, escalate to SIGKILL  if kill -0 "$pid" 2>/dev/null; then    kill -9 "$pid" 2>/dev/null || true    # Give SIGKILL a moment to take effect    sleep 0.1  fi  if kill -0 "$pid" 2>/dev/null; then    echo '{"status": "failed", "error": "process still running"}'    exit 1  fi  rm -f "$PID_FILE" "${STATE_DIR}/server.log"  # Only delete ephemeral /tmp directories  if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi  echo '{"status": "stopped"}'else  echo '{"status": "not_running"}'fi
```

</details>

---

#### Finding 610: 🔴 **CRITICAL** — `SAFE-T1305`

**Location:** Lines 39-41

**Description:**

The script performs file deletion and process termination operations. While not a direct privilege escalation vulnerability itself, if this script were invoked by an MCP server running with root privileges and an attacker could control the `SESSION_DIR` argument, it could lead to arbitrary file deletion or process termination with root privileges. The `if [[ "$SESSION_DIR" == /tmp/* ]]` check for `rm -rf` provides some mitigation against arbitrary path traversal for deletion, limiting it to `/tmp` paths.: Host OS Privilege Escalation exploits operating system vulnerabilities or misconfigurations from within an MCP server process to gain root or administrative access on the host. Attack vectors include setuid binary abuse, kernel exploits, capability escalation, and accessing sensitive system files like /etc/shadow.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:39-41 if [[ "$SESSION_DIR" == /tmp/* ]]; then rm -rf "$SESSION_DIR" fi
```

</details>

---

#### Finding 611: 🔴 **CRITICAL** — `SAFE-T1309`

**Location:** Lines 1-56

**Description:**

The provided code is a shell script that performs server shutdown and cleanup operations. It does not involve an LLM agent or natural language prompt processing. Therefore, it is not directly susceptible to 'Prompt injection tricks LLM into selecting and invoking privileged tools' (SAFE-T1309).: Privileged Tool via Prompt Injection exploits LLM-based tool selection where the model chooses which tool to invoke based on natural language. Attackers craft prompts that manipulate the LLM into selecting privileged or dangerous tools, bypassing access controls that only exist at the prompt/UI layer rather than at the tool execution layer.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 #!/usr/bin/env bash# Stop the brainstorm server and clean up# Usage: stop-server.sh <session_dir>#Kills the server process. Only deletes session directory if it's# under /tmp (ephemeral). Persistent directories (.superpowers/) are# kept so mockups can be reviewed later.SESSION_DIR="$1"if [[ -z "$SESSION_DIR" ]]; then  echo '{"error": "Usage: stop-server.sh <session_dir>"}'  exit 1fiSTATE_DIR="${SESSION_DIR}/state"PID_FILE="${STATE_DIR}/server.pid"if [[ -f "$PID_FILE" ]]; then  pid=$(cat "$PID_FILE")  # Try to stop gracefully, fallback to force if still alive  kill "$pid" 2>/dev/null || true  # Wait for graceful shutdown (up to ~2s)  for i in {1..20}; do    if ! kill -0 "$pid" 2>/dev/null; then      break    fi    sleep 0.1  done  # If still running, escalate to SIGKILL  if kill -0 "$pid" 2>/dev/null; then    kill -9 "$pid" 2>/dev/null || true    # Give SIGKILL a moment to take effect    sleep 0.1  fi  if kill -0 "$pid" 2>/dev/null; then    echo '{"status": "failed", "error": "process still running"}'    exit 1  fi  rm -f "$PID_FILE" "${STATE_DIR}/server.log"  # Only delete ephemeral /tmp directories  if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi  echo '{"status": "stopped"}'else  echo '{"status": "not_running"}'fi
```

</details>

---

#### Finding 612: 🔴 **CRITICAL** — `SAFE-T1702`

**Location:** Lines 1-56

**Description:**

The script reads a process ID from a file (`server.pid`) and uses it in `kill` commands. While the `SESSION_DIR` is derived from user input, the `pid` variable is consistently used within double quotes (`"$pid"`) when passed to `kill`. This prevents shell metacharacters within the `pid` variable from being interpreted as commands by the shell.: Command Injection vulnerabilities occur when MCP server tools execute shell commands with unsanitized user input. Attackers can inject malicious commands that are executed with the server's privileges, potentially leading to arbitrary code execution, data exfiltration, or system compromise.

This technique targets common command execution patterns across multiple languages including Node.js (child_process), Python (subprocess, os.system), Rust (Command), and Go (os/exec). Unsafe practices include string concatenation in shell commands, missing input validation, and using shell=True or shell invocation modes.

Command injection is particularly dangerous in MCP servers because tools often have elevated privileges to interact with system resources, files, or external services. A successful injection can bypass all security boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 skills/brainstorming/scripts/stop-server.sh:19-19 pid=$(cat "$PID_FILE")
skills/brainstorming/scripts/22-22 kill "$pid" 2>/dev/null || true
```

</details>

---

#### Finding 613: 🔴 **CRITICAL** — `SAFE-T1704`

**Location:** Lines 1-56

**Description:**

The script processes a user-controlled file path (`SESSION_DIR`) but does not make any server requests or process URLs. Therefore, it is not vulnerable to Server-Side Request Forgery (SSRF) as described by SAFE-T1704's summary.: Compromised-Server Pivot / SSRF occurs when MCP tools accept user-controlled URLs in HTTP requests without host allowlisting or validation. Attackers exploit this to access internal services, cloud metadata endpoints, localhost services, or private network resources that the MCP server can reach but the attacker cannot directly access.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 #!/usr/bin/env bash# Stop the brainstorm server and clean up# Usage: stop-server.sh <session_dir>#Kills the server process. Only deletes session directory if it's# under /tmp (ephemeral). Persistent directories (.superpowers/) are# kept so mockups can be reviewed later.SESSION_DIR="$1"if [[ -z "$SESSION_DIR" ]]; then  echo '{"error": "Usage: stop-server.sh <session_dir>"}'  exit 1fiSTATE_DIR="${SESSION_DIR}/state"PID_FILE="${STATE_DIR}/server.pid"if [[ -f "$PID_FILE" ]]; then  pid=$(cat "$PID_FILE")  # Try to stop gracefully, fallback to force if still alive  kill "$pid" 2>/dev/null || true  # Wait for graceful shutdown (up to ~2s)  for i in {1..20}; do    if ! kill -0 "$pid" 2>/dev/null; then      break    fi    sleep 0.1  done  # If still running, escalate to SIGKILL  if kill -0 "$pid" 2>/dev/null; then    kill -9 "$pid" 2>/dev/null || true    # Give SIGKILL a moment to take effect    sleep 0.1  fi  if kill -0 "$pid" 2>/dev/null; then    echo '{"status": "failed", "error": "process still running"}'    exit 1  fi  rm -f "$PID_FILE" "${STATE_DIR}/server.log"  # Only delete ephemeral /tmp directories  if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi  echo '{"status": "stopped"}'else  echo '{"status": "not_running"}'fi
```

</details>

---

#### Finding 614: 🔴 **CRITICAL** — `SAFE-T2101`

**Location:** Lines 1-56

**Description:**

The script uses `rm -rf` on a user-controlled session directory, which is intended to be restricted to `/tmp` subdirectories. However, the glob pattern `[[ "$SESSION_DIR" == /tmp/* ]]` incorrectly matches `/tmp` itself, allowing an attacker or misaligned agent to potentially delete the entire `/tmp` directory if `SESSION_DIR` is provided as `/tmp`.: Data Destruction uses MCP tools to permanently delete or corrupt data through destructive operations such as rm -rf, DROP TABLE, file wiping, bulk delete API calls, or database truncation. Attackers may destroy data for sabotage, to cover their tracks, or as part of a ransomware attack.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 skills/brainstorming/scripts/stop-server.sh:40-41   if [[ "$SESSION_DIR" == /tmp/* ]]; then
    rm -rf "$SESSION_DIR"
```

</details>

---

#### Finding 615: 🔴 **CRITICAL** — `SAFE-T2103`

**Location:** Lines 1-56

**Description:**

The script uses user-supplied input (SESSION_DIR) to construct file paths for 'cat' and 'rm -f' commands without proper sanitization against path traversal sequences. This could allow an attacker to read or delete files outside the intended session directory.: Code Sabotage via Malicious PR uses MCP tools with repository access to create automated pull requests containing malicious code changes, backdoors, or CI/CD pipeline modifications. Attackers manipulate build scripts, inject vulnerabilities, or alter deployment configurations through automated commits that bypass normal code review.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 skills/brainstorming/scripts/stop-server.sh:12-12 SESSION_DIR="$1"
skills/brainstorming/scripts/stop-server.sh:16-16 STATE_DIR="${SESSION_DIR}/state"
skills/brainstorming/scripts/stop-server.sh:17-17 PID_FILE="${STATE_DIR}/server.pid"
skills/brainstorming/scripts/stop-server.sh:20-20 pid=$(cat "$PID_FILE")
skills/brainstorming/scripts/stop-server.sh:44-44 rm -f "$PID_FILE" "${STATE_DIR}/server.log"
```

</details>

---

#### Finding 616: 🔴 **CRITICAL** — `SAFE-T2106`

**Location:** Lines 40-42

**Description:**

The script manages server shutdown and session directory cleanup. It explicitly retains persistent session directories (e.g., those not under /tmp) for later review. While this script itself does not interact with vector databases or embeddings, if a vector database containing poisoned embeddings were stored within such a persistent directory, this script's action of not deleting it would allow the poisoned data to persist across server restarts. However, the script is not the mechanism for poisoning the database, nor does it directly manage the content of any vector database.: Context Memory Poisoning via Vector Store Contamination manipulates vector databases storing long-term AI memory. Attackers inject poisoned embeddings that blend legitimate and malicious content, surviving server restarts and reinstallations. The poisoned content can self-replicate across sessions and affect multiple agents sharing the same vector store.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:40-42 if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi
```

</details>

---

#### Finding 617: 🟠 **HIGH** — `SAFE-T1008`

**Location:** Line 17

**Description:**

The script uses `kill` with a PID read from a file, which could be vulnerable to process ID (PID) reuse if not properly secured. However, this is not directly related to the SAFE-T1008 technique.: Tool Shadowing Attack embeds instructions in tool descriptions that manipulate how AI agents use OTHER tools. In multi-server MCP setups, all tool descriptions are aggregated into a single prompt, allowing any tool to influence the behavior of others through cross-tool interference patterns.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:17-17 pid=$(cat "$PID_FILE")
```

</details>

---

#### Finding 618: 🟠 **HIGH** — `SAFE-T1104`

**Location:** Lines 40-41

**Description:**

The `stop-server.sh` script performs a recursive deletion (`rm -rf`) on a user-provided `SESSION_DIR` if it resides under `/tmp/`. If an attacker can manipulate the LLM to invoke this script with an arbitrary path under `/tmp/`, it could lead to denial of service or data loss for other temporary files/directories.: Over-Privileged Tool Abuse occurs when MCP tools are granted permissions far exceeding their functional requirements. Attackers exploit these over-privileged tools through prompt injection or direct manipulation to access sensitive resources, execute system commands, or escalate privileges beyond the intended scope.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:40-41 if [[ "$SESSION_DIR" == /tmp/* ]]; then rm -rf "$SESSION_DIR"; fi
```

</details>

---

#### Finding 619: 🟠 **HIGH** — `SAFE-T1105`

**Location:** Lines 1-56

**Description:**

The script uses an untrusted input (`$1`, assigned to `SESSION_DIR`) to construct filesystem paths for deletion operations (`rm -f` and `rm -rf`). The check `[[ "$SESSION_DIR" == /tmp/* ]]` is insufficient to prevent path traversal, as a crafted path like `/tmp/foo/../../../../etc` would pass the check but resolve to a sensitive system directory.: Path Traversal via File Tool occurs when MCP file-handling tools accept untrusted input as filesystem paths without proper normalization, allowlisting, or base directory enforcement. Attackers can use relative path sequences, Unicode normalization tricks, double encoding, or null byte injection to access files outside the intended directory.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 skills/brainstorming/scripts/stop-server.sh:10-10 SESSION_DIR="$1"
skills/brainstorming/scripts/stop-server.sh:17-17 STATE_DIR="${SESSION_DIR}/state"
skills/brainstorming/scripts/stop-server.sh:45-45 rm -f "$PID_FILE" "${STATE_DIR}/server.log"
skills/brainstorming/scripts/stop-server.sh:48-48 if [[ "$SESSION_DIR" == /tmp/* ]]; then
skills/brainstorming/scripts/stop-server.sh:49-49 rm -rf "$SESSION_DIR"
```

</details>

---

#### Finding 620: 🟠 **HIGH** — `SAFE-T1106`

**Location:** Lines 1-56

**Description:**

The script `stop-server.sh` is a utility designed to terminate a server process. It includes explicit termination conditions for its internal loops (e.g., a fixed-iteration `for` loop for waiting) and exits after attempting to stop the process. It does not contain logic for self-triggering, recursive invocation, or unbounded retries of external tools. The script itself is a tool, not an agent or workflow that makes decisions about repeated tool invocations.: Autonomous Loop Exploit triggers denial-of-service through self-reinforcing tool invocation loops. Attackers craft prompts or tool outputs that suggest non-terminal progress, causing agents to repeatedly invoke tools without convergence checks, rate limits, or iteration budgets.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 #!/usr/bin/env bash# Stop the brainstorm server and clean up# Usage: stop-server.sh <session_dir># # Kills the server process. Only deletes session directory if it's# under /tmp (ephemeral). Persistent directories (.superpowers/) are# kept so mockups can be reviewed later.SESSION_DIR="$1"if [[ -z "$SESSION_DIR" ]]; then  echo '{"error": "Usage: stop-server.sh <session_dir>"}'  exit 1fiSTATE_DIR="${SESSION_DIR}/state"PID_FILE="${STATE_DIR}/server.pid"if [[ -f "$PID_FILE" ]]; then  pid=$(cat "$PID_FILE")  # Try to stop gracefully, fallback to force if still alive  kill "$pid" 2>/dev/null || true  # Wait for graceful shutdown (up to ~2s)  for i in {1..20}; do    if ! kill -0 "$pid" 2>/dev/null; then      break    fi    sleep 0.1  done  # If still running, escalate to SIGKILL  if kill -0 "$pid" 2>/dev/null; then    kill -9 "$pid" 2>/dev/null || true    # Give SIGKILL a moment to take effect    sleep 0.1  fi  if kill -0 "$pid" 2>/dev/null; then    echo '{"status": "failed", "error": "process still running"}'    exit 1  fi  rm -f "$PID_FILE" "${STATE_DIR}/server.log"  # Only delete ephemeral /tmp directories  if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi  echo '{"status": "stopped"}'else  echo '{"status": "not_running"}'fi
```

</details>

---

#### Finding 621: 🟠 **HIGH** — `SAFE-T1110`

**Location:** Lines 1-56

**Description:**

The provided code is a shell script for stopping a server and does not involve any multimodal AI processing, image analysis, or audio processing. Therefore, it is not susceptible to Multimodal Prompt Injection via Images/Audio (SAFE-T1110).: Multimodal Prompt Injection embeds malicious instructions within images, audio, or other non-text media that are processed by LLMs. Techniques include steganographic embedding in pixel data, OCR-extractable text injection, adversarial visual patterns, and metadata injection in media files. These bypass text-based sanitization since the payload is in a non-text modality.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 #!/usr/bin/env bash# Stop the brainstorm server and clean up# Usage: stop-server.sh <session_dir>#Kills the server process. Only deletes session directory if it's# under /tmp (ephemeral). Persistent directories (.superpowers/) are# kept so mockups can be reviewed later.SESSION_DIR="$1"if [[ -z "$SESSION_DIR" ]]; then  echo '{"error": "Usage: stop-server.sh <session_dir>"}'  exit 1fiSTATE_DIR="${SESSION_DIR}/state"PID_FILE="${STATE_DIR}/server.pid"if [[ -f "$PID_FILE" ]]; then  pid=$(cat "$PID_FILE")  # Try to stop gracefully, fallback to force if still alive  kill "$pid" 2>/dev/null || true  # Wait for graceful shutdown (up to ~2s)  for i in {1..20}; do    if ! kill -0 "$pid" 2>/dev/null; then      break    fi    sleep 0.1  done  # If still running, escalate to SIGKILL  if kill -0 "$pid" 2>/dev/null; then    kill -9 "$pid" 2>/dev/null || true    # Give SIGKILL a moment to take effect    sleep 0.1  fi  if kill -0 "$pid" 2>/dev/null; then    echo '{"status": "failed", "error": "process still running"}'    exit 1  fi  rm -f "$PID_FILE" "${STATE_DIR}/server.log"  # Only delete ephemeral /tmp directories  if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi  echo '{"status": "stopped"}'else  echo '{"status": "not_running"}'fi
```

</details>

---

#### Finding 622: 🟠 **HIGH** — `SAFE-T1202`

**Location:** Lines 1-56

**Description:**

The script does not handle OAuth tokens or related credentials. It is a server shutdown and cleanup script that manages process IDs and temporary session directories.: OAuth Token Persistence exploits insecure token storage and handling to maintain long-lived access to MCP-connected services. Attackers steal access or refresh tokens from localStorage, sessionStorage, cookies without secure flags, or file-based caches, then reuse them to persist access even after password changes or session invalidation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 #!/usr/bin/env bash# Stop the brainstorm server and clean up# Usage: stop-server.sh <session_dir>#Kills the server process. Only deletes session directory if it's# under /tmp (ephemeral). Persistent directories (.superpowers/) are# kept so mockups can be reviewed later.SESSION_DIR="$1"if [[ -z "$SESSION_DIR" ]]; then  echo '{"error": "Usage: stop-server.sh <session_dir>"}'  exit 1fiSTATE_DIR="${SESSION_DIR}/state"PID_FILE="${STATE_DIR}/server.pid"if [[ -f "$PID_FILE" ]]; then  pid=$(cat "$PID_FILE")  # Try to stop gracefully, fallback to force if still alive  kill "$pid" 2>/dev/null || true  # Wait for graceful shutdown (up to ~2s)  for i in {1..20}; do    if ! kill -0 "$pid" 2>/dev/null; then      break    fi    sleep 0.1  done  # If still running, escalate to SIGKILL  if kill -0 "$pid" 2>/dev/null; then    kill -9 "$pid" 2>/dev/null || true    # Give SIGKILL a moment to take effect    sleep 0.1  fi  if kill -0 "$pid" 2>/dev/null; then    echo '{"status": "failed", "error": "process still running"}'    exit 1  fi  rm -f "$PID_FILE" "${STATE_DIR}/server.log"  # Only delete ephemeral /tmp directories  if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi  echo '{"status": "stopped"}'else  echo '{"status": "not_running"}'fi
```

</details>

---

#### Finding 623: 🟠 **HIGH** — `SAFE-T1204`

**Location:** Lines 40-42

**Description:**

The `stop-server.sh` script explicitly preserves session directories that are not located under `/tmp`. If these persistent directories (`SESSION_DIR`) are used by the 'brainstorm server' to store vector stores or conversation memory, this behavior directly enables the persistence aspect of SAFE-T1204. A malicious agent, having gained write access to such a persistent `SESSION_DIR`, could implant malicious instructions or data, which would then survive server restarts and user sessions because the directory is not cleaned up.: Context Memory Implant exploits persistent memory systems in MCP-connected AI agents. Malicious agents write themselves into long-term vector stores, conversation histories, or system prompt storage to ensure re-loading in future sessions. The implanted instructions persist across server restarts and session boundaries.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:40-42 if [[ "$SESSION_DIR" == /tmp/* ]]; then rm -rf "$SESSION_DIR" fi
```

</details>

---

#### Finding 624: 🟠 **HIGH** — `SAFE-T1307`

**Location:** Lines 42-43

**Description:**

The `stop-server.sh` script is vulnerable to a Confused Deputy attack due to insufficient validation of the `SESSION_DIR` argument. If this script is executed with elevated privileges (e.g., by a system service or via `sudo`), an attacker can provide an arbitrary path within `/tmp/` as `SESSION_DIR`, causing the privileged script to delete files or directories belonging to other users or critical system components within the `/tmp` directory.: Confused Deputy Attack exploits ambient authority in MCP servers where a privileged service performs actions on behalf of an unauthorized requester. In multi-tenant environments, this enables cross-tenant data access, privilege confusion between service tiers, or unauthorized operations performed through a trusted intermediary.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:42-43 if [[ "$SESSION_DIR" == /tmp/* ]]; then rm -rf "$SESSION_DIR" fi
```

</details>

---

#### Finding 625: 🟠 **HIGH** — `SAFE-T1308`

**Location:** Lines 1-56

**Description:**

The `stop-server.sh` script does not handle or validate any form of access tokens or scopes. Therefore, it is not directly susceptible to SAFE-T1308 (Token Scope Substitution).: Token Scope Substitution occurs when an attacker replaces a limited-scope OAuth token with one that has broader permissions but the same audience, exploiting insufficient scope validation to gain elevated privileges.
This attack targets OAuth implementations that validate tokens but fail to verify that the token scopes match what was originally requested or required for the operation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 #!/usr/bin/env bash# Stop the brainstorm server and clean up# Usage: stop-server.sh <session_dir>#Kills the server process. Only deletes session directory if it's# under /tmp (ephemeral). Persistent directories (.superpowers/) are# kept so mockups can be reviewed later.SESSION_DIR="$1"if [[ -z "$SESSION_DIR" ]]; then  echo '{"error": "Usage: stop-server.sh <session_dir>"}'  exit 1fiSTATE_DIR="${SESSION_DIR}/state"PID_FILE="${STATE_DIR}/server.pid"if [[ -f "$PID_FILE" ]]; then  pid=$(cat "$PID_FILE")  # Try to stop gracefully, fallback to force if still alive  kill "$pid" 2>/dev/null || true  # Wait for graceful shutdown (up to ~2s)  for i in {1..20}; do    if ! kill -0 "$pid" 2>/dev/null; then      break    fi    sleep 0.1  done  # If still running, escalate to SIGKILL  if kill -0 "$pid" 2>/dev/null; then    kill -9 "$pid" 2>/dev/null || true    # Give SIGKILL a moment to take effect    sleep 0.1  fi  if kill -0 "$pid" 2>/dev/null; then    echo '{"status": "failed", "error": "process still running"}'    exit 1  fi  rm -f "$PID_FILE" "${STATE_DIR}/server.log"  # Only delete ephemeral /tmp directories  if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi  echo '{"status": "stopped"}'else  echo '{"status": "not_running"}'fi
```

</details>

---

#### Finding 626: 🟠 **HIGH** — `SAFE-T1404`

**Location:** Lines 1-56

**Description:**

The script's output is fixed JSON strings and does not involve dynamic narrative generation by an AI model. This script is a tool that might be called by an AI, but it is not the AI system itself that would be manipulated to produce a tampered response.: Response Tampering intercepts and modifies MCP tool responses before they are displayed to the user or processed by the LLM. Attackers alter response content to hide evidence of malicious actions, inject false information, bypass content filters, or manipulate the LLM context for subsequent interactions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 #!/usr/bin/env bash# Stop the brainstorm server and clean up# Usage: stop-server.sh <session_dir># # Kills the server process. Only deletes session directory if it's# under /tmp (ephemeral). Persistent directories (.superpowers/) are# kept so mockups can be reviewed later.SESSION_DIR="$1"if [[ -z "$SESSION_DIR" ]]; then  echo '{"error": "Usage: stop-server.sh <session_dir>"}'  exit 1fiSTATE_DIR="${SESSION_DIR}/state"PID_FILE="${STATE_DIR}/server.pid"if [[ -f "$PID_FILE" ]]; then  pid=$(cat "$PID_FILE")  # Try to stop gracefully, fallback to force if still alive  kill "$pid" 2>/dev/null || true  # Wait for graceful shutdown (up to ~2s)  for i in {1..20}; do    if ! kill -0 "$pid" 2>/dev/null; then      break    fi    sleep 0.1  done  # If still running, escalate to SIGKILL  if kill -0 "$pid" 2>/dev/null; then    kill -9 "$pid" 2>/dev/null || true    # Give SIGKILL a moment to take effect    sleep 0.1  fi  if kill -0 "$pid" 2>/dev/null; then    echo '{"status": "failed", "error": "process still running"}'    exit 1  fi  rm -f "$PID_FILE" "${STATE_DIR}/server.log"  # Only delete ephemeral /tmp directories  if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi  echo '{"status": "stopped"}'else  echo '{"status": "not_running"}'fi
```

</details>

---

#### Finding 627: 🟠 **HIGH** — `SAFE-T1407`

**Location:** Lines 1-56

**Description:**

The provided script is a server stop and cleanup utility. It does not implement any server-side logic, network proxying, or traffic interception capabilities. Therefore, it cannot be used to perform a Server Proxy Masquerade (SAFE-T1407).: An attacker operates an MCP server that forwards requests to a legitimate upstream API and relays the responses back to the client. Because the responses originate from the trusted service, the traffic appears normal in monitoring and logs, but the proxy can inspect, alter, or exfiltrate sensitive data. This enables stealthy data collection and response manipulation while hiding the true malicious behavior.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 skills/brainstorming/scripts/stop-server.sh:1-56 #!/usr/bin/env bash
# Stop the brainstorm server and clean up
# Usage: stop-server.sh <session_dir>
#
# Kills the server process. Only deletes session directory if it's
# under /tmp (ephemeral). Persistent directories (.superpowers/) are
# kept so mockups can be reviewed later.

SESSION_DIR="$1"

if [[ -z "$SESSION_DIR" ]]; then
  echo '{"error": "Usage: stop-server.sh <session_dir>"}'
  exit 1
fi

STATE_DIR="${SESSION_DIR}/state"
PID_FILE="${STATE_DIR}/server.pid"

if [[ -f "$PID_FILE" ]]; then
  pid=$(cat "$PID_FILE")

  # Try to stop gracefully, fallback to force if still alive
  kill "$pid" 2>/dev/null || true

  # Wait for graceful shutdown (up to ~2s)
  for i in {1..20}; do
    if ! kill -0 "$pid" 2>/dev/null; then
      break
    fi
    sleep 0.1
  done

  # If still running, escalate to SIGKILL
  if kill -0 "$pid" 2>/dev/null; then
    kill -9 "$pid" 2>/dev/null || true

    # Give SIGKILL a moment to take effect
    sleep 0.1
  fi

  if kill -0 "$pid" 2>/dev/null; then
    echo '{"status": "failed", "error": "process still running"}'
    exit 1
  fi

  rm -f "$PID_FILE" "${STATE_DIR}/server.log"

  # Only delete ephemeral /tmp directories
  if [[ "$SESSION_DIR" == /tmp/* ]]; then
    rm -rf "$SESSION_DIR"
  fi

  echo '{"status": "stopped"}'
else
  echo '{"status": "not_running"}'
fi
```

</details>

---

#### Finding 628: 🟠 **HIGH** — `SAFE-T1605`

**Location:** Lines 40-42

**Description:**

The script handles session directories and process IDs, which are operational details. While not directly exposing MCP capabilities, the handling of session directories, especially the conditional deletion of `/tmp/*` directories, could be a minor informational leak if an attacker could infer system configuration (e.g., where ephemeral sessions are stored vs. persistent ones). However, this is a very weak signal and not a direct capability enumeration.: Capability Mapping systematically discovers MCP server capabilities, supported API versions, feature flags, and permission boundaries. Attackers use introspection endpoints, capability negotiation, and API probing to build a comprehensive map of what the target system can do, identifying the most valuable attack vectors.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:40-42 if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi
```

</details>

---

#### Finding 629: 🟠 **HIGH** — `SAFE-T1703`

**Location:** Lines 10-20

**Description:**

The script uses user-controlled input to construct a file path, reads its content, and then directly uses that content as an argument to the 'kill' command. This allows an attacker to inject an arbitrary Process ID (PID) into the 'kill' command, potentially leading to arbitrary process termination.: Tool-Chaining Pivot exploits the output of one MCP tool as input to another more privileged tool, creating multi-step attack chains. Attackers leverage a low-privilege tool to produce crafted output that, when consumed by a higher-privilege tool, enables unauthorized operations such as command execution, data access, or privilege escalation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:10-20 SESSION_DIR="$1"...PID_FILE="${STATE_DIR}/server.pid"...pid=$(cat "$PID_FILE")...kill "$pid"
```

</details>

---

#### Finding 630: 🟠 **HIGH** — `SAFE-T1801`

**Location:** Lines 1-56

**Description:**

The script handles session directories and process IDs, but does not contain functionality for bulk data access or collection that could be exploited by an AI agent for automated data harvesting. The `rm -rf` command is for deletion, not collection, and is conditionally applied to ephemeral `/tmp` directories.: Automated Data Harvesting uses MCP tools to systematically access and collect data through bulk queries, pagination scraping, and record enumeration. Attackers automate data collection across multiple endpoints or database tables to amass sensitive information for exfiltration or analysis.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:1-56 #!/usr/bin/env bash# Stop the brainstorm server and clean up# Usage: stop-server.sh <session_dir>#Kills the server process. Only deletes session directory if it's# under /tmp (ephemeral). Persistent directories (.superpowers/) are# kept so mockups can be reviewed later.SESSION_DIR="$1"if [[ -z "$SESSION_DIR" ]]; then  echo '{"error": "Usage: stop-server.sh <session_dir>"}'  exit 1fiSTATE_DIR="${SESSION_DIR}/state"PID_FILE="${STATE_DIR}/server.pid"if [[ -f "$PID_FILE" ]]; then  pid=$(cat "$PID_FILE")  # Try to stop gracefully, fallback to force if still alive  kill "$pid" 2>/dev/null || true  # Wait for graceful shutdown (up to ~2s)  for i in {1..20}; do    if ! kill -0 "$pid" 2>/dev/null; then      break    fi    sleep 0.1  done  # If still running, escalate to SIGKILL  if kill -0 "$pid" 2>/dev/null; then    kill -9 "$pid" 2>/dev/null || true    # Give SIGKILL a moment to take effect    sleep 0.1  fi  if kill -0 "$pid" 2>/dev/null; then    echo '{"status": "failed", "error": "process still running"}'    exit 1  fi  rm -f "$PID_FILE" "${STATE_DIR}/server.log"  # Only delete ephemeral /tmp directories  if [[ "$SESSION_DIR" == /tmp/* ]]; then    rm -rf "$SESSION_DIR"  fi  echo '{"status": "stopped"}'else  echo '{"status": "not_running"}'fi
```

</details>

---

#### Finding 631: 🟠 **HIGH** — `SAFE-T1904`

**Location:** Line 11

**Description:**

The script outputs structured JSON messages to stdout, which could potentially be parsed by downstream automated systems. However, these messages are simple status updates and do not contain any encoded commands, hidden data, or steganographic markers indicative of a covert channel.: Chat-Based Backchannel uses MCP chat or messaging tools as a covert communication channel. Attackers embed encoded commands, base64 payloads, or steganographic data within seemingly normal chat messages to exfiltrate data or receive C2 instructions without triggering network-based detection.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:11-11 echo '{"error": "Usage: stop-server.sh <session_dir>"}'
```

</details>

---

#### Finding 632: 🟠 **HIGH** — `SAFE-T1904`

**Location:** Line 40

**Description:**

The script outputs structured JSON messages to stdout, which could potentially be parsed by downstream automated systems. However, these messages are simple status updates and do not contain any encoded commands, hidden data, or steganographic markers indicative of a covert channel.: Chat-Based Backchannel uses MCP chat or messaging tools as a covert communication channel. Attackers embed encoded commands, base64 payloads, or steganographic data within seemingly normal chat messages to exfiltrate data or receive C2 instructions without triggering network-based detection.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:40-40 echo '{"status": "failed", "error": "process still running"}'
```

</details>

---

#### Finding 633: 🟠 **HIGH** — `SAFE-T1904`

**Location:** Line 49

**Description:**

The script outputs structured JSON messages to stdout, which could potentially be parsed by downstream automated systems. However, these messages are simple status updates and do not contain any encoded commands, hidden data, or steganographic markers indicative of a covert channel.: Chat-Based Backchannel uses MCP chat or messaging tools as a covert communication channel. Attackers embed encoded commands, base64 payloads, or steganographic data within seemingly normal chat messages to exfiltrate data or receive C2 instructions without triggering network-based detection.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:49-49 echo '{"status": "stopped"}'
```

</details>

---

#### Finding 634: 🟠 **HIGH** — `SAFE-T1904`

**Location:** Line 51

**Description:**

The script outputs structured JSON messages to stdout, which could potentially be parsed by downstream automated systems. However, these messages are simple status updates and do not contain any encoded commands, hidden data, or steganographic markers indicative of a covert channel.: Chat-Based Backchannel uses MCP chat or messaging tools as a covert communication channel. Attackers embed encoded commands, base64 payloads, or steganographic data within seemingly normal chat messages to exfiltrate data or receive C2 instructions without triggering network-based detection.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:51-51 echo '{"status": "not_running"}'
```

</details>

---

#### Finding 635: 🟠 **HIGH** — `SAFE-T1910`

**Location:** Line 9

**Description:**

The script handles a session directory path provided as an argument. While not a direct covert channel for exfiltration, improper validation of this path could lead to other vulnerabilities like arbitrary file deletion (though mitigated for non-/tmp paths). This is not a SAFE-T1910 finding.: Covert Channel Exfiltration uses non-standard communication channels to exfiltrate data from MCP server environments. Techniques include DNS tunneling (encoding data in DNS queries), timing-based channels, HTTP header manipulation, and steganographic encoding that bypasses traditional network monitoring and DLP solutions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:9-9 SESSION_DIR="$1"
```

</details>

---

### 📁 `skills/brainstorming/scripts/helper.js:30-34 function sendEvent(event) {event.timestamp = Date.now();if (ws && ws.readyState === WebSocket.OPEN) {ws.send(JSON.stringify(event));} else {eventQueue.push(event);}}skills/brainstorming/scripts/helper.js`

#### Finding 636: 🔴 **CRITICAL** — `SAFE-T1705`

**Location:** Lines 83-86

**Description:**

The `sendEvent` function, which allows sending arbitrary JSON objects as messages over the WebSocket, is exposed globally through `window.brainstorm.send`. This creates a direct primitive for agent-to-agent message injection if an attacker can achieve client-side code execution (e.g., via Cross-Site Scripting).: Cross-Agent Instruction Injection targets multi-agent MCP architectures where agents communicate by passing messages or tool results. Attackers inject malicious instructions into one agent that propagate to other agents in the workflow, contaminating the entire multi-agent pipeline through message passing, shared context, or tool output forwarding.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/helper.js:30-34 function sendEvent(event) {event.timestamp = Date.now();if (ws && ws.readyState === WebSocket.OPEN) {ws.send(JSON.stringify(event));} else {eventQueue.push(event);}}skills/brainstorming/scripts/helper.js:83-86 window.brainstorm = {send: sendEvent,choice: (value, metadata = {}) => sendEvent({ type: 'choice', value, ...metadata })};
```

</details>

---

### 📁 `skills/brainstorming/scripts/stop-server.sh:10-10 SESSION_DIR="$1"skills/brainstorming/scripts/stop-server.sh:46-46 if [[ "$SESSION_DIR" == /tmp/* ]]; thenskills/brainstorming/scripts/stop-server.sh`

#### Finding 637: 🟠 **HIGH** — `SAFE-T1302`

**Location:** Line 47

**Description:**

The `stop-server.sh` script is vulnerable to path traversal via the `SESSION_DIR` argument, allowing an attacker to delete arbitrary directories on the system if the script is executed with elevated privileges. The `if [[ "$SESSION_DIR" == /tmp/* ]]` check is insufficient to prevent this.: High-Privilege Tool Abuse occurs when MCP tools are configured with administrative, root, or superuser access to databases, APIs, or system resources. Attackers exploit these tools through prompt injection or direct manipulation to perform privileged operations such as modifying access controls, reading sensitive data, or escalating their own permissions.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/stop-server.sh:10-10 SESSION_DIR="$1"skills/brainstorming/scripts/stop-server.sh:46-46 if [[ "$SESSION_DIR" == /tmp/* ]]; thenskills/brainstorming/scripts/stop-server.sh:47-47 rm -rf "$SESSION_DIR"
```

</details>

---

### 📁 `skills/brainstorming/scripts/start-server.sh:2-3 "# Start the brainstorm server and output connection info# Usage: start-server.sh [--project-dir <path>] [--host <bind-host>] [--url-host <display-host>] [--foreground] [--background]# # Starts server on a random high port, outputs JSON with URL."; skills/brainstorming/scripts/start-server.sh`

#### Finding 638: 🟠 **HIGH** — `SAFE-T1601`

**Location:** Lines 14-15

**Description:**

The `start-server.sh` script is designed to explicitly output the MCP server's connection URL in JSON format upon successful startup. It also provides an option (`--host 0.0.0.0`) to bind the server to all network interfaces, making it discoverable via network scanning. This functionality directly contributes to the "Discovery of MCP server endpoints" technique (SAFE-T1601) if the script's output is accessible to unauthorized parties or if the server is deployed with insecure network bindings.: MCP Server Enumeration involves probing for available MCP server instances, scanning registries, and discovering service endpoints. Attackers enumerate servers to map the attack surface, identify vulnerable or misconfigured instances, and discover high-value targets for subsequent exploitation.

<details>
<summary>Code Evidence</summary>

```
skills/brainstorming/scripts/start-server.sh:2-3 "# Start the brainstorm server and output connection info# Usage: start-server.sh [--project-dir <path>] [--host <bind-host>] [--url-host <display-host>] [--foreground] [--background]# # Starts server on a random high port, outputs JSON with URL."; skills/brainstorming/scripts/start-server.sh:14-15 "  --host <bind-host>    Host/interface to bind (default: 127.0.0.1).                         Use 0.0.0.0 in remote/containerized environments."; skills/brainstorming/scripts/start-server.sh:134 "    grep "server-started" "$LOG_FILE" | head -1"
```

</details>

---

## About This Report

This evidence report was automatically generated by the Safe Agentic Framework Scanner
as part of a manual security scan. The findings represent potential security issues
identified through automated analysis and should be reviewed by a security professional.

---

*Report generated: 2026-03-30T04:36:58.888Z*